# T5.4.6：随机模型失配族验证

- 日期：2026-07-16
- 状态：Done

## 输入与执行方案

输入为 T4.4.1/T4.4.3--T4.4.5、T5.1.2、T5.2.2/T5.2.3、T5.4.1/T5.4.5 的 live
artifacts 与 frozen teacher/student。执行前固定四条不可拼榜的 native lanes、64 个 parent-disjoint random
cells、cutoff12/10-cycle/batch16 物理配置、完整 4×3 readout audit、persistent leakage/reset 与三类新漂移
动力学，并固定 student-retention 撤销阈值。

## 实际完成内容与产物

- 新增 `cnn_fpga/benchmark/randomized_model_mismatch.py`；
- 新增 `tests/test_randomized_model_mismatch.py`；
- 生成 `docs/t5_4_6_randomized_model_mismatch.json`；
- 生成 273-row `docs/t5_4_6_randomized_model_mismatch_source_data.csv`；
- 新增 `docs/randomized_model_mismatch.md`；
- 同步 README、benchmark README、protocol hierarchy、风险与任务板。

物理 lane 对 32 cells×3 strategies 执行 nominal/mismatch 配对；readout lane 对每个随机 4×3 matrix
同时执行协议 g/e trajectory 与四 hidden-row categorical calibration；leakage/reset 保存目标率、实测速率及
校准分母；drift lane 对 8 个随机 vectors 使用 frozen parent decoders 与同 trace 六 comparator。

## 验证与详细检查

- 正式 campaign `19/19` gates，64 个 mismatch vectors 全唯一且与 1,351 个 parent seeds 无重叠；
- 物理轨迹 cutoff12、10 cycles、batch16、float64；96 strategy rows、32 retention rows；
- idle completeness `<1e-10`，phase kernels PSD，全部 trace/Hermiticity/最小本征值门通过；
- 8 个 readout matrices 的 g/e/f/higher 四行均执行 32,768 次分类采样，最大 entry error `0.004806`；
- 16 个 leakage/reset cells 与 8 个 drift cells 全部校准/执行，漂移覆盖三类有状态 dynamics；
- branch 从 raw strategy scores 重算：32/32 qualifying，retention median/Q1/min
  `0.998101/0.990413/0.897630`，compound median `0.995598`；
- focused `30 passed`；T4.4/T5.2.3/T5.4.1/T5.4.5 组合相邻回归 `176 passed`；
- 10 类 shortcut mutations 覆盖固定 bias、缺 cell、重复 strategy、伪改 score/threshold、未访问 hidden
  readout row、伪 reset calibration、OOD 重调参、跨 lane 排名和硬件 claim，全部 fail closed。

## 风险与论文 claim

新增 R-N102。qualified retention 是 student 相对 teacher 的 10-cycle/cutoff12 分布内证据，不是 absolute
robustness。gate-bias/compound teacher worst degradation 分别 `0.424155/0.395654`，不得删除。随机分布尚未
设备校准，readout f/higher audit、effective leakage kernel 与 syndrome drift lane 也不能合写成 multilevel
physical-memory/device robustness。

## 插入任务判断与任务板同步

不插入风险 task。T5.5.1 正常承接 bit-accurate Python golden reference；T5.5/T6 承接综合、定点、RTL、
transport 与板测。任务板将 T5.4.6 置为 Done，并启动 T5.5.1。
