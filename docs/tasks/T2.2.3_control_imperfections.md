# T2.2.3：控制与 active-correction imperfection

**Task ID：** T2.2.3  
**日期：** 2026-07-14  
**状态：** Done  
**实现：** `physics/control_imperfections.py`  
**测试：** `tests/test_control_imperfections.py`

## 输入与方案

输入为现有 Q4.20 `LinearRuntime`、T2.1.2 residual/correction sign、T2.0.4
virtual-rotation stage、T2.2.2 misclassification rotation、PF-C07 DAC sensitivity
边界和 scheduler record-only deadline 缺口。

方案先冻结 request -> encoded command -> hidden physical action -> residual 的因果链，
分别实现 AWG/DAC quantization、affine pulse miscalibration、multiplicative/additive
active displacement、latency drift/diffusion 与 virtual rotation，再用 exact moments、
code-level tests、两种 order 负控和 ideal endpoint 验证。

## 实际完成

- AWG unsigned amplitude/periodic phase codes；
- signed DAC I/Q codes、dequantization 与 saturation；
- pulse gain/crosstalk/bias；
- command-dependent multiplicative 与 full-covariance additive action error；
- latency drift + covariance-per-us diffusion；
- virtual-rotation quantization/gain/bias/noise；
- 两种显式 noncommuting action order；
- deployable command record / simulator truth isolation；
- sequential trajectory、vectorized fixed-request batch 与 exact Gaussian-angle moments；
- existing Q4.20 fast-loop integration、CLI/JSON/public exports。

## 反简化检查

- 33 direct tests；
- two action orders 各 80k exact-moment MC；
- latency/active covariance 各 50k calibration；
- 100k production mean max z `1.5358`、covariance relative error `0.001697`；
- 6/8/10/12-bit RMS error `0.041179/0.010172/0.002460/0.000627` 严格下降；
- latency 0/2/5/10 us covariance trace 严格增长；
- exact ideal endpoint residual 为 0；
- adjacent 110 tests 通过；
- full `tests/`：`528 passed,4 failed`，仍仅 R-N012。

## 产物

- `physics/control_imperfections.py`
- `tests/test_control_imperfections.py`
- `docs/control_imperfections.md`
- `docs/t2_2_3_control_imperfection_validation.json`
- `docs/tasks/T2.2.3_control_imperfections.md`
- `docs/new_tasks/T2.2.3_control_imperfections.md`
- 更新 public exports、protocol hierarchy、README、风险和任务板

## 风险与 claim

新增 R-N036：DAC/AWG bit depth、pulse matrix/covariance、latency law 与 VR error 都是
scenario assumptions，尚无 board/waveform/Fock calibration。R-N032 保持 Open，因为
deadline fallback/CRC/stale/atomicity 不属于本任务。

允许写 causal effective control-imperfection layer；禁止写 device-calibrated pulse
fidelity、hard-real-time board latency 或完整 physical logical channel。

## 治理同步

- T2.2.3：`In Progress -> Done`；
- T2.3.1：`Todo -> In Progress`；
- 当前推荐任务：`T2.3.1`。
