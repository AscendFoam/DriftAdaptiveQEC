# T9.1.1：Phase 9 三条独立 task signature、claim ladder 与结果门

本文件镜像 `docs/new_tasks/T9.1.1_phase9_three_lane_protocol.md` 的完成状态。T9.1.1 已建立 `ROUND_LER_SINGLE_MODE`、`SIX_STATE_LOGICAL_LIFETIME`、`RAW_IQ_DIGITAL_HIL` 三条互不补门的 24-field task signature，并冻结完整分母、matched baseline、compute/wall-clock、split、cluster-level multiplicity、missingness、scoped evidence grade、claim ladder 与未来二值结果门。

协议 verdict 为 `PASS_PHASE9_THREE_INDEPENDENT_LANE_PROTOCOL_FROZEN`，analysis SHA-256 为 `c88110375c358794339e72d672e4624871425fe480e5da091ddd1d6595255e18`，66-row Source Data、36/36 gates/mutations。协议通过不等于性能通过：LER/lifetime 仍为 `NOT_EVALUATED_NULL/null`，HIL 为 `MISSING_BOARD/null`；Puviani official/surpass、physical break-even 与 raw-IQ speed 均为 null。

cycle time 采用 action-conditioned component ledger，未知数值不冒充冻结常数；T9.2/T9.3 必须在 pilot 前不可变填充。T9.1.2 保持局部 Blocked，下一项为 `T9.1.3=In Progress`。完整输入、实现、验证、风险和反简化检查见 canonical new-task record。
