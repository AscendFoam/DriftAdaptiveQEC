# T4.1.3：混合状态输出与 future bank recommendation

- 日期：2026-07-15
- 状态：Done

## 输入与执行方案

冻结连续 noise/calibration、四态 posterior、leakage risk、estimated recovery burden、uncertainty 和
parameter-bank recommendation 的类型/单位/范围/normalization/provenance；输出只面向未来 inactive
bank，禁止逐周期纠错或 hidden truth copy。

## 实际完成内容

实现严格 dataclass schema、observed periodic-moment/Beta-run/block-bootstrap estimator、T4.1.1 HMM
checkpoint adapter、ParamMapper covariance bridge、version/validity/CRC recommendation 和真实 ParamBank
stage/commit。深审计修复 horizon leakage probability 被误作即时 hold signal 的过度保守问题。

## 产物

`cnn_fpga/decoder/hybrid_state_output.py`、
`cnn_fpga/benchmark/hybrid_state_output_validation.py`、两份 tests、
`docs/reports/phase4/hybrid_state_output.md`、17-gate JSON 与 456-row Source Data CSV。

## 验证结果

17/17 gates、16 core、27 focused、181 adjacent（2 skipped）。8×2,048-cycle replay 输出 456 rows，
nominal/stress 各 228；58 stage 全部 commit、K/b 误差 0，398 hold；五种 recommendation profiles
全覆盖。显式全量 `tests/` 为 `1230 passed, 14 skipped, 4 failed`；4 个失败均为 R-N012
已知缺失旧 FR8/P4 文档，未出现 T4.1.3 新回归。

## 风险与插入任务

新增 R-N065：recovery burden/Beta risk/uncertainty 和 stage gate 仍是 registered synthetic pilot；HMM
仍消费旧 14-summary 而非直接重训 53-feature input；无 risk calibration、logical/control gain、
fixed-point/RTL/board/device evidence。T4.1.4、T4.2、T5.1/T5.4/T5.5/T6 已覆盖，故不插入 task。

## 任务板同步

T4.1.3 Done；T4.1.4 In Progress；当前推荐任务为 T4.1.4。
