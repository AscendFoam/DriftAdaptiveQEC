# T4.2.2：设计实验式事件状态机和硬件动作

## 状态

Done（2026-07-15）。

## 输入与通过标准

输入为 T4.2.1 MAP-LUT decision/version、observed X/Z `g/e/leakage`、phase 与 health/reset signals。通过标准
是六态 priority 明确、计数器饱和不回绕、MAP action 原子映射到 frame、安全态 fail closed、周期/version/
action mismatch 事务性拒绝，并且不读取 simulator truth。

## 实际完成

新增独立 `ExperimentalEventFSM`，保留旧 run-length baseline；实现六个 3-bit counter、六态 priority、
leakage hold/reset sticky/ack、health fallback/clear hysteresis、双轴 `e` tie-break、GF(2) Pauli frame 与 8-bit
modulo phase-frame。T4.2.1 5-cycle MAP 后增加 1-cycle action register，端到端固定 6 cycles、II=1。

## 验证与深审计

20/20 production gates、26 focused、98 focused+adjacent 通过；显式全量 `tests/` 为
`1335 passed, 14 skipped, 4 failed`，4 个失败仍仅为 R-N012 缺失旧 FR8/P4 文档。8 场景×128 cycles
产生 1,024-row Source Data，全部六态和
六个 counter saturation 均实际覆盖。深审计修复了两处 demo 假设：不再强迫全-I 的真实 bank 0 产生不可达
flip；把误导性的 `start_action_cycle` 改为 `start_event_cycle`，明确 event/MAP-valid 与下一周期 action register。

## 产物

runtime、benchmark、两份 tests、主说明、20-gate JSON、1,024-row CSV，并同步 terminology、README、
benchmark README、protocol hierarchy、风险和任务板。

## 风险与后续

R-N069 登记为 High/Immediate：当前 leakage/reset/recovery 是 synthetic observed-event contract，phase-frame
是表示镜像；没有 device-calibrated physical action、完整 OOD/version/fault fallback、端到端定点、RTL/综合/
FPGA/board 证据。T4.2.3--T4.2.4、T4.3、T5、T6 正常任务承接，无需插入新 task。
