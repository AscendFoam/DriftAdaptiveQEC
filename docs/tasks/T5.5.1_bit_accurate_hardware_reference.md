# T5.5.1：冻结 Python bit-accurate hardware reference

- 日期：2026-07-16
- 状态：Done

## 输入与执行方案

输入为 T4.2.1 MAP-LUT、T4.2.2 event FSM、T4.2.3 health/fallback、T4.2.4 integer chain、T4.3.2
atomic A/B bank、T4.3.3 fault recovery 与 T5.4.6 branch。先审计既有 `step_codes` 的真实时序和 wire/bank
缺口，再冻结 packed input/output/state、binary parameter image、逐周期 pipeline ordering 与 trace chain。

## 实际完成内容与产物

- 新增 `cnn_fpga/runtime/bit_accurate_hardware_reference.py`；
- 新增 `cnn_fpga/benchmark/bit_accurate_hardware_reference.py`；
- 新增 `tests/test_bit_accurate_hardware_reference.py`；
- 生成正式 JSON、16,503-row Source Data、4,116-row golden trace 与 13,724-byte parameter bundle；
- 新增 `docs/bit_accurate_hardware_reference.md`；
- 同步 README、runtime/benchmark README、protocol hierarchy、风险与任务板。

原 T4.2.4 直接为当前 event 构造 `input_cycle=cycle-5` decision；新 reference 真正推进
`5-stage MAP + 1-stage output register`，在 S0 锁存 bank/image/version，并把 source metadata 随 pipeline
对齐。在线入口为 CRC-protected 58-bit integer word，输出/state 为 118/232 bit CRC words。

## 验证与详细检查

- 16/16 machine gates；
- 8 个 binary images 与 bundle exact roundtrip；1,706 proper prefixes 和 1,706 single-byte corruptions 全拒绝；
- input CRC single-bit flips 58/58 检测；
- 16,384 exhaustive MAP rows 的 LLR/action 与独立 integer reconstruction 0 mismatch；
- 4,110 inputs -> 4,110 unique outputs，严格 6-cycle latency、II=1；
- cycle 4000 defer、4001 commit；in-flight source4000 保持 v0，source4001 使用 v1；
- CRC/invalid/deadline/OOD/leakage/reset 与 3-bit saturation fault paths 可见；
- 双次 full rerun 的 4,116 trace rows/final chain 完全一致；
- focused `25 passed`，core adjacent `157 passed`，closed-loop adjacent `19 passed`；
- 10 类 semantic mutations 全部 fail closed。

## 风险与论文 claim

新增 R-N103。当前只是 Python RTL golden；58-bit input 从 ADC code 开始，binary image 24-bit container 尚未做
目标 BRAM packing；safe-boundary 是内部数字信号；没有 RTL、CDC、synthesis/post-route、Fmax、LUT/FF/
BRAM/DSP、transport 或 board evidence。T5.5.2 只能报告明确工具/器件/约束下的 synthesis/timing estimate。

## 插入任务判断与任务板同步

不插入风险 task。T5.5.2 正常承接目标器件综合与时序估计。任务板将 T5.5.1 置为 Done，并启动 T5.5.2。

