# T5.3.5：QEC-matrix/Petz channel-recovery bound

- 日期：2026-07-16
- 状态：Done

## 输入与执行方案

输入为 T5.3.1 finite-cutoff encoding/channel、T5.3.2 one-cycle actual sBs `F_e`、T4.4.4
teacher/student controller evidence，以及 Zheng et al. 2024 的 QEC-matrix/Petz 解析式和 recovery-Choi SDP。
方案先在 cutoff 4/6/8/10/12×三噪声做 Petz-vs-primal/dual SDP 双证书，再扩到 cutoff 48 与三档
`Delta`；actual sBs 和 teacher/student 分别执行 scheduling/metric comparability audit。

## 实际完成内容与产物

- `physics/channel_recovery_bound.py`
- `cnn_fpga/benchmark/qec_channel_recovery_bound.py`
- `tests/test_channel_recovery_bound.py`
- `tests/test_qec_channel_recovery_bound.py`
- `docs/qec_channel_recovery_bound.md`
- `docs/t5_3_5_qec_channel_recovery_bound.json`
- `docs/t5_3_5_qec_channel_recovery_bound_source_data.csv`

formal artifact 含 15 small-SDP、15 fixed-Delta cutoff、27 energy-extension、21 cutoff diagnostics、12 actual
sBs diagnostics 与 4 teacher/student incomparable rows，共 119 Source Data rows、21/21 gates。

## 验证与详细检查

- core/direct/semantic mutation：`26 passed`；
- T5.3.1--T5.3.5 channel/fidelity/boundary/cost/protocol/terminology adjacent：`218 passed`；
- identity、amplitude-damping、Kraus-unitary invariance、pure-loss TP 与 live GKP-isometry parity 均通过；
- primal 经 PSD+TP repair 后为可行下界，dual 经 slack identity shift 后为可行上界；最大证书宽度
  `1.484154e-7`；
- QEC-matrix 与 direct Petz fidelity 最大差 `1.520695e-10`；高 cutoff support TP residual 最大
  `1.181287e-4`，未隐藏病态性；
- actual sBs 先做 leakage completion，但因 interleaved schedule 只标 diagnostic；teacher/student gap 全部 null。

## 风险与论文 claim

新增 R-N096。`F_Petz` 接近 1 不代表现有 sBs/teacher/student 接近可部署最优 recovery；cutoff 48 不是
无限维证明，raw `optimal_inaccurate` 也不改写为精确 solver status。允许写 finite-cutoff encoding--noise
bound 和 repaired certificate；禁止 Petz decoder、strict actual-controller gap、physical-memory/hardware claim。

## 插入任务判断与任务板同步

不插入风险 task。T5.4 已覆盖 OOD/mismatch/long horizon，T5.5/T6 覆盖量化、RTL、board 与实测。
任务板将 T5.3.5 置为 Done，并启动 T5.4.1。
