# T5.4.5：训练 horizon 到长时部署外推验证

- 日期：2026-07-16
- 状态：Done

## 输入与执行方案

输入为 T4.4.1 frozen GRU teacher、T4.4.3 production student、T4.4.4 gain-retention、T4.4.5 branch
freeze 与 T5.0.1 horizon trend contract。执行前冻结 2/5/10/32-cycle training sweep、validation-only
selection、`10^3/10^5/10^6` deployment horizons、8 条输入流、float64/float32 shadow、256-half-cycle
reset intervention 和 `5e-5` mean+worst action-MSE 门。

## 实际完成内容与产物

- 新增 `cnn_fpga/benchmark/horizon_extrapolation_validation.py`；
- 新增 `tests/test_horizon_extrapolation_validation.py`；
- 生成 `docs/t5_4_5_horizon_extrapolation_validation.json`；
- 生成 `docs/t5_4_5_horizon_extrapolation_candidates.pt`；
- 生成 521-row `docs/t5_4_5_horizon_extrapolation_validation_source_data.csv`；
- 新增 `docs/horizon_extrapolation_validation.md`；
- 同步 README、benchmark README、protocol hierarchy、文献趋势、风险与任务板。

2/5/10 cycles 各训练三个 fresh 4-state restarts，32-cycle 使用 frozen T4.4.3。8 条流在两种精度下均
真实运行 2,000,000 half-cycle updates；dense teacher action 在 13,631 个冻结检查点计算，state recurrence
不抽样。

## 验证与详细检查

- 21/21 machine gates；24 direct/artifact/mutation tests；
- T4.4.3--T4.4.5、T5.4.4、literature/protocol 相邻回归 151 passed；
- 8 streams×`10^6` cycles；每种精度 16M teacher / 64M student updates；
- teacher hidden maximum `0.382517 < 1`；student state 全部处于解析凸包界；
- float32/float64 最大动作差 `1.2630e-6`；
- 120 个 reset interventions 全恢复，最慢 20 half-cycles；
- 2-cycle 独立 32-cycle MSE `9.9540e-5`、long worst `8.1423e-4`，欠训练负结果保留；
- 10-cycle long mean/worst 最高 `7.8447e-6/2.3536e-5`，32-cycle 最高
  `5.0431e-6/7.6121e-6`；
- GRU sequence/GRUCell 与 compiled/exported student 双重等价；六类 demo shortcut mutations fail closed。

## 风险与论文 claim

新增 R-N101。PASS 只证明 observed `g/e` recurrence/state/action 数值稳定与 teacher-action imitation，不证明
长时 Fock logical channel、physical gain/LER、leakage/model-mismatch、device 或 hardware。T4.4.4 的
10-cycle physical retention 不因本任务 action MSE 自动外推。

## 插入任务判断与任务板同步

不插入风险 task。T5.4.6 已正常承接 randomized model mismatch，T5.5/T6 承接定点、RTL、综合和板测。
任务板将 T5.4.5 置为 Done，并启动 T5.4.6。
