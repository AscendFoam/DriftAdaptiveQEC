# T0.2.2：研究缺口与英文 Introduction 草稿

**Task ID：** T0.2.2  
**日期：** 2026-07-14  
**状态：** Done  
**核心产物：** `docs/gap_statement.md`

## 1. 任务目标

将 T0.2.1 的四线文献事实压缩为可证伪、可审稿、不会把 planned evidence 写成 achieved result 的研究缺口，并输出中文主稿与英文 Introduction 草稿。通过标准要求显式包含 direct baselines、oracle MAP 上界、finite-energy-aware 和 hardware-aware simulation 边界。

## 2. 写作方案

使用 `nature-writing` 的 `algorithmic + intro/related-work + zh-to-en + generic journal` 路由：

1. 先写一句论点，且明确它是研究假设而不是结果；
2. 冻结 terminology ledger，分开 decoder oracle、control oracle、channel-recovery bound、teacher 与 student；
3. 按 finite-energy GKP、soft/history、adaptive calibration、NN/FPGA 四个 mechanism groups 综合 related work；
4. 用 collision table 定义“联合证据链缺口”，不使用 “first/unprecedented”；
5. 将缺口拆为 7 个可证伪 RQ、baseline contract 和 claim ladder；
6. 英文 Introduction 用五段 `field -> GKP decoding -> adaptation/hardware -> exact gap -> present study/boundary` 结构。

引用采用 `nature-citation` 的保守 support-grade 思路。由于任务不是 CNS-only 引用检索，范围保持为 T0.2.1 已核验的一级来源，不强行过滤 APS/arXiv/GKP 基础文献；30 个 canonical citation key 均回到本地 BibTeX 核验，4 个 Zotero `MISS` 只使用作者+arXiv ID。

## 3. 实际完成

- 中文研究缺口主稿：已知进展、collision analysis、精确 gap；
- 术语表：10 个 canonical terms 与禁止混用项；
- 7 个可证伪研究问题与逐项失败降级；
- 6 层 direct baseline/bound contract；
- 5 层 claim ladder；
- 5 段英文 Introduction 草稿；
- 8 条 claim–evidence mapping 与 support grade；
- venue/字数/未来结果仍缺失的明确占位说明。

## 4. 验证

### 4.1 结构与必需概念

- `oracle MAP`、`finite-energy-aware`、`hardware-aware`、`static-to-oracle`、`deterministic student`、`English Introduction draft`、`Claim–evidence` 均存在；
- 英文 Introduction 共 5 段，每段只承担一个主要 job；
- direct baseline contract 单列 standard/static/finite-energy MAP、oracle、history/adaptive、learned/control、recovery bound 和 hardware comparison。

### 4.2 引用完整性

```text
citation_keys=30
missing_keys=0
```

Huo/Puviani/Bhardwaj/Yan 的 Zotero 缺口没有伪造 citation key；正文只在需要处用稳定 arXiv ID。

### 4.3 过度主张检查

关键词扫描只命中两处否定性边界：

- “不是首次使用 CNN/analog GKP/FPGA QEC”；
- “不得声称超过 oracle MAP”。

没有把 proposed framework 写成已获得结果；Paragraph 5 使用 `develop/evaluate` 的研究设计语义，并在结尾限制为 protocol-aligned/hardware-aware。

## 5. 反简化复核

| 检查 | 结果 |
| --- | --- |
| 是否只写一段空泛 gap | 否；有四线 collision table、7 个 RQ、baseline contract、claim ladder |
| 是否把 CNN 当 novelty | 否；贡献中心是可审计双回路/三时间尺度控制与证据链 |
| 是否逃避 strongest baselines | 否；Wan Bayesian、Bhardwaj window、NMF、oracle、Petz bound、真实 FPGA/QPU 对照均列入 |
| 是否混淆 oracle/teacher/bound | 否；terminology ledger 明确拆分 |
| 是否把 simulation 写成 hardware | 否；五层证据 ladder 限定措辞 |
| 是否提前声称论文结果 | 否；RQ7 等均有 needs-evidence 和失败降级 |

结论：任务满足研究设计阶段的完整 gap contract，不是装饰性 Introduction demo。

## 6. 风险与后续

- `R-N011`：负向 novelty/gap 只能表述为“本轮审计未发现联合证据”，不能证明全领域绝对不存在；T7.1/T7.2 前需刷新文献；
- 正式 venue、word limit 和 reference style 尚未指定；在 T7.2.1 再按目标期刊重写，不阻塞当前 task；
- 所有性能句仍需 Phase 1–6 结果，当前 gap statement 不能作为 achieved claim 的证据。

