# T5.3.1：重构 logical channel

- 日期：2026-07-16
- 状态：Done

## 输入

- `physics/logical_channel.py` 的 parity-twirled effective channel（只作反例边界）；
- `physics/differentiable_sbs_trajectory.py` 与 `physics/autonomous_sbs.py` 的 finite-cutoff nominal sBs map；
- `docs/t3_2_8_autonomous_sbs_wallclock_validation.json` 的 live timing/channel parent；
- T4.4 physical trajectories 与 T5.2 causal artifacts（审计后因 metric/channel 不匹配而排除）。

## 执行方案

1. 用同一正交 GKP code isometry 编码 `X+/X-/Y+/Y-/Z+/Z-`；
2. QEC-on 执行 fixed nominal sBs channel，QEC-off 只执行相同 `10 us` cavity idle；
3. 逐 cycle 保存 unnormalized code outputs，重构 CPTNI PTM/Choi/TNI/leakage，而非先归一化 post-select；
4. 用 raw Pauli contrast 的 finite-horizon area 与真实 e-fold crossing/censoring报告三轴 lifetime；
5. 从最初低 cutoff 两点扩展为 `12/24/36/40`，要求 terminal `36->40` PTM/leakage 稳定；
6. 从 raw outputs 全链重算所有 derived tables，增加 semantic mutation tests。

## 实际完成内容与产物

- `physics/fock_logical_channel.py`
- `cnn_fpga/benchmark/logical_channel_reconstruction.py`
- `tests/test_fock_logical_channel.py`
- `tests/test_logical_channel_reconstruction.py`
- `docs/logical_channel_reconstruction.md`
- `docs/t5_3_1_logical_channel_reconstruction.json`
- `docs/t5_3_1_logical_channel_source_data.csv`

正式矩阵含 24 lanes、逐 lane 31 cycle points、17,266 Source Data rows、26/26 gates。cutoff 40 的
QEC-on/off signed-area ratios 为 `1.403--2.029`，但 cutoff 12 为 `0.188--0.311`；低 cutoff 方向反转未隐藏，
只把 36/40 作为 terminal finite-cutoff 稳定参考。

## 验证

- direct + mutation：`33 passed`；
- logical/Fock/SBS/wall-clock/cross-fidelity 相邻组合：`196 passed`；
- max pair-linearity residual `8.73e-14`；min Choi eigenvalue `-1.71e-16`；
- max physical trace/Hermiticity errors `4.44e-16/0`，min eigenvalue `-7.07e-16`；
- terminal 36/40 六条 lanes：PTM difference 最大 `0.011078<0.03`，leakage difference 最大
  `0.000734<0.02`；
- raw-output/PTM/lifetime/comparison/cutoff tables 均可重算，semantic tampering fail closed。

## 风险与论文 claim

新增 R-N092：结果依赖 finite-cutoff、orthonormalized code projector、fixed nominal sBs 和 analytic idle/noise；
36/40 稳定不是无限维证明，conditional Bloch 不是 channel source，area ratio 也不是 break-even。T5.3.2 补
`F_avg/F_e` 与 uncertainty，T5.3.3 冻结同模型 operational boundary。

## 插入任务判断与任务板同步

不插入风险 task。现有 T5.3.2--T5.3.5 已依次承接 fidelity、break-even、cost 和 recovery bound；任务板将
T5.3.1 置为 Done，并启动 T5.3.2。
