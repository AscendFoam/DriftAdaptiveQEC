# T3.2.11：memory-specific 消融

- 日期：2026-07-15
- 状态：Done

## 输入与执行方案

复用 T2.3.7 五个 history NMF checkpoint、T3.2.7 五个同参数/MAC retrained latest-only
checkpoint 与注册的 cutoff12/16 held-out seeds。冻结 shuffle/truncation/reset/latest-only 的
因果语义；分开 frozen intervention 和 retrained capacity comparator；采用五 agent paired
bootstrap，不按 evaluation 选择变体或方向。

## 实际完成内容

- 新增只读 causal intervention 模块，parent weights 不变；
- 初版逐 depth 独立 shuffle 在深审计中被判定混入额外 jitter，已改为固定位置优先级的
  prefix-consistent shuffle 并完整重跑；
- 两 cutoff 各执行 12 个注册变体、5 agents，并保存逐 seed/cycle 曲线；
- full-view bit-exact、intervention action distance、weight hash、density physicality、source hash
  和 leakage evidence boundary 全部机器验证；
- 预注册四对照联合规则在两 cutoff 均失败，结论不支持稳健 cross-cutoff memory mechanism。

## 产物

`physics/memory_specific_ablation.py`、`cnn_fpga/benchmark/memory_specific_ablation.py`、
两份 tests、`docs/t3_2_11_memory_specific_ablation_validation.json`、
`docs/t3_2_11_memory_specific_ablation_source_data.csv`、`docs/memory_specific_ablation.md`。

## 验证结果

- 15/15 gates，28,230 Source Data rows，27 focused tests；
- cutoff12 full/retrained latest/frozen latest `6.740785/6.888249/6.031675`；
- cutoff16 `7.708351/7.168269/8.271987`，冻结 latest/reset 反超 full；
- prefix-consistent shuffle full-minus-shuffle：cutoff12
  `+0.068617 [0.050734,0.084185]`，cutoff16 `+0.046558 [-0.014033,0.107150]`；
- DLEnv 治理/相邻组合 `131 passed`；显式 `tests/` 全量回归
  `1122 passed / 12 skipped / 4 failed`，4 个失败均为 R-N012 已登记的两份 FR8
  历史文档缺失，没有 T3.2.11 新回归。

## 风险与插入任务

新增 R-N062：memory 方向对 cutoff 与 intervention 类型敏感，two-level lane 无 physical leakage，
10-cycle/five-agent 不能外推论文长寿命。T5.2/T5.4.4/T5.4.5 已覆盖 cutoff/OOD/leakage/长时，
不插入新 task。

## 任务板同步

T3.2.11 Done；T4.1.1 In Progress；当前推荐任务为 T4.1.1。
