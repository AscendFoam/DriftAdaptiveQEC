# T7.3.2：CNN-centric / simulator-overfitting 审稿风险回答

- Task ID：T7.3.2
- 日期：2026-07-21
- 状态：Done

## 输入材料

- T6.20.4 的 `NO_GO_MULTIMODE_CAUSAL_HEADROOM` 与 static-mixture exact MLD 零 headroom；
- T6.26.3--T6.26.4 的 learning=`DROPPED_ABSENT`、`GO_RTL_ONLY` 和 no-vote 真值表；
- T4.4.1/T4.4.3/T4.4.4 的 legacy teacher、student 与 matched retention 正结果；
- T5.4.3/T6.17.3 的 legacy tiny-CNN 只读 bit-exact replay 与 same-task eligibility 审计；
- T5.4.6/T5.5.4 的 mismatch 与 full-GRU/student 硬件边界；
- T7.2.6 当前 canonical manuscript 的 Abstract、Introduction、Methods、Discussion 与 Supplement learning 边界；
- nature-response 的 reviewer intake、action mapping、tone、中文核对与 QA 规则。

## 实际完成内容

1. 将任务板问题登记为 `PRQ-CNN-1` pre-emptive reviewer risk，而不虚构 Reviewer 1、编辑决定或逐字评论；submission readiness 固定为 `draft_with_placeholders`。
2. 给出 editor-readable 英文逐点回答：旧 CNN--FPGA framing 容易让 learning 显得过度中心化，但当前论文的两个 primary lane 是 multimode strongest-baseline qualification 与 exact single-mode deterministic RTL；learning 没有独立投票。
3. 直接重算 Phase6D 真值：multimode proposed/static-mixture exact MLD 均 `p_L=0.1119791667`，point/LCB=`0%/0%`；无授权 teacher，T6.26.1--T6.26.2 Dropped，无新 training/checkpoint/quantization/formal-retention。
4. 没有用“旧证据无效”回避历史正结果：披露 72,853 参数 GRU、4-state/95-scalar student、evaluation MSE `6.083136e-6`、minimum retention point/LCB `0.981457/0.944501`、mismatch minimum `0.897630`。
5. 补入真正针对 CNN 的深审：legacy tiny CNN 在 206-sample residual-parameter split 上五次重跑 bit-exact，MSE `8.034045e-6 -> 2.414453e-6`；但只有单个 legacy split、无独立 seed-cluster CI、metric 不是 LER/control gain。T6.17.3 的 16-family registry 中 same-task eligible=`0`。
6. 明确 task-signature 不迁移：legacy finite-cutoff two-level sBs controller/action imitation 不等于 current multimode posterior/logical-coset MLD approximation，也不等于 exact production RTL。
7. 明确 full quantized GRU 只有 72,854-cycle optimistic workload lower bound、无 functional RTL/physical retention，仍 Dropped；历史 distilled student 的资格不迁入当前 exact RTL。
8. 冻结未来 learning promotion 的 13 项门：共同 split、observed-only、matched classical budget、calibration、agreement、LER/worst/OOD retention、runtime/memory/quantization、formal LCB 与真实 cost/compression benefit；任一失败回到 `DROPPED_TO_ABLATION`。
9. 建立 24-row lossless evidence ledger、19 个 live artifact bindings、23 个 machine gates 与逐门 23 个 semantic mutations；包括删除 placeholder、伪造 learning vote、隐藏 legacy 正结果、跨 task migration、truth target、删除 worst-family gate、promote GRU、损坏 Source Data 等失败分支。
10. 将回答映射到当前稿的命名 section，不虚构 line number；输出英文 response package、manuscript checklist、risk flags 与中文核对。

## 产物路径

- `cnn_fpga/benchmark/cnn_centric_reviewer_contract.py`
- `configs/phase6d/t7_3_2_cnn_centric_reviewer_contract.json`
- `tests/test_cnn_centric_reviewer_contract.py`
- `docs/cnn_centric_reviewer_response.md`
- `docs/t7_3_2_cnn_centric_reviewer_contract.json`
- `docs/t7_3_2_cnn_centric_reviewer_source_data.csv`

## 验证方式和结果

- verdict：`PASS_CNN_NONCENTRIC_REPLACEABLE_LEARNING_REVIEWER_RESPONSE`；
- gates：23/23；semantic mutations：23/23；
- Source Data：24/24 rows，row ID 唯一，逐行 canonical JSON/SHA-256 可逆；
- 19/19 parent/config/code/manuscript/risk artifact bindings live；
- focused tests：12 passed；
- 联合 reviewer/Phase6D/governance 回归：111 passed（使用 workspace-local `--basetemp`，覆盖因果消融、learning eligibility、双 lane gate/evidence、稿件 delta 与任务板治理）。

## 深审与非简化实现检查

本 task 没有用一句“CNN 不是重点”形式化完成。合同从五组 legacy learning artifacts 和四组 Phase6D/current-manuscript artifacts重算数值、scope、eligibility 与 verdict。尤其补查了最容易被遗漏的 tiny-CNN checkpoint：它确实在自己的参数回归 split 上改善 MSE，因此不能写成“CNN 无效果”；但 206 样本、无 cluster CI、非 LER/control metric、16-family same-task eligible=0 同样意味着它不能回答当前 multimode generalization。

当前主张抵御 overfitting 质疑的逻辑不是“证明 CNN 泛化”，而是 deletion invariance：删除 CNN/teacher/student 后，`MM=false, RTL=true -> GO_RTL_ONLY`、multimode strongest-baseline NO-GO 和 exact RTL property verdict 均不变。未来若重开 learning，则需按新的 observed-only/matched-budget/held-out/OOD/formal-retention 协议重新获得资格。

## 风险复核

- R-N151 维持 `Mitigated/High/Monitor`，缓解证据扩展为 reviewer-level legacy-positive disclosure、task-signature nonmigration 与 23 类 mutation。
- package 不标 `ready_to_submit`：真实 reviewer ID 和逐字评论尚未提供，保留 `ACTUAL_REVIEWER_ID_AND_VERBATIM_WORDING` placeholder。
- 不新增风险或插入 task；T7.4.3 已承接最终发布中删除 Dropped/placeholder 或迁移 legacy evidence 的 mutation。

## 对任务板的同步

- T7.3.2：`In Progress -> Done`；
- T7.3.3：`Todo -> In Progress`，成为当前推荐任务；
- T7.3.1 继续 `Blocked`，没有用本 task 绕过 exact/oracle MAP 缺口。

## 对论文 claim 的影响

允许：当前论文是 contract-centric 双 lane；learning 是可替换、当前 Dropped 的 approximation extension；legacy CNN/teacher/student 仅作 task-local historical evidence。

禁止：声称当前 CNN 已排除 simulator overfitting、legacy retention 验证 Phase6D、student 证明 multimode SOTA 或 RTL safety、当前 exact RTL 执行 learned decoder。
