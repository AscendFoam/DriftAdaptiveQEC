# T3.2.3 任务记录：sliding-window syndrome estimator

**日期：** 2026-07-15  
**状态：** Done

## 输入与通过标准

- `docs/new_task_board.md` T3.2.3、`experiment_plan.md:577`；
- T3.2.2 full-covariance periodic moment 与 latest-window baseline；
- 通过标准：多窗口候选、相同 observation/update bandwidth、one-window causality、training-only
  selection、逐 seed paired evidence 和 incremental cost，不得重复包装 fixed-window demo。

## 执行方案与实际完成

实现 96-sample joint circular-feature chunk 的增量 add/remove estimator，在每次 384 新 observations、
一次 update 的固定预算下扫描 384--1536 history。3 个 training seeds 上按独立 next-window circular-
moment score 冻结选择，8 个 evaluation seeds 只做验证。完整候选面、逐场景 diagnostics、proper
scores、tracking、Student-t CI、storage proxy 与 claim boundary 均保存。

## 产物

- `cnn_fpga/decoder/sliding_window_syndrome.py`
- `cnn_fpga/benchmark/sliding_window_syndrome_estimator.py`
- `tests/test_sliding_window_syndrome.py`
- `tests/test_sliding_window_syndrome_estimator.py`
- `docs/sliding_window_syndrome_estimator.md`
- `docs/t3_2_3_sliding_window_validation.json`
- `docs/t3_2_3_sliding_window_source_data.csv`
- `docs/new_tasks/T3.2.3_sliding_window_syndrome_estimator.md`
- 更新 comparison registry、既有 source-bound artifacts、README、risk 和 task board。

## 生产验证

- 6 window candidates、4 scenarios、8 seeds、1,572,864 paired evaluation samples；
- 32-row Source Data、32 unique trace hashes、14/14 machine gates；
- training 选择 384，evaluation aggregate 事后最低也为 384；selected-minus-latest 严格为零；
- static-minus-selected `0.009740 [0.009529,0.009951]`；四场景 CI 下界均正；
- focused `25 passed`；registry/source-bound adjacent `217 passed`；
- standard/static/oracle/static-protocol/top-K/T3.2.1/T3.2.2 七份 artifact 按最终 registry 重生成，
  分别通过 `15/15、9/9、10/10、11/11、8/8、9/9、15/15` gates。
- 显式 `tests/` 全量 `978 passed, 4 skipped, 4 failed`；4 个失败仍仅为 R-N012 的两份缺失
  FR8/P4 历史文档，不涉及本任务实现或证据。

## 反简化检查与修复

1. 六候选覆盖 384--1536，不删除训练边界最优或 evaluation 负结果。
2. 增量 feature sums 与 raw rolling recomputation 对齐；384 与 latest estimator 对齐。
3. 长窗只存 4-complex chunks，不存 raw history；storage 20--68 complex values 单调。
4. evaluation per-scenario 最佳明确标 diagnostic-only，不作为 selector 或 claim。
5. 所有候选同 trace、同 384 observations、同一次 update、同 one-window delay。
6. truth/state-free signature、非法时间戳/数据/候选负测和 source-bound artifact tests 完整。

## 风险与 claim 影响

新增 R-N054：384 边界选择依赖当前 smooth wrapped-Gaussian mixture、uniform weighting、固定 384/1
预算和 periodic-moment score；局部场景不同最佳窗提示 distribution shift 下固定窗不稳。T3.2.6、
T5.1/T5.4 做 regime/OOD，T5.5/T6 做真实 memory/latency/device 证据。

允许报告 long uniform history 的 aggregate 负结果与 scenario-specific trade-off；禁止把事后逐场景最小
拼成在线 selector，也禁止 universal window、CNN 或硬件 claim。

## 任务板同步

T3.2.3 更新为 Done；T3.2.4 更新为 In Progress；当前推荐切换为 T3.2.4。
