# T2.2.2：协议原生 ancilla/readout/reset 误差

**Task ID：** T2.2.2  
**日期：** 2026-07-14  
**状态：** Done  
**实现：** `physics/protocol_ancilla_errors.py`  
**测试：** `tests/test_protocol_ancilla_errors.py`

## 1. 输入与执行方案

输入为 T2.0.1 protocol hierarchy、T2.0.3 sBs hidden/observed/reset kernel、
T2.0.4 cycle FSM、T2.2.1 physical/observation lane，以及 Sivak 2023 与
Campagne-Ibarcq 2020 的 ancilla-fault 机制说明。

执行方案：

1. 复用 sBs 既有 4×3 readout/reset model，只叠加 constituent × stage fault；
2. sBs bit/phase/readout/backaction 分开，禁止把 phase flip 写成 Z-basis toggle；
3. sharpen--trim 单独实现四轮、`+y/-y`、feedback、reset 与 carry；
4. physical fault truth 与 deployable record 结构隔离；
5. secondary protocols 只给 `executable=false` noise-shaping registry；
6. 用确定性 fault、持久性、schema、negative mixing、统计率和 production seed 验证。

## 2. 实际完成内容

- `SBSAncillaFaultOverlay`：X/Z × small-CD/big-CD/readout bit/phase fault；
- bit parity、middle-big-CD logical backaction、small signed backaction；
- T2.0.3 full classifier/reset/leakage carry 原样接线；
- misclassification-triggered bounded virtual-rotation truth；
- sharpen--trim 四轮 native state machine、3×2 confusion 与三类 reset success；
- bit/phase Y-basis parity、peak middle-window X/Z logical backaction；
- deterministic Pauli frame 与 hidden stochastic physical fault 分离；
- hidden leakage/reset failure carry，不泄漏至 deployable record；
- Steane/Knill/qunaught/P-Steane fail-closed registry；
- CLI、JSON writer、public lazy exports 与协议层级同步。

## 3. 反简化检查

- direct `27 passed`，含 20,000-round middle-window 与 20,000-round full
  leakage-confusion calibration；
- 80,000-cycle sBs 与 80,000-round sharpen--trim production 真跑；
- sBs big-CD bit `0.020175` 对预期 `0.020000`；
- sBs logical backaction `0.009875` 对预期 `0.010000`；
- sBs phase-induced Z-basis toggle 精确为 0；
- sharpen feedback mismatch `0.073788` 对解析 `0.071648`；
- middle-window/logical backaction 为 `0.010325/0.005213`；
- seed replay/prefix、双 fault parity、reset/leakage persistence、非法输入均通过；
- adjacent sBs `115 passed`；
- full `tests/` 为 `494 passed,4 failed`，失败仍仅 R-N012 两个 legacy 文档缺失。

首轮审计发现并修正两处会形成伪实现的问题：

1. 移除了 deployable sharpen--trim record 中由 hidden truth 计算的
   `leakage_run/reset_failure_run`；
2. stochastic bit-flip logical backaction 不再写入 controller-visible Pauli frame，
   只保留为 simulator truth。

## 4. 产物

- `physics/protocol_ancilla_errors.py`
- `tests/test_protocol_ancilla_errors.py`
- `docs/protocol_ancilla_errors.md`
- `docs/t2_2_2_protocol_ancilla_validation.json`
- `docs/tasks/T2.2.2_protocol_ancilla_errors.md`
- `docs/new_tasks/T2.2.2_protocol_ancilla_errors.md`
- 更新 `physics/__init__.py`、`docs/protocol_hierarchy.md/.json`、README、风险和任务板

## 5. 验证

```text
py_compile + CLI/JSON: passed
direct protocol ancilla: 27 passed
adjacent sBs: 115 passed
explicit tests/: 494 passed, 4 failed (R-N012 only)
production: 80,000 sBs cycles + 80,000 sharpen-trim rounds; all gates PASS
```

## 6. 风险与 claim

- R-N025 降为 Mitigated：代码和测试已经强制 protocol-native alphabets；
- R-N027 保持 Open：full confusion/reset/fault rates 仍未设备校准；
- 新增 R-N035：stage-window 与 continuous/logical backaction 仍是 effective
  stochastic assumption，尚未与 T2.2.1/Fock/master-equation 联合校准；
- 不插入新 task，正常 T2.2.3、T2.3.1--T2.3.3、T5.2.3 和 T6 已覆盖。

允许写 protocol-native effective error flow 已实现；禁止写 device-calibrated ancilla
error budget、master-equation fidelity、secondary protocol execution 或实验 lifetime。

## 7. 治理同步

- T2.2.2：`In Progress -> Done`；
- T2.2.3：`Todo -> In Progress`；
- 当前推荐任务：`T2.2.3`。
