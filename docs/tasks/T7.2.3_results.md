# T7.2.3：Results 与结果状态证据合同

- Task ID：T7.2.3
- 标题：起草 Results
- 日期：2026-07-20
- 状态：Done

## 输入材料

- T6.7.1--T6.7.4 的 V4 formal comparison、后验安全、故障注入、CXXRTL 与综合/P&R 证据；
- T6.10.1 与 T6.15.5 的 V5 causal-headroom 诊断、early-stop verdict 和 absence proof；
- T6.17--T6.19 的 Phase 6C 独立 CI/ML/CPD/AQEC/FPGA comparison lanes；
- T7.1.1 的 claim--evidence--boundary matrix，以及 T7.1.3/T7.1.4 的主图与补图合同；
- T7.2.1/T7.2.2 的 Introduction/Related Work 与 Methods prose contracts。

## 实际完成内容

1. 把主 note 的 Results 重构为 10 个有序小节：先冻结结果状态与排名约定，再报告 V4 smooth、action/update、abrupt/OOD、确定性执行；随后报告 V5 causal-headroom 诊断和 early stop，再报告 eligible Phase 6C secondary lanes，最后才保留 legacy CNN、teacher/student 和 Supplement failure-domain 结果。
2. 新增六态结果合同：`PRIMARY_RESTRICTED`、`MANDATORY_NEGATIVE`、`DIAGNOSTIC_STOP`、`SECONDARY_ELIGIBLE`、`NULL_OR_CONTEXTUAL` 与 `HISTORICAL_EXTENSION`，避免把主结果、必须披露的负结果、停止诊断、独立 secondary 结果、空证据和历史扩展混排。
3. 主 Results 同时披露 strongest deployable Window、static、oracle gap、tail、fallback/false-update、recovery cost，不再只保留 locked-EWMA 的窄正结果。BOCD matched-budget 失败被量化为 paired LER 差 `1.00184e-5`、95% CI `[7.66793e-6, 1.23689e-5]`，且 worst update `13004.1 us` 超过 `5000 us` budget并出现 1 次 deadline miss。
4. 对 V4 策略族保留不可约的负证据：static-switch 与 freeze-all 均未通过全部 38 个 posterior-safe pilot tuple；最接近安全门的最小 slack 分别为 `-0.037109375` 与 `-0.044921875`。RTL 故障覆盖以 75 次 host-commit attempt 与 25 次 rollback attempt 为分母，而不是只写“零错误”。
5. V5 仅作为诊断性停止结果：绑定 71,958,528 个 formal diagnostic decisions、186 条 trajectory/4,571,136 个全新 development decisions、负 strict-causal headroom、低于 `0.001` 的 action-only headroom、20 个 Dropped tasks、0 个 downstream outputs，并明确不存在 V5 formal manifest、quantized retention、CXXRTL 或 P&R 结果。
6. Phase 6C 只允许五条由 T7.1.1 派生的 `OFFICIAL_CODE_REPRODUCTION` 或 `PROJECT_NATIVE_MATCHED` secondary 结果进入正文：single-mode CI/Euclidean CPD、Two-GKP CNOT CI/ML、structured CPD、posterior-weighted multimode CPD 和 AQEC project-native replay。learned 0/16、GQF 0/15+13 null、official AQEC blocked 与外部 FPGA 0 个 exact same-task comparator 只作为资格/空结果边界；Results 中 literature-only 数值计数为 0。
7. structured CPD 明确区分 official-data CPD/analog 均值 `0.602456/0.599594` 与独立 `d=3,5,7` crossing 均值 `0.599298/0.600245`，并报告 27/27 cells 中 CPD LER 更低，避免把两类实验混成同一 threshold claim。
8. teacher/student 选择失败不隐藏：teacher restarts 为 601/709/811、caps 为 `[0,2]`；test-hindsight reversal 未替换冻结选择；执行 1/2/4-state × 3 次 student fits，其中六个 2/4-state fits 达到 900-epoch cap，evaluation 未参与选择。
9. 实现 `results_evidence_contract.py`，生成 27-row JSON/CSV/Markdown 结果合同；20 个 gate 与 20 个定向语义 mutation 逐项验证 subsection 顺序、V4 正负边界、V5 absence、Phase 6C eligibility、selection multiplicity、board-null 与 literature non-ranking。
10. 视觉深审发现 teacher/student 浮动图会把 “27 MHz” 所在句子跨整页切断；将图定义移到 subsection 末尾后重新编译和全页复核，最终稿由 42 页收敛为 41 页，未通过缩字号或删除负结果规避版面问题。

## 产物路径

- `docs/paper_notes/Contract_Centric_Regime_Aware_GKP_note_draft.tex`
- `cnn_fpga/benchmark/results_evidence_contract.py`
- `tests/test_results_evidence_contract.py`
- `docs/results_evidence_contract.md`
- `docs/t7_2_3_results_evidence_contract.json`
- `docs/t7_2_3_results_evidence_source_data.csv`

## 验证方式和结果

- generator verdict：`PASS_RESULTS_COMPLETE_NEGATIVE_AND_SECONDARY_BOUNDARIES`；27/27 result rows、20/20 gates、20/20 targeted mutations；
- focused tests：`10 passed`；Introduction/Methods/claim/figure/governance 联合回归 `60 passed`；
- TeX Live/BibTeX：41 页、1,113,584 bytes；overfull、undefined reference/citation、LaTeX/package warning 与 fatal error 均为 0；源码 lone CR 为 0；
- PDF：100-dpi 渲染并检查 5 张 contact sheet；对 Results 关键页和移动后的 teacher/student figure 页做高分辨率复核，未见裁切、重叠、孤立标题、句子被整页浮动图劈开或表格越界；
- 所有 report/raw/code/board 绑定均使用 live hash；任务板最终同步后重新生成并 `--verify`，防止治理变更造成陈旧合同。

## 深审与非简化实现检查

本 task 没有把“整理 Results 文本”视为完成。27 条结果逐行绑定可追溯父证据，Phase 6C 的嵌套 parent/raw hashes 也被实时复算；任何将 V5 Dropped 写成 implemented、删除 mandatory negative、把 literature 数值放入项目 Results、用 P&R estimate 填充 board measurement、改变 eligible secondary 集合或隐藏失败尝试分母的修改都会 fail closed。

深审重点修复了四类易形成 demo 叙事的问题：第一，主结果不能只展示 EWMA 窄正结果，必须让 Window/static/oracle-gap 与 tail/cost 负结果共同出现；第二，V5 设计清单不能伪装成已运行结果，正文现在同时报告 early-stop 与缺失产物；第三，Phase 6C 不能按方法类别混排，eligibility 由既有 claim placement 和 evidence grade 派生；第四，teacher/student 不能只报最佳一次，restart、cap、hindsight disagreement 与未参与选择的 evaluation 均进入正文和机器合同。本 task 未生成新的实验数值，也未用后验调参来营救既有结果。

## 风险复核

R-N142 维持 `Mitigated / High / Monitor`：Results 的 V4/V5/Phase 6C 拼接风险已由 27-row 六态合同显著缓解，但 Discussion/Conclusion、摘要和最终发布包仍可能重新升级 claim。R-N141 的外部 FPGA same-task comparator=0 边界保持；R-N145 的 immutable historical snapshot 与环境锁仍需 T7.4.1 完成。T6.9.2 继续 Blocked，42 个板测字段均为 null。

## 是否需要插入新 task

否。T7.2.4--T7.4 已覆盖 Discussion/Conclusion、Supplement、审稿风险和最终发布治理；性能 rescue、V5 结果填空或伪板测旁路会违反既有 early-stop 与 pre-board 边界。下一步按顺序执行 T7.2.4。

## 对任务板的同步

- T7.2.3：`In Progress -> Done`；
- T7.2.4：`Todo -> In Progress`，成为当前推荐任务；
- T7.2.3 的产物和验证摘要已写回 `docs/new_task_board.md`。

## 对论文 claim 的影响

允许表述：V4 是一个 observed-only、fail-closed、确定性 pre-board 自适应系统；其相对静态/Window 的正负结果、tail 安全代价和硬件估计同时披露；V5 causal/action headroom entry diagnostic 给出 early NO-GO；五条 Phase 6C 结果只支持各自 task signature 下的 secondary claim。禁止表述：V5 已实现或通过 formal/RTL/P&R、项目已取得真实板测 latency/power、外部 FPGA 同任务速度 SOTA、learned/NMF 获得 eligible 同任务胜出，或把 task-local CPD/AQEC 结果升级为全局 decoder 排名。
