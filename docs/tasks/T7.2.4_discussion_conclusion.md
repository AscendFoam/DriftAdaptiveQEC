# T7.2.4：Discussion/Conclusion 与物理证据边界合同

- Task ID：T7.2.4
- 标题：起草 Discussion/Conclusion
- 日期：2026-07-20
- 状态：Done

## 输入材料

- T7.1.1 的 29 条 claim--evidence--boundary matrix 与 T7.2.1--T7.2.3 三份正文合同；
- T6.7/T6.9 的 V4 正负结果、预板确定性证据和 42 项 board-null blocker；
- T6.10.1/T6.15.5 的 V5 causal/action headroom early-stop 与 20 Dropped/0 downstream absence proof；
- T6.16--T6.19 的 CI/ML/CPD/AQEC/NMF/FPGA task-signature comparison lanes；
- `experiment_plan.md §14.4`、T0.1.1 scope freeze、Phase 8 真实数据/量子控制接入任务与 GKP 指标分析。

## 实际完成内容

1. 将 Discussion 重构为八个有序小节：evidence-supported contribution、regime-awareness interpretation、safety/performance/cost、computational/hardware cost、external validity、prior decoder classes、failure-informed priorities 和 staged physical transition。
2. 把论文贡献明确收缩为 integration/falsification：MAP 负责 LER，HMM/event/fallback/rollback 负责安全，FPGA-facing path 负责确定性执行，CNN/teacher/student 是可替换扩展；不把系统写成 winning adaptive decoder 或 physical QEC experiment。
3. 解释为何正负结果符合或不符合直觉：locked EWMA 的 paired 正结果只对该预注册对照有效；static joint MAP 与 Window MAP 的胜出否决 best-decoder claim；V5 truth-oracle gain 未能转化为 observed-only causal/action headroom，因此未来必须先证明 prospective identifiability 与 realizable action value。
4. 新增完整成本表，区分 per-round decoder error、4000-cycle host cadence、59--96% tail intervention occupancy、six-cycle/II=1 fast path、one-million-cycle CXXRTL、三 seed P&R estimate、offline learning 与缺失 physical loop。明确当前 HMM 在软件慢回路，且没有 PPO/CNN/RNN/HMM 板上训练。
5. 冻结外部效度：现有模型是 single-mode square-lattice syndrome/effective stack 加 task-local finite-energy/cutoff cross-check，不是 calibrated cavity--transmon driven open-system twin；synthetic held-out drift 不证明真实设备中的频率/幅度/持续时间；per-round LER 不能冒充 physical decay/lifetime/beyond-break-even；single-mode 结果不支持 outer-code、surface-code 或 fault-tolerant threshold。
6. 分别讨论 static GKP、一般 drift-adaptive、Puviani NMF 与 FPGA decoder：当前只支持 locked-baseline/safe-publication/pre-board integration 的受限主张；官方 GQF/NMF matched lifetime 仍 blocked，18 个外部 FPGA implementation 中 exact same-task comparator=0，Phase 6C 正结果不营救 V5。
7. 新增六级单调证据阶梯：real-data intake、prospective shadow mode、named-board/HIL、guarded QPU frame feedback、physical effectiveness、physical break-even。每一级规定输入、动作权限、fresh split/run 和 promotion evidence；offline data 不授权 actuation，board HIL 不证明 quantum benefit，guarded QPU 不自动证明 break-even。
8. 将真实 break-even 冻结为同设备、同 wall-clock/cycle/duty/initialization/readout 边界下 corrected 与 best-physical reference 的平均逻辑信道/有效衰减比较，并要求 $G_{QEC}=\Gamma_{best\ physical}/\Gamma_{logical}>1$ 的 simultaneous confidence lower bound 大于 1；correction overhead、rejected shots、leakage 与 non-exponential decay 必须保留。
9. 重写 Conclusion，使窄正结果、static/Window 负结果、tail 高干预成本、V5 stop、learned/GQF/FPGA null 和 42 board-null 同时出现；明确无 calibrated cavity/transmon、无真实 logical lifetime/beyond-break-even、无 board-resident training、无 measured speed/power 和无 closed-loop QPU。
10. 实现 27-row 六态 Discussion/Conclusion machine contract、CSV/Markdown、22 个 fail-closed gates、22 个逐门语义 mutation 与 10 项 direct tests；绑定三份既有 prose/results contracts、claim matrix、V4/V5/Phase6C verdict、scope/plan/metric report 与 task board。

## 产物路径

- `docs/paper_notes/Contract_Centric_Regime_Aware_GKP_note_draft.tex`
- `cnn_fpga/benchmark/discussion_conclusion_contract.py`
- `tests/test_discussion_conclusion_contract.py`
- `docs/discussion_conclusion_contract.md`
- `docs/t7_2_4_discussion_conclusion_contract.json`
- `docs/t7_2_4_discussion_conclusion_source_data.csv`

## 验证方式和结果

- generator：`PASS_DISCUSSION_LIMITATIONS_COST_AND_PHYSICAL_TRANSITION_BOUNDARIES`；27/27 rows、22/22 gates、22/22 targeted mutations；
- focused tests：`10 passed`；Discussion/Results/Methods/Introduction/claim/figure/governance 联合回归 `70 passed`；
- 四份正文合同均通过 `--verify`，父 source/hash、gate snapshot、mutation 和 fresh-analysis hash 全部 live；
- TeX Live/BibTeX：45 页、1,128,603 bytes；overfull、LaTeX/package warning、undefined reference/citation、fatal error 均为 0，源码 lone CR 为 0；
- PDF：100-dpi 渲染 45/45 页并生成 5 张 contact sheet；重点高分辨率检查 Discussion 页 28--34 和尾部 37--45，未见裁切、重叠、表格越界、乱码或孤立结论。

## 深审与非简化实现检查

本 task 没有把“写几段限制”视为完成。27 条讨论状态逐项覆盖已建立解释、必要负面、成本、外部效度、future-only gate 与 Conclusion boundary；任一把 simulator 升级为 cavity/transmon、per-round LER 升级为 physical lifetime/break-even、P&R 升级为 board measurement、offline learning 升级为 on-board training、Phase 6C 升级为 V5 或删去 static/Window/42-null 的修改都会 fail closed。

视觉深审发现两个非语义但会误导阅读的问题：terminal verdict table 被延迟到 Discussion 中间，隔整页切断 “prospective identifiability and action-value headroom” 句子；Conclusion 的最后一行被挤到单独空白页。最终通过在终态表后显式 flush 浮动体并精简重复结论修复；没有缩字号、删除负结果或放宽表格内容。当前 45 页中终态表独立成页，Discussion 连续，Conclusion 完整落在一页。

## 风险复核

- R-N142 维持 `Mitigated / High / Monitor`：新增 27-row、22-gate 合同封住 Discussion/Conclusion 的 V4/V5/Phase6C/board 拼接，但 Supplement 与最终发布仍需继续消费；
- R-N005 维持 `Open / High / Soon`：正文现在明确 effective model 与 cavity/transmon 物理实验的断点，物理缺口只能由 T8 真实数据/控制链补齐；
- R-N110 维持 `Open / High / Soon`：四类 prior comparison 已按 task signature 分开，尚无官方 GQF matched lifetime 或 FPGA exact same-task board comparator。

## 是否需要插入新 task

否。T7.2.5 已覆盖 Supplementary，T7.3/T7.4 覆盖审稿与发布，T6.9.2 和 Phase 8 已覆盖板测、真实数据与可选 QPU 接入。为补正结果而新增 V5 rescue、伪 lifetime 或跨任务排行榜会违反已冻结终态。

## 对任务板的同步

- T7.2.4：`In Progress -> Done`；
- T7.2.5：`Todo -> In Progress`，成为当前推荐任务；
- 产物、验证与不插入判断已写回任务板和风险表。

## 对论文 claim 的影响

允许表述：项目提供 observed-only、fail-closed、deterministic pre-board integration；locked-EWMA 对照存在窄正结果；V5 headroom audit 是有价值的 negative stop；真实升级路径已经预先定义。禁止表述：winning/SOTA adaptive decoder、真实 cavity/transmon、真实 logical lifetime 或 beyond-break-even、board-resident training、measured FPGA latency/power/fastest、surface-code threshold，或以 Phase 6C/NMF/legacy CNN 结果替代 V4/V5 的终态。
