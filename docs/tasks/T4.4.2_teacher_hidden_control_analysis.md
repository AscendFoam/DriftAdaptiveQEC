# T4.4.2 任务记录：分析 teacher hidden state 和控制轨迹

- Task ID：T4.4.2
- 日期：2026-07-15
- 状态：Done

## 输入与执行方案

输入为 T4.4.1 selected fresh teacher、canonical 15-residual map 和 differentiable sBs simulator。冻结 teacher
后分别执行 128-half-cycle native response、20-half-cycle forced-path `p(g)`、trajectory-disjoint belief probe、
PCA、30 个单指数拟合、双向 impulse memory 与 g/e fixed-point Jacobian。leakage 只允许 OOD reset+nominal proxy。

## 实际完成与产物

新增 `bounded_residual_teacher_analysis.py`、13 项直接测试、中文报告、2,089-row CSV 和 source-bound JSON；
同步 benchmark/根 README、protocol hierarchy、风险与任务板。

## 验证结果

- 17/17 gates；native replay/repeat/bound error `0`；
- hidden/control 的 95% PCA dimensions 均为 1，hidden 99% 为 2；
- hidden→`p(g)` evaluation `R²=0.667797`，observed-feature probe `0.487393`；
- 30 个 run-parameter single-exponential fits 中 28 个 `R²≥0.95`，virtual rotation 两项保留反证；
- impulse control 1% memory 为 10/12 half-cycles；Jacobian radius g/e `0.618031/0.596212`；
- focused `13 passed`，T4.4.1/T2.3.7/T3.2.9 邻接 `87 passed`；显式 `tests/` 全量为
  `1484 passed, 19 skipped, 4 failed`，4 项仍仅为 R-N012 两份旧 FR8/P4 文档缺失。

## 风险、claim 与插入任务

新增 R-N076：低维、指数和 belief-like 都是 fixed/probe trajectory 上的 post-hoc matched-model 结构；无
leakage-trained response、唯一机制、student gain、OOD/长时/device。任务板既有 T4.4.3--T4.4.5、T5/T6 已覆盖，
不插入风险任务。
