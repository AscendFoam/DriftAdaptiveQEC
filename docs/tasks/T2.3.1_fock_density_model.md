# T2.3.1：finite-cutoff approximate GKP 密度矩阵模型

**Task ID：** T2.3.1  
**日期：** 2026-07-14  
**状态：** Done  
**实现：** `physics/fock_density_model.py`  
**测试：** `tests/test_fock_density_model.py`

## 输入与执行方案

输入为 T1.2.1 normalized damped-projector wavefunction、T2.2.1 finite-squeezing
effective model 及任务板要求的七类物理作用。环境审计显示 NumPy/SciPy 可用，qutip、
Strawberry Fields、thewalrus 不可用；旧 fallback 又是启发式 visualization，故采用
SciPy finite-Fock 独立 reference，先冻结 trace/PSD/cutoff contract，再逐通道用解析
恒等式验收。

## 实际完成

- normalized wavefunction 到 Hermite/Fock basis 的 Simpson projection；
- immutable、trace-one、Hermitian、PSD density wrapper 与 diagnostics；
- displacement matrix exponential；
- exact pure-loss Kraus map；
- sparse thermal Lindblad exponential action；
- exact Gaussian phase-diffusion coherence map；
- Kerr/anharmonicity diagonal unitary；
- modular binary POVM、Lüders conditional/nonselective backaction；
- 明确非 transmon 的 CPTP high-Fock leakage proxy；
- cutoff/grid convergence、解析基准、CLI/JSON 与 public lazy exports。

## 反简化检查

- direct `38 passed`（T2.3.2 增补 coordinate-scale dilation 的 5 项回归）；
- adjacent finite-energy/finitesqueezing `81 passed`；
- full `tests/`：`562 passed, 4 failed`，仍仅 R-N012 两份 legacy 文档缺失；
- cutoff captured probability 单调增至 `0.9999960`；
- 最高相邻嵌入 fidelity `0.9999249`；
- loss/thermal/phase/Kerr/measurement 解析误差分别为
  `4.44e-16/4.16e-17/5.55e-17/3.47e-18/0`；
- 组合通道、top cutoff、zero-contrast、eta endpoint、zero-temperature
  Lindblad-vs-Kraus 和 invalid inputs 均有 direct tests；
- production 10 项 gate 全 PASS。

## 产物

- `physics/fock_density_model.py`
- `tests/test_fock_density_model.py`
- `docs/fock_density_model.md`
- `docs/t2_3_1_fock_density_validation.json`
- `docs/tasks/T2.3.1_fock_density_model.md`
- `docs/new_tasks/T2.3.1_fock_density_model.md`
- 更新 public exports、protocol hierarchy、README、风险和任务板

## 风险与 claim

新增 R-N037：有限 cutoff 和单腔模模型尚不含 transmon、pulse Hamiltonian 或完整协议
周期；high-Fock proxy 不能写成 `|f>` leakage。允许写 independent finite-cutoff
density-matrix validator；禁止写 device/master-equation/full-cycle validation。

## 治理同步

- T2.3.1：`In Progress -> Done`；
- T2.3.2：`Todo -> In Progress`；
- 当前推荐任务：`T2.3.2`。

## T-RISK-20260714-01 回灌

标准 damped-projector Fock preparation 现只接受完整的 decoder→canonical `sqrt(2)`
wavefunction bridge；任意 dilation 留在低层 exploratory API。production cutoff 更新为
`18/24/30/36`，最高 capture `0.99999961`、最后相邻 embedding fidelity
`0.99999882`，两项 gate 均收紧到 `>0.99999`。行为变化修复 width/envelope/Jacobian
漏缩放，不升级 device claim。
