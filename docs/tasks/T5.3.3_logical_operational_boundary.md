# T5.3.3：定义 simulated break-even / operational boundary

- 日期：2026-07-16
- 状态：Done

## 输入与执行方案

输入为 T5.3.1 的 matched six-state CPTNI channel、T5.3.2 的 leakage-inclusive `F_avg` 与 active short-time
rate 失败分支，以及本地 beyond-break-even 论文的 best-passive/coherence-gain 定义。执行方案冻结为 full
curve 两阶段门：先要求 pointwise sustained noninferiority，再要求累计 fidelity deficit 得到偿还；两者均需
36/40 terminal cutoff repeat，且 cutoff12 反例必须保留。

## 实际完成内容与产物

- `physics/operational_boundary.py`
- `cnn_fpga/benchmark/logical_operational_boundary.py`
- `tests/test_operational_boundary.py`
- `tests/test_logical_operational_boundary.py`
- `docs/logical_operational_boundary.md`
- `docs/t5_3_3_logical_operational_boundary.json`
- `docs/t5_3_3_logical_operational_boundary_source_data.csv`

正式结果：36/40 下 high/medium/low sustained boundaries 为 `40/40/60 us`，cumulative payback 为
`60/90/110 us`，两个 cutoffs 的 sampled spread 均为 0。cutoff12 三条均未在 300 us 内建立边界。artifact
包含 12 comparisons、416 rows、25/25 gates。

## 验证与详细检查

- core + report/semantic mutations：`36 passed`；
- T5.3.1--T5.3.3/logical-channel/protocol 相邻收口组合：`152 passed`；
- 每个 comparison 保留全部 31 点，validator 从 parent full curves 重算；
- 早期 penalty、1--3 次 sign reversals、cutoff12 失败和 active-rate unreliable 全部机器门约束；
- parent/paper/implementation provenance 与 17 类结论篡改均 fail closed；
- 不生成 ratio、指数拟合、fake CI、full-cost verdict 或 coherence-gain 数值。

## 风险与论文 claim

新增 R-N094：wall-clock operational boundary 容易被误读为 paper-defined coherence gain。qec-off 只是同编码
matched idle，active control cost 仍未统一定价；因此 coherence gain、full-cost 与 experimental break-even
全部保持 `NOT_ESTABLISHED`。

## 插入任务判断与任务板同步

不插入风险 task。T5.3.4 已明确负责 active pulses、repeats、reset/post-selection、squeezing、classical
resource、latency 和 achieved performance 的统一成本账本。任务板将 T5.3.3 置为 Done，并启动 T5.3.4。
