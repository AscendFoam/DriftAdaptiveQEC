# 旧任务记录（兼容目录）

本目录保存旧任务链记录和仍被历史文档引用的兼容副本，不再接收新的任务记录。当前任务状态与新增完成记录分别使用 [`../new_task_board.md`](../new_task_board.md) 和 [`../new_tasks/`](../new_tasks/)。

目录中同名文件可能与 `new_tasks/` 处于不同历史时点，不能按文件名直接覆盖或去重；只有在引用和证据哈希完成迁移后才可删除。
