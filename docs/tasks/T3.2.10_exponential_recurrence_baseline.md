# T3.2.10：PRL-inspired 指数递推 baseline

- 日期：2026-07-15
- 状态：Done

## 输入与执行方案

输入为 Puviani Supplement 的 exponential-saturation 公式、T2.3.4/T2.3.5 differentiable
trajectory/gradient、T3.2.5 run-length event traces 与 T3.2.9 lookup checkpoint。先冻结 causal
`g/e/leakage` recurrence 与参数量，再分别建立 exact physical fidelity lane 和 abstract event-cost
lane；加入 multi-start/refinement、cutoff transfer、integer mirror、checkpoint replay 和同轨 FSM。

## 实际完成内容

- `physics/exponential_recurrence_control.py`：75-trainable/105-stored 15-control recurrence，
  exact 16-branch objective、3 restarts×`300+250` epochs、software fixed point；
- `cnn_fpga/runtime/exponential_recurrence.py`：X/Z/leakage 三状态 scalar kernel、health priority、
  hysteresis、atomic ParamBank、float/Q4.16 execution；
- `cnn_fpga/benchmark/exponential_recurrence_baseline.py`：lookup/standard/cutoff12/16、72-grid
  recurrence 与 24-grid FSM、384k paired event evaluation、checkpoint/CSV/gates；
- 修正审计中发现的 ordinary `e` 被误并入 leakage score 的错误；leakage score 现在只由
  explicit leakage branch 激活，X/Z recovery evidence 独立；
- 物理 lane 明确 two-level 无 leakage sample，固定 leakage branch 不冒充物理训练。

## 产物

- 核心、runtime、runner 与三份测试；
- `docs/t3_2_10_exponential_recurrence_validation.json`；
- `docs/t3_2_10_exponential_recurrence_checkpoints.pt`；
- `docs/t3_2_10_exponential_recurrence_source_data.csv`；
- `docs/exponential_recurrence_baseline.md`。

## 验证结果

- 19/19 production gates；1,888 Source Data rows；
- cutoff12 standard/recurrence/lookup `0.396787/0.784921/0.815799`；Q-fixed minus float
  `-7.62e-8`；cutoff16 recurrence `0.773930`；
- event recurrence/run-length/memoryless cost `0.073618/0.202829/0.022917`；run-length minus
  recurrence CI `[0.128225,0.130196]`，但 recurrence 仍显著弱于 memoryless；
- event fixed-point 384k cycles mode parity `0.999716`、max state error `1.56e-5`、0 conflicts；
- focused core/runtime/artifact `39 passed`；含 terminology/protocol governance `85 passed`；
  相邻 lookup/FSM/MF/NMF/trajectory/gradient DLEnv `235 passed`；默认解释器显式全量
  `1110 passed, 11 skipped, 4 failed`，4 个失败仍仅是 R-N012 的两份历史治理文档缺失，
  torch 专项已由 DLEnv 真正执行。

## 深审计与风险

新增 R-N061。cutoff16 时 recurrence 高于 frozen lookup，证明 lookup 不是跨 cutoff 全局上界；
recurrence 的 `p(g)=0.98466` 表明 branch reshaping 很强；event false intervention 为 31.59%；
two-level physical lane 未训练 leakage，integer mirror 不是 RTL/board。以上均进入 artifact/doc gates。

## 插入任务判断

不插入。T3.2.11 处理 memory-specific mechanism；T4.4/T5.1/T5.2/T5.4 处理 teacher/student、
OOD/cutoff/horizon；T5.5/T6 处理 fixed-point RTL、综合与实板。现有正常任务足以覆盖。

## 任务板同步

T3.2.10 标为 Done；T3.2.11 标为 In Progress；当前推荐任务更新为 T3.2.11。
