# T5.0.2 任务记录：跨保真度和独立 holdout 验证

- Task ID：T5.0.2
- 日期：2026-07-16
- 状态：Done

## 输入、通过标准与失败分支

输入为 T2.3.3 cross-fidelity artifact、T5.0.1 reproduction registry、既有 noise-transfer/direct-syndrome/
finite-Fock/effective-model 四个 fidelity lanes，以及 P-Steane 一手预印本 Eqs. 36/37、40、41、43。

正式执行前冻结 calibration `3/5/8/10/12 dB`、排除 reconnaissance `4/11/14 dB`，再冻结正式
`2.5/10.25/11.75 dB` 与四个 fresh seeds。task 通过条件是至少一个未用于调参的 family 通过，同时逐族
保留失败；main family 只有两个高 squeezing 点全部通过冻结门才可升级。失败时不得重选点，必须降级 claim。

## 实际完成

在 `physics/cross_fidelity_validation.py` 抽出公开单点 evaluator，并保持原固定 calibration grid 不变。
新增 T5.0.2 runner：主族在 3 个 disjoint 点重放四条 fidelity lanes，每点用 4×100,000 effective samples；
secondary P-Steane 族在 252 点新参数网格上用 closed-form、独立 coefficient covariance propagation、直接
variance product 和两个 special cases 交叉核验。

结果没有被包装：main cross-fidelity 为 `FAIL`。`10.25 dB` 的 deterministic gaps、localized 和 clipping
均通过，但 pooled maximum z-score 为 `2.293338 > 2.0`；`11.75 dB` 为 `1.867765`，低域 `2.5 dB`
clipping 负边界也通过。secondary P-Steane 为 `PASS`，252 点最大 covariance/product 误差为
`5.55e-17/4.80e-16`，所有 `k>1` slice 的唯一 grid argmin 都是 `m=1`（`2a=b`）。

## 产物

- `physics/cross_fidelity_validation.py`
- `cnn_fpga/benchmark/independent_cross_fidelity_holdout.py`
- `tests/test_independent_cross_fidelity_holdout.py`
- `docs/independent_cross_fidelity_holdout.md`
- `docs/t5_0_2_independent_cross_fidelity_holdout.json`
- `docs/t5_0_2_independent_cross_fidelity_holdout_source_data.csv`
- `docs/protocol_hierarchy.json` / `docs/protocol_hierarchy.md`

## 验证与结果

- runner：task `PASS`，acceptance `6/6`，main `FAIL`，secondary `PASS`，Source Data `291` 行；
- focused：`118 passed`；
- finite-squeezing/noise-transfer/cross-fidelity/Fock/quadrature/governance adjacent：`319 passed`；
- 原 T2.3.3 calibration file 全量回归包含在上述测试中，证明 public evaluator 重构未改变原路径；
- artifact 绑定 T2.3.3/T5.0.1 SHA-256，CSV 明确把 `10.25 dB` 和主 pooled gate 标为 false；
- repeated build contract hash 稳定，缺失/非法参数、历史快照改写和 secondary 升主线均被测试拒绝。

## 非 demo 检查

主族不是单点展示：包含 3 个正式点、12 个新 seed records、共 1.2M effective samples，并保留失败轴和
out-of-domain 负对照。secondary 不是只代入论文例值：使用排除来源示例 ratio 的 252 点网格、两条独立
推导路径、ME-Steane/teleportation special cases 和 unique-argmin 检查。总 PASS 不允许覆盖 main FAIL。

## 风险与后续

新增 R-N081：main surrogate 在独立高域的一点未通过冻结统计门，noise-transfer high-domain validity 仍只能写
calibration-qualified，不能泛化。T5.1/T5.2/T5.4 将用完整 comparison/scenario/fault/OOD 正常任务继续验证；
P-Steane 只保留 secondary analytic status。不插入新风险 task。
