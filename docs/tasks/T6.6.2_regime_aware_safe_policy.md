# T6.6.2 regime-aware safe adaptive policy 完成记录

- Task ID：T6.6.2
- 标题：实现 proposed regime-aware safe adaptive policy
- 日期：2026-07-17
- 状态：Done（V4 架构复核后重新关闭）

## 输入与完成内容

输入为 T6.5.2 unified contract、T6.5.3 preregistration、T6.6.1 comparator、T4.2
event/reset、T4.3 atomic bank/LKG，以及 T6.6.3 V2/V3 pre-formal NO-GO 反证。

1. 将旧 tail→initial static / uncertain→unconditional LKG 改为 observed-only Window/EWMA 双影子 bank。
2. Window 仅在 smooth posterior、pre-update predictive NLL 和 commit hysteresis 同时通过时 promotion；
   tail/uncertain 只允许 continuously updated、validated EWMA shadow。
3. integrity/leakage 仍 fail closed，只有 integrity 类故障允许 monotonic LKG republish。
4. candidate 强制携带 router SHA、两专家 score 与完整双影子成本，少报单专家 MAC 会被拒绝。
5. 保留 6-cycle fast path、4000-cycle residency、CRC/SHA/CAS/ack/readback 与逐 action provenance。

## 产物与验证

产物包括 runtime policy、production validation runner、两组 tests、docs/regime_aware_safe_policy.md、
machine JSON 与 20,061-row Source Data。

- 20/20 gates、11/11 mutations、20,061 cycles；
- source-to-action 全部 6 cycles；双影子成本 1,218/8,192 MAC；
- commits versions 1,2,3,4 at cycles 4001,8002,12003,16005，readback 全 confirmed；
- tail 在 cycle 8000 接收 EWMA shadow，8002 起 source 为 trusted_ewma_shadow，不回切 static；
- cycle 16001 integrity fault 真实取消 pending Window，随后以 version 4 完成 LKG rollback；
- focused T6.6.2 tests 25/25；与 T6.6.3 联合 focused suite 30/30。

## 非简化、风险与任务板

首次 V4 probe 保留旧版“deferred cycles >=3900”门而得到 19/20；该数量来自已删除的 static
republish，不是安全本身。没有降低 residency，而是改为直接验证相邻正常 commits 间隔均 >=4000，
实际仍有 6 个 deferred cycles。另增加低 smooth posterior、Window score 不胜、router hash 错误、
少报完整 MAC 四类负向测试。

R-N115/R-N116 更新：V4 只建立结构与 pilot-entry compatibility，不建立 formal LER 或硬件 claim。
不插入旁路 task；T6.6.3 在 V4 source hash 重锁后方可关闭。

