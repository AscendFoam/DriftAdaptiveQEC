# T5.4.3：因果消融与负结果表

- 日期：2026-07-16
- 状态：Done

## 输入与执行方案

输入为 T3.2.11 history intervention、T3.2.6 same-emission regime temporal ablation、T3.2.5
run-length/memoryless/ParamBank same-trace event campaign、T5.1.4 branch verdict、T5.4.2 matched fallback
campaign，以及保存的 legacy residual-b TinyCNN checkpoint/test split。

执行前先审计六个 mechanism 是否真的共存于一个已验证系统。结论是否定的，因此冻结
`NATIVE-LANE-CAUSAL-ABLATION-NONMIXING-V1`：每个开关只在其原生 metric 和 same-trace 单元内解释，
统一正 benefit 方向，但禁止总分、排名或跨 lane 因果归因。

## 实际完成内容与产物

- 新增 `cnn_fpga/benchmark/causal_ablation_negative_results.py`；
- 新增 `tests/test_causal_ablation_negative_results.py`；
- 生成 `docs/t5_4_3_causal_ablation_negative_results.json`；
- 生成 `docs/t5_4_3_causal_ablation_negative_results_source_data.csv`；
- 新增 `docs/causal_ablation_negative_results.md`；
- 同步 README、benchmark README、protocol hierarchy、风险与任务板。

正式表包含恰好六项 intervention。history、regime、run-length/update 与 fallback 从 parent raw/native rows
重算；CNN residual 重新加载 206 个 test samples 和真实 checkpoint，active 实际推理，off 精确置零。

## 验证与详细检查

- 18/18 machine gates 与独立 `validate_artifact()` 通过；
- 338-row Source Data 的 canonical/byte hashes 通过；
- 18 个 direct/artifact/semantic-mutation tests 通过；
- history cutoff12 benefit `0.709110 [0.568837,0.894025]`，cutoff16
  `-0.563636 [-0.665556,-0.461717]`，方向反转保留；
- CNN active/off MSE `2.41445e-6/8.03405e-6`，但明确只有一个 legacy split、无独立
  seed-cluster CI，performance branch 仍撤销；
- regime NLL benefit `0.401514 [0.366352,0.436676]` 与 delay cost
  `1.228754 [1.111948,1.345561]` 同报；
- run-length benefit `-0.179911 [-0.180782,-0.179041]`，32/32 cells 为负；
- parameter-update 组件 benefit `0.401710 [0.399014,0.404407]`，不升级 decoder/LER claim；
- fallback aggregate 正向但 compound 与 nominal 负向，avoided/induced/unnecessary counts 保留；
- 重算 contract hash 后，隐藏任一负结果、伪造跨 lane 排名或 device claim 均 fail closed。

## 风险与论文 claim

新增 R-N099。六项不是一个 integrated stack；history/run-length 不能写成正贡献，CNN 不能复活性能
分支，regime 必须带 delay cost，update 只能作为 event-cost component，fallback 必须带 mixture/scenario
限定。当前结果不建立 physical-memory、device 或 hardware 机制贡献。

## 插入任务判断与任务板同步

不插入风险 task。T5.4.4 已按顺序承接 agent/seed selection-bias 审计；T5.4.5/T5.4.6 承接 horizon
与 randomized mismatch。任务板将 T5.4.3 置为 Done，并启动 T5.4.4。
