# T6.2.2 长序列与故障路径软件/RTL 资格验证完成记录

- Task ID：T6.2.2
- 标题：完成独立 golden、长序列与故障路径的软件/RTL 资格验证
- 日期：2026-07-17
- 状态：Done

## 输入材料

T6.2.1 production RTL、T5.5.1 bit-accurate golden、冻结 A/B LUT、event/fallback/atomic
bank contract，以及任务板的 `10×1e5`/聚合 `1e6`、全字段对拍和抽象故障验收。

## 实际完成内容

新增独立快速整数 golden、production qualification top、binary CXXRTL driver、10-family
runner、抽象 bounded FIFO/receiver sequence checker、family-specific coverage/recovery gates、
comparator/report mutation audit 和定向测试。正式执行两次百万周期；首轮发现 reorder 实际为
0 后没有关单，修复注入与门后完整重跑。

## 产物路径

详见 `docs/new_tasks/T6.2.2_long_rtl_qualification.md`；核心机器产物为
`docs/t6_2_2_long_rtl_qualification.json`、Source Data 与 82 MB hash-bound trace，中文说明为
`docs/long_rtl_qualification.md`。

## 验证方式和结果

10 family 各 100,000 cycles；legacy 10,000 rows 0 mismatch；CXXRTL 1,000,000 cycles 全
visible fields 0 mismatch；0 undefined action/CRC error/silent overflow；6 modes、5 health、
3 actions、9 reachable fault bits、rollback/untrusted reject、LLR/counter saturation、三类
transport disturbance 和 recovery 全通过；shadow/semantic mutations 8/8、16/16 gates，8 tests。

## 风险复核

新增 R-N112；two-state CXXRTL/抽象 transport 不替代 CDC、bitstream、SEU、真板 deadline/
power/HIL。R-N111 保持 Open。

## 是否需要插入新 task

否；缺口已有 T6.1.2、T6.2.3、T6.4、T6.7.3 和 T6.9 正常任务承接。

## 对任务板的同步

T6.2.2 置 Done，Phase 6A 入口 T6.5.1 置 In Progress。

## 对论文 claim 的影响

仅支持 board-independent million-cycle software/CXXRTL qualification；不支持 measured hardware、
真实 transport 或 fastest FPGA claim。

