# T5.5.4 完整 GRU、量化 GRU 与蒸馏 student 硬件可行性完成记录

正式完成记录见 `docs/new_tasks/T5.5.4_gru_student_hardware_feasibility.md`。本兼容入口保留在
`docs/tasks/`，以维持既有 task 索引和引用稳定性。

核心结论：完整 float GRU 参数存储先验不适配目标器件；完整参数 int8/Q3.14 lower-bound 虽以
41/46 BSRAM 完成三 seed P&R，但 72,854-cycle 乐观延迟远超 5 us，且不是 functional GRU、没有
quantized physical gain。唯一 eligible route 是既有 4-state Q3.14 student。
