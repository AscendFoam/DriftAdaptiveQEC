# T3.1.3：full-state/regime/leakage oracle integration

- **日期：** 2026-07-15
- **状态：** Done

## 输入与通过标准

- T1.3.2 exact `DriftState` periodic Gaussian-mixture oracle；
- T2.1.1 observed/truth-isolated protocol stream；
- T3.1.1/T3.1.2 required comparison schemas；
- 通过标准：oracle 进入显式选择 full-state reference 的主要 decoder comparisons，扩展
  regime/leakage state 上界，始终标为不可部署且不进入 deployable action path。

> 2026-07-15 T3.2.1 集成复核：registry 现允许 task-specific nondeployable reference。memory
> comparison 使用 full-episode truth，不再被错误强制加入 full-state oracle；v2 artifact 已刷新。

## 执行方案与实际完成

1. 为 T1.3.2 result/trajectory 增加 regime/burst provenance，不改 mixture MAP 数学。
2. 冻结 nondeployable oracle descriptor、canonical/legacy alias 边界和 required-schema gate。
3. 实现 `OracleHiddenContext`：只能从 simulator `SyndromeTruthStep` 构建，normal cycle 调真实
   oracle MAP，leakage cycle 只输出 erasure flag。
4. 运行 4 regimes × 4 seeds × 20k paired matrix；另运行 4-seed、8k-cycle protocol leakage lane。
5. 保存 Source Data、paired CI、flag sensitivity/specificity、两端 cost envelope 与源码 hash。

## 产物

- `cnn_fpga/benchmark/oracle_baseline.py`
- `physics/oracle_map.py`
- `cnn_fpga/benchmark/standard_binning_baseline.py`
- `tests/test_oracle_baseline.py`、更新 `tests/test_oracle_map.py`
- `docs/oracle_baseline.md`
- `docs/t3_1_3_oracle_validation.json`
- `docs/t3_1_3_oracle_source_data.csv`
- `docs/new_tasks/T3.1.3_oracle_baseline.md`

## 生产结果与反简化检查

- regime matrix 320k：static/oracle `0.076419/0.025103`，paired gap
  `0.051316 [0.050474,0.052157]`；16/16 row CI 下界正；
- leakage lane 8k cycles：1,616 leakage，flag sensitivity/specificity `1/1`；
- leakage cost envelope `[0.159,0.361]`，不选择免费恢复端作为主结果；
- normal oracle 复用真实 mixture likelihood；leakage 不生成伪 logical action；
- observed truth isolation、alias non-conflation、registry mutation 与 provenance 覆盖；
- 10/10 gates，focused+adjacent `99 passed`。
- 术语源码锚漂移修复后 focused `37 passed`；显式 `tests/` 全量为
  `858 passed, 4 skipped, 4 failed`，4 个失败仍仅来自 R-N012 两份旧 FR8/P4 文档缺失；
  `compileall` 与 `git diff --check` 通过。

## 风险与插入任务判断

R-N018 证据增强但保持 Open：oracle 仍是 assumed displacement model。新增 R-N049：perfect hidden
leakage flag 的乐观端可能被误写成免费纠错或在线 detector。

不插入 task；T3.1.4/T5.2/T5.3/T6 正常任务覆盖 protocol-aware action、channel bound 与实测
leakage detection，不阻塞下一 static optimized decoder。

## 对论文 claim 的影响

允许 nondeployable model upper reference 和显式 leakage cost envelope；禁止 deployable/free-
recovery/channel-optimum/CNN-over-oracle claim。

## 任务板同步

T3.1.3 标为 Done；T3.1.4 标为 In Progress；当前推荐更新为 T3.1.4。
