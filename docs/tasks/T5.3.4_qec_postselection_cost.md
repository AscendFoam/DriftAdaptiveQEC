# T5.3.4：核算真实纠错和 post-selection 成本

- 日期：2026-07-16
- 状态：Done

## 输入与执行方案

输入为 T3.2.4 post-selection raw diagnostic、T5.1.5 time/cost fairness、T5.1.6 feasibility/safety、
T5.3.1 event accounting、T5.3.2 fidelity 和 T5.3.3 operational boundary。方案按原生 estimand 分 lane：
online channel、offline rejection diagnostic、software safety 和 missing fields，不生成跨 lane scalar cost。

## 实际完成内容与产物

- `physics/qec_cost_accounting.py`
- `cnn_fpga/benchmark/qec_postselection_cost.py`
- `tests/test_qec_cost_accounting.py`
- `tests/test_qec_postselection_cost.py`
- `docs/qec_postselection_cost.md`
- `docs/t5_3_4_qec_postselection_cost.json`
- `docs/t5_3_4_qec_postselection_cost_source_data.csv`

在线六条 rows 均为 30 cycles、60 measurements、60 resets、540 active gates、`Delta=0.34`/`6.360122 dB`；
同时保留 `F_avg`、code survival 与 boundary，LER/latency/pulse energy 为 null。post-selection 八个 target 的
conditional error 均下降，但 unit rejection penalty 下 total cost 均恶化。正式 artifact 94 rows、27/27 gates。

## 验证与详细检查

- core + report/20 semantic mutations：`38 passed`；
- post-selection/time-cost/feasibility/T5.3/protocol 相邻收口组合：`264 passed`；
- T5.3.1 native counts 与 T5.1.5 standard protocol cross-check 一致；
- code survival 与 trajectory acceptance 分列；
- post-selection accepted-failure/penalty identity 逐 target 重算，cluster CI 与 truth-only boundary 保留；
- 12 项未测量字段全部 null；无 global score 或跨 lane cost aggregation。

## 风险与论文 claim

新增 R-N095：成本账本完整不等于 full cost 已测。当前只有操作计数、解析 fixed-control resources 与软件
campaign；pulse duration/energy、matched/board/frontend latency、device reset、physical-memory LER 和
best-passive reference 均缺失。full-cost/coherence-gain/postselected break-even 继续未建立。

## 插入任务判断与任务板同步

不插入风险 task。T5.5/T6 已承接量化/RTL/board latency/resource，T5.4 承接 OOD/长时，T5.3.5 继续完成
recovery bound。任务板将 T5.3.4 置为 Done，并启动 T5.3.5。
