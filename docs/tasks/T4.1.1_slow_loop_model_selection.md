# T4.1.1：匹配预算慢回路模型选型

- 日期：2026-07-15
- 状态：Done

## 输入与执行方案

复用 T3.2.6 canonical observed-window four-regime task，冻结 8-window history、
3/3/8 seed split、4096 MAC/4096 B 常驻/4096 B scratch envelope 和 validation-only
NLL 选择规则；比较 causal TCN、small GRU、Gaussian HMM、diagonal Kalman、指数递推
与 run-length FSM，不预设 CNN 优先。

## 实际完成内容

实现统一数据 adapter、TCN/GRU 五 restart 训练、四类经典模型训练/validation 网格、
temperature calibration、同指标 evaluation、资源/host latency 画像、checkpoint/Source Data
绑定。深审计补上 exact rolling-HMM emission cache 和 FSM 极端概率数值修复。

## 产物

`cnn_fpga/decoder/slow_loop_model_selection.py`、
`cnn_fpga/benchmark/slow_loop_model_selection.py`、两份 tests、
`docs/t4_1_1_slow_loop_model_selection_validation.json`、24,240-row CSV、checkpoint
与 `docs/slow_loop_model_selection.md`。

## 验证结果

13/13 gates、33 focused tests、79 focused+governance、182 adjacent。显式 `tests/`
全量为 `1153 passed / 14 skipped / 4 failed`，4 个失败仍仅是 R-N012 的两份旧 FR8
文档缺失。validation HMM/TCN/GRU NLL 为
`0.454975/0.476180/0.503134`；evaluation 为
`0.455711/0.511936/0.509619`。validation runner-up-minus-HMM evaluation paired NLL
`+0.056225 [0.046709,0.065742]`。HMM rolling adapter 为 926 MAC、3728 B 常驻、
104 B scratch；所有六族满足共同 envelope。

## 风险与插入任务

新增 R-N063：winner 依赖当前 synthetic generator、summary 和 NLL；HMM delay 较慢，
neural/classical 次序在 evaluation 有局部反转。T4.1.2--T4.1.4 与 T5.1/T5.4 已覆盖
richer input、多目标/OOD/calibration，故不插入 task。

## 任务板同步

T4.1.1 Done；T4.1.2 In Progress；当前推荐任务为 T4.1.2。
