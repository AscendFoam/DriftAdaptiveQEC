# T3.2.9：有限时域 trajectory lookup control oracle

- 日期：2026-07-15
- 状态：Done

## 输入与执行方案

输入为 Puviani Supplement S3/S4D 的 lookup-table 定义、T2.3.4/T2.3.5 可微 simulator/gradient、
T1.4.5 术语 registry 与任务板 T3.2.9 合同。先冻结 causal prefix tree 与 15-action contract，
再 exact enumerate 全部分支；增加 optimized time-indexed open-loop 嵌套对照、3 restarts、
cutoff-16 frozen transfer、指数资源表和 checkpoint replay。失败分支禁止 future outcome、
evaluation 选模、单 restart、未收敛上限或“全局最优”升格。

## 实际完成内容

- 实现两-cycle 15-node/225-scalar causal lookup，当前 action 只读过去 g/e prefix；
- 16 个 terminal branches 一次 exact evaluation，优化 `sum P_theta * fidelity`；
- 同预算训练 60-scalar time-indexed open-loop，并将其精确嵌入 lookup warm start；
- 两 families×3 restarts×`300+250` epochs，每条 refinement 后重新选模；
- 保存 3,418-row 全曲线/分支/资源 Source Data、全部 restart checkpoint 和 20 gates；
- cutoff 16 只在选择后重放；更新 `TERM-CONTROL-ORACLE` 为 empirical nondeployable code binding；
- 深审计 branch skew、p(g)、bounded-action saturation、cutoff reversal 和指数增长。

## 产物

- `physics/trajectory_lookup_control_oracle.py`；
- `cnn_fpga/benchmark/trajectory_lookup_control_oracle.py`；
- `tests/test_trajectory_lookup_control_oracle.py`；
- `tests/test_trajectory_lookup_control_oracle_artifact.py`；
- `docs/t3_2_9_trajectory_lookup_control_oracle.json/.pt/.csv`；
- `docs/trajectory_lookup_control_oracle.md`。

## 验证结果

- cutoff12 standard/open/lookup fidelity `0.396787/0.769403/0.815799`；lookup-open
  `+0.046396`，lookup-standard `+0.419012`；
- cutoff16 frozen transfer `0.559221/0.503415/0.638688`，lookup-standard `+0.079467`；
- 20/20 gates，所有 12 phase/family/restart 组合全节点 gradient/update coverage；
- selected refinement tail gain open/lookup `1.58e-5/6.40e-5`；
- focused core/artifact DLEnv `29 passed`；含 terminology/protocol governance 为 `75 passed`；
  相邻 trajectory/Feedback-GRAPE/MF-NMF/autonomous DLEnv 回归 `237 passed`；默认解释器显式
  全量 `1084 passed, 10 skipped, 4 failed`，4 个失败仍仅是 R-N012 两份历史治理文档缺失，
  torch 专项已由 DLEnv 回归真实执行；
- checkpoint live hash/replay、Source Data 3,418 rows、probability normalization 和 density gates 通过。

## 风险与 claim

新增 R-N060：continuous non-convex optimization 不提供全局最优证书；lookup 将 terminal
probability 推到 `7.57e-5--0.6230`，p(g) 比 standard 低 `0.10065`，且 open-loop 在 cutoff16
反转。10 cycles 已需 1,048,575 nodes、15,728,625 scalars 和至少 9 GiB terminal states。

## 插入任务判断

不插入。T3.2.11/T5.4.5 处理 history/horizon，T4.4/T5.1.3 比较 teacher/student/control gap，
T5.2/T5.3 处理 model/cutoff/leakage，T6 处理真实部署；正常任务已覆盖。

## 任务板同步

T3.2.9 标为 Done；T3.2.10 标为 In Progress；当前推荐任务更新为 T3.2.10。
