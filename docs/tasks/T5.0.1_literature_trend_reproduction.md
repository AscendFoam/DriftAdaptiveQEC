# T5.0.1 任务记录：文献趋势 reproduction table

- Task ID：T5.0.1
- 日期：2026-07-16
- 状态：Done

## 输入与执行方案

只读消费 Campagne 2020、Sivak 2023、Puviani PRL 2025 本地全文，以及 Knill/qunaught、P-Steane、
noise-transfer 和 2026 trapped-ion 的官方期刊/arXiv 元数据。现有项目证据限定为 T2.0.5、T2.0.6、
T2.2.2、T2.3.3、T2.3.7 和 T4.4.5 artifacts。

方案先按“外部 reference fact / 已验证项目趋势 / 未来 holdout”拆分，再为每行冻结 hierarchy role、目标类型、
容差、calibration/holdout 用途、model-selection access、当前状态、下一 gate 和禁止迁移。表格完成本身不得
触发新 physics simulation，也不得把 pending secondary 写成 reproduced。

## 实际完成

实现 `literature_trend_reproduction.py`，建立 7-source、8-local-anchor、6-artifact-binding、14-target registry。
14 行覆盖：2020 sharpen--trim 与 QEC on/off、2023 displacement/occupancy/gain/timing、PRL NMF direction 与
10→1000 horizon、Knill 等价、qunaught squeezing、P-Steane `2a=b`/noise-ratio shaping、noise-transfer 正负
有效域，以及 2026 trapped-ion Pauli-resolved on/off 报告结构。

当前状态严格分为 4 个 `QUALIFIED_DIRECTIONAL_PASS`、1 个 structure-only、1 个 negative-boundary、
5 个 `REGISTERED_PENDING`、2 个 `REFERENCE_ONLY` 和 1 个 `REPORTING_TEMPLATE_ONLY`。registry 总体
`PASS` 只代表 17 个完整性/防泄漏 gates 通过，不代表所有 reproduction 通过。

## 产物

- `cnn_fpga/benchmark/literature_trend_reproduction.py`
- `tests/test_literature_trend_reproduction.py`
- `docs/literature_trend_reproduction_table.md`
- `docs/t5_0_1_literature_trend_reproduction.json`
- `docs/t5_0_1_literature_trend_reproduction_source_data.csv`
- `docs/protocol_hierarchy.json` / `docs/protocol_hierarchy.md`

## 验证与结果

- 17/17 registry gates；14 targets；52 Source Data rows；
- 6 个现有 artifact SHA-256 与 machine gate 全部当前；8 个本地 source fragments/hash 全部当前；
- direct `15 passed`；direct + protocol hierarchy `52 passed`；覆盖六个 parent evidence lanes 的邻接回归 `224 passed`；
- mutation 将 high-squeezing q-LER gap 改为 `1e-2` 时 registry fail closed；缺失 parent artifact 直接拒绝；
- noise-transfer high-dB 当前 gap/z/q-p 为 `3.92617e-5/1.70763/1.50991e-7`；3 dB 负边界为
  `0.015408` 与 clipping `0.357665`；
- NMF 注册 lane CI 为 `[0.084161,0.328067]`，同时强制携带 T4.4.5 exact-budget MF cutoff reversal；
- trapped-ion 只保存逐 Pauli on/off、ratio、约 500 us/round、reset/parallel cost 报告模板，不转移数值。

## 非 demo 检查

没有用空表或 narrative checklist 冒充实现：runner 会重查 source line、artifact hash、machine status、split 与
secondary nonmixing；CSV 逐行记录来源、锚、binding、target 和 gate。5 个 secondary/horizon pending 行的
`passed` 均为 false，reference/report rows 禁止选模；低 squeezing 失败域没有被删除或重调掩盖。

## 风险复核与后续

新增 R-N080：近期 preprint 与 external numeric references 可能在后续写作中漂移或被误当 project target。
T5.0.2、T5.3、T5.4、T7 已覆盖刷新、holdout 和 claim audit，不插入风险任务。下一任务为 T5.0.2。
