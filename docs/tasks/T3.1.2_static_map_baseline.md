# T3.1.2：training-average static MAP

- **日期：** 2026-07-15
- **状态：** Done

## 输入与通过标准

- T1.1.3 periodic correlated Gaussian MAP；
- T3.1.1 standard row、major-comparison registry 与 R-N047 反证；
- T1.3.4 materialized paired trace；
- 通过标准：只用 evaluation-independent training-set average noise/observation parameters，冻结
  static MAP，并出现在显式选择该 static anchor 的 decoder comparisons。

> 2026-07-15 T3.2.1 集成复核：comparison registry 增加 task-specific static/reference role。
> 本任务 validator 不再把所有 required standard-binning comparison 强绑到 training-average MAP；
> v2 artifact 已按最终 registry 刷新。

## 执行方案与实际完成

1. 用 training `DriftState` 的 weighted mean、mixture covariance 和 total-covariance law 做一次性
   marginal Gaussian moment match；不使用 evaluation buffer。
2. 新增 frozen `StaticMAPParameters`、observed-only chunked decoder 和无更新 descriptor。
3. 将 active T1.3.4 与 future T5 schema 的旧 static row 替换/锁定为 formal static。
4. 运行 8 独立 evaluation seeds × 72k paired samples；保存 per-seed Source Data、aggregate CI、
   参数本体、training/trace hashes 与源码 hash。
5. 原样保留 formal static 推翻旧 EKF primary gate 的 counterevidence。

## 产物

- `cnn_fpga/benchmark/static_map_baseline.py`
- `cnn_fpga/benchmark/adaptive_drift_alignment.py`
- `cnn_fpga/benchmark/standard_binning_baseline.py`
- `tests/test_static_map_baseline.py`
- 更新 `tests/test_adaptive_drift_alignment.py`、`test_standard_binning_baseline.py`
- `docs/static_map_baseline.md`
- `docs/t3_1_2_static_map_validation.json`
- `docs/t3_1_2_static_map_source_data.csv`
- `docs/new_tasks/T3.1.2_static_map_baseline.md`

## 生产结果与反简化检查

- 576,000 paired samples；8 个独立 trace hashes、单一 training hash；
- standard/static/oracle LER `0.058870/0.024498/0.011340`；
- standard-minus-static `0.034372 [0.033798,0.034946]`，8/8 seed CI 下界为正；
- 默认 trace formal static `0.024792`，EKF `0.025319`；static-minus-EKF CI 跨零，旧 primary
  gate 降为 false；
- frozen mean/covariance 写入 JSON；loss fail closed，outlier moment match 显式；
- 9/9 machine gates；focused+adjacent `119 passed`。
- 显式 `tests/` 全量为 `844 passed, 4 skipped, 4 failed`；4 个失败仍仅来自 R-N012
  的两份旧 FR8/P4 缺失文档；`compileall` 与 `git diff --check` 通过。

## 风险与插入任务判断

- R-N047 降为 Mitigated：formal static 已替换弱 calibration row并恢复对 standard 的稳定优势；
- 新增 R-N048：training schedule 与 evaluation synthetic distribution 同族且使用 state metadata，
  仍可能高估真实 calibration/mismatch 表现。

不插入 task。R-N048 由 T5.1 train/eval scenario split、mismatch 与 T6 真实 calibration provenance
覆盖；不阻塞 T3.1.3 model oracle integration。

## 对论文 claim 的影响

允许 synthetic registered-distribution 的 fair static-vs-standard 结论；旧 T1.3.4 EKF 72.1%
gap-closure 不再是当前强 baseline 结论。禁止 universal、device 或 hardware claim。

## 任务板同步

T3.1.2 标为 Done；T3.1.3 标为 In Progress；当前推荐更新为 T3.1.3。
