# T2.3.5：验证 Feedback-GRAPE 随机轨迹梯度

- **日期：** 2026-07-14
- **状态：** Done

## 输入

- T2.3.4 joint cavity--ancilla differentiable trajectory；
- Puviani et al. 本地一手全文 467--495 行的 reward + trajectory-score identity；
- task gate：两项路径均进入梯度，autograd 与 finite difference 对齐。

## 实际完成

1. 实现三参数 causal compact policy 与 1/2-cycle binary trajectory 完整枚举。
2. 分解 exact expected-return gradient 为 reward path 与 score path。
3. 验证常数 baseline 不偏、`E[grad log P]=0`。
4. 对 total、reward、score 三项分别 central finite difference，并做四点 step sweep。
5. 运行 32×384=12,288 条随机 trajectory，按 repeat 估计 mean/SE/z-score。
6. 对比 baseline 前后 score trace variance。
7. 新增 32 项 direct tests、机器 JSON、协议/风险/README 同步。

## 产物

- `physics/feedback_grape_gradient.py`
- `tests/test_feedback_grape_gradient.py`
- `docs/feedback_grape_gradient_validation.md`
- `docs/t2_3_5_feedback_grape_gradient_validation.json`
- `docs/tasks/T2.3.5_feedback_grape_gradient.md`
- `docs/new_tasks/T2.3.5_feedback_grape_gradient.md`

## 验证结果

- exact decomposition error `5.55e-17`；baseline/score-normalization residual `1.11e-16/1.51e-16`；
- total/reward/score FD relative error `1.68e-10/2.44e-10/3.22e-10`；
- 四步长 sweep 最差 relative error `<2.55e-7`；
- 12,288 条随机 trajectory 最大 `|z|=1.120`；baseline variance ratio `0.04443`；
- focused `32 passed`，协议相邻 `70 passed`；
- 全量 `tests/` 为 `753 passed, 2 skipped, 4 failed`；四项失败仅来自 R-N012 已登记的
  `docs/fr8_statcalib_extension_lane_benchmark.md` 与 `docs/P4_benchmark_formal_protocol.md`
  缺失，没有 T2.3.5 新增回归。

## 非 demo 复核、风险与后续

首版总梯度 FD 已通过后，又补了 reward/score 分项 FD 和 step sweep，避免两项错误
相消。新增 R-N043：compact audit policy/短 horizon 梯度正确不等于 RNN teacher 可训练或
有性能增益。不插入任务：T2.3.6、T2.3.7 与 T4.4 已覆盖资源、ranking 和 teacher 训练。

## 任务板同步

T2.3.5 -> Done；当前推荐任务与唯一 In Progress 更新为 T2.3.6。
