# T2.3.2：在 Fock space 中实现一轮协议对齐纠错

- **日期：** 2026-07-14
- **状态：** Done
- **协议：** `PROTO-SBS-MAIN`
- **证据范围：** finite-cutoff completed analytic SBS one-round reference；非 pulse/device model

## 输入与通过标准

- `docs/new_task_board.md` T2.3.2；
- `docs/experiment_plan.md:447` 与 §14.1；
- T2.3.1 `physics/fock_density_model.py`；
- 本地 Sivak 2023 正文/补充材料中的 analytic SBS Kraus、X→Z 次序、paired outcome 与
  Pauli-frame 语义；
- 覆盖 initialization、idle、extraction、noisy measurement、classical action、frame
  update、logical projection，并以 exact branches、CPTP、cutoff 和纠错方向证明非 demo。

## 实际完成

1. 将 T2.3.1 GKP→Fock 投影扩展为显式保持归一化的 source-coordinate dilation；
2. 使用 canonical `x,p` 逐矩阵实现论文 `K_g/e^X` 与严格 `(x,p)->(-p,x)` 的
   `K_g/e^Z`；
3. 保留 raw truncation defect，并用共享 `G^(-1/2)` 完成有限维 CPTP pair；
4. 实现 idle→X hidden/observed→observed-routed residual phase→frame→Z→projection；
5. 实现 perfect-readout 四 Kraus path、noisy-readout 16 hidden/observed branches、exact
   mixture 与 seedable sampling；
6. 加入六 Pauli eigenstates、photon loss/gain trickle-down、100k MC、五点 cutoff sweep、
   噪声负对照和 fail-close scope 验证。

## 产物

- `physics/fock_sbs_cycle.py`
- `tests/test_fock_sbs_cycle.py`
- `physics/fock_density_model.py`（坐标 dilation 接口）
- `tests/test_fock_density_model.py`（dilation 直接验证）
- `docs/fock_sbs_cycle.md`
- `docs/t2_3_2_fock_sbs_cycle_validation.json`
- `docs/tasks/T2.3.2_fock_sbs_cycle.md`
- `docs/new_tasks/T2.3.2_fock_sbs_cycle.md`

## 验证摘要

- T2.3.1+T2.3.2 direct：`99 passed`；
- adjacent protocol/Fock/error-space/control slice：`177 passed`；
- full `tests/`：`629 passed, 4 failed`，四个失败仍仅 R-N012 的缺失 legacy FR8/P4 文档；
- production 16/16 gates PASS；
- clean conditional fidelity/survival/weighted fidelity：
  `0.999612/0.822991/0.822670`；
- noisy：`0.998022/0.766867/0.765350`；
- X/Z completed completeness error：`1.11e-14/1.36e-14`；
- photon loss/gain 单轮平均 code-survival gain：`0.423853`；
- 100k branch MC 最大 z-score：`1.7582`；
- `N=36→42` conditional/survival change：`2.93e-5/0.001730`。

## 反简化检查

最初的 modular-pump 拼装虽满足 CPTP 和分支测试，但不是论文 analytic SBS channel，已
整体替换。官方 arXiv TeX 核对排除了 OCR 漏项；raw finite-cutoff defect 没有被逐 branch
归一化掩盖，而是单列完整谱诊断后做共享 completion。测试独立重构两组公式，且以
`a/a^dagger` error-space 回泵而非只测输入 codeword，避免“只保真、不纠错”的 demo。

## 风险与 claim

新增 R-N038。T2.3.2 关闭“没有协议对齐一轮 Fock reference”的缺口，但不关闭
finite-cutoff completion bias、explicit transmon/pulse、装置校准或跨 fidelity 失配。
只允许写 finite-cutoff completed analytic SBS one-round reference。

## 后续

不插入风险任务；T2.3.3 按既定顺序执行跨 fidelity 趋势、有效区/失效区与误差归因。

## T-RISK-20260714-01 回灌

code basis 已用修正后的完整 canonical dilation 重算。`N=24` clean conditional
fidelity/survival 为 `0.999953/0.969508`，photon-error survival gain `0.518107`；五点
cutoff 最后 conditional/survival change 约 `2.00e-7/7.70e-4`。raw finite-cutoff
completion 风险仍保留，不能把数值改善解释为无限维或装置结果。
