# T4.1.2：实验式 history 输入与信息泄漏审计

- 日期：2026-07-15
- 状态：Done

## 输入与执行方案

按任务板冻结 analog/residual syndrome、g/e/leakage、X/Z phase、recent actions、LLR、
run length、deadline/update status 的逐 cycle causal schema；每组字段追到真实 producer，另行冻结
cycle alignment、padding/mask、clipping/saturation、provenance allowlist 和 truth-leak denylist。

## 实际完成内容

实现 256×53 observed-only history builder、immutable sample、real FSM action adapter、real scheduler
status adapter、registered-calibration LLR 和递归 fail-closed leakage audit。深审计修复 commit+conflict
双事实、silent clipping、string-value leakage 与 malformed observed record 四类边界。

## 产物

`cnn_fpga/data/experimental_history.py`、
`cnn_fpga/benchmark/experimental_history_validation.py`、两份 tests、
`docs/reports/phase4/experimental_history_input.md`、17-gate JSON 与 16,384-row Source Data CSV。

## 验证结果

17/17 machine gates；39 core unit tests；50 focused tests；156 adjacent producer/runtime tests；
138 focused+governance tests。
生产 replay 覆盖 8×2,048 cycles、6 update statuses、5 FSM modes、3 observed outcomes、deadline/
pause/CRC/failure/conflict/commit/FIFO/saturation 路径，且 negative probe 能拒绝 nested truth object。
显式 `tests/` 全量为 `1203 passed / 14 skipped / 4 failed`；4 个失败仍只来自 R-N012
登记的 `docs/fr8_statcalib_extension_lane_benchmark.md` 与根级
`docs/P4_benchmark_formal_protocol.md` 两份旧文档缺失，与本 task 无关。

## 风险与插入任务

新增 R-N064：schema 仍是 software/synthetic producer contract；LLR calibration、raw IQ/ADC/CRC
wire、device semantics、richer-input model compatibility 和 target-board timing 尚未验证。T4.1.3--T4.1.4、
T5.1/T5.4/T5.5 与 T6 已有正常任务覆盖，故不插入 task。

## 任务板同步

T4.1.2 Done；T4.1.3 In Progress；当前推荐任务为 T4.1.3。
