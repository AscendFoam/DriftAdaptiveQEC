# T5.4.1：held-out/OOD 验证

- 日期：2026-07-16
- 状态：Done

## 输入与执行方案

输入为 T5.1.2 frozen decoder、T5.1.4 branch verdict、T5.1.6 feasibility、T5.2.2/5.2.3 fault
kernels、T4.2.3/T4.3.3 safety runtime 与 T2.4.2 timing scheduler。执行前冻结四条原生 lane、32 个全新
seed、3 类 drift、3 张 confusion matrix、3 档 leakage rate 与 3 类 communication pattern；OOD 结果不参与
model/threshold/method selection。

## 实际完成内容与产物

- 新增 `cnn_fpga/benchmark/held_out_ood_validation.py`；
- 新增 `tests/test_held_out_ood_validation.py`；
- 生成 `docs/t5_4_1_held_out_ood_validation.json`；
- 生成 `docs/t5_4_1_held_out_ood_source_data.csv`；
- 新增 `docs/held_out_ood_validation.md`；
- 同步 README、benchmark README、protocol hierarchy、风险与任务板。

正式执行含 24 drift cells/786,432 paired decisions、24 measurement cells/393,216 sBs cycles、24
leakage cells，以及 32 communication cells/768,000 scheduler cycles；共 104 seed cells、280 Source Data
rows、20/20 gates。

## 验证与详细检查

- direct/artifact/semantic mutation：15 passed；
- parent/protocol adjacent combined closeout：224 passed；
- formal runner 连续执行并写出 hash-bound JSON/CSV；
- validator 从 raw rows 重算场景、rate、matrix、communication event 与 parent/code hashes；即使重算顶层
  contract hash，measurement/leakage/communication/cross-lane mutations 仍 fail closed；
- telegraph 下 static-minus-EWMA/Kalman 点估计为负且 CI 跨 0，未删除或改 expected-direction gate；
- periodic micro-outage 对当前指标为精确 null，increasing/compound faults 则出现显著 LER/availability
  degradation，正负结果同时保留；
- 四条异构 lane 不拼 universal score，device/system robustness 保持 `NOT_ESTABLISHED`。

## 风险与论文 claim

新增 R-N097。task PASS 是 coverage/integrity PASS，不是系统稳健性；confusion/leakage 是 effective model，
communication 是 software scheduler。允许写预注册 OOD replay 与 lane-local negative result；禁止 all-noise、
device、physical-memory 或 fallback-benefit claim。

## 插入任务判断与任务板同步

不插入风险 task。T5.4.2 已按顺序承接 matched gated-vs-no-fallback 因果比较；T5.4.3--T5.4.6 承接
ablation、agent/seed bias、long horizon 与 randomized mismatch。任务板将 T5.4.1 置为 Done，并启动
T5.4.2。
