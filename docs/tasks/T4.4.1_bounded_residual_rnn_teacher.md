# T4.4.1 任务记录：训练 bounded residual RNN/GRU teacher

- Task ID：T4.4.1
- 日期：2026-07-15
- 状态：Done

## 输入与冻结方案

复用 T2.3.7 的真实 Feedback-GRAPE/sBs simulator 和 72,853 参数 GRU 架构，复核 T3.2.9 的
`SBS-NOMINAL-PLUS-BOUNDED-RESIDUAL-15` 动作合同，并读取 T4.1.5 的旧 teacher provenance 作为禁止复用清单。
执行前冻结三条硬约束：新 restart seeds 与旧 checkpoint 不交；validation-only 选模；evaluation/cutoff-16
confirmation 不调参。

## 实际完成

新增 source-bound/resumable trainer 和 14 项直接测试。训练三个 fresh restart（各 320 epochs、6 条
10-cycle trajectories/epoch），保存全部 initial/final state SHA、训练/validation 曲线、失败标志与 budget-cap
标志。动作使用 `nominal + [2×14,1]·tanh(raw)`；全零 residual 等于 nominal，全零物理 gate 未被冒充 initializer。

## 产物

`cnn_fpga/benchmark/bounded_residual_rnn_teacher.py`、`tests/test_bounded_residual_rnn_teacher.py`、
`docs/bounded_residual_rnn_teacher.md`、JSON/PT/CSV 三份机器产物及 README/protocol/risk/task-board 同步。

## 验证

- 21/21 production gates；三个 validation gain 为 `0.284132/0.281901/0.284383`；
- selected restart 601；primary/confirmation score gain `0.253603/0.141557`；
- primary logical-Z lifetime paired gain `4.125518 [3.899115,4.339331]` cycles；
- 1,074-row Source Data，checkpoint/source/implementation hash 和 reload output 通过；
- 所有参数张量 gradient nonzero fraction `1.0`，causal/cached/replay error `0`；
- focused `14 passed`，T2.3.7/T3.2.9/T4.1.5 邻接 `102 passed`；显式 `tests/` 全量为
  `1472 passed, 17 skipped, 4 failed`，4 项仍仅为 R-N012 的两份旧 FR8/P4 文档缺失。

601/811 的最佳 epoch 位于训练上限，已显式保留；没有声称 optimizer 全局收敛。

## 风险与论文 claim

新增 R-N075。当前是 two-level finite-cutoff matched-model teacher，不含 multilevel leakage/SPAM/pulse、OOD
mismatch、1000-cycle channel lifetime、在线 student、RTL/FPGA 或真实装置。只允许写“fresh bounded-residual
teacher 在当前 held-out finite-horizon 模型中优于 nominal”；不得写 paper-exact 或部署增益。

## 插入任务判断

不插入。T4.4.2--T4.4.5 已顺序覆盖 hidden-state 解释、student 拟合、同轨 gain retention 与强/证否分支；
T5/T6 覆盖 OOD/long-horizon/hardware。
