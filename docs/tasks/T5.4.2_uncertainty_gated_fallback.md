# T5.4.2：uncertainty-gated fallback 验证

- 日期：2026-07-16
- 状态：Done

## 输入与执行方案

输入为 T5.1.2 frozen static/EWMA MAP、T5.1.4 branch verdict、T4.1.4/T4.2.3 fallback 负证据和
T5.4.1 held-out OOD artifact。执行前冻结 catastrophic/avoided/induced/unnecessary 定义、same-trace
primary/fallback/no-fallback 对照、observed-only 三分量 score、41 点 threshold grid、development-only
选择规则、12 个全新 confirmation seeds 和 base-seed cluster bootstrap。

## 实际完成内容与产物

- 新增 `cnn_fpga/benchmark/uncertainty_gated_fallback.py`；
- 新增 `tests/test_uncertainty_gated_fallback.py`；
- 生成 `docs/t5_4_2_uncertainty_gated_fallback.json`；
- 生成 `docs/t5_4_2_uncertainty_gated_fallback_source_data.csv`；
- 新增 `docs/uncertainty_gated_fallback.md`；
- 同步 README、benchmark README、protocol hierarchy、风险与任务板。

正式 campaign 用 T5.4.1 的 8 个 seeds 仅作 development calibration，并在阈值 `0.45` 选定后运行
12 个 fresh parent-disjoint clusters。确认包含 36 OOD cells/1,179,648 decisions 和 12 nominal
cells/393,216 decisions；保存 517-row Source Data、逐 cell/window counts 和 21/21 gates。

## 验证与详细检查

- 17 个 direct/artifact/semantic-mutation tests 全通过；
- 180 个 direct + parent/protocol adjacent closeout tests 全通过；
- 21/21 machine gates 与独立 `validate_artifact()` 全通过；
- 逐 threshold/window/cell 重算 `primary-gated=avoided-induced` 及全部 rate；
- 聚合 OOD absolute reduction `0.00107490 [0.00001950,0.00227615]`，avoided/induced
  counts 为 `6170/4902`；
- telegraph reduction `0.00806936 [0.00506585,0.01131185]`，compound reduction
  `-0.00488536 [-0.00557454,-0.00418854]`，方向反转原样保留；
- nominal reduction `-0.00001272 [-0.00003052,0.00000254]`，未把小代价删除；
- threshold、局部 count、negative-lane、truth-input mutation 在重算 contract hash 后仍 fail closed；
- score/action 先于本窗 predictor update，hidden truth 不进入 score/action；
- always-static 更差只作为 comparator，未被事后选为 gate。

## 风险与论文 claim

新增 R-N098。当前 aggregate PASS 的 CI 下界很接近 0，且 benefit 由 telegraph 驱动、compound 显著
受损；fallback precision 仅 `33.68%`，nominal 也存在 induced cost。因此只允许写 syndrome-decision、
mixture-qualified aggregate evidence，禁止 universal OOD、physical-memory、device 或 hardware safety claim。

## 插入任务判断与任务板同步

不插入风险 task。T5.4.3 已按顺序承接 history/CNN residual/regime/run-length/update/fallback 因果消融；
T5.4.5/T5.4.6 承接 long horizon 与 randomized mismatch。任务板将 T5.4.2 置为 Done，并启动 T5.4.3。
