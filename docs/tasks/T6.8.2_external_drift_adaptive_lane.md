# T6.8.2 一般 drift-adaptive decoder matched-budget lane

- **Task ID**：T6.8.2
- **日期**：2026-07-18
- **状态**：Done（外部预算资格负结果）

## 输入材料

- T6.5.2 unified execution contract 与 5,000-us strict host-update ceiling；
- T6.5.3 pilot/formal split、endpoint cells 与 24 formal seed clusters；
- T6.7.1/T6.7.2 raw formal trajectories 和 static/Window/EWMA/Kalman/Route-A 结果；
- Adams–MacKay BOCD 论文、`y-bar/bocd` MIT 源码；
- Bhardwaj overlapping-window QEC drift、Sivak calibration prior 与 Stein calibration-conditioned decoder 一手来源。

## 实际完成内容

1. 导入并固定 `y-bar/bocd@5f272b1f...`，保存 MIT license、source hash 和未修改边界，上游 3 tests 通过。
2. 实现 observed-only BOCD Window/EWMA bank router；27 个候选仅在 pilot 选择，冻结 `h8_r1_p35` 后执行 formal。
3. 在 21 endpoint cells × 24 formal seeds 上重建 504 条 trace，共 24,772,608 decisions/method；逐轨迹与 T6.7 parent 做 input/truth hash 对齐。
4. 比较 static、Window、EWMA、Kalman、Route-A 与 external BOCD 的 Pauli/average/p95/worst/family LER、paired CI、detection/false detection/lag 与 budget。
5. 建立证据完整性与性能资格双层 verdict。Route-A 相对 external paired LER 较低，但 external 发生 1 次 strict wall-clock deadline miss，故 matched-budget claim 失败。
6. 深审修复两个非显然问题：selector 不再允许零动态检测的退化候选；mutation audit 不再因已有性能 gate 失败而产生全通过假象。

## 产物路径

- `cnn_fpga/benchmark/external_drift_adaptive_lane.py`
- `third_party/bocd/`
- `docs/t6_8_2_external_drift_adaptive_lane.json`
- `docs/t6_8_2_external_drift_adaptive_lane_source_data.csv`
- `docs/external_drift_adaptive_lane.md`
- `tests/test_external_drift_adaptive_lane.py`

## 验证方式和结果

- verdict：`COMPLETE_EXTERNAL_DRIFT_ADAPTIVE_LANE_ROUTE_A_LOWER_LER_BUDGET_FAIL`；
- evidence integrity 11/11；performance gate 0/1；semantic mutations 10/10；
- trace binding：504 external = 504 parent，input/truth mismatch 均 0；
- external 与 EWMA 在 102/504 条 error traces 上不同；
- external − Route-A LER：`1.00184e-5 [7.66793e-6,1.23689e-5]`；
- update wall-clock p95 `631.5 us`，worst `13,004.1 us`，1/13,104 超过 5,000 us；
- `python -m pytest -q tests/test_external_drift_adaptive_lane.py third_party/bocd/tests`：12 passed。

## 简化实现 / 伪实现复核

没有把 external paper 名称贴到内部 EWMA；上游 BOCD 源码被真实执行且输出影响动作。没有用 formal 调参、没有删除超时 outlier、没有以 p95 冒充 worst deadline，也没有把 cross-code 文献映射写成 exact reproduction。缓存绑定执行语义并有 reviewed one-time migration；current-state verifier 绑定 runner、upstream、parents、CSV 和 mutation targets。

## 风险复核

新增 R-N123：LER paired outcome 为正但外部 strict budget 失败；若正文省略 budget failure，会把描述性结果误写成 matched external superiority。Bhardwaj exact reproduction 与 general SOTA 也仍不成立。

## 是否需要插入新 task

不插入。T6.8.7 负责 claim matrix 与撤销条件；下一项 T6.8.3 按既定顺序导入 official GQF。

## 对任务板的同步说明

`T6.8.2 -> Done`；`T6.8.3 -> In Progress`；当前推荐任务切换为 T6.8.3。
