# T3.1.1：standard-binning baseline integration

- **日期：** 2026-07-15
- **状态：** Done

## 输入与通过标准

- T1.1.1 已验证的 `standard_binning_1d` 数学与半开 cell convention；
- T1.3.4 的同 trace、paired decoder comparison；
- T2.4.2/T2.4.3 sensitivity 表与冻结 legacy P4 runner；
- 通过标准：fixed half-cell nearest-lattice recovery 出现在所有当前/未来主要 decoder 比较，
  且不通过重命名旧 `static_linear` 伪造接入。

## 执行方案与实际完成

1. 将 recovery decision 与 hidden truth evaluator 分离；decision 只接受 centered 2D syndrome。
2. 冻结无调参 descriptor 和 major-comparison registry；required schema 漏项/重复均 fail closed。
3. 把 standard failures 接入 T1.3.4 五方法同 buffer 账目，并新增 standard-to-static paired metric。
4. 生成 source-bound production JSON；更新当前主报告、README、风险和任务板。
5. 对边界、truth isolation、schema mutation、账目、artifact reproducibility 做直接测试。

## 产物

- `cnn_fpga/benchmark/standard_binning_baseline.py`
- `cnn_fpga/benchmark/adaptive_drift_alignment.py`
- `tests/test_standard_binning_baseline.py`
- `tests/test_adaptive_drift_alignment.py`
- `docs/standard_binning_baseline.md`
- `docs/t3_1_1_standard_binning_validation.json`
- `docs/new_tasks/T3.1.1_standard_binning_baseline.md`

## 生产结果与反简化检查

- 72,000 个 paired samples；五方法共享 trace SHA
  `975a606105be9ba2b28f81466367d38142e941515276f0556f7dde1aae770439`；
- Standard / old static / Window / EKF / oracle LER：
  `0.060417/0.061389/0.022639/0.025319/0.011389`；
- standard minus old static `-0.000972 [-0.001280,-0.000664]`，不是按预期方向硬编码的通过门；
- 10/10 machine gates；focused+adjacent `92 passed`；
- 显式 `tests/` 全量为 `817 passed, 4 skipped, 4 failed`；4 个失败仍仅来自 R-N012
  的两份旧 FR8/P4 缺失文档；`compileall` 与 `git diff --check` 通过；
- `standard_binning_logical_class` 无 truth/noise/history 参数，hidden cell parity 只在 paired evaluator；
- frozen P4 `static_linear` 明确登记为 N/A，未改历史语义。

## 风险与插入任务判断

新增 R-N047：旧 static-calibration MAP 在当前 trace 显著但小幅差于 standard，不能作为 T3.1.2
的正式 static MAP 结论。不插入 task；紧接的 T3.1.2 本身就要求以 training-set average
noise/observation parameters 重建 static MAP，并必须保留同 trace standard row。

## 对论文 claim 的影响

可报告显式 standard anchor 和当前场景反证；不得写成 general MAP ranking、finite-energy/
protocol-aware/FPGA/QPU 结论。

## 任务板同步

T3.1.1 标为 Done；T3.1.2 标为 In Progress；当前推荐任务更新为 T3.1.2。
