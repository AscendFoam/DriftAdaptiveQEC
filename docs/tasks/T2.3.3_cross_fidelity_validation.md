# T2.3.3：执行跨保真度交叉验证

- **日期：** 2026-07-14
- **状态：** Done
- **证据范围：** q-axis common residual-parity metrics + separated model-native metrics

## 输入与通过标准

- `docs/new_task_board.md` T2.3.3；
- T2.2.1 finite-squeezing effective model；
- T2.3.1 generic finite-Fock、T2.3.2 completed analytic SBS；
- 前移完成的 T2.3.8 noise-transfer surrogate；
- Fock/effective/noise-transfer/syndrome 的 LER、occupancy、`F_avg` 方向一致，高/低
  squeezing 分区，不一致必须形成误差归因表。

## 实际完成

1. 冻结 3/5/8/10/12 dB 与同一 channel/measurement/ancilla-peak input contract；
2. 实现 finite-Fock q/p folded MAP response、Gaussian convolution、capture/mass diagnostics；
3. 运行 effective 200k/点、exact noise-transfer、direct state-density syndrome MAP；
4. 运行 Fock SBS 六 Pauli state native survival/conditional/code-weighted fidelity；
5. 分开 common Pauli metrics 与 model-native occupancy，不做不同分母的数值排名；
6. 完成 12 dB 五点 cutoff sweep、高/低区门、四项 error attribution；
7. 将 operational Fourier-p 失败保留为 negative evidence，并依规则插入
   `T-RISK-20260714-01`。

## 产物

- `physics/cross_fidelity_validation.py`
- `tests/test_cross_fidelity_validation.py`
- `docs/cross_fidelity_validation.md`
- `docs/t2_3_3_cross_fidelity_validation.json`
- `docs/tasks/T2.3.3_cross_fidelity_validation.md`
- `docs/new_tasks/T2.3.3_cross_fidelity_validation.md`

## 验证摘要

- direct：`42 passed`；
- adjacent Fock/effective/protocol slice：`278 passed`；
- full `tests/`：`718 passed, 4 failed`；四个失败仍仅 R-N012；
- production：14/14 gates PASS；
- 10/12 dB noise-transfer↔syndrome q-LER gap 最大 `3.93e-6`；
- effective 200k MC↔noise-transfer 最大 z `0.8904`；
- high-dB Fock-q↔syndrome 最大绝对差 `3.54e-4`；
- 3 dB clipping mismatch `0.016766`；
- 12 dB `N=24→48` q-LER `0.006046→0.000422`，capture
  `0.943168→0.996693`；
- operational Fourier-p minus q high-dB gap `>0.48`，没有被隐藏或平均掉。

## 反简化检查

没有只画一条同源 Gaussian 曲线：Fock density、direct state density、stochastic envelope、
analytic alias 和 protocol SBS 分别执行。共同指标和原生 occupancy 分表；p-axis 失败不会
通过 square-symmetry projection 冒充 coherent 结果。每个 dB 都有六态 Fock SBS，12 dB
另做五 cutoff，不以单点 `N=24` 下结论。

## 风险与 claim

新增 R-N041 并插入 `T-RISK-20260714-01`。R-N037/R-N038/R-N040 部分量化但不关闭。
当前只允许 q-axis/cutoff-aware/Pauli-twirled 趋势；two-axis coherent Fock `F_avg` 保持
blocked。

## 后续

先执行插入任务 `T-RISK-20260714-01` 冻结两轴 symplectic normalization，再按原顺序进入
T2.3.4 differentiable SBS trajectory。

## T-RISK-20260714-01 回灌

坐标根因已修复并保留 legacy negative path。10/12 dB canonical Fock 最大 q/p LER 差
`1.51e-7`，legacy ambiguous path 最小差 `0.4182`；production 更新为 15 gates。
R-N041 降为 Mitigated。允许 axis-resolved q/p 与明确的 independent-axis projection；
coherent joint-axis correlation/process fidelity 仍不在证据范围。
