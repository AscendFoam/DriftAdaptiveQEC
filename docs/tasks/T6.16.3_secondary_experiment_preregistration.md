# T6.16.3 Phase 6C 二级实验预注册与只读边界

- Task ID：`T6.16.3`
- 日期：2026-07-20
- 状态：Done

## 输入材料

- `docs/t6_16_1_secondary_method_source_audit.json`：11 个冻结来源、12 个具体方法与来源级指标边界。
- `docs/t6_16_2_comparison_ontology.json`：六条 comparison lane、46 个 metric 与 exact task-signature 规则。
- `docs/t6_10_1_causal_headroom.json`、`docs/t6_15_5_route_a_v5_final_evidence_gate.json`、`docs/t6_9_3_route_a_final_evidence_gate.json`：V5 early-stop、Phase 6B 终态和历史 V4 evidence gate。
- T3.2.8、T4.4.5、T5.5.2、T5.5.4、T6.8.3--T6.8.6 的既有 AQEC、checkpoint、硬件与外部复现产物。

## 实际完成内容

1. 新增 `configs/literature/t6_16_3_secondary_preregistration.json`，对 T6.17.1--T6.19.3 九项 downstream task 逐项冻结 21 个字段：lane、execution type、entry gate、来源/源码版本、环境、adapter、split、seed、config、sample size、CRN pairing、primary metrics、统计量、停止规则、runtime/memory budget、失败分支、允许 evidence grade 和禁止行为。
2. 冻结统一统计合同：95% CI、20,000 次 paired bootstrap、2,000 次 threshold bootstrap、seed-cluster/trajectory resampling、task 内 Holm、多 lane 不汇总 p-value/胜场/总分、零事件 exact one-sided upper bound，以及保存全部 raw counts/denominator/attempted cells。
3. 建立独立 `phase6c-secondary-v1` seed namespace。T6.17.2 的 32 个 CNOT seeds、T6.18.1 的 24 个 AQEC seeds、T6.18.2/T6.18.3 各 32 个 seeds 和 T6.19.1 三个 P&R seeds 均不重叠；T6.17.3 只读复用 parent trace，不允许训练、超参搜索或 checkpoint 重选。
4. 冻结外部复现边界：Noh CNOT 无已确认官方代码时先走 source-sufficiency gate；structured CPD 固定 official `amazon-science/LatticeAlgorithms.jl@01f9bf1f6970b3e229b43aac9da3325c75518db8` / Apache-2.0；AQEC 只做 project-native common-wall-clock replay；工具或协议缺失可输出 `BLOCKED/PARTIAL/INELIGIBLE/NEGATIVE/null`，不得自写简化替代栈求正值。
5. 建立 Phase 6B 只读锁：固定 V5 `10%` LER gate、`12%` incremental-action gate、`NONE_EARLY_STOP` comparator、未创建 formal split、未运行 step/telegraph tail endpoint、20 个 Dropped task 和零 V5 downstream output；Phase 6C 不得改变 T6.15.5、挽救 V5 或解锁 T6.9.2。
6. 同时保存 exact-byte 与 scientific-semantic lock。任务板/上游 provenance 重建可改变时间戳和 parent byte hash，但不能改变 verdict、gates、claims、headroom、dropped/absence 或权限。每次验证都会实时重算 6 个 Phase 6B lock 和 14 个 implementation/config/source/ontology/parent/Source-Data lock，而不是只检查哈希字符串长度。

## 产物路径

- `configs/literature/t6_16_3_secondary_preregistration.json`
- `cnn_fpga/benchmark/secondary_experiment_preregistration.py`
- `tests/test_secondary_experiment_preregistration.py`
- `docs/t6_16_3_secondary_preregistration.json`
- `docs/t6_16_3_secondary_preregistration_source_data.csv`
- `docs/secondary_experiment_preregistration.md`

## 验证方式和结果

- `python -m cnn_fpga.benchmark.secondary_experiment_preregistration`
  - verdict：`PASS_PHASE6C_READONLY_SECONDARY_PREREGISTRATION`
  - gates：`17/17`
  - targeted semantic mutations：`17/17` detected
  - Source Data：158 rows
  - Phase 6B live locks：`6/6`
  - live input locks：`14/14`
- `python -m pytest tests/test_secondary_experiment_preregistration.py -q --basetemp .pytest_tmp_t6163`
  - `12 passed`
- 语义突变覆盖：删除 task、错 lane/metric、替换 official commit、静默工具替代、伪写 adapter 已执行、seed 复用、按 round bootstrap、跨 lane 汇总、删除 runtime failure、绕过 conditional entry、发明 V5 RTL、改变 Phase 6B 语义锁、把 10% 门降为 1%、允许 atlas 升级主结论、截断 parent hash 和伪造 mutation count，均 fail closed。

## 深度检查与修正

- 首版实现错误假设 T6.10.1 仍有最终 gate 字段；已改为从权威 T6.15.5 `headroom_recomputation` 读取并锁定 `0.10/0.12`，避免把旧中间 schema 当最终合同。
- T6.9.3 没有 `state_legend`；semantic projection 已改为冻结 `aggregation_policy/claims/paper_decision/figure_table_plan/verdict`，既允许 provenance 重建，又不能升级论文结论。
- 首版 G16 只检查 64 字符哈希，存在生成后输入被替换仍通过的风险；已增加 live recomputation，并用测试证明 metadata-only 变化不影响语义锁、科学 gate 变化必然改变语义哈希。
- 所有 adapter 明确为 `PLANNED/EXISTING/CONDITIONAL` 而非“已执行”。本 task 只冻结实验，不把计划写成结果；缺 Julia/Yosys/nextpnr 只触发后续预注册失败分支。

## 风险复核

- `R-N135` 降为 `Mitigated/Monitor`：post-formal comparator reselection、10% 门修改、tail endpoint 回填和 formal 回写已有 live semantic lock 与 mutation gate；T6.19.3 仍需做终态复核。
- `R-N136` 保持 `Open/Immediate`：预注册已封住近似实现冒充 exact reproduction，但 Noh circuit source sufficiency、official Julia CPD reproduction 和 downstream finite-size checks 尚未真正执行。

## 是否需要插入新 task

不插入。现有 T6.17.1--T6.19.3 已覆盖所有执行、失败和完整性分支；为获得正值而新增 substitute/rescue task 会违反本预注册。

## 任务板同步

- `docs/new_task_board.md`：T6.16.3 `In Progress -> Done`；当前推荐切换为 T6.17.1，并将其设为 `In Progress`。
- `docs/new_risks.md`：更新 R-N135 和本 task 的“不插入”判断。

