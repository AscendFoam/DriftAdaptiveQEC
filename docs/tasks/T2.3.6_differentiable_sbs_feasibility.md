# T2.3.6：cutoff/batch/horizon 可行性扫描

- **日期：** 2026-07-14
- **状态：** Done

## 输入与通过标准

- T2.3.4 joint cavity--ancilla differentiable trajectory；
- T2.3.5 已验证的 reward/score Feedback-GRAPE gradient；
- 任务板要求：报告 CPU memory、CUDA VRAM、runtime 与 numerical stability，并确认
  2--10 cycles teacher-training envelope 或触发证否。

## 实际完成

1. 新增 paper-scale 72,913-parameter GRU10--256--256--15 causal policy。
2. 每点真实运行随机 trajectory、reward/score backward 与 Adam update，记录非零 gradient
   和 parameter-update norm。
3. 新增子进程隔离、1 warm-up + 3 repeats、CPU 2-ms RSS 与 CUDA allocated/reserved peak。
4. 执行 56 CUDA + 9 CPU 点，覆盖 cutoff `8--48`、batch `1--576`、每个 2--10 cycles。
5. 初版全绿后继续扩大 frontier；发现 forward/backward-only 后再加入 Adam 并完整重跑。
6. 生成机器 JSON、65-row source CSV 和 Python-only SVG/PDF/TIFF/PNG 多面板图。

## 主要结果

- cutoff 16、batch 16 的 2--10 cycles 全通过：median `0.208--1.050 s`，peak
  `83.2--303.8 MB`；
- cutoff 16、batch 512、10 cycles：`8.624 s`、`6.233 GB (72.60%)`，pass；
- batch 576：`9.514 s`、`7.015 GB (81.71%)`，memory frontier；
- cutoff 48、batch 16、10 cycles：`13.954 s`，runtime frontier；
- 65 点无 numerical/worker failure；max trace `6.66e-16`、min eigenvalue
  `-9.37e-16`、min gradient/update norm `0.3277/0.0150`。

## 产物

- `physics/differentiable_sbs_feasibility.py`
- `physics/plot_differentiable_sbs_feasibility.py`
- `tests/test_differentiable_sbs_feasibility.py`
- `docs/differentiable_sbs_feasibility.md`
- `docs/t2_3_6_differentiable_sbs_feasibility.json`
- `docs/t2_3_6_differentiable_sbs_feasibility.csv`
- `docs/figures/t2_3_6_differentiable_sbs_feasibility.{svg,pdf,tiff,png}`
- `docs/tasks/T2.3.6_differentiable_sbs_feasibility.md`
- `docs/new_tasks/T2.3.6_differentiable_sbs_feasibility.md`

## 验证、风险与任务板同步

- direct `40 passed`（含 artifact/figure/source-data gate）；显式 `tests/` 全量回归为
  `754 passed, 3 skipped, 4 failed`，四项失败仍仅来自 R-N012 登记的两份缺失旧文档，
  没有 T2.3.6 新回归；
- R-N042/R-N043 更新：resource/horizon 子门缓解，训练收敛、ranking、device fidelity 不升级；
- 不插入新 task：T2.3.7 与 T4.4 已承接 ranking/training；
- T2.3.6 -> Done；T2.3.7 -> In Progress。
