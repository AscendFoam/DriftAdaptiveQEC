# T6.19.1 项目原生同任务预板与软件慢路径 profile

- Task ID：`T6.19.1`
- 标题：建立本项目 CI、MAP、eligible Direct NN 与 V5 Hybrid 的同任务预板 latency/resource profiles
- 日期：2026-07-20
- 状态：Done
- verdict：`PASS_STATIC_MAP_LUT_PREBOARD_PROFILE_OTHERS_NA_HOST_STAGES_SEPARATE`

## 输入材料

- `E6191_PROJECT_PREBOARD_PROFILES` 预注册：只允许已有可综合 RTL 进入硬件表；target=`GW2AR-LV18QN88C8/I7`、27 MHz、P&R seeds `1/7/19`、host repeats=1000。
- T5.5.2 / T-RISK-20260716-01 的 MAP-LUT fast-path RTL、整数 reference、CXXRTL 与目标器件工具链。
- T6.17.3 learned eligibility：16 个 family 中 same-task eligible=0。
- T6.15.5：V5 在 T6.10.1 headroom gate early-stop，未产生 downstream RTL/profile。
- T4.3.2 atomic parameter bank 与 Window/EWMA/Kalman 真实实现。

## 实际完成内容

1. 先执行资格审计，而不是为表格补 demo RTL：
   - `CI_if_rtl`：`N_A_NO_INDEPENDENT_CI_RTL`；
   - `static_map_lut_if_rtl`：唯一合格硬件行；
   - `v5_fast_path_if_rtl`：`N_A_NO_V5_RTL_EARLY_STOP_AT_T6_10_1`；
   - `eligible_direct_nn_if_rtl`：`N_A_NO_SAME_TASK_ELIGIBLE_DIRECT_NN_RTL`。
2. 初次汇总检出 `gkp_fast_path_core.sv` 已不再匹配旧 T5.5.2/T-RISK 源哈希，因此没有复用过期数字，而是重新执行完整 CXXRTL build、fault/commit trace、双银行×双 phase×1024 code 穷举、Yosys 综合和三种子 nextpnr。
3. 当前 RTL 的 CXXRTL 共 4,316 个 map-valid 行，mismatch=0；fault/commit 226 cycles 与 exhaustive 4,102 cycles 均 exact。
4. seeds 1/7/19 全部通过 27 MHz：
   - Fmax=`41.0829/42.2119/41.0240 MHz`，min/median/max=`41.0240/41.0829/42.2119 MHz`；
   - LUT4=`3377/3379/3387`，FF=865，BSRAM=8，MULT18X18+MULT9X9=2；
   - critical period=`24.341/23.690/24.376 ns`。
5. 6-cycle、II=1 在 27 MHz 下严格换算为 source-to-action `222.222 ns`、II `37.037 ns`。资源范围是完整 `gkp_fast_path_synth_top`（MAP-LUT、event/fault/state 与小引脚 activity/observability harness），不是 MAP ROM 单体面积。
6. 对 Window/EWMA/Kalman 各执行 32 次 warm-up + 1000 次真实更新，使用同一确定性 observed residual trace；逐次记录 update、MAP-LUT compiler、512-byte chunk 的内存内事务传输、production 4000-cycle residency software commit/readback。
7. MAC/state/workspace 采用既有 method-private 成本账本：Window `128/32 B/512 B`、EWMA `136/80 B/512 B`、Kalman `7121/1740 B/2048 B`；明确不包含共享 periodic-characteristic frontend 的 NumPy exp/reduction，也不作为硬件资源。
8. 生成 3 条 hardware-seed 与 3000 条 host-repeat 原始行。gate 从 CSV 逐方法逐阶段重算 p50/p95/p99/worst，并逐行验证 image SHA=readback SHA。
9. power、jitter、deadline、board-measured latency、physical transfer/commit 全部保持 null；software 与 hardware 表不做 cross-table ranking。

## 产物路径

- `configs/literature/t6_19_1_preboard_profiles.json`
- `cnn_fpga/benchmark/phase6c_preboard_profiles.py`
- `docs/t6_19_1_project_preboard_profiles.json`
- `docs/t6_19_1_project_preboard_profiles_source_data.csv`（3,003 rows）
- `docs/project_preboard_profiles.md`
- `tests/test_phase6c_preboard_profiles.py`
- 当前源码重新生成的：
  - `docs/t_risk_20260716_01_rtl_equivalence.json` 与 Source Data；
  - `docs/t5_5_2_target_device_synthesis.json`、三种子 raw reports/logs 与 Source Data；
  - `docs/t4_3_2_atomic_parameter_bank_validation.json` 与 7,518-row Source Data。

## 验证方式和结果

- T6.19.1：12/12 live gates，13/13 semantic mutations。
- CXXRTL：8/8 gates，4,316/4,316 map-valid rows exact。
- target synthesis/P&R：12/12 gates，三种子均通过 27 MHz。
- atomic parameter bank：17/17 gates；3,745 prefix cuts、3,745 byte flips、10 chunk/commit cases、15 negative paths。
- focused + RTL/P&R：`45 passed, 1 skipped`；skip 仅为可选工具发现环境，不是证据缺失。
- expanded adjacent 首轮：`170 passed, 1 skipped`，4 个失败全部定位为合法重生成证据后的下游 exact-hash lock 过期；未放宽测试，而是依次重建 T6.17.1--T6.18.3 与 T6.19.1 的 live bindings。
- 最终联合回归：`259 passed, 1 skipped`（151.87 s），覆盖 Phase 6C source/ontology/preregistration、CI/CPD/learned/AQEC/multimode、原子银行、统一 comparator、RTL 等价与三种子 P&R；skip 仍仅为可选工具发现环境。
- `python -m cnn_fpga.benchmark.phase6c_preboard_profiles --verify-only` 通过，并实时复核源文件、父工件、CSV 与 Phase6B/learned lock。

## 深度检查与非 demo 修正

- 没有把旧哈希不匹配当作“无关变化”；实际重新编译和三 seed route 后，Fmax/LUT 确有变化。
- 没有把 CI 的算法 O(1) 复杂度填成 FPGA latency，也没有给 V5/Direct NN 写占位 RTL。
- 没有把 host Python p50/p99 除以样本数变成纳秒 decoder latency。
- 没有把 software atomic-bank 内存传输写成物理 DMA/UART；production residency 只复现软件 commit code path。
- 没有把 full fast-path core 的资源误称 MAP ROM 面积；harness 与 transport 排除项均显式保留。
- 初版只用 CSV 哈希保护摘要，深审后增加逐原始行统计重算、readback digest、P&R逐字段父报告比对与 runtime-budget mutation。
- 旧 `test_target_device_synthesis.py` 固定 router placement 数字会拒绝合法源码重生成，已改为验证全种子汇总、目标余量、资源容量和关键路径不变量；没有放松 27 MHz 或三种子门。

## 风险复核

- 更新 R-N105 的当前源码 P&R 数值，旧 39.7--40.4 MHz/3357--3362 LUT 不再作为当前证据。
- 新增 R-N140：唯一硬件行是完整 project fast-path 的 pre-board estimate；CI/V5/Direct NN 均 N/A，外部 same-task comparator 仍待 T6.19.2，因此不得声称 fastest/SOTA 或方法级硬件优胜。
- R-N125/R-N127/R-N129 保持 Open/Critical：真实 transport、board source-to-action、deadline、jitter、power 仍由 T6.9.2 阻塞。
- 不插入新 task；T6.19.2 已负责外部 FPGA source normalization，T6.19.3 负责最终分面 integrity，新增 rescue task 会破坏只读预注册边界。

## 任务板同步

- `docs/new_task_board.md`：T6.19.1 从 In Progress 更新为 Done。
- T6.19.2 从 Todo 更新为 In Progress，当前推荐任务同步为 T6.19.2。
