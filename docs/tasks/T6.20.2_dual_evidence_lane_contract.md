# T6.20.2：双证据 lane 机器合同

- **Task ID：** T6.20.2
- **标题：** 冻结双证据 lane 的 claim、task signature、接口与禁止跨 lane 迁移合同
- **日期：** 2026-07-21
- **状态：** Done

## 输入材料

- `docs/multimode_strong_baseline_registry.md` 与 T6.20.1 的 strong-baseline/task-signature 边界；
- `docs/t7_1_1_claim_evidence_boundary_matrix.json` 的历史 claim/evidence 层级；
- `docs/t6_16_2_comparison_ontology.json` 的 13-field task signature、value-state 与禁止跨 lane 排名规则；
- `cnn_fpga/runtime/unified_execution_contract.py` 的当前 observed/action/MAP-LUT/A/B-bank 合同源码；历史 T6.5.2 JSON 在邻接回归中暴露上游 binding stale，因此不作为本合同父证据；
- `docs/two_domains_three_timescales.json` 的 host/FPGA ownership 与 transaction bridge；
- T6.2.1/T6.2.2/T6.19.1 的 actual single-mode production RTL、CXXRTL 与 P&R 证据；
- pinned `third_party/LatticeAlgorithms.jl/src/gkp.jl`，仅作 official-source 绑定，不冒充 exact MLD reproduction。

## 实际完成内容

- 新增 `cnn_fpga/benchmark/dual_evidence_lane_contract.py`，生成并校验 Phase 6D 机器合同；
- 固定两个不可互相补门的 primary lane：
  - `MULTIMODE_SOFTWARE_ALGORITHM` 只以 frozen-benchmark per-round `p_L` 为主指标，时间边界为 software source-to-decision/update；
  - `SINGLE_MODE_DETERMINISTIC_RTL` 只以 actual single-mode RTL 的 6-cycle、II=1、atomic/fail-closed property 为主对象；
- 将 `LEARNED_APPROXIMATION_EXTENSION` 固定为依赖 multimode parent 的可替换 posterior/LLR/coset/action 近似，主指标和独立 gate 均为空，失败即 `DROPPED_TO_ABLATION`；
- 每条 lane 均固定完整 13-field task signature、observable、action、precision、deployment status、required evidence 和 prohibited claims；
- 固定 4 个 integration interfaces，并把 slow proposal→candidate image 明确标成 `CONTRACT_REUSE_ONLY_REQUIRES_SCHEMA_EQUIVALENCE`，不产生 multimode RTL deployment implication；
- 固定 9 条禁止迁移：multimode LER→current RTL、6-cycle→multimode、CNN→algorithm SOTA、CNN→RTL safety、pre-board→board measured、opened development→formal、source presence→reproduction、true-metric CPD→exact oracle、cross-lane weighted score；
- 固定 5 条原子 claim。完成前深审将“历史 bit-accurate/CXXRTL/P&R 当前证据”和“未来 T6.25.4 property 主张”拆开，避免把未完成形式性质提前写成当前结果；
- 绑定 10 个 live 父证据/源码，生成 31-row 可逆 Source Data、human-readable 合同、16 个 fail-closed gates 与逐门 semantic mutation audit；
- T7.3.1 的用户已有 partial 草稿/代码/输出未读取写入流程、未修改、未暂存。

## 产物路径

- `cnn_fpga/benchmark/dual_evidence_lane_contract.py`
- `tests/test_dual_evidence_lane_contract.py`
- `docs/dual_evidence_lane_contract.md`
- `docs/t6_20_2_dual_evidence_lane_contract.json`
- `docs/t6_20_2_dual_evidence_lane_contract_source_data.csv`
- `README.md`
- `docs/new_task_board.md`
- `docs/new_risks.md`
- 本记录及 `docs/new_tasks/` 镜像

## 验证方式和结果

- 端到端重建/verify：verdict=`PASS_DUAL_LANE_CONTRACT_FROZEN`，16/16 gates 通过；
- semantic mutation：16/16 检出，逐门覆盖 schema identity、lane fields、task signature、multimode/RTL timing、learning promotion、bridge deployment、forbidden transfer、claim state、board-null、artifact hash、Source Data、human contract、parent IDs 与 global-score 禁令；
- 定向测试：`python -m pytest tests/test_dual_evidence_lane_contract.py -q`，15 passed；
- 本任务与 comparison/claim/board-governance 邻接回归：44 passed；历史 unified-execution 套件单独复核为 12 passed、1 个 R-N145 stale-binding failure；
- Source Data：31 行逐对象 canonical JSON，可逐行重算 SHA-256，且与 JSON contract 双向一致；
- live evidence：10 个 report/source/RTL/official-source binding 的 bytes 与 SHA-256 现场复算通过；
- 负向测试显式证明：给 multimode lane 添加 RTL cycles、把 RTL code family 改成 multimode、给 student 添加 primary gate、把 bridge 改成 deployed、填入 board latency，均使对应 gate 失败。
- 邻接回归首次运行 `55 passed, 2 failed`：任务板固定状态测试已随 T6.20.2 完成而修正；另一个失败是历史 T6.5.2 JSON 对 `t4_3_2_atomic_parameter_bank_validation.json` 的 SHA-256 stale（expected `c2c3e6...`，live `7495bb...`）。本合同随后改为绑定当前 unified execution 源码并从源码常量提取 schema IDs，不再以 stale 报告构造父证据；历史快照问题保留给 R-N145/T7.4.1，未事后覆盖旧 formal 链。

## 非简化实现复核

本 task 没有只写一张叙事表。机器 validator 会校验 closed schema、完整且互异的 13-field signature、证据层级、claim 状态、接口 deployment implication、禁止迁移全集、live hashes、lossless Source Data 与 human contract。反例是对真实字段逐项变异后重新执行 gate，而不是信任预写 `passed=true`。深审还主动拆分历史 pre-board 实现与未来 property 终门，说明验证确实能发现叙事层级问题。当前合同也没有把 pinned exact-MLD source 冒充 reproduction，multimode SOTA 保持 `CONDITIONAL_FUTURE`，board measurement 保持 `BLOCKED_NULL`。

## 风险复核

- R-N147 从 Open 降为 Mitigated、迫切度从 Immediate 降为 Soon；机器合同已阻断当前最直接的跨 lane 冒领；
- R-N145 保持 Open：历史 T6.5.2 JSON 的 T4.3.2 source binding 已 stale；本 task 通过改绑 current source 隔离，而不是重写历史 canonical artifact；
- R-N146/R-N148--R-N153 继续保持其原状态，分别由 T6.20.3--T6.24、T6.26 承接；
- T7.3.1 partial 草稿继续 Blocked/read-only，不能绕过 T6.21 exact/oracle 层级和 T6.26 final claim gate。

## 是否需要插入新 task

不需要。剩余风险已有 T6.20.3--T6.26.4 和 T7.3.8--T7.3.9 正常任务承接；新增旁路会破坏顺序和新 formal 的 untouched 边界。

## 对任务板的同步说明

- T6.20.2 标记 Done；
- T6.20.3 标记 In Progress 并成为当前推荐；
- T7.3.1 继续 Blocked，用户已有 partial 文件保持未触碰。
