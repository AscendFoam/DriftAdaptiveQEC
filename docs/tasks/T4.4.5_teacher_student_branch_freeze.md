# T4.4.5 任务记录：teacher-student strong/falsified branch freeze

- Task ID：T4.4.5
- 日期：2026-07-16
- 状态：Done

## 输入与目标

只读消费 T4.4.1 fresh teacher、T4.4.2 frozen analysis、T4.4.3 selected student 与 T4.4.4 physical
gain-retention artifacts。目标是在不重新选择 evaluation 结论的前提下，机器冻结 qualified strong 或
falsified fallback 分支、允许/禁止 claim 和后续撤销 gate。

## 实际完成

新增 fail-closed 判定器、17 项 direct tests、JSON 和 112-row Source Data。判定器逐项复核 72 个 parent
gates、4 个 implementation hash、7 个 checkpoint/student/source file hash、fresh/post-hoc/strict-split 语义、
六个预注册 retention 指标和 cutoff-dependent MF reversal。

当前 8/8 evidence predicates 通过，激活 `qualified_student_retention`。允许范围仅为 matched two-level
finite-cutoff、10-cycle、新 paired seeds、cutoff12/16 的 student retention 和 float 解析压缩；universal
NMF-over-MF、机制唯一性、global optimizer/oracle、leakage/OOD/long-horizon、quantized RTL/FPGA/board
仍禁止。fallback `drift_regime_aware_map_lut` 已完整登记但未激活。

## 产物

- `cnn_fpga/benchmark/teacher_student_branch_freeze.py`
- `tests/test_teacher_student_branch_freeze.py`
- `docs/reports/phase4/teacher_student_branch_freeze.md`
- `docs/t4_4_5_teacher_student_branch_freeze.json`
- `docs/t4_4_5_teacher_student_branch_freeze_source_data.csv`

## 验证

- 11/11 machine gates；112 Source Data rows；
- direct `17 passed`；
- T4.4.1--T4.4.5 邻接与 protocol contract 使用 workspace `--basetemp` 后 `129 passed`；protocol/branch 子集 `53 passed`；
- 全量测试在 `605.7 s` 的执行上限处运行到 `19%`，终止前未输出失败；该结果只记为“超时且当时未见失败”，不冒充全量通过；
- 9 类 mutation 覆盖 status/gate/source/file/threshold/undefined ratio/fresh/split/counterevidence；
- 最低 point/CI-lower retention `0.981457/0.944501`；
- cutoff12 MF mean/teacher `0.557115/0.552952`，cutoff16 `0.579684/0.593930`，反转保留。

## 风险复核与后续

新增 R-N079：qualified branch 容易在后续写作中被误读为一般 NMF/硬件结论；机器 artifact 已固定七类
禁止 claim 和 T5.2/T5.4/T5.5/T6 撤销条件。正常任务已覆盖，不插入新 task。
