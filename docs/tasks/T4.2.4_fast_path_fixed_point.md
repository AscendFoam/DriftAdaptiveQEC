# T4.2.4：完成 fast path 定点化

## 状态

Done（2026-07-15）。

## 输入与通过标准

输入为 T4.2.1 MAP image/runtime、T4.2.2 event/frame、T4.2.3 health/fallback 和旧 T2.4.3 component stress。
通过标准是全字段位宽/signedness/rounding/saturation 明确、在线 Python bit-accurate、完整 code domain 与
paired float LER impact 可复算、fault boundary fail closed，且资源/时序不冒充硬件。

## 实际完成

新增 integer-only `BitAccurateFastPath.step_codes` 和离线 replay encoders；冻结 selected 10-bit ADC、8+2
address/interpolation、Q9.12 LLR、event/frame/health/version/digest 全 word contract。对 4 个 precision profiles
编译全部 8 banks，并把 MAP→health→event→frame 组合在同一逐周期 reference 中。

## 验证与深审计

21/21 production gates、15 focused、152 focused+adjacent tests；显式全量 `tests/` 为
`1378 passed, 14 skipped, 4 failed`，4 个失败仍仅为 R-N012 缺失旧 FR8/P4 文档。87,040 exhaustive code rows 全部 hard mismatch 0；selected mean/max
LLR value error `9.46671e-5/0.00488281`。128 paired LER clusters 的 selected ΔLER 为
`+3.05176e-5 [-4.57764e-5, 1.22070e-4]`，CI 跨零。独立重编译/重跑 hash 一致。深审计修正 T4.2.3
health proxy 漏算累计 fault/leakage 两个 uint8 counters（166 -> 182 bits），并把 rounding/saturation 九类规则
写入 machine artifact。

## 产物

runtime、benchmark、两份 tests、主说明、21-gate JSON、128-row LER CSV、87,040-row exhaustive-code CSV，
并同步上游 T4.2.3 artifact 与治理文件。

## 风险与后续

R-N071 登记为 High/Immediate：当前 LER 是 model-matched axis-marginal finite trace；没有 correlated/OOD/fault
LER、设备 ADC/OOD、RTL/综合/FPGA/board。T4.3、T5.1/T5.4/T5.5、T6 正常任务承接。
