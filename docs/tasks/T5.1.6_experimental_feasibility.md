# T5.1.6 任务记录：实验可行性约束

- Task ID：T5.1.6
- 日期：2026-07-16
- 状态：Done

## 输入、通过标准与失败分支

输入为 T4.4.4/T5.1.5 matched-controller lifetime、occupancy、events、cost，T4.3.3 fault-recovery
campaign，T4.2.3 fallback taxonomy，T4.4.3 student runtime failure contract 与 T5.1.4 branch verdict。

通过要求是同报 `p(g)`、e/leakage occupancy、reset burden、parameter slew/saturation、fallback 与 unsafe-action
rate，并且 lifetime 峰值不能覆盖不可部署代价。不同 model layer 不得合成 device feasibility；缺测必须 null。

## 执行方案

1. 只读绑定六个 parent 的 machine gates、artifact/source/checkpoint hash 与实现 hash；
2. 建立 common-100 μs controller table，分开 two-level e events、protocol resets 与 multilevel leakage；
3. 从全部 32 个 fault runs 重算场景及全局 denominator、fallback/reset/unsafe/undefined/ack counts；
4. 单列 component fallback 与 student safe baseline；
5. 冻结 missing-evidence/nonmixing/deployment contracts；
6. 用 semantic mutations 主动尝试填 0、隐藏成本、伪造总体上界、改写 peak/branch/readiness。

## 实际完成与关键结果

- 10 个 controller rows：每行 20 measurements/20 resets/180 active gates；
- cutoff12/16 peak lifetime 为 86.227870/95.253959 μs，但均携带 72,853 scalars、72,266 MAC、
  latency null 与 nondeployable parent flag；
- 8×4 fault runs、767,872 cycles，fallback 11,552、reset request 4、unsafe/undefined 均 0；
- host timeout 单独贡献 11,232 fallback，ack-timeout/awaiting-readback 为 1,596/1,604；
- component 4,096 cycles 全分类守恒；
- 7 个缺失可行性字段显式登记，总体 `NOT_ESTABLISHED`；
- 408-row Source Data、21/21 gates。

## 开发期详细检查

首轮产物为 21/21 PASS。继续做反证审计时发现 validator 对 student fail-closed、component status distribution、
global fallback/reset 总量和 nonmixing ledger 约束不足，存在“报告表看似完整但可被静默改写”的 demo 风险。
随后补充 exact distribution/denominator/peak-max/fail-closed 校验及 19 类参数化 mutation。没有通过放宽 tolerance、
删除高成本场景或用 0 填补缺测来换取 PASS。

## 产物

- `cnn_fpga/benchmark/experimental_feasibility.py`
- `tests/test_experimental_feasibility.py`
- `docs/experimental_feasibility.md`
- `docs/t5_1_6_experimental_feasibility.json`
- `docs/t5_1_6_experimental_feasibility_source_data.csv`
- `docs/protocol_hierarchy.json` / `docs/protocol_hierarchy.md`
- `README.md` / `cnn_fpga/benchmark/README.md`

## 验证

- runner：21/21 gates，408 Source Data rows；
- direct/mutation：`34 passed`；
- T4.4.4/T5.1.5/T4.3.3/T4.2.3/T4.4.3/T5.1.4 adjacent：`94 passed`；
- writer repeated build 的 contract hash 与 CSV 一致；
- missing parent 直接拒绝，stale implementation 使 integrity fail closed。

## 风险与后续

新增 R-N088，监控 two-level occupancy、定向软件 campaign 与装置 feasibility 的边界。T5.2.1--T5.2.3
补 causal physical-fault proxies，T5.3/T5.4 补 finite-energy/mismatch/long-horizon，T5.5/T6 补量化、RTL、板测和
device calibration；均为既有正常任务，不插入风险 task。
