# T2.3.8：实现 Heisenberg noise-transfer 中保真度代理

- **日期：** 2026-07-14
- **状态：** Done
- **前移原因：** T2.3.3 的显式前置依赖；任务 ID 和范围未改变
- **证据范围：** operational-coordinate signal/fluctuation/logical-jump surrogate

## 输入与通过标准

- `docs/new_task_board.md` T2.3.8；
- `docs/experiment_plan.md` §16.1；
- Ralph et al., *Noise Transfer in Fault-Tolerant Quantum Error Correction*, Entropy 26,
  874 (2024), arXiv:2411.05262；
- 在约 10 dB 及以上与 state/Fock reference 对齐，并以低 squeezing/clipping case 明确
  证否；必须独立传播 covariance、loss、measurement efficiency、gain 和 discrete jump。

## 实际完成

1. 建立不可混写的 lattice signal、continuous offset/covariance、logical parity state；
2. 实现 loss signal bias/vacuum injection、measurement equivalent noise、任意 2×2 gain；
3. 解析积分全部 Gaussian alias cells，并输出 central/odd/folded/conditioned/clipping 指标；
4. 对 diagonal covariance 给 exact I/X/Z/Y law，对 correlated case 仅给 marginals 与
   Fréchet bounds；
5. 实现 seedable sample path、3--12 dB sweep、200k production MC；
6. 显式映射 squeezing dB 到 damped-projector Delta，并对四逻辑态做 q-domain
   direct-state/Fock alignment；
7. 加入 45 项 direct tests，包含独立数值积分、解析端点、schema/scope 负测与 demo-size
   MC rejection。

## 产物

- `physics/noise_transfer_surrogate.py`
- `tests/test_noise_transfer_surrogate.py`
- `docs/noise_transfer_surrogate.md`
- `docs/t2_3_8_noise_transfer_validation.json`
- `docs/tasks/T2.3.8_noise_transfer_surrogate.md`
- `docs/new_tasks/T2.3.8_noise_transfer_surrogate.md`

## 验证摘要

- direct：`45 passed`；
- adjacent Fock/finite-squeezing/protocol slice：`194 passed`；
- full `tests/`：`675 passed, 4 failed`；四个失败仍仅 R-N012 的 legacy FR8/P4 文档缺失；
- production：14/14 gates PASS；
- 200k MC alias 最大 z-score `1.3966`，covariance relative error `0.002934`；
- 10/12 dB proxy→direct max relative error 分别 `6.79e-6/1.19e-9`；
- 10/12 dB Fock→direct max relative error分别 `0.00215/0.05710`；
- 3 dB 四态 spread `0.4233`、proxy error `0.2979`，正确判为 clipping dominated；
- unity-gain refresh、measurement inefficiency、loss signal-index scaling、correlated-joint
  fail-close 全部通过。

## 反简化检查

没有把已有 `finite_squeezing_noise.py` 改名冒充新 lane。alias 概率和 cell moments 在测试
中由独立 quadrature 重构；低 squeezing 的 state dependence 和 clipping gap 被作为失败区
保留。Fock alignment 包含四逻辑态、真实 Hermite projection/capture，而不是单个 Gaussian
数值对齐。相关轴不输出伪精确 Pauli joint rate，scope 字符串也被 fail-close validator 锁定。

## 风险与 claim

新增 R-N040：本层仍是 signal/noise moment surrogate；q-domain alignment 不能替代完整
q/p coherent cross-fidelity，相关轴 joint logical rate 只有 bounds。只能用于中保真趋势和
失效区定位，不能替代 T2.3.2 Fock SBS 或 device evidence。

## 后续

不插入新任务。T2.3.8 完成后立即恢复 T2.3.3，执行四 lane 统一 sweep 与误差归因。

## T-RISK-20260714-01 回灌

surrogate 的 stored axes 现明确为 decoder-standardized classical axes，vacuum variance
为 `1`；state/Fock moment 对照显式转为 canonical。10/12 dB proxy→state 最大误差更新为
`0.748%/0.0135%`，Fock→state 为 `0.171%/4.39%`；3 dB proxy mismatch `56.04%`
继续作 clipping 证否。R-N040 的非 joint/coherent 边界不变。
