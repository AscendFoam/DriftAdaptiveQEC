# T2.0.3：sBs g/e/leakage observation 与 reset model

**Task ID：** T2.0.3  
**日期：** 2026-07-14  
**状态：** Done  
**实现：** `physics/sbs_observation_reset.py`  
**测试：** `tests/test_sbs_observation_reset.py`

## 1. 输入与方案

输入包括 `PROTO-SBS-MAIN`、T2.0.2 ideal branch sequence、Sivak readout/reset/leakage Supplement、paper-parameter registry 的 partial fidelity/null matrix，以及 T2.0.3 hidden/observed separation 通过标准。

执行前先拆开四层：ideal Kraus、hidden `g/e/f/higher`、observed `g/e/leakage`、observation-conditioned reset。另从 `K_ge=K_g^Z K_e^X` 确认 Kraus 字符顺序为 `(Z,X)`、执行顺序为 `(X,Z)`，避免 e-run 象限互换。

## 2. 实现内容

- `preparation_kernel[carry,ideal,hidden_pre]`；
- 完整 `readout_confusion[hidden,observed]` 4×3 matrix；
- `reset_kernel[observed,hidden_pre,hidden_post]`；
- f 与 higher reset success 分离；
- X/Z e-run 分开、constituent/cycle leakage run 分开、counter saturation；
- `HiddenAncillaMemory` 与 `ObservedSyndromeMemory` 分离；
- `SBSObservedCycle.as_deployable_dict()` 无 hidden/ideal/truth/carry fields；
- 单步和 seeded multi-cycle simulation；
- 与 T2.0.2 ideal trajectory 直接集成。

## 3. 反简化检查

没有用一个 `error=True` bit 代替观测链：

- hidden higher 可被误分类为 g，truth streak 增长但 observed counter 为 0；
- hidden f 被误分类为 e 时只执行 e-action，不能 truth-assisted 调用 f reset；
- 同一 quadrature 的 e-run 不把 X/Z 交错串相加；
- 文献只有 Fg/Fe，对完整 Ff/off-diagonal 保持 `null`，constructor 强制 full 4×3 输入和 provenance；
- Monte Carlo 分别验证 confusion frequencies、f reset success、higher streak survival；
- 非归一化/负 kernel、缺 provenance、坏 label、counter/memory mismatch 全部拒绝。

## 4. 验证

```text
focused observation/reset: 20 passed
adjacent T1.4.4--T2.0.3: 81 passed
explicit tests/: 299 passed, 4 failed
py_compile: passed
```

4 个全量失败仍仅为 R-N012 两份旧治理文档缺失。

## 5. 风险与 claim

- 新增 R-N027：full 4×3 confusion、injection/reset probability 仍是显式 assumption，不是装置校准；higher 是聚合 state；
- 不插入新任务：T2.2.2、T2.3.2/T2.3.3、T6 和当前后续任务已承接高保真/校准/硬件证据；
- 可称 protocol-aligned hidden/observed/reset effective model；
- 不可称复现 Fig. S5 matrix、真实 f/higher discrimination、真实 reset fidelity 或装置 leakage rate。

## 6. 治理同步

- README 注册模型文档；
- protocol hierarchy 只将 effective observation/reset 升为 implemented；
- `docs/new_risks.md` 新增 R-N027；
- board：T2.0.3 Done，T2.0.4 In Progress。
