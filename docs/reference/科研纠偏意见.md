可以自动化，但你现在**最该做的不是继续自动推进 paper skeleton**，而是立刻把这个量子纠错项目切换到：

> **科研真实性恢复模式 / Research Reality Recovery Mode**

你的直觉是对的：现在项目已经有失控迹象。尤其是你列出的风险：

```text id="36vzbw"
R5/R9：benchmark 范围限制
R10：机制证据只有 single seed
R11：训练复现性未证明
R12：.tflite runtime 不可用
R13/R14：真板仍是 placeholder
R20：correction saturation 触发性未证明
R24：statcalib 未集成 comparator lane
```

这些不是“普通待办事项”，而是**论文可信度风险**。在这些风险没有解决前，paper skeleton 最多只能当内部草稿，不能继续往“论文成品”方向推进。

---

# 我的判断

你现在的问题不是：

```text id="vyuefq"
agent 不够多
自动化不够强
review 文档不够详细
```

而是：

```text id="x8h7ox"
你没有一个足够强的科研证据闸门；
agent 在不断生成内容，但你无法判断哪些内容真实可靠；
项目已经从“做研究”滑向“让 AI 填充一篇看起来像 paper 的文本”。
```

所以接下来要做三件事：

```text id="41fhpl"
1. 暂停 paper skeleton 推进。
2. 停止新增功能和新叙事。
3. 重建代码、实验、图表、结论之间的证据链。
```

---

# 一、立刻冻结当前状态

先让 captain 做一次冻结，不要再让 worker 写新功能。

给 captain：

```text id="9i94bf"
当前项目进入 Research Reality Recovery Mode。

从现在开始，禁止推进 paper skeleton，禁止新增论文 claim，禁止新增功能。
当前目标是验证已有代码、实验、图表和结论是否可信。

请创建或更新：

docs/reality_recovery/
  00_freeze_snapshot.md
  01_claim_evidence_table.md
  02_code_truth_audit.md
  03_experiment_reproducibility_audit.md
  04_figure_and_result_ledger.md
  05_paper_claim_risk_table.md
  06_human_brief.md

要求：
- 不要改代码。
- 不要修风险。
- 只做现状盘点。
- 所有结论必须引用文件路径、脚本、实验日志或图表来源。
```

这一步的目标是把项目从“AI 继续推进”切回“事实盘点”。

---

# 二、把 paper skeleton 降级为“claim inventory”

不要让 AI 继续写完整论文。现在应该把论文草稿拆成一个表：

```markdown id="aj3r0e"
# Claim Evidence Table

| Claim | 出现在论文哪里 | 需要什么证据 | 当前证据 | 证据等级 | 风险 | 是否允许保留 |
|---|---|---|---|---|---|---|
| 方法优于 baseline | Sec 4 | 多 seed + baseline + metric | single seed | L1 | 高 | 不允许 |
| correction saturation 存在 | Sec 5 | 触发实验 + 曲线 | 未证明 | L0 | 高 | 删除或标为假设 |
| .tflite 可部署 | Sec 6 | runtime 成功运行 | runtime 不可用 | L0 | 高 | 删除 |
| 真板验证 | Sec 6 | 真实 hardware / board log | placeholder | L0 | 极高 | 删除 |
```

以后论文中每一句重要 claim 都必须能在这个表里找到证据。

你不需要读一堆长文档，只需要每天看这个表和 `06_human_brief.md`。

---

# 三、不要再让 reviewer 写长篇 for-human 文档

你的 reviewer 文档已经超过你的阅读能力了。现在需要换成**三层人类摘要**。

## 1. 30 秒摘要

```markdown id="7w3t09"
# Human Brief

## 当前结论
项目目前不能继续推进 paper skeleton。

## 最大风险
1. 主要实验只有 single seed。
2. 真板和 .tflite 仍是 placeholder / unavailable。
3. 多个论文 claim 没有图表或日志证据。

## 人类今天只需要决定
是否把下一阶段目标改成：复现实验 + baseline + 图表重建。
```

## 2. 5 分钟摘要

```markdown id="y7keu1"
## What changed
## What is verified
## What is unverified
## What is suspicious
## What decision is needed from human
```

## 3. 详细审查附录

长文档可以继续存在，但你不应该每天读它。

你的规则应该是：

> reviewer 可以写长文档，但必须先写 `Human Brief`，而且 captain 只能把 `Human Brief` 交给你。

---

# 四、重新定义项目目标：先不要写 paper，先做“可信实验包”

现在项目应该从：

```text id="ab7gza"
写 paper skeleton
```

改成：

```text id="h1mtxk"
构建一个可信的最小实验包
```

最低标准：

```text id="qn46wi"
1. 代码能从 clean environment 跑通。
2. 有明确 dataset / simulator / benchmark 范围。
3. 有 baseline。
4. 至少 3 个 seed，最好 5 个。
5. 每张图都能追溯到脚本、配置、日志。
6. 所有 placeholder 明确标记，不能进入 paper claim。
7. 所有失败结果也记录。
```

量子纠错的 ML decoder 领域已经有较高基准和强相关工作，例如 AlphaQubit/transformer decoder 在 surface code 和真实硬件数据上做了系统评估；也有专门的 ML-for-QEC benchmark 工作强调长程依赖建模与基准化评估。所以你的 draft 不能只靠“像样的结果”，必须有清楚的 baseline、实验范围、噪声模型、seed、指标和限制说明。([Nature][1])

---

# 五、先做“反作弊代码审计”，不要做普通 review

普通 reviewer 会检查 bug，但你现在需要的是：

```text id="mgpoyt"
AI 偷懒/兜底/伪实现审计
```

给 reviewer 这个 prompt：

```text id="m5c1mw"
你现在不是普通代码 reviewer。
你是科研代码反作弊审计员。

只检查以下问题：

1. 是否有 mock / placeholder / fallback 被包装成真实实现？
2. 是否有 hardcoded result / hardcoded path / hardcoded metric？
3. 是否有 try-except 吞错后返回默认值？
4. 是否有实验失败后自动走简化路径？
5. 是否有 baseline 不公平？
6. 是否有 single seed 被包装成稳定结论？
7. 是否有图表不是从真实实验日志生成？
8. 是否有 paper claim 找不到代码证据？
9. 是否有真板 / runtime / deployment 仍是 placeholder？
10. 是否有配置项写了但没有真正接入 pipeline？

输出只写三类：

A. Blocking Evidence Fraud Risk
B. Suspicious But Not Proven
C. Clean / Verified

不要写长篇解释。
每条必须引用文件路径和行号。
```

这里关键词是 **Evidence Fraud Risk**。不是说 AI 有主观欺骗，而是它可能生成了“看起来完成、实际没完成”的科研证据。

---

# 六、把当前风险分成三类处理

你列出的风险可以直接分类。

## A 类：论文阻断风险

这些不解决，不能继续写论文 claim：

```text id="52ktkl"
R10：single seed
R11：训练复现性未证明
R13/R14：真板 placeholder
R20：correction saturation 未证明
R24：statcalib 未集成 comparator lane
```

处理：

```text id="z37a55"
不允许进入主论文结果。
只能写在 Limitations / Future Work / Not Yet Verified。
```

---

## B 类：范围限制风险

```text id="nd8vxy"
R5/R9：frozen-set benchmark 范围限制
```

处理方式不是硬修，而是重新定义论文范围：

```text id="9w6ks3"
本文只声称在 frozen-set benchmark 上观察到……
不声称泛化到完整 QEC setting。
不声称真实硬件可用。
```

但如果这个范围太小，项目就应该降级成 workshop / tech report / internal prototype。

---

## C 类：工程可用性风险

```text id="k1fqq1"
R12：.tflite runtime 不可用
```

处理：

```text id="s1rz3u"
如果论文主 claim 不依赖部署，就删除部署 claim。
如果论文 claim 依赖 runtime，就必须先修 runtime。
```

不要把 `.tflite` 写成贡献点，除非它真的跑通。

---

# 七、建立 Result Ledger：每个图表都要可追溯

现在“没有很好的图表和数据结果产出”，说明 paper skeleton 太早了。

新建：

```text id="sd9u2d"
docs/reality_recovery/04_figure_and_result_ledger.md
```

格式：

```markdown id="1b6mmd"
# Figure and Result Ledger

| Figure/Table | Claim supported | Script | Config | Data/log | Seeds | Status |
|---|---|---|---|---|---|---|
| Fig 1 | method overview | 手绘 | - | - | - | OK |
| Fig 2 | accuracy vs baseline | scripts/run_eval.py | configs/frozen.yaml | logs/exp_042 | 1 | Not enough |
| Fig 3 | correction saturation | - | - | - | - | Missing |
| Table 1 | runtime comparison | scripts/bench.py | configs/tflite.yaml | failed | - | Blocked |
```

规则：

```text id="0kgrr0"
没有 ledger 的图，不进 paper。
没有 script/config/log 的结果，不进 abstract。
single seed 的结果，不写稳定结论。
placeholder 的结果，不写成实验。
```

---

# 八、你本人不应该读所有文档，只读“控制面板”

你现在的问题是文档生成速度超过阅读速度。解决方式不是少写文档，而是**分级摘要**。

你每天只看一个文件：

```text id="a9uz0n"
docs/reality_recovery/06_human_brief.md
```

内容固定为：

```markdown id="16yezn"
# Human Brief

## Project State
GREEN / YELLOW / RED

## Can paper writing continue?
No.

## Top 3 blockers
1.
2.
3.

## What was verified today?
- 

## What is still unverified?
-

## What looks suspicious?
-

## Human decision needed
Choose one:
A. Continue recovery
B. Narrow paper claim
C. Pause project
D. Salvage code only
```

这样你每天只需要 5 分钟抓项目，而不是读 reviewer 长文档。

---

# 九、接下来 7 天建议这样做

## Day 1：冻结 + claim inventory

目标：

```text id="lqo17s"
停止写 paper
拆出所有 claim
标记证据等级
```

输出：

```text id="hdylix"
01_claim_evidence_table.md
06_human_brief.md
```

---

## Day 2：代码反作弊审计

目标：

```text id="9kygo1"
查 mock / placeholder / fallback / hardcode
```

输出：

```text id="uxktym"
02_code_truth_audit.md
```

---

## Day 3：实验复现审计

目标：

```text id="larbhb"
找出哪些实验能从 clean command 复现
```

输出：

```text id="7oas6j"
03_experiment_reproducibility_audit.md
```

---

## Day 4：图表和结果重建

目标：

```text id="7b2oqw"
每个结果绑定 script/config/log/seed
```

输出：

```text id="bzdqh3"
04_figure_and_result_ledger.md
```

---

## Day 5：文献和 baseline 最小补全

现在你不需要完整综述，先补“最低必读相关工作”。

起点可以包括：

```text id="e2bl06"
1. 早期 ML-assisted QEC decoder / recurrent decoder 工作
2. ML QEC benchmark 工作
3. AlphaQubit / transformer neural decoder 工作
4. universal / scalable neural decoder 方向
```

比如 Baireuther 等人的 recurrent neural decoder 工作是 ML-assisted QEC 的早期代表之一；Zhao 等人的 benchmark 工作专门把 QEC 解码构造成 ML benchmark；Bausch 等人的 AlphaQubit 工作则代表了较强的神经解码器基准。([Quantum][2])

---

## Day 6：决定论文范围

根据证据决定：

```text id="5vskfg"
A. 能否继续 paper?
B. 只能写 workshop-style technical report?
C. 只能保留为 prototype?
D. 是否需要重做实验？
```

---

## Day 7：恢复开发，但只允许补证据

恢复 worker，但 task 类型只能是：

```text id="m1qcy1"
补 baseline
补 multi-seed
补 logging
补 figure script
补 clean reproduction
删除 placeholder claim
修复 comparator lane
```

不能做：

```text id="4kx9no"
新增 fancy model
扩写 related work
润色 abstract
包装 contribution
```

---

# 十、你需要改造 human-in-loop

你之前的 human-in-loop 是：

```text id="flcr5v"
agent 生成大量内容
reviewer 写大量解释
你试图追上所有细节
```

这个不可持续。

新的 human-in-loop 应该是：

```text id="d0aiyl"
agent 生成证据表
reviewer 标红风险
captain 压缩成 1 页 human brief
你只做关键裁决
```

你不需要亲自读所有代码，但你必须亲自看：

```text id="bvruvg"
1. Claim Evidence Table
2. Figure and Result Ledger
3. Top Blocking Risks
4. Human Brief
5. 最终进入 paper 的 claims
```

也就是说，你不审核每行代码，但你审核“结论是否有证据”。

---

# 十一、自动化仍然有用，但现在要自动化“证据流”，不是“任务推进流”

你之前问自动化协调 agent。现在我会更明确地说：

> 在这个项目上，自动化的第一目标不是让 captain/worker/reviewer 跑得更快，而是让它们自动维护证据账本。

你要自动化的是：

```text id="rqr2oa"
worker 完成实验
→ 自动生成 result ledger entry
→ reviewer 检查证据完整性
→ captain 更新 claim table
→ human brief 提醒你哪些 claim 仍不能写
```

而不是：

```text id="1br9tk"
worker 完成 task
→ reviewer review
→ captain 自动推进下一 task
→ paper 越写越长
```

---

# 十二、给 captain 的下一条命令

你可以直接复制：

```text id="ozt9c7"
当前项目停止推进 paper skeleton，进入 Research Reality Recovery Mode。

你的任务不是继续写论文，而是恢复科研可信度。

请完成以下事情：

1. 读取当前 paper skeleton、task board、risk log、review notes、experiment logs、figure scripts。
2. 创建 docs/reality_recovery/01_claim_evidence_table.md。
3. 将 paper skeleton 中所有重要 claim 拆成表格。
4. 对每个 claim 标记：
   - claim 内容
   - 出现位置
   - 所需证据
   - 当前证据
   - 证据等级 L0-L5
   - 是否可以保留在 paper
   - 需要哪个 task 修复
5. 创建 docs/reality_recovery/06_human_brief.md。
6. 明确说明 paper 是否允许继续推进。

禁止：
- 不要新增 claim
- 不要润色论文
- 不要修代码
- 不要新增实验
- 不要把 single seed 写成稳定结论
- 不要把 placeholder 写成实现
```

---

# 最关键建议

你现在应该把这个项目从：

```text id="e64wft"
AI 自动科研写作模式
```

切回：

```text id="st87c6"
证据驱动科研恢复模式
```

你的当前目标不是“更快把 draft 写出来”，而是回答：

```text id="9ts9nh"
这篇 paper 里到底有哪些 claim 是真的？
哪些只是 AI 根据代码表象编出来的？
哪些结果能复现？
哪些图表有真实日志支持？
哪些地方必须删除、降级或重做？
```

在这个答案出来之前，不要继续 paper skeleton。这个项目不是废了，但现在必须**刹车、验真、缩范围、补证据**。只有这样，后面再让 agent 自动化才是增效；否则自动化只会把不可信内容更快地包装成论文。

[1]: https://www.nature.com/articles/s41586-024-08148-8?utm_source=chatgpt.com "Learning high-accuracy error decoding for quantum ..."
[2]: https://quantum-journal.org/papers/q-2018-01-29-48/?utm_source=chatgpt.com "Machine-learning-assisted correction of correlated qubit ..."
