# 投稿稿外部指标标尺讨论补强记录

日期：2026-07-06

## 对象

- `docs/paper_notes/CNN_FPGA_GKP_submission_draft.tex`

## 修改内容

- 在 Discussion 的 supported-advantage 表之前新增一段外部指标标尺解释。
- 将 calibration-conditioned / learned decoder 的 real-processor LER、large calibration data、microsecond-scale neural latency anchors，与 real-time hardware decoder 的 ns/sub-us timing、lookup-memory compression、scale、area、power 和 cycle budget 归纳为比较标准。
- 明确这些外部指标不是本稿 software study 的 baseline 或结果，而是 future stronger version 必须满足的验证要求。
- 将本稿当前贡献限定为 architecture-level hypothesis：slow-loop learning 负责 drift adaptation，per-shot path 保持 deterministic affine datapath。

## 证据边界

- 本次修改不新增实验、不改数值、不升级 statistical inference、finite-energy logical-channel fidelity、hardware、real-board、`.tflite`、benchmark 或 deployment 证据。
- 外部数值只作为 literature scale / validation standard，不作为本稿实验结果或 normalized leaderboard。
- 硬件相关陈述仍是 future board-level measurement requirement，不是板级成功证据。

## 验证

- 预期验证：重新编译投稿稿，扫描 LaTeX warning、内部项目语体、审稿元语言残留、硬件/统计过强主张和行尾空白。
