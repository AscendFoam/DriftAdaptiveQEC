# 投稿稿内部验证语体转投稿语体清理记录

日期：2026-07-06

## 修改对象

- `docs/paper_notes/CNN_FPGA_GKP_submission_draft.tex`

## 修改内容

本次清理主文 Results 与 appendix 中面向读者可见的内部验证语体，并同步更新对应的 Phase A validation-threshold gate 生成链路：

- 将 `planning artifact only` 改为正式论文语境下的 future validation requirements / not an additional result；
- 将 `runner-level feasibility check`、`smoke-length`、`smoke timing` 改为 execution-coverage rehearsal / short-run / shortened configuration；
- 将 `source package` 改为 reproducibility package；
- 将 `claim-language gate` 与 `Claim-language upgrade gates` 改为 validation-threshold table / validation thresholds；
- 将表头中的 `Allowed wording`、`Upgrade condition or forbidden inference` 改为 `Permitted statement`、`Validation requirement or unsupported inference`。

保留了 `tab:runner-smoke-matrix`、`tab:phase-a-upgrade-gate` 等 LaTeX label，以避免破坏交叉引用；这些 label 不面向读者。

同步更新：

- `docs/paper_materials/build_submission_draft_phase_a_upgrade_gate.py`
- `docs/paper_materials/submission_draft_phase_a_upgrade_gate.csv`
- `docs/paper_materials/submission_draft_phase_a_upgrade_gate.json`
- `docs/paper_materials/投稿稿phase_a_upgrade_gate记录.md`
- `docs/paper_materials/build_submission_draft_source_manifest.py`
- `docs/paper_materials/submission_draft_source_data_manifest.csv`
- `docs/paper_materials/submission_draft_source_data_manifest.json`
- `docs/figure_assets/submission_draft_python_figures/audit_submission_draft_source_data.py`

## 证据边界

本次没有新增 benchmark、没有改动任何数值、没有补硬件测量，也没有提升统计或 FPGA 证据等级。短运行覆盖演练仍只用于检查执行覆盖、缺失行和 collector 逻辑，不作为主文性能证据、expanded benchmark、CI/p-value、holdout robustness 或 hardware evidence。

## 预期验证

- LaTeX 编译 `CNN_FPGA_GKP_submission_draft.tex`；
- source-data audit；
- 内部项目语体 / overclaim 扫描；
- trailing whitespace 扫描。
