# 投稿稿项目状态语体清理记录

日期：2026-07-03

对象：`docs/paper_notes/CNN_FPGA_GKP_submission_draft.tex`

## 1. 本轮目标

本轮只处理投稿稿中残留的项目验收式、状态式或内部治理式语气。目标是让审稿人读到的是正式论文的证据边界，而不是本地任务进度或项目 closeout 语言。

## 2. 修改内容

- 将 `deployment closure` 改为 `validated deployed operation` 等正式论文表述。
- 将 `HIL closure` 改为 `closed-loop hardware-in-the-loop validation`。
- 将 `GPU/Linux closure` 改为 `GPU/Linux reproducibility`。
- 将 `causal closure` 改为 `causal mechanism` / `proven causal mechanism` / `causal mechanism is not identified`。
- 将 `hash closure` / `provenance closure` 改为 `hash audit` / `provenance audit`。
- 将 `hardware closure` 改为 `hardware validation`。

## 3. 保留内容

以下表述保留，因为它们是正式技术语境，不是本地项目进度语气：

- `timing closure`：硬件工程和 FPGA 论文中常用的时序收敛/时序闭合术语。
- `deployment-level claims` / `deployment readiness`：用于描述未来验证等级，不代表当前已经完成部署。

## 4. 边界

本轮不新增实验、不改变任何表格数值、不运行 repeat-expanded benchmark、不补硬件测量、不补 CI / p-value、不晋升 `statcalib` 或任何 controlled diagnostic。它只降低正式投稿稿的审稿语气风险。

## 5. 验证

本轮已通过以下检查收口：

- `rg -n "T[0-9]+|FR[0-9]+|NO_GO|frozen|mainline|extension lane|evidence boundary|deployment closure|HIL closure|hash closure|causal closure|hardware closure|blocked surface|submission-ready|current-host|task package|post-freeze|handoff|closeout" docs\paper_notes\CNN_FPGA_GKP_submission_draft.tex`：无命中。
- `python docs\figure_assets\submission_draft_python_figures\audit_submission_draft_source_data.py`：`PASS_WITH_LIMITATIONS`。该状态是预期边界，不代表 CI、硬件或 full reproducibility 已完成。
- `python -m py_compile docs\figure_assets\submission_draft_python_figures\audit_submission_draft_source_data.py docs\paper_materials\build_submission_draft_source_manifest.py`：通过。
- `latexmk -pdf -interaction=nonstopmode -halt-on-error -outdir=D:\Codes\Quantum\DriftAdaptiveQEC\build\submission_draft_cd CNN_FPGA_GKP_submission_draft.tex`：通过，生成 52 页 PDF。
- LaTeX log 扫描 `Overfull|Undefined|undefined|Citation|LaTeX Error|Package .* Error|Warning`：只命中 `infwarerr` 包名，不是实际 warning/error。
- `git diff --check -- docs\paper_notes\CNN_FPGA_GKP_submission_draft.tex docs\paper_materials\投稿稿项目状态语体清理记录.md docs\paper_materials\README.md docs\paper_notes\README.md`：仅报告 README 的 LF/CRLF 提示，无内容 whitespace error。
