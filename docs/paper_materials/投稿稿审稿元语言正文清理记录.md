# 投稿稿审稿元语言正文清理记录

日期：2026-07-06

## 目的

本记录说明 `docs/paper_notes/CNN_FPGA_GKP_submission_draft.tex` 中一次小范围正式投稿语体清理。修改目标是去除正文中的“reviewer-facing / reviewer question / reviewer failure mode”元语言，使稿件以论文内部的科学问题、claim hierarchy 和验证负担来组织叙述，而不是显得在向审稿人解释防守策略。

## 修改范围

- `Experimental Design / Claim-to-test map`：将“common reviewer failure mode”改写为全稿 claim hierarchy，明确一个指标成功不能自动支持更强 endpoints。
- `Experimental Design / Comparator ladder and stress-test roles`：将“reviewer-facing questions”改写为 drift-adaptive GKP fast path 的 scientific questions。
- `Results / Controlled nearest-syndrome, oracle and wrapped-Gaussian checks`：将“reviewer question”改写为 local one-step setting 下的 technical question。

## 边界

本次修改不新增实验、不改数值、不改变 source-data、benchmark、`.tflite`、real-board、finite-energy fidelity 或 hardware validation 证据等级。硬件仍是 future board-level measurement；`final_ler_mean` 仍是 software-in-the-loop protocol proxy；residual-boundary surrogate 仍不是 finite-energy logical-channel fidelity。

## 预期验证

- LaTeX 编译通过。
- 稿件正文不再出现 `reviewer`。
- 项目状态词扫描不出现任务编号、证据边界英文短语、任务流转或治理流转等内部工作流语体。
