# 投稿稿符号边界机械审计报告

日期：2026-07-06

对象：`docs/paper_notes/CNN_FPGA_GKP_submission_draft.tex`

## 作用域

本文档由 `docs/figure_assets/submission_draft_python_figures/audit_submission_draft_symbol_boundary.py` 生成。它只做投稿稿符号、源码字段和 source-data 统计边界的机械一致性检查。

本文档不是新实验，不运行 benchmark，不导入 runtime，不证明完整物理噪声模型，不补 CI / p-value，不验证 real-board，也不改变 `.tflite`、HIL、statcalib 或当前治理任务的证据等级。

## 审计结论

- Status: `PASS_WITH_LIMITATIONS`
- Checks passed: `9`
- Warnings: `1`
- Checks failed: `0`
- 主结论：当前投稿稿中的 `lambda=sqrt(2pi)`、`final_ler` definition box、wrap 区间、半晶格逻辑边界、fast-loop 指标、mock drift 字段和 Fig. 2 descriptive-SD 口径都有可定位的源码或 source-data 锚点。
- 解释边界：这些锚点只支撑 fixed software-HIL protocol 下的可追溯写法；不能写成真实硬件 logical error rate、完整 finite-energy GKP 模型、holdout generalization 或显著性结论。

## 检查明细

| ID | Status | Topic | Paths | Detail | Boundary |
| --- | --- | --- | --- | --- | --- |
| `SB-A01` | `PASS` | TeX symbol and metric boundary | `docs/paper_notes/CNN_FPGA_GKP_submission_draft.tex` | Submission draft declares lambda=sqrt(2pi), includes the physics-metric boundary table, and names final_ler as a logical-error proxy. | This anchors wording in the draft only; it does not prove physical correctness. |
| `SB-A09` | `PASS` | Metric definition box | `docs/paper_notes/CNN_FPGA_GKP_submission_draft.tex` | Submission draft defines final_ler as (N_X+N_Z)/T, maps final_ler_mean/final_ler_sd to repeat-level source data, and states the non-hardware/non-inferential boundary. | This is a manuscript definition check only; it does not validate the metric statistically or physically. |
| `SB-A02` | `PASS` | Shared GKP lattice constant | `physics/constants.py` | physics/constants.py defines LATTICE_CONST as sqrt(2.0 * pi). | This checks the shared constant value, not every downstream physical assumption. |
| `SB-A03` | `PASS` | Syndrome wrap interval | `physics/syndrome_measurement.py` | Ideal and realistic syndrome measurement paths wrap displacements into the shared half-lattice interval. | This is a code-symbol consistency check; it is not a wrapped-posterior decoder analysis. |
| `SB-A04` | `PASS` | Logical-error proxy rule | `physics/logical_tracking.py` | LogicalErrorTracker uses half-lattice q/p thresholds, wraps accumulated residuals, and exposes get_logical_error_rate. | This supports a software protocol metric only, not hardware logical-channel tomography. |
| `SB-A05` | `PASS` | Fast-loop metric and histogram boundary | `cnn_fpga/runtime/fast_loop_emulator.py` | Fast-loop config uses half-lattice syndrome/histogram limits, realistic syndrome measurement, LogicalErrorTracker, and final_logical_error_rate output. | This checks source anchors only; it does not run the emulator or validate fallback-free execution. |
| `SB-A06` | `PASS` | Noise-state to affine-parameter mapping | `cnn_fpga/decoder/param_mapper.py` | ParamMapper maps NoisePrediction(sigma, mu_q, mu_p, theta_deg) into K,b and records covariance/gain metadata. | This supports the affine-map implementation story, not optimality or universal calibration claims. |
| `SB-A07` | `PASS` | Mock drift-state fields | `cnn_fpga/benchmark/run_hil_suite.py` | The mock noise provider emits sigma, mu_q, mu_p, theta_deg and metadata.signal_type for synthetic/effective drift probes. | This is not board-generated drift evidence and does not validate real-device distributions. |
| `SB-A08` | `PASS` | Source-data statistical boundary | `docs/figure_assets/submission_draft_python_figures/source_data_fig02_main_results.csv` | Fig. 2 source data carries final_ler_mean, final_ler_sd and n_repeats=2 for every checked row. | The SD column remains descriptive; this does not provide CI, p-value or stronger statistical evidence. |
| `SB-W01` | `WARN` | Known limitation | `physics/syndrome_measurement.py`, `physics/logical_tracking.py`, `cnn_fpga/runtime/fast_loop_emulator.py` | The checked anchors do not constitute a complete finite-energy GKP physical model, wrapped-posterior decoder, holdout drift protocol, or real-board validation. | Keep manuscript language at approximate-model and fixed software-HIL protocol level. |

## 可写入稿件的保守结论

- 可以写：the implementation shares the GKP lattice constant across syndrome wrapping, logical-boundary scoring and fast-loop limits.
- 可以写：`final_ler` is a protocol-defined logical-error proxy produced by the current software-HIL emulator.
- 可以写：the mock provider exposes an effective drift state through `sigma`, `mu_q`, `mu_p` and `theta_deg`.
- 不能写：the results are hardware logical-error measurements, complete physical-noise validation, statistically significant improvements, or generalization across realistic device drift.

## 后续投稿前仍需补的材料

1. 将 `final_ler` definition box 与 `LogicalErrorTracker` 行为纳入轻量单元测试或 CI preflight。
2. 补 oracle/known-noise affine baseline 或 wrapped-Gaussian / nearest-lattice baseline。
3. 补 unseen drift family 或 holdout protocol。
4. 把主结果 repeats 从 `2` 提高到可报告 paired CI 的水平。
5. 若保留硬件叙事，补 real-board source data、bitstream/RTL provenance、DMA trace、latency/resource report 和 failure logs；否则继续保留 placeholder。
