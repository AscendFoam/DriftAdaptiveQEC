# 投稿稿结果证据层级主文补强记录

**日期**：2026-07-06

**对象**：`docs/paper_notes/CNN_FPGA_GKP_submission_draft.tex`

## 修改内容

- 在 `Results` 开头新增一段 reviewer-facing evidence hierarchy。
- 新段落把结果按 claim strength 分成四层：
  1. predeclared software-HIL ranking；
  2. single-scenario paired-interval check；
  3. controlled oracle-affine / wrapped-Gaussian / holdout-drift / commit-lag diagnostics；
  4. analytical cost / fixed-point software parity / runtime-discipline checks。
- 明确该层级只支持 software-evidence 下的 affine-calibration systems method，不升级 finite-energy channel fidelity、all-scenario statistical robustness 或 hardware execution。

## 边界

- 不新增实验。
- 不运行 benchmark。
- 不改变 `final_ler_mean`、paired interval、source-data CSV/JSON 或 figure source data。
- 不升级 `.tflite`、FPGA、real-board、hardware latency、resource、power、source-vs-board agreement、finite-energy logical-channel fidelity、p-value 或 all-scenario confidence-interval 证据等级。

## 验证要求

- LaTeX 编译投稿稿。
- 投稿稿 source-data 机械审计。
- 内部项目/治理词残留扫描。
- hardware / fidelity / statistical overclaim 扫描。
