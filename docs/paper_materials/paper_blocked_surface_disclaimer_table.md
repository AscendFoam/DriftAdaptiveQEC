# T88 Blocked Surface Disclaimer Table

本表把 `T88` 之后绝不能被手工终修稀释掉的 blocked/excluded surface disclaimer 固定下来。

| disclaimer_id | blocked_surface | mandatory_boundary_wording | expected_location | status | note |
| --- | --- | --- | --- | --- | --- |
| `BD01` | real-board execution / timing / resource | `read-only real-board gate / regeneration / provenance boundary with current-host NO_GO; hardware-dependent execution surfaces remain blocked until a Linux + FPGA host, openable device paths, and measured board timing/resource rows exist.` | `Embedded runtime and board-level validation`; `Discussion`; `Conclusion`; supplement-side gate note | `preserved` | 当前 note 与补充材料都仍保留 `NO_GO_REAL_BOARD_HOST_OR_DEVICE_PATH_UNAVAILABLE` 边界。 |
| `BD02` | default-env / cross-host `.tflite` portability | `isolated current-host true runtime verified for selected preserved artifacts; not default-environment compatibility, software-HIL closure, or deployment closure.` | `Runtime, quantization, and fixed-point degradation`; `Embedded runtime and board-level validation`; `Discussion`; `Conclusion` | `preserved` | 该免责声明必须继续阻断 portability/deployment retelling。 |
| `BD03` | full training reproducibility | `canonical training/material provenance chain plus one clean CPU-only bounded rerun; not full reproducibility closure, repeated-run closure, or cross-host portability.` | `Discussion` appendix-route paragraph; `Conclusion` appendix support sentence; supplement-side exclusion note | `preserved` | 当前 note 通过 `training/material provenance` 口径与外部 exclusion note 共同保留这一边界。 |
| `BD04` | `FR8/statcalib` mature comparator / unique clean threshold | `separately labeled extension-lane closure with no-promotion and no unique clean threshold.` | `Supplement-side statistical calibration extension lane`; `Discussion`; `Conclusion`; supplement-side no-promotion note | `preserved` | 任何把 persistent tie set 写成 final promoted comparator 的改写都越界。 |
| `BD05` | expanded benchmark / stronger oracle baseline | `future benchmark lane / separate bounded tasks; not a ranking proven beyond the frozen set.` | `Unseen drift generalization`; `Oracle and wrapped-Gaussian baselines`; `Conclusion`; exclusion register | `preserved` | 这条免责声明防止把 `T24` 结果外推为 paper-grade expanded benchmark closure。 |
| `BD06` | unified portability/deployment closure figure or prose | `no unified portability/deployment closure claim; layered evidence remains intentionally split across appendix and supplement.` | `Discussion`; `Conclusion`; boundary schematic / supplement note family | `preserved` | `T88` 冻结的是分层 route，不是闭环部署故事。 |
