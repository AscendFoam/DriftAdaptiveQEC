# T1.1.2 完成记录：单轴 MAP / soft-output likelihood

**Task ID：** T1.1.2  
**标题：** 偶/奇逻辑陪集 likelihood、LLR 与 MAP hard/soft 解码  
**日期：** 2026-07-14  
**状态：** Done

## 输入材料

- `docs/new_task_board.md` T1.1.2 当前主表；
- T1.1.1 已验收的 `physics/ideal_gkp_decoder.py` 与 shared `LATTICE_CONST`；
- `physics/syndrome_measurement.py` 的 centered modular convention；
- T0.2.2 baseline/oracle/claim 边界。

## 实际完成内容

- 推导并实现 even/odd periodic Gaussian coset likelihood；
- 用 log-sum-exp 实现带 prior 的 LLR，不通过 raw density ratio；
- 实现同时包含 hard parity、posterior、confidence 和 log likelihood 的
  `MAPDecodeResult` / `map_decode_1d`；
- 实现 `standard/map/soft` 三模式入口并冻结 raw displacement 与 centered syndrome
  的不同输入语义；
- 支持向量、可广播非零 mean 和跨 cell parity；
- 实现半开区间、模型参数、广播和 `int64` index overflow 的失败关闭；
- 反查并修复首次实现中有限超大输入可能静默 cast overflow、底层 warning 外泄的问题；
- 新增 12 项 MAP/soft tests，使该文件累计 24 项测试。

## 产物路径

- `physics/ideal_gkp_decoder.py`
- `physics/__init__.py`
- `tests/test_ideal_gkp_decoder.py`
- `docs/tasks/T1.1.2_map_likelihood.md`
- `docs/new_tasks/T1.1.2_map_likelihood.md`

## 验证方式和结果

- focused + warnings-as-errors：`24 passed`；
- 独立 alias 穷举与实现一致到 `2e-15` 绝对误差门；
- 三组 `sigma/mean` 下，even+odd density 在一个 syndrome period 积分为 1，误差小于
  `3e-13`；
- `sigma/lambda=0.01` 时 raw odd density 下溢为 0，但 log likelihood/LLR 保持有限；
- 邻近 statcalib runtime：`28 passed`；
- 显式 `tests/`：`90 passed, 4 failed`，4 项仍仅为 R-N012 的旧 FR8/P4 文档缺失。

## 风险复核

- R-N004 进一步缓解：新增 ideal MAP API 已有解析、退化、恒等式、边界、极端数值和
  failure-path 直接测试；二维相关和旧 full/legacy physics 仍由 T1.1.3 及后续任务覆盖；
- R-N003 保持 `Mitigated`：继续使用当前 shared `lambda=sqrt(2pi)`，并明确旧日志
  `sqrt(pi)` 与当前 operational convention 冲突；
- R-N012 不变：它影响全量绿灯，但不改变本任务 focused 数值证据。

## 是否需要插入新 task

不插入。二维相关噪声、finite-energy/non-Gaussian 模型和旧 physics 路径验证均已有
正常顺序任务；R-N012 在首次依赖旧 FR8/P4 evidence 前处理，不阻塞 T1.1.3。

## 对论文 claim 的影响

允许写成 ideal 1D periodic-Gaussian MAP/soft-output reference 已实现并经独立数值与
物理恒等式验证；不允许升级为 finite-energy、correlated、adaptive、experimental
或 hardware result。

## 对任务板的同步

- T1.1.2：`In Progress -> Done`；
- T1.1.3：`Todo -> In Progress`；
- 当前推荐任务：`T1.1.3`。
