# T3.1.4 完成记录：static protocol-aware decoder

- **Task ID：** T3.1.4
- **标题：** static finite-energy/protocol-aware optimized decoder
- **日期：** 2026-07-15
- **状态：** Done

## 输入材料

finite-energy/folded response、finite-squeezing decomposition、sBs observation/reset model，以及
T3.1.1--T3.1.3 baseline/oracle contract。

## 实际完成内容

没有用 hard action 退化为 standard 的对称 finite-energy response 交差；改为实现完整 sBs
静态 branch Bayes decoder。它精确边缘化 persistent hidden carry 与 conditional reset，部署端
只消费当前 `g/e/leakage` pair；显式比较 direct、forced MAP、cost-aware policy 和 truth-only
branch reference。fallback 只允许 observed leakage，成本单列。

## 产物路径

- 实现/测试：`cnn_fpga/benchmark/static_protocol_decoder.py`、
  `tests/test_static_protocol_decoder.py`；
- 报告：`docs/static_protocol_decoder.md`；
- 数据：`docs/t3_1_4_static_protocol_decoder_validation.json`、`.csv`；
- 详细记录：`docs/tasks/T3.1.4_static_protocol_decoder.md`。

## 验证方式和结果

- 4×8×20k = 640,000 cycles，32 行 Source Data，11/11 gates；
- exact/MC likelihood 最大误差 `0.009812`，policy cost 最大误差 `0.001771`；
- aggregate cost gain `0.025622 [0.024873,0.026371]`；3 个非 control 场景显著，balanced
  control 为零；3 个场景存在 nonleak override；
- focused+adjacent `98 passed`；首轮 CI pseudoreplication 已修复并增加重算测试。
- 全量 `874 passed, 4 skipped, 4 failed`；4 个 failure 仍仅为 R-N012 旧文档缺失。

## 风险复核

R-N049 降为 Mitigated；新增 R-N050 监控 synthetic kernel/prior/stationarity 和 branch-target
边界。结果不是 logical LER、full recovery 或 device calibration。

## 是否需要插入新 task

不插入；T3.2/T5/T6 正常任务承接 history、mismatch、Fock/channel 和硬件校准。

## 对任务板和论文 claim 的同步

T3.1.4 标为 Done，T3.1.5 标为 In Progress，当前推荐改为 T3.1.5。只允许
assumption-driven static sBs branch-selection claim。
