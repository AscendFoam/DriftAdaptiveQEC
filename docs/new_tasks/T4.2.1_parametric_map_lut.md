# T4.2.1 完成记录：parametric MAP-LUT

- Task ID：T4.2.1
- 标题：设计 parametric MAP-LUT
- 日期：2026-07-15
- 状态：Done

## 输入材料

T1.1.2 periodic-Gaussian LLR、T-RISK quadrature convention、`ParamMapper`、真实双 `ParamBank`、T4.2.1
syndrome/phase/latency contract。

## 实际完成内容

从 active K/b 与 measurement covariance 反解 effective mean/covariance，重新验证 K identity 后编译 X/Z
LLR ROM。在线 runtime 只消费 10-bit code、phase bit 和 bank version，执行纯整数 address/ROM/
interpolation/sign action；image/version/hash 锁存跨越 5-stage pipeline。

## 产物路径

`cnn_fpga/decoder/parametric_map_lut.py`、`cnn_fpga/runtime/parametric_map_lut.py`、
`cnn_fpga/benchmark/parametric_map_lut_validation.py`、两份 tests、主说明、20-gate JSON、16,384-row CSV
和 8-bank full-table image artifact。

## 验证方式和结果

20/20 gates、26 focused、210 adjacent；显式全量 `tests/` 为
`1309 passed, 14 skipped, 4 failed`，4 个失败仍仅为 R-N012 缺失旧 FR8/P4 文档。8 banks×X/Z×1024 codes hard action mismatch `0`；mean/max LLR code error
`0.387756/20`，相对 nearest mean-error ratio `0.004340`；5--8 address bits 严格收敛。初版 half-bin
插值偏差已修复，stale version/bad code/bad phase/tamper、II=1、in-flight bank latch 和 no-float-likelihood
均有直接测试。

## 风险复核

新增 R-N068：只完成 axis-marginal software pipeline contract；correlated 2D、event/frame/fallback、完整定点
LER、RTL/post-route/FPGA/board 尚未验证。

## 是否需要插入新 task

不插入；T4.2.2--T4.2.4、T5.1/T5.4/T5.5、T6 已覆盖剩余集成、稳健性和硬件 evidence gate。

## 对任务板同步说明

T4.2.1 Done；T4.2.2 In Progress；当前推荐任务更新为 T4.2.2。
