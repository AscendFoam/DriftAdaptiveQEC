# T1.2.1 完成记录：approximate-GKP finite-energy 态族

**Task ID：** T1.2.1  
**标题：** Gaussian-envelope 与 damped-projector 四逻辑态 reference  
**日期：** 2026-07-14  
**状态：** Done

## 输入材料

- `docs/new_task_board.md` T1.2.1；
- `physics/gkp_state.py` legacy SF/fallback 路径；
- T1.1 operational lattice 与 centered syndrome contract；
- `Real-time quantum error correction beyond break-even` 中
  `N_Delta=exp(-Delta^2 a^dagger a)` finite-energy code family；
- `Quantum error correction of a qubit encoded in grid states of an oscillator`
  关于 peak width、envelope 与实验/模拟边界的说明。

## 实际完成内容

- 新增 `physics/finite_energy_gkp.py`；
- 实现独立 `peak_sigma/envelope_kappa` Gaussian-comb family；
- 从 Mehler kernel 实现 `exp(-Delta^2 n)` damped-projector family；
- 支持 `0/1/+/-` 四态、解析 Gram normalization、wavefunction、probability、
  inner product、peak audit table；
- 实现由 `abs(psi)^2` folding 得到的 syndrome distribution；
- 实现由 wavefunction correlation FFT 得到的 signed Wigner-like grid，并返回 marginal、
  mass 和 negativity diagnostics；
- 增加 tail/component/derived-float/Wigner-grid 失败关闭；
- 将 `gkp_state.py` analytical fallback 文档更正为 legacy heuristic visualization，
  不改其历史数值行为；
- 新增 14 项 direct tests 和公共惰性导出。

## 产物路径

- `physics/finite_energy_gkp.py`
- `tests/test_finite_energy_gkp.py`
- `physics/__init__.py`
- `physics/gkp_state.py`
- `docs/tasks/T1.2.1_finite_energy_state_family.md`
- `docs/new_tasks/T1.2.1_finite_energy_state_family.md`
- `README.md`

## 验证方式和结果

- finite-energy focused：`14 passed`；
- finite-energy + ideal decoder：`48 passed`；
- 加邻近 statcalib runtime：`52 passed`；
- 四态最大解析范数误差 `2.22e-16`；
- `abs(<+|->)` 不高于 `2.22e-16`；
- syndrome captured mass `1.0`；
- Wigner q-marginal 最大误差不高于 `1.14e-14`，total mass 误差小于 `3e-10`；
- legacy Wigner smoke 通过；
- 显式 tests/：`114 passed, 4 failed`，仍仅是 R-N012 的两份旧文档缺失。

## 风险复核

- R-N004 继续缓解：finite-energy 新接口具有直接解析/数值/极限/失败分支测试；
- 新增 R-N013：legacy `ApproximateGKPState` heuristic fallback 仍可被旧调用误选，已更正
  文档，但 T1.2.2/T2 的主实验路由必须显式选择新模型或更高保真路径；
- R-N012 不变且不阻塞 T1.2.2。

## 是否需要插入新 task

不插入。R-N013 的路由/claim 约束可在 T1.2.2 logical channel 和 T2 multi-fidelity
protocol 的正常任务中关闭；提前改写所有 legacy 调用会扩大当前任务。

## 对论文 claim 的影响

允许写成两种 normalized approximate-GKP pure-state reference family 已实现；不允许
写成完整 Fock-space logical channel、mixed-state dynamics、protocol/experiment validation。

## 对任务板的同步

- T1.2.1：`In Progress -> Done`；
- T1.2.2：`Todo -> In Progress`；
- 当前推荐任务：`T1.2.2`。
