# T4.3.1 完成记录：三时间尺度 cadence 与 adaptation lag

- Task ID：T4.3.1
- 标题：定义 fast/event/slow 三种更新频率
- 日期：2026-07-15
- 状态：Done

## 输入材料

T4.2.1--T4.2.4 MAP/event/fallback/bit-accurate fast path、现有 scheduler/ParamBank、
`hardware_hil.yaml`、T1.4.3 双域三时间尺度 contract 和任务板的 lag/分钟级重标定要求。

## 实际完成内容

实现 exact integer-epoch cadence calculator、两种 evidence policy、4000-phase sweep、minute/end-run due schedule；
用真实 scheduler 和 T4.2 fast path 回放本地 event、window、slow finish/stage、commit、first-use。修复独立 64-cycle
stale pilot 与 4000-cycle生产 cadence 不兼容的问题，生产 max age 冻结为 8192 cycles。

## 产物路径

`cnn_fpga/runtime/three_timescale_cadence.py`、
`cnn_fpga/benchmark/three_timescale_cadence_validation.py`、两份 tests、
`docs/three_timescale_cadence.md`、14-gate JSON、8000-row phase CSV 和 execution trace CSV；
同步 `hardware_hil.yaml` / `hardware_emulation.yaml`。

## 验证方式和结果

14/14 production gates；26 focused、106 focused+adjacent；显式全量 `tests/` 为
`1404 passed, 14 skipped, 4 failed`，4 个失败仍仅为 R-N012 缺失旧 FR8/P4 文档。first-influenced lag min/median/max 为
`1.000/10.9975/20.995 ms`；full-post-change 为 `11.235/21.2325/31.230 ms`。所有 8000 rows
component closure 精确，local event 固定 1 cycle。真实 trace 的 version `0→1` 在 commit 周期 callback 前完成，
独立 compile/replay hash 一致。

## 风险复核

新增 R-N072。`995 us` 为软件 latency-model 均值，不含 jitter/queue/pause；首个受影响窗口可只有一个 onset 后
样本；60 s 和 8192-cycle age 是 policy，不是装置最优值或测量值。T4.3.2 前不声称完整 wire atomic/hysteresis，
T4.3.3 前不声称闭环 fault stability。

## 是否需要插入新 task

不插入；T4.3.2--T4.3.3 已顺序覆盖 bank metadata/atomic/hysteresis 和 fault/race recovery，T5/T6 覆盖 OOD、
long-horizon、RTL/board/device evidence。

## 对任务板同步说明

T4.3.1 更新为 Done；T4.3.2 更新为 In Progress；当前推荐任务更新为 T4.3.2。
