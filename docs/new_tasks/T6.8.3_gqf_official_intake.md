# T6.8.3：导入并固定 Puviani 官方 GQF 源码与运行环境

- 日期：2026-07-18
- 状态：Done

## 输入材料

- 官方仓库 `https://github.com/Matteo-Puviani/GQF.git`
- 固定 commit `c9ab1ef2b3ff6fa6d6d24cd95fbd06e2872e016d`
- `docs/new_task_board.md` T6.8.3 的 source/adapter 分离、环境锁和 minimum-smoke 要求
- 官方 `README.md`、`requirements.txt`、`GQF/runner.py` 与七个 Python 源文件

## 实际完成内容

1. 将官方仓库导入隔离的 `third_party/GQF`，保存 MIT license、URL、commit、commit date、12 文件清单与完整 tracked-tree SHA-256；官方 tracked checkout 保持 pristine。
2. 创建 `GQFEnv`，固定 Python/TensorFlow/NumPy/Gym/Matplotlib/CUDA Toolkit/cuDNN，并保存 conda explicit、pip freeze、history 三类重建锁。
3. 发现并如实记录官方 HEAD 的 `mesolve.py:13` `IndentationError`，另将 string identity、runner token 和 evaluation-buffer 缺陷整理成四个 hash-bound patches。
4. 补丁仅应用到独立 local-Git-clone 派生工作树；首版普通目录复制会误命中父 Git 仓库，已废弃并重构，新增“补丁后树哈希必须变化、改动文件集合必须精确匹配”门。
5. 实现真实 CPU smoke：构造有限 Fock GKP 态、`FeedbackGRAPE`、`GKPEnv`，执行一轮 sBs step，并检查归一性、迹、Hermitian、PSD、概率和 syndrome。
6. 以独立进程执行 GPU GKP-state probe；当前环境发生 cuSolver 原生 fatal，fail closed 为 `UNQUALIFIED_CUSOLVER_FATAL`。
7. 建立 12 个机器门、12 个逐门语义突变、live hash verifier 和 7 个 focused tests；明确 intake 不等于 paper-exact。

## 产物路径

- `third_party/GQF/`
- `third_party/patches/GQF/c9ab1ef2b3ff6fa6d6d24cd95fbd06e2872e016d/`
- `configs/gqf_official/runner_manifest.json`
- `configs/gqf_official/patch_manifest.json`
- `configs/gqf_official/GQFEnv.conda-explicit-win-64.txt`
- `configs/gqf_official/GQFEnv.pip-freeze.txt`
- `configs/gqf_official/GQFEnv.history.yml`
- `cnn_fpga/benchmark/gqf_official_intake.py`
- `scripts/gqf_official_smoke.py`
- `docs/t6_8_3_gqf_official_intake.json`
- `docs/t6_8_3_gqf_cpu_smoke.json`
- `docs/t6_8_3_gqf_gpu_probe.stdout.log`
- `docs/t6_8_3_gqf_gpu_probe.stderr.log`
- `docs/gqf_official_intake.md`
- `tests/test_gqf_official_intake.py`

## 验证方式和结果

- `python -m cnn_fpga.benchmark.gqf_official_intake`：12/12 gates，12/12 mutations，verdict 为 `PASS_GQF_OFFICIAL_INTAKE_UNQUALIFIED_CUSOLVER_FATAL`。
- `python -m pytest tests/test_gqf_official_intake.py tests/test_new_task_board_governance.py -q`：`13 passed`。
- CPU real-step：state norm `1.0`，density trace `0.9999999148`，Hermitian residual `7.16e-8`，minimum eigenvalue `0.0`。
- GPU probe：非零退出、无成功 artifact、stderr 含 cuSolver failure；未把失败改写为 GPU pass。

## 风险复核

- 新增 R-N124：官方 HEAD 本身不可导入、依赖未固定、无 checkpoint/seed/evaluation artifact；当前 Windows GPU GQF path 还发生 cuSolver fatal。T6.8.4 必须把“可运行适配”和“paper-exact”分开。
- intake 的 cutoff 8 / one-step CPU smoke 只验证 API 与基本物理不变量，不能验证论文寿命、MF/NMF ordering 或训练收敛。

## 是否需要插入新 task

不插入。T6.8.4 已负责论文/补充材料/官方 runner 的预注册、全 agents 与 1000-cycle/six-state exact gate；GPU 或复现缺失将按其失败分支降为 partial/directional，而不是旁路降低标准。

## 对任务板与论文 claim 的影响

- `docs/new_task_board.md`：T6.8.3 改为 Done，T6.8.4 改为 In Progress。
- 论文当前只允许写 official-source intake、隔离补丁、CPU minimum smoke 和 GPU unqualified；继续禁止 paper-exact 与“超过 Puviani NMF”。

