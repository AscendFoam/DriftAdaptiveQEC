# T3.1.3 完成记录：oracle MAP

- **Task ID：** T3.1.3
- **标题：** 接入 full-state/regime/leakage oracle
- **日期：** 2026-07-15
- **状态：** Done

## 输入材料

T1.3.2 oracle、T2.1.1 syndrome stream truth isolation、T3.1.1/T3.1.2 comparison contract 与
任务板不可部署上界要求。

## 实际完成内容

完成 oracle schema 接入、regime/burst provenance、truth-only leakage flag、4-regime 320k paired
matrix、8k protocol leakage cost envelope 和 source-bound JSON/CSV。legacy `oracle_delayed` 与
decoder oracle 明确分离。

## 产物路径

- 实现：`cnn_fpga/benchmark/oracle_baseline.py`、`physics/oracle_map.py`；
- 测试：`tests/test_oracle_baseline.py`、`tests/test_oracle_map.py`；
- 报告：`docs/oracle_baseline.md`；
- 数据：`docs/t3_1_3_oracle_validation.json`、`docs/t3_1_3_oracle_source_data.csv`；
- 详细记录：`docs/tasks/T3.1.3_oracle_baseline.md`。

## 验证方式和结果

- 10/10 gates；320,000 regime samples + 8,000 protocol cycles；
- static/oracle `0.076419/0.025103`，gap `0.051316 [0.050474,0.052157]`；
- 1,616 leakage cycles exact flag，cost envelope `[0.159,0.361]`；
- 20 Source Data rows/unique trace hashes；focused+adjacent `99 passed`。
- oracle/terminology 锚 focused `37 passed`；全量 `858 passed, 4 skipped, 4 failed`，4 个失败
  均为 R-N012 既有 FR8/P4 文档缺失；`compileall` 与 `git diff --check` 通过。

## 风险复核

R-N018 保持 Open；新增 R-N049，防止把 hidden perfect flag 写成免费 recovery 或 deployable
detector。

## 是否需要插入新 task

不插入；T3.1.4/T5/T6 已承接协议 action、channel bound 和 device leakage evidence。

## 对论文 claim 的影响

只允许 nondeployable assumed-model upper reference；禁止 free leakage correction/CNN-over-oracle。

## 对 `docs/new_task_board.md` 的同步说明

T3.1.3 更新为 Done，T3.1.4 更新为 In Progress，当前推荐切换至 T3.1.4。
