# T4.2.4 完成记录：fast path 端到端定点化

- Task ID：T4.2.4
- 标题：完成 fast path 定点化
- 日期：2026-07-15
- 状态：Done

## 输入材料

T4.2.1--T4.2.3 runtime/artifacts、T2.4.3 旧 fixed-point chain、任务板位宽/舍入/bit-accurate/量化误差/LER
要求和 canonical lattice convention。

## 实际完成内容

实现 float replay 与 integer online 明确分层的完整 fast-path reference；冻结 ADC/address/fraction/LLR、
event counters/mode、Pauli/phase frame、OOD/age/version/fault mask/health counters、CRC/SHA 的位宽与九类运算规则。
四档 precision 均编译 8 个真实 parameter profiles，并执行完整 code audit 与 paired LER。

## 产物路径

`cnn_fpga/runtime/fast_path_fixed_point.py`、`cnn_fpga/benchmark/fast_path_fixed_point_validation.py`、
两份 tests、`docs/reports/phase4/fast_path_fixed_point.md`、21-gate JSON、128-row LER CSV、87,040-row code CSV。

## 验证方式和结果

21/21 gates、15 focused、152 focused+adjacent；显式全量 `tests/` 为
`1378 passed, 14 skipped, 4 failed`，4 个失败仍仅为 R-N012 缺失旧 FR8/P4 文档。selected exhaustive 16,384 rows hard mismatch 0，mean/max LLR value error
`9.46671e-5/0.00488281`。8 banks×4 seeds 的 selected paired ΔLER
`+3.05176e-5 [-4.57764e-5,1.22070e-4]`；dense finite trace action disagreement 0，low disagreement
`1.11389e-3`。两次独立 compile/replay Source Data hash 一致；在线 AST 无 float division/math。

## 风险复核

新增 R-N071：结果只属于 model-matched axis-marginal software reference；correlated/OOD/fault LER、设备量化、
资源 packing、RTL/post-route/FPGA/board 均未验证。representation proxy 不得写成综合利用率。

## 是否需要插入新 task

不插入；T4.3、T5.1/T5.4/T5.5、T6 已覆盖闭环、稳健性、Pareto 和硬件 evidence gates。

## 对任务板同步说明

T4.2.4 Done；T4.3.1 In Progress；当前推荐任务更新为 T4.3.1。
