# T7.2.2：Methods 与 evidence-state contract

- Task ID：T7.2.2
- 标题：起草 Methods
- 日期：2026-07-20
- 状态：Done

## 输入材料

- T2.1--T2.2 的 protocol-aligned syndrome/effective simulator、observed/truth 分离与 finite-squeezing/ancilla error records；
- T6.5.2--T6.7.4 的统一执行合同、V4 HMM/Window/EWMA、预注册统计、formal 与 integrated RTL；
- T6.9.1/T6.9.2 的三 seed P&R estimate、UART candidate 与 42 个 board-measured null fields；
- T6.10.1/T6.15.5 的 V5 causal/action headroom negative result、20 个 Dropped task 与零 downstream output；
- T7.1.1/T7.2.1 的 claim matrix、Introduction/Related Work prose contract 和主图证据边界。

## 实际完成内容

1. 将主 note 的 Methods 扩展为 11 个 subsection 与 3 个统计 subsubsection，覆盖 simulator、系统、统一合同、V4 posterior/policy、baseline、统计、V5 entry gate、fixed-point/CXXRTL/formal/P&R 边界与真板 future procedure。
2. 明确每个 simulated cycle 的 observed record 与 read-only truth record；truth 只允许 channel generation、offline scoring、evaluation-only event interval 和 oracle，禁止进入 adapter/posterior/image/action。
3. 完整写出 10-bit ADC、8-bit address、Q9.12、ties-to-even/saturation、5+1 cycle、II=1、CRC/SHA/CAS A/B、6-cycle drain、2,048 scalar/4,000-cycle cadence 与 8,192 MAC/B/5,000-us budget。
4. 披露 V4 的 four-state causal HMM、32-decision update、1,728 threshold tuples、选中 tuple、Window/EWMA promotion、tail trusted shadow、leakage/reset、integrity-only monotonic LKG 及 V1--V4 prior-informed 修订历史。
5. 区分 V4 实际 calibration/pilot/formal 三分割和 V5 条件性 train/calibration/pilot/formal 四分割；后者在入口 NO-GO 后没有生成 manifest、power plan 或 formal output。
6. 报告 V5 仅执行的 186-trajectory/4,571,136-decision causal-headroom diagnostic，并保留 `-0.2322%` router、9 errors/`0.02549%` incremental action-space negative evidence。
7. 将 multiscale wrapped features、IMM/BOCPD、activation prediction、posterior-predictive compiler、LER/CVaR calibration、typed V5 bank、V5 fixed-point/formal/CXXRTL/P&R 全部写为 conditionally registered/stopped，不借 V4 结果填补。
8. 新增 18-row evidence-state/source/privilege matrix，冻结 `IMPLEMENTED_EVALUATED`、`DIAGNOSTIC_ONLY_EXECUTED`、`CONDITIONALLY_REGISTERED_STOPPED`、`FUTURE_PHYSICAL_WORK_BLOCKED` 四态。
9. 实现 machine contract、CSV Source Data、18 个 gates、18 个定向语义 mutation 和 9 项 direct tests；因全文 hash 变化同步重建 T7.2.1 contract，Introduction/Related Work 内容与 verdict 未改变。
10. 修复旧 Methods 中三个由 `\rm` 字符串生成遗留的 lone-CR 字符，并确认全文 lone CR 为 0。

## 产物路径

- `docs/paper_notes/Contract_Centric_Regime_Aware_GKP_note_draft.tex`
- `cnn_fpga/benchmark/methods_evidence_contract.py`
- `tests/test_methods_evidence_contract.py`
- `docs/methods_evidence_contract.md`
- `docs/t7_2_2_methods_evidence_contract.json`
- `docs/t7_2_2_methods_evidence_source_data.csv`
- 刷新的 `docs/t7_2_1_introduction_related_work_contract.json`

## 验证方式和结果

- generator：`PASS_EVIDENCE_STATE_BOUNDED_METHODS`；18/18 gates，18/18 targeted mutations；
- focused：`9 passed`；
- Methods + Introduction/Related Work + claim/figure/governance 联合回归：`50 passed in 24.10s`；
- TeX Live/BibTeX：41 页、1,107,839 bytes；overfull、undefined、citation/reference undefined、LaTeX/package warning、fatal error均为0；
- PDF：100-dpi 渲染 41/41 页，5 张 contact sheet 全页巡检；高分辨率复核 Methods 页 10、13--15，未见裁切、重叠、孤立标题、乱码或表格越界；
- 源文本 lone CR：0；`docs/paper_notes/build_t7_2_2/` 仅为临时编译目录，不进入提交。

## 深审与非简化实现检查

本 task 没有把“写一段 Methods”视为完成。机器合同逐行绑定 14 个 live source，包括 simulator、V4 lock、formal/RTL、P&R、V5 NO-GO、board blocker 和任务板；任一状态升级或源 hash 漂移都会 fail closed。18 个 mutation 一一对应 section/order、权限、V4/V5 身份、四分割 absence、formal absence、P&R estimate 和 board-null 门。

深审发现两项易形成伪实现的问题并修正：第一，旧任务文字容易把 V5 四分割写成已执行预注册，正文现明确“conditionally registered; not instantiated”；第二，V4 CXXRTL/mutation coverage 不能等价于 actual-module formal proof，正文与表格都明确 V5 SVA/SMT 未运行。未重跑不存在的 V5 实验，也未用 V4 integer、CXXRTL 或 P&R 数值填补 V5 空白。

## 风险复核

R-N142 维持 `Mitigated / High / Monitor`：Methods 越界由 18-row 四态合同缓解，但 Results/Discussion/Conclusion 仍可能重新拼接 V4、V5 与 Phase 6C。R-N131/R-N132 没有因为 V5 未执行而虚假关闭；它们仍是未来新协议的 production-LUT/two-bank/formal-safety 门。T6.9.2 继续 Blocked，42 个 measured fields 全 null。

## 是否需要插入新 task

否。当前缺口由正常顺序的 T7.2.3--T7.4 与既有 T6.9.2 承接。T7.2.3 的旧措辞已修正为报告 V5 early-stop 与结果缺席，而不是要求生成不存在的 V5 formal/RTL 结果；不插入性能 rescue 或伪板测旁路。

## 对任务板的同步

- T7.2.2：`In Progress -> Done`；
- T7.2.3：`Todo -> In Progress` 并成为当前推荐任务；
- T7.2.3 验收条件同步为只消费 V4、V5 diagnostic/absence 与 eligible Phase 6C evidence。

## 对论文 claim 的影响

允许表述：implemented V4 是 observed-only HMM + Window/EWMA + atomic fail-closed pre-board system；V5 causal/action-space entry diagnostic 为 negative result；V4 integer/CXXRTL/P&R 只支持板前实现性。禁止表述：已实现 IMM/BOCPD/posterior-predictive risk-aware V5、已有 V5 formal/RTL/P&R、已有真板 latency/deadline/power，或用 V4/Phase 6C 结果挽救 V5 主张。
