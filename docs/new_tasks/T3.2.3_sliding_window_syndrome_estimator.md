# T3.2.3 完成记录：sliding-window syndrome estimator

- **Task ID：** T3.2.3
- **标题：** training-selected uniform sliding-window periodic syndrome estimator
- **日期：** 2026-07-15
- **状态：** Done

## 输入材料

新任务板 T3.2.3、T3.2.2 full-covariance periodic moments/latest-window，以及 frozen
observation/update/causal-delay contract。

## 实际完成内容

实现只存 joint circular-feature chunks 的增量 sliding window，在相同 384 observations/一次 update
预算下扫描 384--1536 history；窗口只在 training traces 选择，evaluation 保留全候选和负结论。

## 产物路径

- 实现：`cnn_fpga/decoder/sliding_window_syndrome.py`、
  `cnn_fpga/benchmark/sliding_window_syndrome_estimator.py`；
- 测试：`tests/test_sliding_window_syndrome.py`、
  `tests/test_sliding_window_syndrome_estimator.py`；
- 报告：`docs/sliding_window_syndrome_estimator.md`；
- 数据：`docs/t3_2_3_sliding_window_validation.json`、`.csv`；
- 详细记录：`docs/tasks/T3.2.3_sliding_window_syndrome_estimator.md`。

## 验证方式和结果

- 6 candidates、1,572,864 paired samples、32 seed rows、14/14 gates；
- training 与 evaluation aggregate 均选择 384；latest-minus-selected `0 [0,0]`；
- static-minus-selected `0.009740 [0.009529,0.009951]`；
- focused `25 passed`、registry/source-bound adjacent `217 passed`；
- 七份受 registry 影响 artifact 全规模重生成并全部通过。
- 显式 `tests/` 全量 `978 passed, 4 skipped, 4 failed`；4 个失败仅为 R-N012 缺失历史文档。

## 风险复核

新增 R-N054：当前 boundary selection 不能外推到 non-Gaussian、regime/OOD、不同 window budget 或
真实 device；局部场景最佳窗不同，evaluation diagnostics 不得转成 oracle selector。

## 是否需要插入新 task

不插入。T3.2.6、T5.1/T5.4、T5.5/T6 已覆盖 regime/OOD、budget Pareto、综合与实板证据。

## 对任务板和论文 claim 的同步

T3.2.3 标为 Done，T3.2.4 标为 In Progress，当前推荐改为 T3.2.4。允许报告 uniform long-history
negative result；禁止 evaluation-tuned selector、universal window、CNN superiority 或 FPGA claim。
