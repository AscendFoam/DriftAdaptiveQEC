# T3.2.9 完成记录：有限时域 trajectory lookup control oracle

- Task ID：T3.2.9
- 日期：2026-07-15
- 状态：Done

## 输入材料

`docs/new_task_board.md`、Puviani lookup-table 补充材料、T2.3.4/T2.3.5 differentiable
trajectory/Feedback-GRAPE gradient contract、T1.4.5 terminology registry。

## 实际完成内容

建立两 full-cycle、4 measurements、15 causal prefix nodes、225 controls、16 terminal branches
的 exact lookup reference。全部 branch 同时进入 probability-weighted fidelity objective；三次
独立 restart 各执行 300-epoch phase-one 与 250-epoch low-rate refinement。另行优化
time-indexed open-loop并精确嵌入 lookup，防止普通 retuning 被误当 history gain。

## 产物路径

核心/runner、两份 tests、JSON/PT/CSV、`docs/trajectory_lookup_control_oracle.md`、详细任务记录，
以及 physics export、README/protocol/terminology/risk/task-board 同步。

## 验证方式和结果

- 20/20 gates，3,418 Source Data rows；focused DLEnv `29 passed`，governance `75 passed`，
  adjacent DLEnv `237 passed`；默认解释器全量 `1084 passed, 10 skipped, 4 failed`，4 个失败
  仍仅为 R-N012 的 FR8/P4 历史治理文档缺失，torch 专项已在 DLEnv 执行；
- cutoff12 lookup/open/standard `0.815799/0.769403/0.396787`；
- cutoff16 frozen lookup/standard `0.638688/0.559221`；
- 全节点 causal mapping、future suffix invariance、中心差分、full-node gradient/update、嵌套等价、
  refinement tail、checkpoint/hash/replay、branch normalization/PSD 和指数资源均自动验证；
- compileall、live JSON/checkpoint/CSV hash 与 git diff check 另行通过。

## 风险复核

新增 R-N060。该结果是 empirical multi-start assumed-model reference，不是全局最优；branch
distribution 明显 skew、p(g) 降低、open-loop cutoff transfer 反转，且资源按 `2^(2C)` 增长。

## 是否需要插入新 task

不插入；T3.2.11、T4.4、T5.1.3、T5.2/T5.3/T5.4.5、T6 已覆盖。

## 对任务板和论文 claim 的同步

T3.2.9 Done，T3.2.10 In Progress。允许 empirical finite-horizon control-oracle reference；
禁止 global optimum、decoder/channel bound、long-horizon deployable、paper-exact 或 device/board claim。
