# T1.3.2 完成记录：full-state oracle MAP

**Task ID：** T1.3.2  
**标题：** 已知真实时间依赖 DriftState 的 mean/covariance/mixture-aware oracle  
**日期：** 2026-07-14  
**状态：** Done

## 输入材料

- `docs/new_task_board.md` T1.3.2；
- T1.3.1 `DriftState` 与七类 trajectory；
- T1.1.2/T1.1.3 periodic Gaussian 1D/2D MAP；
- T1.2.2 q/p parity 到 Pauli logical action 的映射；
- R-N016：禁止 oracle 走 legacy scalar RMS adapter。

## 实际完成内容

- 新增 `physics/oracle_map.py`；
- 对 core/outlier 两个 correlated 2D periodic Gaussian 的四陪集 log likelihood 做稳定
  mixture log-sum-exp；
- 输出 posterior、parity、logical class、`I/Z/X/Y` logical action、centered analog
  correction、confidence 和 state provenance；
- 增加逐时间步 full-state trajectory 接口；
- loss 默认 separate，仅支持显式、可追踪的 `gamma/2` additive proxy；
- 新增 9 项 direct tests、公共惰性导出、任务文档和 README 入口。

## 产物路径

- `physics/oracle_map.py`
- `tests/test_oracle_map.py`
- `physics/__init__.py`
- `docs/tasks/T1.3.2_oracle_map.md`
- `docs/new_tasks/T1.3.2_oracle_map.md`
- `docs/new_risks.md`
- `docs/new_task_board.md`
- `README.md`

## 验证方式和结果

- focused：`9 passed`；
- M1.1/M1.2/drift 邻近回归：`96 passed`；
- 四 coset mixture likelihood 与独立 `61x61` 双重 alias 穷举差小于 `5e-15`；
- `p=0`、`p=1`、`scale=1` 精确退化；
- step trajectory 在真值边界从 `I` 精确切到 `X`；
- 4 万独立样本 empirical error `0.318725`，posterior Bayes risk `0.318930`，差
  `-0.093 SE`；
- 显式 tests/：`162 passed, 4 failed`，仍仅为 R-N012。

## 风险复核

- R-N016 处置进展：oracle 已从接口和测试上直接消费 full state，不接受 legacy adapter；
  风险仍保留到后续主 benchmark/config 同样锁定该路由；
- 新增 R-N018：当前 oracle 只是 assumed syndrome-level mixture 的 Bayes reference，不能
  升级为 finite-energy/channel-recovery fundamental bound；
- R-N017 不变：state temporal law 仍是 synthetic，未做设备校准；
- R-N012 不影响本任务。

## 是否需要插入新 task

不插入。R-N016 的 main benchmark 路由由 T3.1.3/T5 配置门继续关闭；R-N018 由 T2
multi-fidelity、T3.1.3 命名边界和 T5.3.5 channel-recovery bound 正常任务覆盖。当前
oracle label 已降级，不阻塞 regret 指标。

## 对论文 claim 的影响

允许称为 “nondeployable full-state Bayes oracle under the assumed periodic Gaussian-mixture
model”。不得简称为真实 recovery optimum、实验 oracle 或 deployable decoder。

## 对任务板的同步

- T1.3.2：`In Progress -> Done`；
- T1.3.3：`Todo -> In Progress`；
- 当前推荐任务：`T1.3.3`。
