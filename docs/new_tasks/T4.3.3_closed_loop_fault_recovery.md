# T4.3.3 完成记录：闭环稳定性与故障恢复

- Task ID：T4.3.3
- 标题：测试闭环稳定性和故障恢复
- 日期：2026-07-15
- 状态：Done

## 输入材料

T4.3.1 三时间尺度 cadence、T4.3.2 atomic image bank、T4.2 bit-accurate fast fallback、T0.1.3/T1.4.3 failure contract 和 T2.4.2 timing-fault stress 经验。

## 实际完成内容

新增 closed-loop supervisor、observed/integrity-only cycle DTO、ack/readback uncertainty、host heartbeat/timeout、post-commit guard、LKG semantic hash、monotonic content republish、fresh candidate recovery、完整 cadence refresh 和逐周期 reason/action trace。bad transfer、unregistered same-version image、pending ack writer、wrong LKG semantics、nonsequential cycle 均 fail closed。

## 产物路径

`cnn_fpga/runtime/closed_loop_fault_recovery.py`、`cnn_fpga/benchmark/closed_loop_fault_recovery_validation.py`、`tests/test_closed_loop_fault_recovery.py`、`tests/test_closed_loop_fault_recovery_validation.py`、`docs/closed_loop_fault_recovery.md`、`docs/t4_3_3_closed_loop_fault_recovery_validation.json` 和 `docs/t4_3_3_closed_loop_fault_recovery_source_data.csv`；同步 public API 与文档地图。

## 验证方式和结果

production CLI 17/17 gates PASS；32 runs/767872 cycles/436 compressed rows，所有周期进入 trace hash。undefined action、blocking fault correction、frame overflow 均为0；drift 覆盖约670 ADC codes；burst/leakage/timeout/ack loss/corrupt/race/guard recovery 全部 reason-coded；guard campaign 独立重跑 hash/commit epochs 一致。focused `19 passed`、相邻 `124 passed`；显式最终全量 `tests/` 为 `1460 passed, 14 skipped, 4 failed`，4 项仍仅为 R-N012 两份旧治理文档缺失。

## 风险复核

新增 R-N074。该证据只证明 software control-safety/stability contract；timeout/OOD/guard/leakage/reset 是未校准 policy；compressed segments 不是 transport trace；monotonic LKG republish 不是自动物理 rollback；无物理 LER/lifetime、RTL/综合/FPGA/board。

## 是否需要插入新 task

不插入。T5.1/T5.2/T5.4 直接覆盖 OOD/fault/long-horizon 和物理指标；T5.5/T6 覆盖 wire、bit-accurate golden、CDC/RTL、综合、HIL/board。R-N012 与本任务独立。

## 对任务板同步说明

T4.3.3 更新为 Done；T4.4.1 更新为 In Progress；当前推荐任务更新为 T4.4.1。
