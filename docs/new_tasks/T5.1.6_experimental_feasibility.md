# T5.1.6 完成记录：实验可行性约束

- Task ID：T5.1.6
- 标题：报告实验可行性约束
- 日期：2026-07-16
- 状态：Done

## 输入材料

T4.4.4 controller gain/burden/cost、T5.1.5 time-cost lanes、T4.3.3 closed-loop fault campaign、
T4.2.3 component fallback、T4.4.3 student fail-closed artifact 与 T5.1.4 active fallback branch。

## 实际完成内容

建立只读、hash-bound feasibility consolidation：10 个 matched-controller rows 同报 p(g)/p(e)、reset、
residual/slew、lifetime 与 analytic cost；8 个故障场景同报 fallback/reset/unsafe/undefined/ack burden；
另保留 4,096-cycle component fallback taxonomy、student fail-closed contract 和 7 项 missing evidence。
multilevel leakage、saturation rate、matched/board/frontend latency 不用 0 补空，总体 deployment readiness 固定为
`NOT_ESTABLISHED`。

## 产物路径

- `cnn_fpga/benchmark/experimental_feasibility.py`
- `tests/test_experimental_feasibility.py`
- `docs/experimental_feasibility.md`
- `docs/t5_1_6_experimental_feasibility.json`
- `docs/t5_1_6_experimental_feasibility_source_data.csv`
- `docs/protocol_hierarchy.json` / `docs/protocol_hierarchy.md`

## 验证方式和结果

runner 输出 10 controller rows、8 fault scenarios、408-row ledger 和 21/21 gates。direct/mutation suite
`34 passed`；六组 parent adjacent `94 passed`。测试拒绝 missing=null 填 0、隐藏 reset/fallback、错误 peak、
软件 campaign 伪 population bound、student safe baseline 改写、branch/deployment promotion 与 stale/missing parent。

## 非 demo 检查

初版 runner 虽 21/21 PASS，但语义 validator 未逐项拒绝 component status、student fail-closed、global
fallback/reset 总量和 nonmixing contract 的篡改。开发期已收紧这些约束并新增逐项 mutation；没有降低门槛、
删除 host-timeout 大成本或把缺测字段填成有利数值。peak lifetime 仍携带 72,853-scalar/72,266-MAC 与
latency-null，不允许支撑 deployment claim。

## 风险复核

新增 R-N088：当前 p(g)/p(e) 为 two-level simulator occupancy，软件故障率不是装置总体率，且 multilevel
leakage/saturation/reset fidelity/latency 尚未匹配测量。T5.2--T5.5 与 T6 已正常承接，不插入风险 task。

## 任务板同步

T5.1.6 Done；T5.2.1 In Progress；当前推荐任务更新为 T5.2.1。
