# T5.3.2 logical-channel fidelity 完成记录

- Task ID：T5.3.2
- 标题：报告 `F_avg`、`F_e` 与短时有效退极化率
- 日期：2026-07-16
- 状态：Done

## 输入材料

T5.3.1 六态 CPTNI artifact/raw outputs、`physics/fock_logical_channel.py`、本地 beyond-break-even 论文的
六态 channel fidelity 与 leading-order rate 公式，以及任务板的 leakage/uncertainty/claim 边界。

## 实际完成内容

实现 CPTNI identity-target `F_e/F_avg`、六态 direct replay、conditional diagnostic、TP 公式高估审计；实现
无指数拟合的 1/3/4-point short-time rate、reliability gate、36/40 cutoff interval 和完整 artifact validator。
正式 qec-on 初始曲线出现下降—回升瞬态，未按 demo 方式输出 `1/Gamma` 寿命，而是保留 raw rate、标记
`unreliable_cycle_scale_transient` 并把 qualified lifetime 置空。

## 产物路径

- `physics/logical_channel_fidelity.py`
- `cnn_fpga/benchmark/logical_channel_fidelity.py`
- `tests/test_logical_channel_fidelity.py`
- `tests/test_logical_channel_fidelity_report.py`
- `docs/logical_channel_fidelity.md`
- `docs/t5_3_2_logical_channel_fidelity.json`
- `docs/t5_3_2_logical_channel_fidelity_source_data.csv`
- `docs/tasks/T5.3.2_logical_channel_fidelity.md`

## 验证方式和结果

- 24 lanes、5,294 Source Data rows、23/23 gates；
- direct/mutation：31 passed；
- parent/logical-channel/protocol adjacent closeout：116 passed；
- TP formula overstatement、六态/PTM identity、conditional role、no-fit、uncertainty null、cutoff reversal、
  no-gain 和 provenance 均机器验证；
- cutoff40 qec-off lifetime qualified，qec-on rate spread `0.528--0.908` 且 lifetime null；
- terminal 36/40 `F_avg` spread 最大 `0.003475`，低 cutoff 方向反转完整保留。

## 风险复核

新增 R-N093。deterministic cutoff/time-grid sensitivity 不是 statistical CI；active raw short-time rate 不是
寿命或 break-even。T5.3.3 必须读取完整 matched curve，而不能只消费终点或不合格的 rate 倒数。

## 是否需要插入新 task

否。T5.3.3--T5.3.5 已按顺序承接 operational boundary、成本和 recovery bound。

## 对 `docs/new_task_board.md` 的同步说明

T5.3.2 `In Progress -> Done`；T5.3.3 `Todo -> In Progress`；当前推荐任务改为 T5.3.3。
