# T3.1.5 完成记录：top-K lattice-coset MAP

- **Task ID：** T3.1.5
- **标题：** single-mode top-K lattice-coset truncated MAP baseline
- **日期：** 2026-07-15
- **状态：** Done

## 输入材料

新任务板 T3.1.5、`experiment_plan.md` §16.1、现有 2D periodic Gaussian MAP，以及 v2.2
关于“只借鉴 top-K accumulation、不实现 surface K-MWM”的范围边界。

## 实际完成内容

实现四逻辑陪集内的 joint 2D alias ranking、稳定 top-K likelihood prefix sum、full-MAP 同候选
矩形参考、K=1--128 扫描和确定性 probability-domain operation/storage proxy。在线输入只有当前
q/p modular syndrome，所有模型参数冻结；异常输入和过大候选工作量 fail closed。

## 产物路径

- 实现/测试：`cnn_fpga/benchmark/topk_lattice_coset_map.py`、
  `tests/test_topk_lattice_coset_map.py`；
- 报告：`docs/topk_lattice_coset_map.md`；
- 数据：`docs/t3_1_5_topk_map_validation.json`、`.csv`；
- 详细记录：`docs/tasks/T3.1.5_topk_lattice_coset_map.md`。

## 验证方式和结果

- 288,000 samples、192 行 Source Data、8/8 gates；
- 6/6 场景的 K=1 soft LLR 非精确，scenario convergence K 为 2--4；
- K=128 在 6/6 场景与 full candidate sum 浮点一致；
- independent enumeration、full-MAP、prefix reuse、translation、fail-close、CSV/hash/cost tests；
- focused+adjacent `133 passed`；全量 `895 passed, 4 skipped, 4 failed`，失败仍仅 R-N012。

## 风险复核

新增 R-N051，监控 synthetic Gaussian/tail/prior/mismatch 和未综合 cost proxy。有限 K 的 LER
不要求单调；K=2--4 只对当前场景成立。

## 是否需要插入新 task

不插入；T5.1/T5.5 已覆盖跨场景 Pareto、precision/parallelism 与具名器件 synthesis/P&R。

## 对任务板和论文 claim 的同步

T3.1.5 标为 Done，T3.2.1 标为 In Progress，当前推荐改为 T3.2.1。允许写 single-mode
top-K periodic-MAP approximation；禁止 surface K-MWM、universal K 和真实 FPGA 资源 claim。
