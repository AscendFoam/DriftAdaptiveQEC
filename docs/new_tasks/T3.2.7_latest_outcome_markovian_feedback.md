# T3.2.7 完成记录：latest-outcome FNN / Markovian feedback

- Task ID：T3.2.7
- 日期：2026-07-15
- 状态：Done

## 输入材料

`docs/new_task_board.md`、PRL 定向改进记录、T2.3.7 simulator/artifact/checkpoint、R-N044 与
decoder/controller terminology registry。

## 实际完成内容

审计旧 70,159 参数 MF 后，新增 72,853 参数、72,266 dense-MAC 的 exact-budget FNN；其 learned
path 只消费当前 g/e/leakage token，输出与 history GRU 相同的 15 维 sBs residual。按 T2.3.7
同一 5-agent、300-epoch、validation-only checkpoint、test/confirmation split 训练，并在同
trace 重评 standard、旧 MF、exact MF 和冻结 NMF。保留 cutoff 12 无 memory gain与 cutoff 16
反向显著 gain 的冲突结果。

## 产物路径

核心/runner、两份 tests、JSON/PT/CSV、`docs/latest_outcome_markovian_baseline.md`、详细任务记录、
terminology binding、README/protocol hierarchy/risk/task-board 同步。

## 验证方式和结果

- 13/13 gates，18,023-row Source Data；
- primary `NMF-exact=-0.147464 [-0.386866,0.147532]`；
- cutoff16 `NMF-exact=+0.540082 [0.231972,0.785521]`；
- exact-old `+0.353578 [0.084842,0.571642]`；
- direct 全参数梯度、production 仅 10 个未观测 leakage-column weights、hash/resume、raw curve、
  density diagnostics 均机器验证；
- focused DLEnv `27 passed`；governance 组合 `72 passed`；相邻 DLEnv `172 passed`；默认解释器
  全量 `1083 passed, 6 skipped, 4 failed`，4 个失败均为 R-N012，torch skips 已在 DLEnv 真实执行。

## 风险复核

R-N044 的 memory-specific claim 被削弱；新增 R-N058 记录 cutoff reversal、上限 epoch 与 leakage
缺口。不能用旧弱 MF、单一 cutoff 或 best agent 得出稳定 memory mechanism。

## 是否需要插入新 task

不插入；T3.2.11、T4.4、T5.2、T5.4.4/T5.4.5 已覆盖。

## 对任务板和论文 claim 的同步

T3.2.7 Done，T3.2.8 In Progress。允许 exact-budget signed comparison；禁止稳定 memory、
multilevel leakage、optimizer optimality、device/FPGA claim。
