# T3.2.8 完成记录：autonomous sBs 物理时间 baseline

- Task ID：T3.2.8
- 日期：2026-07-15
- 状态：Done

## 输入材料

`docs/new_task_board.md`、Puviani 正文/补充材料、T2.3.4 differentiable simulator、T2.3.7
sBs protocol/noise/checkpoint evidence，以及 R-N044/R-N058 的单位与短时机制边界。

## 实际完成内容

实现 measurement-feedback 10 us 与 autonomous 7 us 的 protocol-native nonselective density
evolution，在共同 700 us 而非共同 cycle count 下比较。两条路径复用相同 gate sequence；
autonomous 移除 measurement event 但保留 ancilla trace/reset，并按其原生 duration 施加 noise。
生产矩阵覆盖 cutoff 12/16、high/medium/low noise、1,020 cycles，同时输出每条 curve、原始
事件数和 per-cycle/per-us lifetime。

## 产物路径

核心 simulator/runner、两份 tests、JSON/CSV、`docs/autonomous_sbs_wallclock_baseline.md`、
详细任务记录，以及 README/protocol hierarchy/risk/task-board 同步。

## 验证方式和结果

- 17/17 gates，4,362-row Source Data；
- 非选择性 channel 与四分支显式枚举最大 density 差 `1.42e-16`；zero-noise duration
  invariance 差 `5.38e-15`；
- 每 lane 700 us：measurement/autonomous `70/100` cycles、`140/0` measurements、
  `140/200` resets、`1260/1800` active gates；
- per-cycle autonomous/measurement ratio `1.151287--1.346101`，per-us ratio却为
  `0.805901--0.942271`；cutoff 12 的最终单点更高但 lifetime 仍更低；
- focused DLEnv `35 passed`，相邻 DLEnv `161 passed`；默认解释器全量
  `1083 passed, 8 skipped, 4 failed`，4 个失败仍仅为 R-N012 的 FR8/P4 历史治理文档缺失，
  torch 专项已在 DLEnv 执行；compileall、JSON 17 gates 和 CSV 4,362 rows 复核通过。

## 风险复核

新增 R-N059：当前固定控制与 two-level finite-cutoff model 不等于 autonomous-specific
optimization 或实验复现；单位、cutoff 与末端单点会改变表面排序。报告必须保留共同物理时间、
完整 trajectory/AUC 和原始事件成本。

## 是否需要插入新 task

不插入。既有 T5.0.1、T5.2、T5.3/T5.4 和 T6 已覆盖复现、multilevel、稳健性和实测 timing。

## 对任务板和论文 claim 的同步

T3.2.8 Done，T3.2.9 In Progress。允许 finite-cutoff nominal-control wall-clock signed result；
禁止 trained optimum、Fig. 3(b) replication、device/board timing 与 autonomous 一般优劣 claim。
