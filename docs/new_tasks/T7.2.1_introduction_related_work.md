# T7.2.1：起草 Introduction 与 Related Work

- 日期：2026-07-20
- 状态：Done
- 终态：`PASS_EVIDENCE_BOUNDED_INTRODUCTION_RELATED_WORK`

## 输入材料

- `docs/t7_1_1_claim_evidence_boundary_matrix.json` 与 29 条 manuscript claim placement
- `docs/t6_16_1_secondary_method_source_audit.json`
- `docs/t6_16_2_comparison_ontology.json`
- `docs/t6_19_3_secondary_evidence_integrity.json`
- `docs/literature_matrix.md`
- V4/V5 terminal gates、GQF exact attempt、learned eligibility 与 external FPGA normalization
- `docs/paper_notes/Contract_Centric_Regime_Aware_GKP_note_draft.tex`
- Nature-writing 的 `algorithmic / intro+related-work / zh-to-en / generic high-impact` 写作轴，以及 LaTeX/PDF 编译与视觉审查规范

## 执行方案与实际完成内容

1. 先按当前终态证据重定论文论点：不再以 CNN winner 为中心，而把 contribution unit 定义为 execution contract；MAP 负责 LER，typed event/fallback/rollback 负责 safety，versioned six-cycle fast path 负责 deterministic execution，learned modules 为可替换 extension。
2. 在 Route-A 主 note 中新增 6 段 Introduction：finite-energy/真实反馈约束、analog/history prior art、drift/calibration 与 timing boundary、精确 systems question、Route-A authority split、restricted current conclusion。
3. 将原有两个短定位章节重写成 5 个 Related Work 机制组：
   - analog/history-aware/structured GKP；
   - calibration/drift adaptation；
   - Direct NN、model-free RL、model-based NMF 与 AQEC；
   - deterministic/FPGA QEC decoder；
   - present-work positioning。
4. 所有外部比较按六条 task-signature lane 分开；明确 code/modes、noise、syndrome、action、observability、budget、precision、timing boundary 与 evidence grade 未匹配时不得排名。
5. 建立可执行 prose contract，逐行绑定 14 条 literature/project claims、citation keys、project source IDs、evidence grade 与禁止外推边界；生成 JSON、Source Data CSV 和中文 human report。
6. 把主 note 从 6 条手写 bibliography 切换为独立 BibTeX；新增/规范化 `sivak2023`、`puviani2025`、`lachance2024`、`lin2023`，清理会渲染为 `_eprint`/`version: 1` 的占位样式，并为 Spitz/Wagner/Chen 补 DOI、卷期页。
7. 保留并纳入上一任务尚未提交的 T7.1.4 两份 note supplement overlay；这些改动只同步 S1--S5 边界，不产生新性能 claim。

## 关键叙事与负证据

- 允许：restricted simulator/pre-board contract integration；对 locked EWMA 的 smooth aggregate paired 改善；六周期/II=1 deterministic architecture 与 post-route estimate。
- 必须显式保留：static joint MAP 平均 LER 更低、Window MAP 是强反例、tail 仅为 locked-EWMA non-inferiority、V5 因 causal deployable headroom 失败而在 formal/RTL 前停止。
- NMF：官方 GQF 不足以完成 paper-exact matched reproduction；不得写 reproduce/surpass Puviani NMF。
- learned/controller：same-task eligible checkpoint 数为 0；不能由此反推 Route-A 更优。
- FPGA：external exact same-task comparator 数为 0；这阻止而不是支持 speed ranking。板卡 latency/jitter/power 保持 null。
- Phase 6C multimode CPD 等正结果只在各自 task lane 内成立，不能救回 V5 或形成跨协议总榜。

## 产物路径

- `docs/paper_notes/Contract_Centric_Regime_Aware_GKP_note_draft.tex`
- `docs/paper_notes/CNN_FPGA_GKP_submission_refs.bib`
- `cnn_fpga/benchmark/introduction_related_work_contract.py`
- `docs/t7_2_1_introduction_related_work_contract.json`
- `docs/t7_2_1_introduction_related_work_source_data.csv`
- `docs/introduction_related_work_contract.md`
- `tests/test_introduction_related_work_contract.py`
- `docs/paper_notes/README.md`

## 验证方式和结果

- prose contract：18/18 gates 通过。
- semantic mutation audit：18/18 定向篡改被检出；覆盖 section/order、引用缺失、lane 删除、task-signature 降级、static/Window/V5/NMF/learned/FPGA 边界删除、global ranking 与 absolute-first/SOTA 注入。
- focused tests：`python -m pytest tests/test_introduction_related_work_contract.py -q` -> 8 passed。
- BibTeX：Introduction/Related Work 共 30 个 citation keys，全部存在于 active `.bib`。
- TeX Live/latexmk：生成 38 页 PDF；日志中 overfull、undefined citation/reference、LaTeX/package warning、fatal/error 均为 0。仅证据路径长表有预期 underfull box。
- PDF QA：先对 38/38 页生成 contact sheets 全览，再以原始/高分辨率检查 Introduction、Related Work、section transition 与参考文献页；无裁切、重叠、孤立标题、乱码或占位 token。清理 `_eprint` 后重新编译并复核参考文献末页。

## 反简化与深审

- 未把“有章节文本”当作完成：正文、引用、任务签名、负证据、边界和 citation/project source 同时受机器合同约束。
- 未把截图类别当作统一方法榜：CI/ML、Direct NN/RL/NMF、AQEC、CPD 与 FPGA 分 lane，same-task=0 不算优势。
- 未把计划中的 posterior-predictive V5 写成已实现：正文明确其在 formal/RTL 前 early stop；Methods 下一任务必须继续保留这一分支语义。
- 未隐藏当前最不利证据：static/Window、fallback/tail、NMF reproduction、learned eligibility 和 board-null 都进入读者可见 prose。
- 引用不是只过编译：补了缺失的一手来源条目，清理了 PDF 中暴露的 `_eprint` 与 `version: 1` 元数据噪声，并重新视觉检查。

## 风险复核

- R-N010 仍 Open/Medium/Monitor：2025--2026 preprint 的 venue/year/DOI 仍可能更新；T7.4.1/投稿前需刷新。
- R-N142 保持 Mitigated/High/Monitor：T7.2.1 已给 prose 增加 18 门 fail-closed 合同，但后续 Methods/Results/Discussion 仍可能发生 claim 拼接，必须继续消费该合同。
- R-N145 不变：historical prereg snapshot 与 latest artifact 的分路径/环境锁仍由 T7.4.1/T7.4.3 处理。

## 是否需要插入新 task

不插入。当前缺口分别由正常顺序的 T7.2.2--T7.4 与既有 T6.9.2 承接；新增 post-hoc 文献值排名、V5 rescue 或伪板测任务会破坏 evidence boundary。

## 对任务板的同步

- T7.2.1：`In Progress -> Done`。
- T7.2.2：`Todo -> In Progress` 并成为当前推荐任务。
- README、paper-notes README、R-N010/R-N142 与风险插入判断同步完成。
