# T1.2.2 完成记录：finite-energy Pauli-twirled logical channel

**Task ID：** T1.2.2  
**标题：** Parity confusion、Pauli probabilities、PTM 与 fidelity metrics  
**日期：** 2026-07-14  
**状态：** Done

## 输入材料

- `docs/new_task_board.md` T1.2.2；
- T1.1.2/T1.1.3 的 1D/2D parity-output decoders；
- T1.2.1 normalized finite-energy wavefunction families；
- Jafarzadeh et al. 2025 approximate-GKP logical-channel 指标边界；
- T0.2.2 中 channel/recovery bound 与 claim downgrade contract。

## 实际完成内容

- 新增严格校验的 `PauliChannel`，输出 `p_I/p_X/p_Y/p_Z`、PTM、logical error、
  `F_e/F_avg`；
- 实现 residual distribution、paired samples、4x4 joint confusion 与显式独立轴
  confusion 四种构造路径；
- 冻结 q residual -> X、p residual -> Z、joint -> Y 的 XOR mapping；
- 实现 `FiniteEnergyGKPState` probability 的 exact Gaussian-displacement convolution；
- 实现 finite-energy alias joint density、soft posterior、MAP decisions 和 captured-mass
  diagnostics；
- 实现任意 parity decision array/callable 的 confusion evaluation；
- 为 response、probabilities、weights、shape、grid 和 workload 增加失败关闭；
- 新增 13 项 direct tests 和公共惰性导出。

## 产物路径

- `physics/logical_channel.py`
- `tests/test_logical_channel.py`
- `physics/__init__.py`
- `docs/tasks/T1.2.2_finite_energy_logical_channel.md`
- `docs/new_tasks/T1.2.2_finite_energy_logical_channel.md`
- `README.md`

## 验证方式和结果

- logical-channel focused：`13 passed`；
- logical/finite/ideal/statcalib 组合：`65 passed`；
- 12 万样本独立 finite-state mixture MC 与解析 error 相差 `1.74 SE`；
- correlated joint-MAP 样本保留 `p_Y=0.22195`，显著不同于 marginal 独立乘积；
- PTM、`F_e=p_I`、`F_avg=(2F_e+1)/3` 的已知 channel 测试通过；
- 显式 tests/：`127 passed, 4 failed`，仍仅为 R-N012。

## 风险复核

- R-N013 部分缓解：新 channel 显式依赖 `finite_energy_gkp`，不消费 legacy heuristic；
  其他主实验配置仍需 T2 路由审计；
- 新增 R-N014：当前 channel 是 Pauli twirl，不能覆盖 non-Pauli/coherent/leakage；
- R-N004 继续缓解：logical-channel 新接口有 direct analytic/MC/failure-path tests；
- R-N012 不变且不阻塞 T1.2.3。

## 是否需要插入新 task

不插入。R-N014 已由 T5.3.1 的六 Pauli eigenstate/PTM/non-Pauli/leakage 诊断与 T2
multi-fidelity protocol 正常任务覆盖；当前指标保持明确降级，不影响 T1.2.3 趋势复现。

## 对论文 claim 的影响

允许写 Pauli-twirled effective logical channel metrics 已实现并经独立 MC 验证；不允许
写 full Fock/recovery channel、non-Pauli/leakage 或 experimental channel fidelity。

## 对任务板的同步

- T1.2.2：`In Progress -> Done`；
- T1.2.3：`Todo -> In Progress`；
- 当前推荐任务：`T1.2.3`。
