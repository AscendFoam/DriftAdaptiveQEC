# T7.1.5：Phase 6D 双 lane claim/figure delta

- **Task ID：** T7.1.5
- **标题：** 在保留历史快照的前提下冻结 Phase 6D 双 lane claim/figure delta
- **日期：** 2026-07-21
- **状态：** Done

## 输入材料

- T6.26.3 不可迁移的 10-claim/12-element evidence matrix；
- T6.26.4 四态 final gate：`MM=false, RTL=true -> GO_RTL_ONLY`；
- T6.20.4 multimode strongest-baseline causal-headroom NO-GO 与 T6.18.3 opened task-local context；
- T6.25.2 formal、T6.25.3 million-cycle CXXRTL、T6.25.4 three-seed whole-harness P&R；
- T7.1.1--T7.1.4 的 report、Source Data 与 figure manifest 历史快照；
- Nature figure contract/QA 规则，绘图后端按用户既定要求固定为 Python/matplotlib。

## 绘图前合同

1. **Figure 5 核心结论：** Phase6D v1 相对 strongest deployable static-mixture exact MLD 的 causal headroom 为零，因此不能建立 frozen-benchmark multimode LER SOTA；opened task-local 的 LER/tail/compute 只作上下文。
2. **Figure 6 核心结论：** exact single-mode converged RTL 建立 6-cycle/II=1、atomic、fail-closed 的 pre-board 实现证据；board latency/jitter/deadline/power 仍未测。
3. **证据隔离：** Figure5 不含 RTL 数值，Figure6 不含 multimode LER；两图无连接箭头、共同性能轴、LER--latency 总分或相互补门。
4. **学习模块：** CNN/student 只在 Figure5d 以 `DROPPED · ABLATION ONLY` inset 出现，不进入任一 primary verdict。
5. **导出合同：** 183 mm 双栏；editable SVG、PDF、300-dpi PNG、600-dpi LZW-TIFF；旧图路径、编号、颜色和 hash 不复用。

## 实际完成内容

1. 新增 machine contract `phase6d_claim_figure_delta.py`，直接验证 T6.26.3/T6.26.4 与 T7.1.1--T7.1.4 的父门及 bundle。
2. 将 12 个 T6.26.3 parent elements 逐 payload exact-preserve，并新增 1 个显式 `SCALING=NOT_RUN` null element；不是用零填补缺失实验。
3. 将 T6.26.4 的 10 条 final claims 逐字段保留，并为每条 claim/element 绑定 report、raw、config、code、source、selector、SHA-256 和 revocation；meta nontransferability claim 只绑定 bundle caption，不画跨 lane edge。
4. 冻结 11 个 T7.1.1--T7.1.4 report/Source Data/manifest 只读快照；三个旧 figure manifest 均重新验证，新输出 hash 与全部历史输出 hash 无交集。
5. 生成 94-row lossless Source Data：2 figure contract + 13 element + 10 claim + 11 snapshot + 58 artifact；逐行 canonical payload 与 SHA-256 可重建。
6. 新建 Figure 5：
   - a：static-mixture exact MLD 与 proposed 均 `p_L=0.111979`，relative point/95% LCB=`0%/0%`，显式 NO-GO；
   - b：opened task-local LER/tail/host-compute context，灰色 nonranking 边界；
   - c：train-only NO-GO、pilot/formal 未访问、scaling 未运行；
   - d：CNN/student Dropped inset。
7. 新建 Figure 6：
   - a：C0--C6、6-cycle/II=1 pipeline；
   - b：CRC/version admission→inactive A/B bank→safe atomic commit→LKG/hysteresis；
   - c：17/17 formal、21/21 mutants、1,000,000-cycle CXXRTL、0 full-vector mismatch；
   - d/e：GW2AR seeds 1/7/19 Fmax 与 complete-harness resources，明确 critical path 终止 observability fold；
   - f：六个 physical-board 字段 null 与 no speed/SOTA/current RTL≠multimode boundary。
8. 建立 22 项 gate 和 22 项独立 semantic mutation，覆盖父 verdict、历史快照、element/claim payload、evidence、MM 数值/缺失状态、RTL latency/board-null、learning、caption、导出、Source Data、board 生命周期与旧图复用。

## 产物路径

- 实现与配置：
  - `cnn_fpga/benchmark/phase6d_claim_figure_delta.py`
  - `configs/phase6d/t7_1_5_claim_figure_delta.json`
  - `docs/figures/make_t7_1_5_phase6d_delta.py`
- 合同与 Source Data：
  - `docs/t7_1_5_phase6d_claim_figure_delta.json`
  - `docs/t7_1_5_phase6d_claim_figure_delta_source_data.csv`
  - `docs/phase6d_claim_figure_delta.md`
- 图包：`docs/figures/t7_1_5_phase6d_delta/`
  - `figure5_multimode_software_delta.{svg,pdf,png,tiff}`
  - `figure6_single_mode_rtl_delta.{svg,pdf,png,tiff}`
  - `figure_manifest.json`
- 测试：`tests/test_phase6d_claim_figure_delta.py`

## 验证方式和结果

- 合同生成/验证：`python -m cnn_fpga.benchmark.phase6d_claim_figure_delta` / `--verify`；
- 图生成：`python docs/figures/make_t7_1_5_phase6d_delta.py --manual-visual-qa PENDING`；
- 视觉复核后冻结：同脚本 `--manifest-only --manual-visual-qa PASS`；
- bundle 验证：`python -m cnn_fpga.benchmark.phase6d_claim_figure_delta --verify-bundle`；
- verdict=`PASS_PHASE6D_CLAIM_FIGURE_DELTA_RTL_ONLY`；22/22 gates、22/22 mutations；final board-normalized analysis SHA-256=`a212efcb59f158648a2b47aa0b08dbac0f6a6387be37b3955e1264e0f570f925`；
- focused：17/17；覆盖 multimode headroom、T6.26 matrix/final、converged formal/long/P&R、T7.1.1--T7.1.4 历史 bundles、新 delta 与任务板治理的联合回归 149/149；
- 图形 QA：SVG editable text nodes=56/88、embedded raster=0；TIFF 最短边=3000/3236 px，PNG 最短边=1500/1618 px；nonwhite fraction=21.3%--23.0%，edge ink=0；人工视觉 QA=PASS。

## 深度检查与非简化实现复核

- 初版 Figure5 出现左标签与右注释裁切，Figure6 出现 full-vector 卡片挤压和 P&R caveat 重叠；没有直接把 PENDING 标 PASS，而是调整边距/换行/注释位置、重导出并再次查看原分辨率图。
- 初版合同只保存 parent payload hash，却未逐字段验证当前展示值等于 parent。已强化为所有 parent element 的 lane/namespace/title/value/wording/placement/claim 映射 exact equality，以及所有 final claim 字段 exact equality；只改图值而不改 hash 字段会 fail closed。
- 初版配置中的 matrix expected verdict 是旧命名但未被消费。已改成 live exact verdict，并把 parent verdict equality 纳入 G01。
- board 正常迁移揭示 T6.26.4 对 T7.1.5/T7.2.6 状态过度硬编码。已将这两个下游 task 规范化为 `TODO_ACTIVE_OR_DONE`，只接受 Todo/In Progress/Done，仍拒绝 Blocked/Dropped/Missing；重建 T6.26.4 后仍为21/21、22/22、`GO_RTL_ONLY`，未改变数值和 claim。
- unavailable 状态没有画成 0：pilot、formal、scaling 和 board measurements 都以文字/null状态呈现；只有实际观测为零的 relative headroom 和 mismatch 才显示 0。
- Figure6 的 Fmax 是 whole-harness observability top，critical path caveat 和 no-speed claim 同时进入 panel、caption、Source Data、claim revocation 与 mutation；没有偷换成裸 core 或真板 latency。

## 风险复核

- R-N158：降为 Mitigated/Monitor。新 Figure5--6 和 11 个旧快照同时 hash-bound，旧图 bundle 不能无 delta 发布。
- R-N159：保持 Open/High/Soon。图层已阻断 `GO_RTL_ONLY` 外溢，但 T7.2.6 仍需在摘要、正文和结论完整消费相同限定。
- 未新增风险。当前残余属于论文文字消费与最终发布审计，已由 T7.2.6/T7.4.3 覆盖。

## 是否需要插入新 task

不插入。T7.2.6 已按顺序开始，直接承担正文 delta；T7.3.8--T7.3.9 和 T7.4 已承担 reviewer answer 与发布审计，另开旁路会产生多个状态源。

## 对任务板与论文 claim 的同步

- T7.1.5：`In Progress -> Done`；T7.2.6：`Todo -> In Progress`。
- 允许主图 claim：exact single-mode deterministic/atomic/fail-closed pre-board RTL contribution。
- 强制并列负边界：multimode v1 strongest-baseline NO-GO、opened context only、pilot/formal/scaling unavailable、learning Dropped、board-null、no speed/SOTA、current RTL 不执行 multimode decoder。
