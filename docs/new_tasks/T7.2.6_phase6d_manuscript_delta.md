# T7.2.6：Phase 6D 双 lane canonical manuscript delta

- Task ID：T7.2.6
- 标题：按 T7.1.5 重写高水平论文的摘要、Introduction、Methods、Results、Discussion 与 Supplement delta
- 日期：2026-07-21
- 状态：Done

## 输入材料

- T6.26.4 的最终真值表：`MM=false, RTL=true -> GO_RTL_ONLY`；
- T7.1.5 的 10 条 final claims、Figure 5--6、94-row Source Data 和非迁移边界；
- T6.20.1 的 multimode strongest-baseline registry，以及 T6.20.4 的 79,872-round causal-headroom NO-GO；
- T6.25.2--T6.25.4 的 exact converged production top formal、百万周期 CXXRTL 与三 seed whole-harness P&R；
- T6.26.1--T6.26.2 的 CNN/student Dropped 状态；
- T7.1.1--T7.2.5 的 Phase 6C 历史 claim/figure/prose snapshots；
- 一手文献：Lin--Noh 2025 exact MLD 与 Lin 2025 K-MWM。

## 实际完成内容

1. 新建独立 canonical manuscript，而不覆盖原 51 页 restricted Phase 6C 历史稿。新稿从摘要到结论只消费 Phase 6D 最终真值表，历史稿仅作 provenance snapshot。
2. 摘要同时呈现两条 lane：multimode software 的 strongest-baseline `0%/0%` causal-headroom NO-GO，以及 exact single-mode RTL 的 six-cycle/II=1、atomic、fail-closed 正结果；没有把 `GO_RTL_ONLY` 写成整篇论文或真实 FPGA GO。
3. Introduction 把贡献改写为两个并列问题，并明确三类禁止迁移：RTL 不能救援 LER，软件 LER 不能证明 RTL，learning 不能越过独立 retention/budget gate。未定义 LER--latency 加权总分。
4. Related Work 按 task signature 分离 static/analog/multimode decoding、online adaptation/NMF/physical lifetime 与 FPGA decoder。深审补入 exact logical-coset MLD 和 K-MWM 的直接一手引用，而不是只间接引用 CPD。
5. Methods 分开定义 multimode observed-only causal-headroom 门、exact single-mode converged RTL 的时序与 A/B/LKG 事务、CNN/student 的 replaceable approximation 资格和 measured/estimate/context/negative/dropped/blocked 状态。
6. Results 原样报告 strongest baseline 与 proposed 均 `p_L=0.1119791667`、79,872 paired rounds、point/LCB=`0%/0%`、pilot/formal/scaling 未访问；opened 9.6M-cycle结果只作弱分母 context。
7. RTL Results 报告 17/17 formal gates、21/21 formal mutants、1,000,000 cycles、998,435/998,435 II=1 pairs、0 full-vector mismatch、三 seed最低 Fmax 36.794 MHz和 whole-harness资源；六个 physical-board 字段保持 null。
8. Discussion 解释 zero-headroom 的直觉与未来重开条件；把 contract bridge 限定为架构接口，不声称当前 RTL 执行 multimode MLD。
9. Limitations 明确没有 calibrated cavity--transmon、real GKP record、physical lifetime、break-even、board measurement 或 FPGA speed advantage。
10. Supplement delta 给出 canonical terminology、完整 multimode baseline eligibility、10-claim boundary、revocation/negative denominator 和历史 snapshot policy。
11. 实现机器合同，直接保留 T7.1.5 的 10 个完整 claim payload，逐节检查正文、引用、figure inclusion、parent 数值、board-null、learning Dropped、nontransfer、历史 snapshot 与 live hashes。
12. 实现 27 个 gates 和 27 个逐门独立语义 mutation；57-row Source Data 用 canonical JSON + SHA-256 逐行可逆。
13. 使用 TeX Live/latexmk/pdflatex + BibTeX 编译当前稿，生成 12 页 PDF。首轮编译发现 Supplement 单元格 4.02676 pt overfull 后缩短状态标签；最终日志 overfull/underfull/warning/undefined/fatal/package error 全为 0。
14. 按 PDF workflow 以 140 dpi 渲染 12 页并逐页人工检查。两张主图、跨页 longtable、引用与末页均无裁切、重叠、乱码、孤立标题或异常空白；12 页 edge-ink fraction 均为 0。
15. 联合回归首次发现把新引用追加到旧共享 bibliography 会使历史 contract hash 失效。没有重建旧快照掩盖问题，而是撤回共享库修改，新增 Phase 6D 独立 bibliography 并让新稿同时加载两库。

## 产物路径

- `docs/paper_notes/Phase6D_Dual_Lane_GKP_manuscript.tex`
- `docs/paper_notes/Phase6D_Dual_Lane_GKP_refs.bib`
- `output/pdf/t7_2_6_phase6d_dual_lane/Phase6D_Dual_Lane_GKP_manuscript.pdf`
- `cnn_fpga/benchmark/phase6d_manuscript_delta.py`
- `configs/phase6d/t7_2_6_manuscript_delta.json`
- `tests/test_phase6d_manuscript_delta.py`
- `docs/phase6d_manuscript_delta.md`
- `docs/t7_2_6_phase6d_manuscript_delta.json`
- `docs/t7_2_6_phase6d_manuscript_delta_source_data.csv`
- `docs/t7_2_6_phase6d_manuscript_visual_qa.json`

## 验证方式和结果

- Machine verdict：`PASS_PHASE6D_DUAL_LANE_MANUSCRIPT_DELTA_RTL_ONLY`；
- machine contract：27/27 gates；
- semantic mutation：27/27 detected；
- Source Data：57/57 rows，ID 唯一且 canonical JSON/SHA-256 可逆；
- exact parent checks：multimode baseline/equal-LER/zero-headroom/unopened downstream，以及 RTL latency/II/long-run/formal/mutation/Fmax/non-multimode 全部精确一致；
- citation：13 个正文 citation keys 全解析；exact MLD/K-MWM 直接引用独立 Phase 6D bibliography；
- LaTeX：12 pages，零 overfull、underfull、warning、undefined、fatal、package error；
- PDF text：36,196 characters，零 placeholder/tool token；
- visual QA：12/12 pages PASS；每页 nonwhite fraction 0.04122--0.13879，edge ink=0；
- focused/parent/governance：`68 passed`；加上 Introduction/Methods/Results/Discussion/Supplement live contracts 的十文件联合回归：`117 passed`。四份旧 prose 合同仅刷新当前 live hashes 与 PASS 快照，未改旧稿正文或共享 bibliography。

## 深审与非简化实现检查

本 task 不是在旧稿末尾追加一句 `GO_RTL_ONLY`。它建立了新的 current manuscript，并让 Abstract、Introduction、Methods、Results、Discussion、Limitations、Conclusion 与五段 Supplement 都显式消费相同 truth table。MM 的 zero-headroom、未运行的 pilot/formal/scaling、ineligible/dropped baselines、learning absence 和 board-null 不是脚注，而是主文与 Supplement 的结构字段。

机器合同不只查字符串：它从 T7.1.5 复制完整 claim payload 并逐项精确比对当前 parent；从 T6.20.4/T6.25.2--T6.25.4 重算关键数值；hash-bind manuscript、两份 bibliography、Figure 5--6、compiled PDF、compile log、visual-QA payload、历史 snapshots 与父 reports。27 个 mutation 分别伪造 parent verdict、数值、board measurement、learning promotion、global score、引用、图、段落、snapshot、Source Data 和 PDF QA，均被目标门拒绝。

联合回归发现并修复了一个真实历史污染路径：若把新 citation 写入共享旧 `.bib`，旧 T7 prose contracts 会因 hash 漂移失效。最终方案使用独立 bibliography，保持历史文件字节级不变。编译首轮 overfull 也没有用缩小全稿字号或删除负结果来回避，而是局部修复表格状态文字。

## 风险复核

- R-N159：`Open/High/Soon -> Mitigated/High/Monitor`。摘要到 Supplement 已同时保留 MM NO-GO、RTL pre-board scope、learning Dropped、board-null 与 no-global-score。
- R-N158：维持 `Mitigated/High/Monitor`。新稿独立路径，旧稿和 T7.1.1--T7.2.5 不覆盖；新 bibliography 也不污染历史共享引用库。
- R-N157：维持 `Mitigated/High/Monitor`。正文把 36.794 MHz 定义为 observability whole-harness estimate，并明示三条 critical path 终止于 fold。
- 实板和物理 lifetime 风险不关闭；T6.9.2、Phase 8 与 T7.4.3 继续承接。

## 是否需要插入新 task

否。当前发现的 overfull、直接引用缺失与历史 bibliography 污染均在本 task 内完成实质修复。CNN-centric 审稿风险、两个 lane 是否拼贴、SOTA comparator 完整性和最终发布 mutation 已分别由 T7.3.2、T7.3.8--T7.3.9 与 T7.4.3 承接；新增旁路写作任务会削弱唯一 canonical manuscript 状态源。

## 对任务板的同步

- T7.2.6：`In Progress -> Done`；
- T7.3.1：保持 `Blocked`；
- T7.3.2：`Todo -> In Progress`，成为当前推荐任务；
- 当前推荐摘要写入 10 claims、57-row Source Data、27/27 gates/mutations、12页编译与视觉QA。

## 对论文 claim 的影响

允许：
- Phase 6D multimode v1 在 strongest retained static-mixture exact MLD 前没有可用 causal headroom，因此未进入 pilot/formal，frozen-benchmark SOTA 未建立；
- exact single-mode converged RTL 在 pre-board 范围建立 six-cycle/II=1、atomic/fail-closed、formal mutation closure、百万周期 full-vector equivalence 与三 seed whole-harness timing closure；
- 两条 lane 通过 contract pattern 并列，CNN/student 是可替换但当前 Dropped 的 approximation。

禁止：
- Phase 6D 或整篇论文整体 GO；
- multimode SOTA、current RTL executes multimode MLD；
- board-measured latency/power/zero deadline miss；
- fastest FPGA decoder；
- physical logical lifetime 或 beyond-break-even；
- CNN/student 改变 classical algorithm 或 RTL verdict。
