# T4.1.4 完成记录：多目标 loss 与 calibration

- Task ID：T4.1.4
- 标题：定义多目标 loss 与 calibration
- 日期：2026-07-15
- 状态：Done

## 输入材料

T4.1.3 hybrid output/Source Data、registered syndrome stream、full-state model oracle、任务板六项目标。

## 实际完成内容

冻结 state/oracle-gap/regime/uncertainty/false-fallback/update-cost 六项 loss 和权重；实现 training-only
normalization、validation-only calibration、evaluation-blind scoring、safety recall constraint、proper scores
与 frozen-output leave-one-objective-out。448 个 32-cycle future-aligned records 使用 3/2/3 disjoint seeds。

## 产物路径

`cnn_fpga/decoder/hybrid_multiobjective.py`、
`cnn_fpga/benchmark/hybrid_multiobjective_calibration.py`、两份 tests、主说明、19-gate JSON 和 448-row CSV。

## 验证方式和结果

19/19 gates、25 focused、179 adjacent（2 skipped）；显式全量 `tests/` 为
`1255 passed, 14 skipped, 4 failed`，4 个失败仍仅为 R-N012 缺失旧 FR8/P4 文档。validation regime NLL `8.563501 -> 1.262520`，marginal 95% coverage
`0.479167 -> 0.947421`；evaluation candidate/oracle `0.115988/0.101887`，total loss `0.756840`。

反简化审计修复全样本 unsafe label 和 evaluation zero update-cost。负结果仍为 fallback recall/false rate
`1.0/1.0`，regime accuracy 不变；不把概率校准写成分类/控制增益。

## 风险复核

新增 R-N066：旧 HMM bridge 与 risk/OOD score 缺选择性，仍不是 physical/device calibration。

## 是否需要插入新 task

不插入；T4.1.5、T4.2、T5.1/T5.4、T5.5/T6 已覆盖后续 student、richer input、OOD 与硬件 gate。

## 对任务板同步说明

T4.1.4 Done；T4.1.5 In Progress；当前推荐任务更新为 T4.1.5。
