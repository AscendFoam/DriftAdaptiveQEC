# T9.2.4 双后端资格对拍

- Task ID：T9.2.4
- 标题：六态、动作干预、故障与截断收敛的双后端资格对拍
- 日期：2026-07-26
- 状态：In Progress（预正式 child seal 已冻结，formal outcome 尚未访问）

## 输入材料

- `docs/t9_2_4_twin_qualification_preregistration.json`
- `configs/phase9/t9_2_4_twin_qualification.json`
- T9.2.1 causal contract 与 T9.2.2/T9.2.3 两个 backend 的 canonical evidence
- 原始 prereg analysis：`98e72c457dab941daab270b3bd63eec939564a6f1fedaad061eab59280988695`
- 原始 prereg commit：`4d9d92bbcec211f3c150b70a98f340d67fb975e3`

## 本阶段实际完成内容

1. 审查 formal runner 可执行性、统计 estimand、无 postselection 定义、cutoff 资格、异常分类、证据不可变性及下游 fail-closed。
2. 发现 backend B 原 squeezed-comb projector 依赖有限维 `expm`，在 cutoff 12 出现约 `0.417838` 的 projector Frobenius 偏差；该问题会让同一截断上的 A/B 接近掩盖共同的截断误差。
3. 新增独立 Hermite generating-function/Mehler 解析 Fock 投影。其逻辑映射不导入 backend A、既有 GKP projector 或截断矩阵指数；由 tail tolerance 自动生成对称 parity support。
4. 新增冻结 formal runner 与不导入 physics module 的独立 verifier。runner 保存完整逐行账本和 density/isometry/projector 原始 NPZ；verifier从原始状态重算 IQ、物理态、logical block/PTM、leakage/reset、fault terminal 与 cutoff gate。
5. 原始14,976 backend-round与18项 tolerance保持不变；只增量加入1,824个 cutoff-12 共同确认 round，总计16,800。统计冻结为2,000次 cluster max-T bootstrap，A/B seed独立，within-backend cutoff配对。
6. 签发 additive child seal，analysis=`f2a2ab287c59e0947768cd08824856d7b9046b79c3d713b18043efe09de22944`；`formal_result_accessed=false`，30/30 gates与30/30 targeted mutations通过。

## 当前产物

- `physics/phase9_backend_b_logical_bridge.py`
- `configs/phase9/t9_2_4_formal_runner_amendment.json`
- `cnn_fpga/benchmark/phase9_twin_formal_runner_amendment.py`
- `cnn_fpga/benchmark/phase9_dual_backend_qualification.py`
- `cnn_fpga/benchmark/phase9_dual_backend_verifier.py`
- `docs/t9_2_4_formal_runner_amendment_seal.json`
- `tests/test_phase9_backend_b_logical_bridge.py`
- `tests/test_phase9_twin_formal_runner_amendment.py`
- `tests/test_phase9_dual_backend_qualification.py`
- `tests/test_phase9_dual_backend_verifier.py`

## 验证方式和结果

- 解析 Hermite 系数与 SciPy quadrature 在多组阶数、中心和方差上对拍。
- cutoff 8/12、多个 projector delta、tail-support 收敛与旧实现 negative witness 均有可杀测试。
- runner development matrix 检查所有预期行、exception row、trace/conservation与 seed receipt；未使用正式 seed。
- verifier development bundle重算1,042个唯一 gate metric，并由 postselection mutation证明不是只读报告字段。
- focused+adjacent：`240 passed`。
- child seal：30/30 gates、30/30 mutations；正式结果文件不存在，任何性能与外部 claim 仍为 typed null。

## 反简化检查

- 不是把 backend B 指向 backend A：解析映射、transition solver、IQ likelihood与BLAKE2b RNG继续独立。
- 不是只比较均值：冻结逐 cell estimand、simultaneous max-T CI、worst metric与 familywise verdict。
- 不是用相同 cutoff 的 agreement 代替收敛：新增 within-backend cutoff 8→12 gates并保存 captured probability。
- 不是删除失败样本：异常仍生成有 identity/seed/terminal 信息的 ledger row；scientific failure与 infrastructure incomplete分离。
- 不是事后调阈值：parent tolerance字节不变，child只能增加执行/验证语义和确认矩阵。

## 风险复核

- R-N163保持 Open：只有 frozen formal A/B distributional qualification 得出 PASS 后才能关闭。
- R-N166保持 Open：当前只验证短程 exact anchors，不能外推长时 lifetime。
- 新增 R-N181：同截断一致性可能掩盖 finite-cutoff 非收敛；已由解析映射和共同 cutoff 子集承接。

## 是否需要插入新 task

不插入。问题发生在 T9.2.4 正式结果访问前，能够通过 additive child seal 在本 task 内修复；另开旁路会弱化既有 prereg lineage。

## 任务板同步

`docs/new_task_board.md` 保持 T9.2.4 为 `In Progress`。只有预正式提交推送并执行 frozen formal runner/verifier后，才根据 `PASS`、科学 `NO_GO` 或基础设施 `INCOMPLETE` 更新终态和下一任务。
