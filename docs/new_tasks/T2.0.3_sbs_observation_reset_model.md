# T2.0.3 完成记录：sBs g/e/leakage observation 与 reset model

**Task ID：** T2.0.3  
**标题：** 分离 paired ideal syndrome、hidden ancilla、observed class 与 conditional reset  
**日期：** 2026-07-14  
**状态：** Done

## 输入材料

- `docs/new_task_board.md` T2.0.3；
- `docs/protocol_hierarchy.md/.json`；
- `physics/sbs_error_space.py`；
- Sivak readout/reset/leakage 一手段落和 paper-parameter registry。

## 实际完成内容

- 实现 `g/e/f/higher` hidden state 与 `g/e/leakage` observed class；
- 实现 4×3 readout confusion、3×4×4 observation-conditioned reset 和 carry/preparation kernel；
- 冻结 `K_ge` 字符 `(Z,X)`、执行 `(X,Z)` 的解析；
- 支持 f reset、未覆盖 higher persistence、X/Z e-runs、leakage constituent/cycle runs；
- deployable records 与 truth records 结构分离；
- 实现单步/trajectory API 和 T2.0.2 集成；
- 20 项 tests 覆盖语义、统计、集成与失败分支。

## 产物路径

- `physics/sbs_observation_reset.py`
- `physics/__init__.py`
- `tests/test_sbs_observation_reset.py`
- `docs/sbs_observation_reset_model.md`
- `docs/tasks/T2.0.3_sbs_observation_reset_model.md`
- `docs/new_tasks/T2.0.3_sbs_observation_reset_model.md`
- 更新 `docs/protocol_hierarchy.md/.json`

## 验证方式和结果

- focused：`20 passed`；
- 相邻：`81 passed`；
- 显式 `tests/`：`299 passed, 4 failed`；仅 R-N012；
- `py_compile` 通过。

## 风险复核

新增 R-N027：当前 confusion/injection/reset 数值必须由调用者显式提供，但仍是 modeling assumptions；`higher` 是聚合 state，不能推断逐能级动力学。关闭需 full calibrated matrix/reset provenance、Fock cross-check 和真实 readout/HIL gate。

## 是否需要插入新 task

不需要。T2.2.2、T2.3.2/T2.3.3、T6 已覆盖 ancilla/readout/reset 高保真与硬件证据；当前应按顺序进入 cycle state machine。

## 对论文 claim 的影响

允许写“实现 paired syndrome、hidden/observed separation、leakage/reset effective state model”。禁止写装置实测 confusion/reset、逐能级 leakage、真实 FPGA 分类或真实量子控制。

## 对 `docs/new_task_board.md` 的同步说明

- T2.0.3：`In Progress -> Done`；
- T2.0.4：`Todo -> In Progress`；
- 当前推荐任务：`T2.0.4`。
