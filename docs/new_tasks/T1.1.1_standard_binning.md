# T1.1.1 完成记录：单轴 standard binning

**Task ID：** T1.1.1  
**标题：** 单轴 standard binning 与 Gaussian logical-flip 概率  
**日期：** 2026-07-14  
**状态：** Done

## 输入材料

- `docs/new_task_board.md` T1.1.1；
- `physics/constants.py`、`syndrome_measurement.py`、`logical_tracking.py`、`gkp_state.py`；
- GKP 2001、Glancy–Knill、finite-energy/logical-channel 文献边界；
- T0.2.1/T0.2.2 的 baseline 与 claim contract。

## 实际完成内容

- 新增 vectorized standard-binning reference，返回 syndrome/correction/index/parity/flip；
- 推导并实现 Gaussian odd-cell interval sum 与独立 Fourier series；
- 冻结半开边界和 `lambda=sqrt(2pi)` 仓库 operational normalization；
- 将 `gkp_state.py` 的重复常数改为共享 import；
- 增加公共惰性导出和 12 项单测；
- 修正首次代码复核发现的标量 `int/bool` 被错误转成 float 的 API 类型问题。

## 产物路径

- `physics/ideal_gkp_decoder.py`
- `tests/test_ideal_gkp_decoder.py`
- `docs/tasks/T1.1.1_standard_binning.md`
- `docs/new_tasks/T1.1.1_standard_binning.md`
- `physics/constants.py`
- `physics/gkp_state.py`
- `physics/__init__.py`

## 验证方式和结果

- focused：`12 passed`；
- interval/Fourier：4 个噪声比值差异小于 `2e-13`；
- Monte Carlo：40 万样本与解析值差 `1.175e-4`，仅 `0.23` standard error；
- adjacent statcalib smoke：合计 `16 passed`；
- analytical Wigner smoke：finite、shape 正确、归一化；
- tests/ 全量：`78 passed, 4 failed`，失败均为既有缺失 FR8/P4 文档，登记 R-N012。

## 风险复核

- R-N003 降为 `Mitigated`：新模块、shared constant、measurement syndrome 已对齐；跨论文 normalization 仍需 T1.4 registry 监控；
- R-N004 部分缓解：新 ideal decoder 有直接测试，旧 full/legacy physics 仍待后续任务；
- 新增 R-N012：旧治理文档缺失导致 4 个全量测试失败。

## 是否需要插入新 task

当前不插入。R-N012 不影响本任务 focused 数值结论；在依赖 FR8/P4 legacy evidence 前必须修复或明确退役。R-N003/R-N004 由后续正常理论/物理任务继续覆盖。

## 对论文 claim 的影响

允许把 ideal 1D standard binning 写成已实现且解析/Monte-Carlo 验证的 reference baseline；不允许写成 finite-energy、2D correlated、hardware 或实验验证。

## 对任务板的同步

- T1.1.1：`In Progress -> Done`；
- 当前推荐任务：`T1.1.2`。

