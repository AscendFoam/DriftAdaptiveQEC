# T1.4.2 完成记录：约 300 元 FPGA 的工程与测量边界

**Task ID：** T1.4.2  
**标题：** 冻结低成本 FPGA 的型号、I/O、时钟、存储、资源和可测量范围  
**日期：** 2026-07-14  
**状态：** Done

## 输入材料

- `docs/new_task_board.md` T1.4.2；
- `docs/claim_ladder.md/.json`；
- `docs/tasks/T0.1.3_dual_loop_interface.md` 的 `DLIF-v1`；
- `cnn_fpga/config/hardware_hil.yaml`、`cnn_fpga/hwio/board_backend.py`；
- T72 current-host real-board gate；
- Sipeed Tang Nano 20K 官方板卡页、v1.3 datasheet 和上手页。

## 实际完成内容

- 冻结 Sipeed Tang Nano 20K / `GW2AR-LV18QN88C8/I7` 为低成本 reference target；
- 区分 reference selection、厂商规格、实物/采购、综合、板测和 HIL 六种身份；
- 固定 LUT/FF/SRAM/multiplier/PLL、27 MHz/MS5351、SDRAM/Flash、USB-C/BL616 和 free I/O；
- 固定 JTAG/UART/USB-SPI/GPIO 的允许角色与禁止推断；
- 证明 115200 UART 无法承载 reference 20 ms 全 histogram 数据面；
- 审计当前 ZCU111 UIO/AXI/DMA 后端与 Tang Nano 不兼容，冻结后续 serial adapter 要求；
- 固定数字控制平面可测项和量子/模拟前端禁止项；
- 新增机器契约和 9 项 direct tests。

## 产物路径

- `docs/low_cost_fpga_boundary.md`
- `docs/low_cost_fpga_boundary.json`
- `tests/test_low_cost_fpga_boundary.py`
- `docs/tasks/T1.4.2_low_cost_fpga_boundary.md`
- `docs/new_tasks/T1.4.2_low_cost_fpga_boundary.md`
- `README.md`
- `docs/new_risks.md`
- `docs/new_task_board.md`

## 验证方式和结果

- JSON 解析；
- vendor resource exact-field tests；
- capacity/UART arithmetic 独立重算；
- HIL YAML 和 T72 JSON 直接证据回归；
- Markdown/JSON ID 同步；
- focused `9 passed`，与 claim-ladder 相邻 contract `17 passed`；
- 显式 `tests/` 为 `207 passed, 4 failed`；4 项仍仅因 R-N012 的
  `docs/fr8_statcalib_extension_lane_benchmark.md` 与 `docs/P4_benchmark_formal_protocol.md` 缺失。

## 风险复核

新增 R-N022：参考板未采购、BL616 sustained transport 未测，且当前 ZCU111 mmap 后端不兼容。
该风险不阻塞当前边界冻结，但会阻塞任何 CL3/CL4 claim；由 T5.5/T6 正常任务关闭。

## 是否需要插入新 task

不插入。T5.5 已覆盖目标器件综合与 Pareto，T6.1 已覆盖实物、replay protocol 和计时方法，
T6.2/T6.4 已覆盖 RTL、板级 correctness、latency/resource 与 negative path。提前实现硬件会打乱顺序。

## 对论文 claim 的影响

当前只允许把 Tang Nano 20K 写成 selected reference target，并引用厂商规格。不得把选择、容量
算术、USB link 标称值、ZCU111 placeholder 或 T72 gate 改写为板级实测、real-board HIL 或
quantum control。

## 对任务板的同步

- T1.4.2：`In Progress -> Done`；
- T1.4.3：`Todo -> In Progress`；
- 当前推荐任务：`T1.4.3`。
