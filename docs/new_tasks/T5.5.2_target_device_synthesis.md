# T5.5.2 目标器件综合与时序估计完成记录

- Task ID：T5.5.2
- 标题：运行目标器件 synthesis / timing estimate
- 日期：2026-07-16
- 状态：Done

## 输入材料

- T5.5.1 packed-word Python RTL golden 与冻结参数 bank；
- T-RISK-20260716-01 可综合 RTL、CXXRTL 全字逐周期等价报告；
- `GW2AR-LV18QN88C8/I7` / Tang Nano 20K 目标合同；
- 27 MHz SDC、QN88 small-pin activity harness CST；
- YoWASP Yosys/nextpnr Himbaechel Gowin/Apycula 工具链。

## 实际完成内容

1. 用真实 `synth_gowin` 对 core+harness 综合，保留 8 BSRAM、两个 multiplier、865 registers，
   Yosys structural check 为 0 problems；
2. 复核发现 raw 9-bit LFSR 可能越过 257-entry 配置 ROM；改为 modulo-257 地址并做 512-code
   穷举后，重跑 seed 1/7/19 P&R，Fmax `40.4318/39.8661/39.7456 MHz`，全部通过 27 MHz；
3. 报告三 seed 最大资源：LUT4-equivalent 3,362、DFF 865、BSRAM 8、
   MULT18X18 1、MULT9X9 1、ALU 340、IOB 18；
4. 保存每个 seed 的 critical period、logic/routing 分解、起止 cell 和完整 raw report；
5. 按 6-cycle/II=1 合同计算 27 MHz core latency `222.222 ns`，且明确排除 transport/ADC/CDC；
6. 新增可重跑 runner、12 项 machine gates、9 类 shortcut mutations 和 20 项 direct tests；并修复
   tool-version query 的 workspace-local YoWASP cache 与并行 seed 失败日志/串行 retry。

执行期间修复了真实 2R1W BSRAM 映射失败、同步读寄存器未合并、单体 signature 关键路径过深，
以及 PowerShell UTF-16 工具日志解析问题；没有用空 top、单 seed 最优值或 Python proxy 冒充报告。

## 产物路径

- `cnn_fpga/benchmark/target_device_synthesis.py`
- `tests/test_target_device_synthesis.py`
- `docs/target_device_synthesis.md`
- `docs/t5_5_2_target_device_synthesis.json`
- `docs/t5_5_2_target_device_synthesis_source_data.csv`
- `docs/t5_5_2_yosys_synthesis.log`
- `docs/t5_5_2_nextpnr_seed{01,07,19}_report.json`
- `docs/t5_5_2_nextpnr_seed{01,07,19}_place_route.log`

## 验证方式和结果

- parent RTL equivalence：4,316 valid MAP rows，0 mismatch；
- formal runner：`PASS`，12/12 gates，9/9 mutations rejected；
- focused tests：`20 passed`；
- 三 seed post-route：最差 Fmax `39.7456 MHz > 27 MHz`；
- 所有正式源码、约束、父证据、raw report/log 均有实时 SHA256/bytes 核验。

## 风险复核

- R-N103/R-N104 降为 `Mitigated / Medium / Monitor`：RTL、target synthesis/P&R estimate 已建立；
- 新增 R-N105：这是开源工具 estimate，critical path 含 activity harness 观测端点；vendor signoff、
  bitstream、transport、真实板卡、板测 latency/power/throughput 均未建立。

## 是否需要插入新 task

否。T5.5.3/T5.5.4 正常承接 precision Pareto 与 GRU/student hardware feasibility；T6 承接 bitstream、
transport 和真板实测。R-N105 不阻塞 T5.5.2 的窄结论。

## 对任务板的同步

T-RISK-20260716-01 `In Progress -> Done`；T5.5.2 `Blocked -> In Progress -> Done`；
当前推荐任务更新为 T5.5.3。

## 对论文 claim 的影响

允许写“在固定开源工具链、目标器件和约束下，三 seed post-route estimate 均通过 27 MHz，并报告
资源/Fmax/关键路径”。禁止写 vendor timing closure、bitstream 已生成、真实 FPGA/板卡已运行、
实测 latency/throughput/power，或量子硬件实验结果。
