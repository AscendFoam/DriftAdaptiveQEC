# T2.3.3 执行跨保真度交叉验证

## 基本信息

- **Task ID：** T2.3.3
- **日期：** 2026-07-14
- **状态：** Done

## 输入材料

T2.2.1、T2.3.1、T2.3.2、T2.3.8 的实现与机器证据，normalized damped-projector
state、logical-channel syndrome response，以及任务板的四 lane/high-low/error-attribution
通过标准。

## 实际完成内容

完成共同 dB/noise contract、finite-Fock folded response、effective 200k/点、exact
noise-transfer、direct syndrome MAP、Fock SBS 六态 native metrics、共同 Pauli
LER/occupancy/`F_avg`、原生 occupancy 分离、五点 cutoff sweep和四项误差归因。

## 产物路径

- `physics/cross_fidelity_validation.py`
- `tests/test_cross_fidelity_validation.py`
- `docs/cross_fidelity_validation.md`
- `docs/t2_3_3_cross_fidelity_validation.json`
- `docs/tasks/T2.3.3_cross_fidelity_validation.md`
- `docs/new_tasks/T2.3.3_cross_fidelity_validation.md`

## 验证方式和结果

- direct `42 passed`；
- adjacent Fock/effective/protocol slice `278 passed`；
- full `tests/`：`718 passed, 4 failed`，失败仍仅 R-N012；
- production 14 gates PASS；
- 四条共同 lane 的 LER 严格下降、occupancy/`F_avg` 严格上升；
- high-dB noise↔syndrome q-LER gap `3.93e-6`，effective MC max z `0.8904`；
- Fock-q high-dB absolute gap `3.54e-4`，由 12 dB cutoff sweep解释；
- 3 dB clipping gap `0.016766`；p-axis coordinate audit 显式失败。

## 风险复核

新增 R-N041：operational period、canonical Fock commutator 和 q dilation 尚未构成可验证
的 two-axis symplectic map，可能导致错误 coherent `F_avg` claim。依规则插入
`T-RISK-20260714-01`。R-N037/R-N038/R-N040 继续开放。

## 是否需要插入新 task

需要。插入 `T-RISK-20260714-01`，位于 T2.3.3 后、T2.3.4 前；目标是用一手 convention、
analytic Fourier/commutator identities 和 numerical q/p moments/alias 三向冻结坐标合同。

## 对论文 claim 的影响

允许：q-axis common trend、高/低 squeezing 有效区、cutoff-aware absolute agreement。

禁止：当前 Fock two-axis coherent fidelity、模型绝对等价、不同 occupancy 数值排名、
device result。

## 对 `docs/new_task_board.md` 的同步说明

- T2.3.3：`In Progress -> Done`；
- 插入 `T-RISK-20260714-01`：`Todo`；
- 当前推荐任务更新为 `T-RISK-20260714-01`。
