# T3.2.5 完成记录：run-length FSM / parameter-bank baseline

- **Task ID：** T3.2.5
- **标题：** observed-only run-length event FSM 与原子参数银行 baseline
- **日期：** 2026-07-15
- **状态：** Done

## 输入材料

新任务板 T3.2.5、observed syndrome/reset、mixed-state syndrome stream、真实 `ParamBank` 和
decoder/controller 术语合同。

## 实际完成内容

实现 3-bit 饱和 X/Z e-run、leakage/clean/health counters、phase tie-break、五态优先级和真实双 bank
原子 commit。补强 conflict 后持续 local-safe 直到 bank 可重新同步，修复只安全一拍的状态/bank 分裂。
通过 training-only 24-grid 冻结非退化 run-length 参数，并在相同 trace 上与 static、memoryless、truth
oracle 比较。

## 产物路径

- 实现：`cnn_fpga/runtime/run_length_fsm.py`；
- benchmark：`cnn_fpga/benchmark/run_length_fsm_baseline.py`；
- 测试：`tests/test_run_length_fsm.py`、`tests/test_run_length_fsm_baseline.py`；
- 报告：`docs/run_length_fsm_baseline.md`；
- 数据：`docs/t3_2_5_run_length_fsm_validation.json`、`.csv`；
- 详细记录：`docs/tasks/T3.2.5_run_length_fsm_baseline.md`。

## 验证方式和结果

- 24-grid ×49,152 training cycles = 1,179,648 真实 FSM replay；
- 4 场景 ×8 evaluation seeds ×12,000 = 384,000 cycles，32 unique traces/rows；
- 生产选择 `e/leak-enter/leak-clear/fallback-clear = 2/1/2/1`；
- static-minus-FSM `0.401710 [0.399014,0.404407]`；memoryless-minus-FSM
  `-0.179911 [-0.180782,-0.179041]`；
- 15/15 gates、40 focused、54 registry+focused、131 adjacent；版本/写入、truth isolation、冲突持续
  安全和 source hash 均通过；
- 全量 `1046 passed, 4 skipped, 4 failed`；4 个失败仅为已登记 R-N012，未出现本任务回归。

## 风险复核

新增 R-N056。synthetic event cost 不等于 LER/physical lifetime；当前 run-length 的一拍 detection lag
使其显著弱于 memoryless，这一负结果不能隐藏或调成 `run=1`。

## 是否需要插入新 task

不插入。T3.2.10、T4.2、T5.1/T5.2、T5.5/T6 已分别承接更强递推、完整 FSM、物理 ranking 和
RTL/板测证据。

## 对任务板和论文 claim 的同步

T3.2.5 标为 Done，T3.2.6 标为 In Progress，当前推荐改为 T3.2.6。只允许
software event-controller/atomic-bank claim，不允许 LER、RTL、综合或硬件实测 claim。
