# T4.4.1 完成记录：fresh bounded-residual GRU teacher

- Task ID：T4.4.1
- 标题：训练 bounded residual RNN/GRU teacher
- 日期：2026-07-15
- 状态：Done

## 输入材料

T2.3.7 strict-split NMF、T3.2.9 bounded 15-action/control-oracle contract、T4.1.5 frozen teacher provenance、
Puviani nominal-residual 初始化与当前 differentiable sBs simulator。

## 实际完成内容

以三个全新 seeds 从随机网络权重训练 72,853 参数 GRU teacher；物理动作从 nominal sBs 邻域出发并经
`tanh` hard bounds。全部 restart、训练曲线、失败/cap-hit、validation-only selection、held-out/cutoff-16
evaluation、checkpoint reload、causality、gradient 与 parent-hash non-reuse 均进入机器 gate。

## 产物路径

训练器、一份测试、中文报告、JSON/PT/1,074-row CSV，以及 protocol/README/risk/task-board 同步；详见
`docs/tasks/T4.4.1_bounded_residual_rnn_teacher.md`。

## 验证方式和结果

21/21 gates；三 restart validation score 从约 `0.303` 提升到 `0.588/0.585/0.587`；selected teacher
相对 nominal 的 cutoff12/cutoff16 score gain 为 `0.253603/0.141557`，primary logical-Z lifetime paired
gain 为 `4.125518 [3.899115,4.339331]` cycles。focused 14、邻接 102 项通过。两 restart 在 epoch 320
达峰，已保留且不声明全局收敛。显式全量 `tests/` 为 `1472 passed, 17 skipped, 4 failed`，4 项仍仅为
R-N012 两份旧 FR8/P4 文档缺失。

## 风险复核与插入任务

新增 R-N075，不插入任务。T4.4.2--T4.4.5 和 T5/T6 已覆盖剩余解释、蒸馏、同轨 gain、OOD/长时与硬件门。

## 对任务板同步说明

T4.4.1 更新为 Done；T4.4.2 更新为 In Progress；当前推荐任务更新为 T4.4.2。
