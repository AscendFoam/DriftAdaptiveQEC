# T5.4.2 uncertainty-gated fallback 完成记录

- Task ID：T5.4.2
- 标题：验证 uncertainty-gated fallback
- 日期：2026-07-16
- 状态：Done

## 输入材料

T5.1.2/T5.1.4、T4.1.4/T4.2.3、T5.4.1 正式 artifacts 与 live decoder implementation；任务板要求的
matched gated/no-fallback catastrophic-failure 对照及不必要 fallback/性能代价报告。

## 实际完成内容

冻结 EWMA primary、static last-known-good fallback、same-sample hidden evaluator、observed-only uncertainty
score 和 development-only threshold selection。在 12 个 parent-disjoint confirmation seed clusters 上实际
运行 OOD 与 nominal matched decisions，同时逐 window 记录 avoided、induced、unnecessary 和 selected-without-
benefit counts；新增逐场景 bootstrap，机器保留 compound 负结果。

## 产物路径

- `cnn_fpga/benchmark/uncertainty_gated_fallback.py`
- `tests/test_uncertainty_gated_fallback.py`
- `docs/uncertainty_gated_fallback.md`
- `docs/t5_4_2_uncertainty_gated_fallback.json`
- `docs/t5_4_2_uncertainty_gated_fallback_source_data.csv`
- `docs/tasks/T5.4.2_uncertainty_gated_fallback.md`

## 验证方式和结果

- 21/21 machine gates；517 Source Data rows；
- 1,179,648 OOD + 393,216 nominal matched decisions；
- 17 direct/artifact/mutation tests；
- 180 direct + parent/protocol adjacent closeout tests；
- aggregate OOD reduction `0.00107490 [0.00001950,0.00227615]`；
- telegraph 显著正向、compound 显著负向、sinusoidal CI 跨 0；
- nominal 点估计轻微负向；
- 每个 threshold/window/cell 的 count/rate identity 均重算；
- provenance、split、threshold、truth isolation、scenario heterogeneity、Source Data canonical/byte hashes
  均 fail-closed。

## 风险复核

新增 R-N098：总体获益很小且非场景普适；fallback 可能诱发错误，在 nominal 也不是免费；证据只在
synthetic syndrome-decision 层成立。

## 是否需要插入新 task

否。T5.4.3--T5.4.6 已覆盖因果消融、selection bias、长时与 randomized mismatch，T5.5/T6 覆盖硬件。

## 对 `docs/new_task_board.md` 的同步说明

T5.4.2 `In Progress -> Done`；T5.4.3 `Todo -> In Progress`；当前推荐任务改为 T5.4.3。
