# T0.1.2 完成记录：平台抽象冻结

- **Task ID：** `T0.1.2`
- **标题：** 冻结 superconducting cavity + transmon-like primary mapping 与平台无关数学核心
- **日期：** 2026-07-14
- **状态：** `Done`

## 输入材料

- `docs/new_task_board.md` 的 T0.1.2 目标；
- `docs/tasks/T0.1.1_scope_freeze.md`；
- 三份本地核心论文 Markdown：2020 grid-state QEC、real-time QEC beyond break-even、Non-Markovian feedback；
- `physics/gkp_state.py`、`noise_channels.py`、`syndrome_measurement.py`、`error_correction.py`、`logical_tracking.py`；
- `cnn_fpga/` 既有主机/快路径工程基础；
- `docs/paper_materials/paper_claim_evidence_ledger.md` 等硬件证据边界材料。

## 实际完成内容

1. 冻结 cavity storage mode + transmon-like ancilla + readout/reset + classical feedback 的 primary mapping；
2. 建立平台无关 `state-observation-controller-action` 数学 contract；
3. 给出覆盖 idle noise、syndrome extraction、ADC/measurement、decoder、frame/correction、reset/next cycle 的 Mermaid cycle diagram；
4. 为每阶段定义 I/O、主平台映射、当前代码实现和缺失项；
5. 区分 quantum/analog domain、FPGA fast path 与 host slow path；
6. 冻结 readout invalid、leakage、reset failure、stale bank、deadline miss、host timeout、actuator unavailable 的失败分支；
7. 把文献时序与本项目尚未测量的 latency 明确分层。

## 产物路径

- `docs/tasks/T0.1.2_platform_abstraction.md`；
- `docs/new_tasks/T0.1.2_platform_abstraction.md`；
- `docs/new_risks.md`；
- `docs/new_task_board.md`。

## 验证方式和结果

1. 本地论文锚点检查：确认 2020 实验的 single oscillator/transmon/measurement-conditioned feedback，以及 beyond-break-even 的 cavity/transmon/FPGA/GPU/sBs/readout/reset 分层；通过。
2. 现有代码映射：逐阶段对照 `physics/` 和 `cnn_fpga/`，没有把缺失的 ECD/sBs、IQ/ADC、leakage/reset 状态机写成已实现；通过。
3. 本地 conda smoke：`GKPErrorCorrector.run_qec_round([0.2,-0.1])` 返回完整 `syndrome/correction/residual/success/error`，并得到有界残差；证明软件代理阶段可执行；通过。
4. cycle contract 机械检查：文档包含六个必需阶段、next-cycle commit、有效/无效双分支、阶段 I/O 和失败表；通过。
5. 时序边界检查：`4.924 us` 只标为文献 constituent timing，未写成本项目板级 deadline；通过。

## 非简化实现复核

- 不是单纯架构示意图：每个节点都有输入、输出、物理映射、代码状态和失败行为。
- 没有用 `SyndromeMeasurement` 的直接取模代理冒充 ancilla interaction/ADC/readout chain。
- 没有提前实现或虚构 T2.0 的 sBs state machine，也没有提前冻结 T0.1.3 的位宽/频率。
- 软件 smoke 只证明接口执行，不升级为实验或硬件证据。

## 风险复核

- 新增 R-N005：当前有效 syndrome model 绕过 transmon interaction、IQ/ADC 和 reset/leakage 状态；`High / Soon`，由 T2.0/T2.1 关闭。
- 新增 R-N006：文献 microsecond cycle 与本项目廉价板卡/core/transport deadline 混用风险；`High / Monitor`，由 T1.4/T2.4/T6 分层关闭。
- R-N002 根级 pytest 收集风险保持 Open；T0.1.2 为文档 task，尚未阻塞本次验收。
- R-N003/R-N004 保持 Open，未被平台抽象虚假关闭。

## 是否需要插入新 task

不插入。物理协议层缺口由既有 T2.0/T2.1 覆盖，latency 分层由 T1.4/T2.4/T6 覆盖；当前没有必须抢占 T0.1.3 的阻塞项。

## 现有代码冲突、行为变化与论文 claim 影响

- 未修改任何 runtime/physics 行为；只建立平台 contract 和证据边界。
- 论文允许把系统写为 cavity–transmon-inspired / protocol-aligned abstraction，但不能写成当前仓库已运行真实 cavity/transmon QEC。
- 文献时序、软件 runtime、synthesis estimate、board measurement 和真实量子反馈必须分别报告。

## 任务板同步

- T0.1.2：`Todo -> In Progress -> Done`；
- 当前推荐任务：`T0.1.3`；
- 2026-07-14 进度日志已记录产物、smoke 和证据边界。

