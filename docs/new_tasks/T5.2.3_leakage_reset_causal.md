# T5.2.3 完成记录：leakage 与 reset failure 独立因果注入

- Task ID：T5.2.3
- 标题：注入 leakage 与 reset failure
- 日期：2026-07-16
- 状态：Done

## 输入材料

T2.0.3 full hidden/observed/reset kernel、T2.0.6 occupancy/correlation、T5.1.2 component lane、T5.1.6
feasibility contract、T5.2.2 独立通道 contract 和本地 Sivak 2023 leakage 行锚。

## 实际完成内容

新增 higher-leakage-injection-only 与 higher-reset-failure-only 两条 formal lanes，2×6 rates×8 seeds×256
trajectories×512 evaluation cycles，带 128-cycle burn-in、CRN pairing、whole-seed bootstrap。报告 detection/false
alarm/false negative、observed correlation/covariance tail、declared/safe availability、reset requests/attempts/successes/
failures和显式 leakage-free control；无 postselection 或 combined score。

## 产物路径

- `cnn_fpga/benchmark/leakage_reset_causal.py`
- `tests/test_leakage_reset_causal.py`
- `docs/leakage_reset_causal.md`
- `docs/t5_2_3_leakage_reset_causal.json`
- `docs/t5_2_3_leakage_reset_source_data.csv`
- `docs/protocol_hierarchy.json` / `docs/protocol_hierarchy.md`

## 验证方式和结果

96 seed rows、12 summaries、2,508-row ledger、23/23 gates；direct/mutation `27 passed`，adjacent `145 passed`。
leakage-free hidden occupancy 为 0、false alarm 非零、delay 为 null。0.004 injection 的 occupancy/run/long-correlation
为 `0.042331/10.948/0.224123`；0.95 reset failure 的对应值为 `0.041299/20.471/0.404017`。

## 非 demo 检查

没有复用 T2.0.6 离线 post-selection PASS。formal config 拒绝缩小 family/grid/seed/trajectory/horizon/bootstrap，
也拒绝把 classifier 设为完美。semantic validator 重算 summary、null、方向、intervention calibration和全部 bindings；
mutations 覆盖双通道、truth、fake-zero、coherent direction rewrite、global score 和 postselected primary field。
noise-floor correlation 没有被错误包装成逐点严格单调。

## 风险复核

新增 R-N091。所有 rate/classifier/reset 参数仍为 project assumptions；finite sample detection fraction 1 不能写成
device 零漏检，safe availability 不是板卡 uptime，correlation 不是 physical-memory LER。T5.3/T5.4/T6 正常承接，
不插入风险 task。

## 任务板同步

T5.2.3 Done；T5.3.1 In Progress；当前推荐任务更新为 T5.3.1。
