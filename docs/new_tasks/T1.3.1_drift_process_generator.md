# T1.3.1 完成记录：非平稳 drift process generator

**Task ID：** T1.3.1  
**标题：** mean/variance/loss/outlier/step/telegraph/burst 漂移生成器  
**日期：** 2026-07-14  
**状态：** Done

## 输入材料

- `docs/new_task_board.md` T1.3.1；
- `physics/noise_channels.py` 的 gamma/effective-sigma 口径；
- `physics/error_correction.py::QECSimulator.run_with_drift` 的三元回调契约；
- T1.1.3 correlated covariance 与 T1.2.2 mixture/logical-channel 接口。

## 实际完成内容

- 新增不可变 `DriftState`，统一 mean、2D covariance、loss、outlier mixture、burst 与
  provenance；
- 实现 mean、variance、loss、outlier-rate、step、telegraph、burst 七类 drift process
  和 constant reference；
- stochastic process 固定 seed、prefix reproducible，telegraph 使用 Markov chain，
  burst 使用 onset/duration/cooldown 状态机；
- 新增二分量 Gaussian mixture sampler；
- 新增 lossy 但可审计的现有 `run_with_drift` callback adapter；
- 新增 17 项 direct tests、公共惰性导出和 README 文档地图入口。

## 产物路径

- `physics/drift_processes.py`
- `tests/test_drift_processes.py`
- `physics/__init__.py`
- `docs/tasks/T1.3.1_drift_process_generator.md`
- `docs/new_tasks/T1.3.1_drift_process_generator.md`
- `docs/new_risks.md`
- `docs/new_task_board.md`
- `README.md`

## 验证方式和结果

- focused：`17 passed`；
- M1.1/M1.2 邻近回归：`87 passed`；
- 不对称 telegraph 12 万步经验 transition `0.07900/0.23567`，目标
  `0.08/0.24`；burn-in 后 B occupancy `0.25048`，理论 `0.25`；
- burst 2 万步得到 1384 个完整 episode，长度严格在 2--6，最小 gap 等于 cooldown 3；
- 30 万 mixture samples 回收 mean/covariance/outlier rate；
- adapter 已接入真实旧 `QECSimulator.run_with_drift` 契约；
- 显式 tests/：`153 passed, 4 failed`，4 项仍仅为 R-N012。

## 风险复核

- 新增 R-N016：legacy adapter 的 scalar RMS 压缩不能用于 oracle/主结果；
- 新增 R-N017：七类过程是 synthetic effective laws，未做设备校准或完整协议动力学；
- R-N004 进一步缓解：新 drift 核心 API 有字段、解析、统计、状态机和失败分支直测；
- R-N012 不影响本任务，但继续阻止全 tests/ 绿灯。

## 是否需要插入新 task

不插入。R-N016 由紧接的 T1.3.2 full-state oracle contract 处理；R-N017 由 T1.3.4
baseline 对齐、T2 protocol/multi-fidelity 和后续真实数据/硬件 gate 正常任务覆盖。当前
claim 已明确降级，不阻塞 T1.3.2。

## 对论文 claim 的影响

允许写“已实现并统计验证七类 synthetic nonstationary hidden-state generators”；不能写
成真实设备 drift calibration、完整 loss dynamics 或 hardware evidence。旧 callback adapter
只属于 compatibility smoke。

## 对任务板的同步

- T1.3.1：`In Progress -> Done`；
- T1.3.2：`Todo -> In Progress`；
- 当前推荐任务：`T1.3.2`。
