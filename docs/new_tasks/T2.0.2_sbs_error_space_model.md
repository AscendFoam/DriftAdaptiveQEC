# T2.0.2 完成记录：sBs Kraus / error-space transition model

**Task ID：** T2.0.2  
**标题：** 实现 `K_gg/K_ge/K_eg/K_ee`、`C_i` trickle-down 与 Pauli-frame transition layer  
**日期：** 2026-07-14  
**状态：** Done

## 输入材料

- `docs/new_task_board.md` T2.0.2；
- `docs/protocol_hierarchy.md/.json`；
- Sivak 2023 本地 Supplement 的 sBs/error-space/Kraus/Pauli-frame 段落；
- 前序 paper-parameter 与 terminology contracts。

## 实际完成内容

- 新增 grouped CPTP `SBSErrorSpaceInstrument`；
- 支持 `K_gg/K_ge/K_eg/K_ee` branch 和每 branch 多 effective Kraus components；
- 支持多个 `C_i` 同一 recovery depth 的 general topology；
- 强制 0/1/1/2 层 branch semantics、C0 no-error invariant 和逐 source trace preservation；
- 实现 full-cycle `X_LZ_L` 与 `PauliFrame`；
- 实现 quantum/population/seeded trajectory 三条一致路径；
- 显式标记 inter-subspace dephasing、非 Fock、非 device-calibrated 范围；
- 21 项 direct tests 覆盖 completeness、Choi、no-error、gradual recovery、随机 density、Monte Carlo 与失败分支。

## 产物路径

- `physics/sbs_error_space.py`
- `physics/__init__.py`
- `tests/test_sbs_error_space.py`
- `docs/sbs_error_space_model.md`
- `docs/tasks/T2.0.2_sbs_error_space_model.md`
- `docs/new_tasks/T2.0.2_sbs_error_space_model.md`
- 更新 `docs/protocol_hierarchy.md/.json`

## 验证方式和结果

- focused：`21 passed`；
- 相邻 contracts/model：`61 passed`；
- 显式 `tests/`：`279 passed, 4 failed`；仅 R-N012 两份旧文档缺失；
- `py_compile` 通过。

## 风险复核

新增 R-N026：本模型为 block-dephased error-space instrument，transition probability 是显式 modeling assumptions 而非装置校准；若直接外推 coherent/Fock fidelity 或实验恢复率，会形成错误 claim。关闭需完成 T2.0.5 趋势门、T2.3.2 Fock 协议实现、T2.3.3 跨保真度归因，并为正式参数保存 provenance。

## 是否需要插入新 task

不需要。T2.0.3、T2.0.5、T2.3.2、T2.3.3 已覆盖 observation/reset、趋势、Fock 和跨保真度验证，不应提前打乱顺序。

## 对论文 claim 的影响

允许写“实现了 protocol-aligned coarse-grained CPTP sBs error-space instrument”。禁止写“精确复现实验 Kraus operators”“已校准 cavity/transmon 数字孪生”“保持 error-subspace coherence”或“真实 sBs 性能”。

## 对 `docs/new_task_board.md` 的同步说明

- T2.0.2：`In Progress -> Done`；
- T2.0.3：`Todo -> In Progress`；
- 当前推荐任务：`T2.0.3`。
