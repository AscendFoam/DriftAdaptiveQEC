# T4.3.2 完成记录：双参数库、原子切换与 hysteresis

- Task ID：T4.3.2
- 标题：实现双参数库、原子切换和 hysteresis
- 日期：2026-07-15
- 状态：Done

## 输入材料

T4.2 parametric MAP-LUT/trusted-image/bit-accurate fast path、T4.3.1 exact cadence、legacy ParamBank/scheduler 和任务板的 version/CRC/timestamp/safe-boundary/no-half-update 要求。

## 实际完成内容

实现 canonical full-image payload、CRC32/SHA256 双层 transfer/image/manifest integrity、version/CAS、cycle-domain timestamp、source-window provenance、anti-replay transaction、独立 transfer coverage、A/B atomic pointer switch、两窗同 key hysteresis、minimum residency、freshness、thread-safe single writer、structured ack 和 independent readback。修复 manifest header 未被完整保护及公共只读属性未显式加锁两项深审计问题。

## 产物路径

`cnn_fpga/runtime/atomic_parameter_bank.py`、`cnn_fpga/benchmark/atomic_parameter_bank_validation.py`、`tests/test_atomic_parameter_bank.py`、`tests/test_atomic_parameter_bank_validation.py`、`docs/atomic_parameter_bank.md`、`docs/t4_3_2_atomic_parameter_bank_validation.json` 和 `docs/t4_3_2_atomic_parameter_bank_source_data.csv`；同步 public API、协议层级与文档地图。

## 验证方式和结果

production CLI 17/17 gates PASS，7518 rows；3745 个所有 proper prefix 均 incomplete，3745 个 byte-position flip 均在 decode 前拒绝；10 个 chunk/order cases、15 个 reason-coded negatives、双 bank 两次 commit、pipeline in-flight latch、concurrent writer 和 deterministic/source binding 全通过。focused `37 passed`、相邻 `115 passed`；显式全量 `tests/` 为 `1441 passed, 14 skipped, 4 failed`，失败仍仅为 R-N012 两份旧治理文档缺失。

## 风险复核

新增 R-N073。该实现是 software transactional primitive，不是 wire/CDC/RTL/board atomicity；timestamp 为 cycle-domain，不是 UTC；CRC/SHA 不是 authentication；safe boundary 仍是 caller contract；旧 scheduler 未迁移，automatic rollback 与通信中断恢复留给 T4.3.3。

## 是否需要插入新 task

不插入。T4.3.3 已直接覆盖 timeout、通信中断、pause/jitter、race、fallback/rollback/stability；T5.1/T5.4 校准 policy 与 OOD/长时，T5.5/T6 覆盖 transport、RTL、综合和实板。

## 对任务板同步说明

T4.3.2 更新为 Done；T4.3.3 更新为 In Progress；当前推荐任务更新为 T4.3.3。
