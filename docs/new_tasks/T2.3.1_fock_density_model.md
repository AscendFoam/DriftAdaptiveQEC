# T2.3.1 完成记录：finite-cutoff approximate GKP density matrix

**Task ID：** T2.3.1  
**标题：** 构造 finite-cutoff approximate GKP density-matrix model  
**日期：** 2026-07-14  
**状态：** Done

## 输入材料

- `docs/new_task_board.md` T2.3.1；
- `docs/experiment_plan.md:436`；
- `physics/finite_energy_gkp.py`；
- `physics/finite_squeezing_noise.py`；
- 现有 conda NumPy/SciPy 能力与旧 `gkp_state.py` fallback 审计。

## 实际完成内容

实现 independent finite-Fock density reference，包括 normalized GKP projection、
displacement、loss、thermal excitation、phase diffusion、small Kerr、modular
measurement backaction 和显式降级的 cavity high-Fock leakage proxy；加入完整的
state/channel validation、cutoff convergence、解析 gate、CLI 和机器 JSON。

## 产物路径

- `physics/fock_density_model.py`
- `tests/test_fock_density_model.py`
- `docs/fock_density_model.md`
- `docs/t2_3_1_fock_density_validation.json`
- `docs/tasks/T2.3.1_fock_density_model.md`
- `docs/new_tasks/T2.3.1_fock_density_model.md`

## 验证方式和结果

- direct `38 passed`（T2.3.2 增补 coordinate-scale dilation 的 5 项回归）；
- adjacent `81 passed`；
- full `tests/`：`562 passed, 4 failed`，四个失败仍仅 R-N012；
- production `12/18/24/30` cutoff sweep 捕获率严格增长；
- 10 个 convergence/analytic/CPTP gates 全 PASS；
- 所有受测通道 trace-one/Hermitian/PSD；最小 eigenvalue `-1.33e-16`；
- pure-loss、thermal-vacuum、phase coherence、Kerr population 和 measurement
  completeness 均命中解析值。

## 风险复核

新增 R-N037。当前只关闭“无 independent density-matrix validator”缺口；不关闭
protocol-round alignment、cavity--transmon、device calibration 或 infinite-cutoff
convergence 风险。

## 是否需要插入新 task

不需要。T2.3.2 已承接一轮协议，T2.3.3 承接跨保真度与失效区，后续 device/HIL task
承接真实泄漏、波形与硬件证据。

## 对论文 claim 的影响

允许写 finite-cutoff single-oscillator density-matrix reference 与解析 channel checks；
禁止写完整 sBs cycle、真实 transmon leakage、device-calibrated master equation、无限维
精确结果或硬件 fidelity。

## 对 `docs/new_task_board.md` 的同步说明

- T2.3.1：`In Progress -> Done`；
- T2.3.2：`Todo -> In Progress`；
- 当前推荐任务：`T2.3.2`。
