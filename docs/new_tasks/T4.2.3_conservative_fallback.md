# T4.2.3 完成记录：conservative fallback 与健康标志

- Task ID：T4.2.3
- 标题：设计 conservative fallback 和健康标志
- 日期：2026-07-15
- 状态：Done

## 输入材料

T4.2.1 version-bound MAP image/decision、T4.2.2 six-mode FSM、`two_domains_three_timescales.md` FB01--FB14
失败合同，以及任务板 OOD/leakage/stale/CRC/version/deadline 要求。

## 实际完成内容

实现 controller-owned trusted image registry、14 个固定 bit health/integrity flags、uint8 saturating cycle
counters、完整 reason trace、last-trusted version 和 deterministic status/action。operational fault 可用 absent MAP
推进 FSM；blocking fault frame hold，leakage hold/reset，good-cycle hysteresis 后恢复。

## 产物路径

`cnn_fpga/runtime/conservative_fallback.py`、`cnn_fpga/benchmark/conservative_fallback_validation.py`、
两份 tests、`docs/reports/phase4/conservative_fallback_health.md`、20-gate JSON 与 4,096-row CSV。

## 验证方式和结果

20/20 gates、28 focused、126 focused+adjacent；显式全量 `tests/` 为
`1363 passed, 14 skipped, 4 failed`，4 个失败仍仅为 R-N012 缺失旧 FR8/P4 文档。16×256 rows 覆盖全部 14 flags、OOD 192/193 与 age 64/65 边界、CRC/SHA、
8-bank commit、rollback/unknown/mismatch、deadline/MAP missing/action corruption/ack、组合 mask/trace、恢复、
counter saturation、双轴健康 action、6-cycle/II=1、deterministic replay 和 truth denylist。

## 风险复核

新增 R-N070：OOD 生成/校准、片上 image CRC 重算、automatic rollback、transport watchdog、physical LER、
完整定点与硬件证据尚未完成。当前 fallback 是 no-map frame hold，不是物理 recovery efficacy。

## 是否需要插入新 task

不插入；T4.2.4、T4.3、T5、T6 已覆盖量化、闭环、OOD/robustness、RTL 与 board fault injection。

## 对任务板同步说明

T4.2.3 Done；T4.2.4 In Progress；当前推荐任务更新为 T4.2.4。
