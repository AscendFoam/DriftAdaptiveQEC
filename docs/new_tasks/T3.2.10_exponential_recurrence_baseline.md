# T3.2.10 完成记录：PRL-inspired 指数递推 baseline

- Task ID：T3.2.10
- 标题：实现 PRL-inspired 指数递推 baseline
- 日期：2026-07-15
- 状态：Done

## 输入材料

`docs/new_task_board.md`、Puviani exponential saturation 段落、T2.3.4/T2.3.5 exact
differentiable sBs、T3.2.5 run-length 基准、T3.2.9 lookup checkpoint 与术语 registry。

## 实际完成内容

完成 15 维 `pi[t+1]=a[m]pi[t]+(1-a[m])pi_inf[m]` 因果策略。物理 lane 三重启两阶段
exact 优化 `g/e`；leakage branch 固定且 fail-closed 标注。事件 lane 使用 X/Z/leakage 三状态、
真实 ParamBank、training-only 72-grid，并与重新选择的 run-length FSM 在同 384k trace 比较。
提供 float、software integer、checkpoint reload 与逐分支/逐轨迹证据。

## 产物路径

`physics/exponential_recurrence_control.py`、`cnn_fpga/runtime/exponential_recurrence.py`、
`cnn_fpga/benchmark/exponential_recurrence_baseline.py`、三份 tests、JSON/PT/CSV、
`docs/exponential_recurrence_baseline.md` 及治理同步。

## 验证方式和结果

- 19 production gates，1,888 rows；3×300 phase-one + 3×250 refinement 全 75 参数
  gradient/update coverage，selected tail gain `2.24e-6`；
- cutoff12 recurrence 相对 standard `+0.388134`、距 lookup `-0.030878`；Q-fixed loss
  `7.62e-8`；cutoff16 frozen recurrence `0.773930`；
- event recurrence 比 run-length cost 低 `0.129211 [0.128225,0.130196]`，但比 memoryless
  高 `0.050700 [0.049893,0.051508]`；
- 39 focused、85 governance、235 adjacent tests 通过；默认解释器显式全量
  `1110 passed, 11 skipped, 4 failed`，4 个失败仍仅为 R-N012 的 FR8/P4 历史文档缺失；
  checkpoint hash/replay、branch normalization、density diagnostics、causality、closed form、
  fixed-point parity 与 source-domain separation 通过。

## 风险复核

R-N061 登记 physical leakage 缺口、branch reshaping、cutoff transfer 反转、event false
intervention 和非 RTL/board 边界。没有把 event cost 当 LER，也没有把 lookup transfer 反转写成
“超越 oracle”。

## 是否需要插入新 task

不插入；T3.2.11、T4.4、T5.1/T5.2/T5.4、T5.5/T6 已覆盖。

## 对任务板同步说明

T3.2.10 Done；T3.2.11 In Progress；当前推荐任务为 T3.2.11。
