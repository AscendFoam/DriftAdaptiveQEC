# T1.2.3 完成记录：finite-energy shrinkage 趋势复现

**Task ID：** T1.2.3  
**标题：** Standard gain=1 次优与 shrinkage advantage 随 Delta 下降收缩  
**日期：** 2026-07-14  
**状态：** Done

## 输入材料

- `docs/new_task_board.md` T1.2.3；
- T1.2.1 damped-projector peak variance；
- T1.1.1 centered standard binning/logical parity；
- finite-energy/optimized decoder 文献趋势与 T0.2.2 claim downgrade contract。

## 实际完成内容

- 新增 `physics/finite_energy_trends.py`；
- 定义 data displacement + finite-energy syndrome noise + modular observation + linear
  correction action 的 effective model；
- 用独立 train samples 闭式拟合 `[0,1]` shrinkage gain；
- 用 held-out paired eval 同时报告 MSE、logical parity error、absolute/relative gain、
  paired SE/CI、discordant counts 和 McNemar z；
- 使用跨 Delta common random numbers 并机器检查 gain/advantage/MSE 三类趋势；
- 报告 unwrapped MMSE reference，显式区分 periodic fitted gain；
- 新增 9 项 direct tests 和公共惰性导出。

## 产物路径

- `physics/finite_energy_trends.py`
- `tests/test_finite_energy_trends.py`
- `physics/__init__.py`
- `docs/tasks/T1.2.3_finite_energy_trend_reproduction.md`
- `docs/new_tasks/T1.2.3_finite_energy_trend_reproduction.md`
- `README.md`

## 验证方式和结果

- focused：`9 passed`；
- 组合回归：`74 passed`；
- Delta `0.60->0.22` 时 fitted gain `0.3630->0.8010`；
- absolute logical gain `0.02406->0`，前三点 paired z `84.70/50.54/16.22`；
- 所有五点 shrinkage MSE 更低，MSE advantage 随 Delta 下降收缩；
- fixed-seed 两次完整 result dataclass bit-identical；
- 显式 tests/：`136 passed, 4 failed`，仍仅为 R-N012。

## 风险复核

- 新增 R-N015：当前趋势只在 additive finite-energy syndrome-noise effective model
  成立，尚未纳入 envelope-driven state deformation、loss、ancilla 或 protocol；
- R-N014 不变：logical error 是 Pauli parity effective metric；
- R-N013 进一步缓解：趋势显式使用 T1.2.1 damped-projector state variance，不走 legacy；
- R-N012 不影响本任务。

## 是否需要插入新 task

不插入。R-N015 的 fidelity cross-check 由 T2 protocol/multi-fidelity 和 T5.3 logical
channel 正常任务覆盖；当前结果明确降级，不阻塞 T1.3 drift model。

## 对论文 claim 的影响

允许写 syndrome-level effective model 的 finite-energy shrinkage qualitative trend；
不允许升级为完整 approximate-GKP recovery、memory-assisted 或实验复现。

## 对任务板的同步

- T1.2.3：`In Progress -> Done`；
- T1.3.1：`Todo -> In Progress`；
- 当前推荐任务：`T1.3.1`。
