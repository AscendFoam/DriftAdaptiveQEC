# T2.0.1 完成记录：主/次参考协议层级冻结

**Task ID：** T2.0.1  
**标题：** 冻结 sBs 主数字孪生、sharpen--trim 交叉验证和 secondary protocol 范围  
**日期：** 2026-07-14  
**状态：** Done

## 输入材料

- `docs/new_task_board.md` T2.0.1；
- Sivak 2023 与 Campagne-Ibarcq 2020 本地一手正文/补充材料；
- 六篇实用论文改进记录和 Zotero 阅读卡；
- T1.4.4 参数注册表与 T1.4.5 术语合同。

## 实际完成内容

- 建立 1 个主协议、1 个一手交叉验证协议和 2 个 secondary reproduction 协议；
- 冻结 mathematical object、cycle、observation、action、unmodeled 和 allowed/forbidden use；
- 保留 sBs `4.924 us` constituent 与 `9.848 us` full X+Z cycle 双口径；
- 固定 full-cycle `gg/ge/eg/ee` paired semantics，并将 `f` 限定到 reset/leakage control branch；
- 对 sharpen--trim timing 使用 `null`，不从组件叙述臆造完整周期；
- 限制 Knill/qunaught、ME/P-Steane 只进入 secondary/Supplement；
- 建立 14 条 source anchors、6 条 nonmixing rules、4 个 promotion gates 和 12 项直接 tests。

## 产物路径

- `docs/protocol_hierarchy.md`
- `docs/protocol_hierarchy.json`
- `tests/test_protocol_hierarchy.py`
- `docs/tasks/T2.0.1_protocol_hierarchy.md`
- `docs/new_tasks/T2.0.1_protocol_hierarchy.md`

## 验证方式和结果

- focused：`12 passed`；
- 相邻 T1.4.4/T1.4.5/T2.0.1：`40 passed`；
- 显式 `tests/`：`258 passed, 4 failed`；仅 R-N012 两份旧文档缺失。

## 风险复核

新增 R-N025：后续实现可能把 sBs constituent/full cycle、paired outcome、`f` leakage class、sharpen--trim `+y/-y` 或 secondary 协议混在同一模型/排名中。关闭条件是 T2.0.2--T2.0.5 直接消费 protocol ID 并通过 negative mixing tests，主 benchmark provenance 固定协议和 cycle 口径。

## 是否需要插入新 task

不需要。T2.0.2--T2.0.5 已承接 Kraus/error hierarchy、readout/reset、cycle state machine 和故障趋势复现；提前插入会打乱顺序。

## 对论文 claim 的影响

允许写“以 measurement-feedback sBs 为主数字孪生，以 2020 sharpen--trim 作交叉验证，其他协议为 secondary”。禁止写任何协议已经实现；禁止把文献 timing 写成实板 timing；禁止用 secondary 证据支持主排名、FPGA physical squeezing 或 cavity/transmon claim。

## 对 `docs/new_task_board.md` 的同步说明

- T2.0.1：`In Progress -> Done`；
- T2.0.2：`Todo -> In Progress`；
- 当前推荐任务：`T2.0.2`。
