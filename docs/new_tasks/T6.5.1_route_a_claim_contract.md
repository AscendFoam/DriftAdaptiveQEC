# T6.5.1 Route-A claim contract、canonical role 与 comparison lane 完成记录

- Task ID：T6.5.1
- 标题：冻结 Route-A claim contract、canonical role 和三类比较 lane
- 日期：2026-07-17
- 状态：Done

## 输入材料

- 用户确认的 Route A 系统叙事与 integration bridge；
- `docs/experiment_plan.md §18.1`、Phase 6A 任务板与 T-RISK-20260717-02；
- `docs/claim_ladder.json` 与 `docs/decoder_controller_terminology.json`；
- T5.1.4 learned/CNN 强分支证否、T4.4.5 bounded student extension；
- T5.5.2 target P&R estimate 与 T6.2.2 million-cycle RTL qualification。

## 实际完成内容

1. 冻结唯一 paper-level 主角 `regime-aware safe adaptive dual-loop`；
2. 冻结 11 个 canonical roles 及 output authority：MAP、regime posterior、event FSM、
   fallback/health、trusted A/B bank、FPGA fast path、CNN、teacher、student 与 oracle 不互相冒领；
3. 将 CNN/teacher/student 标为 replaceable learning extension，hidden oracle 标为
   nondeployable privileged reference；
4. 建立 decoder、official GQF controller、task-normalized FPGA hardware 三条 metric namespace
   互斥 lane，明确不存在 global leaderboard/score；
5. 建立 11 条 claim registry，每条包含中英文 wording、metric、data domain、privilege、
   required evidence level、当前证据、激活门、撤销条件和禁用措辞；
6. 当前只允许架构 contract、百万周期 CXXRTL qualification、目标器件 P&R estimate 与 bounded
   student component evidence；static/drift/tail 为 conditional，CNN 为 ablation-only，GQF surpass、
   FPGA faster、physical break-even 为 prohibited-now；
7. 将现有 T4.4.5 student 证据隔离到 governance/extension，不提前挂入 official GQF lane；
8. 增加 20 个 machine gates、10 个角色/metric/evidence/wording semantic mutations、11-row
   Source Data 和 6 个 direct tests。

## 产物路径

- `cnn_fpga/benchmark/route_a_claim_contract.py`
- `tests/test_route_a_claim_contract.py`
- `docs/route_a_claim_contract.md`
- `docs/t6_5_1_route_a_claim_contract.json`
- `docs/t6_5_1_route_a_claim_contract_source_data.csv`

## 验证方式和结果

- 11/11 roles、3/3 lanes、11/11 claims schema 完整且 ID 唯一；
- 三条 evaluation lane metric sets 两两不相交；global score/oracle mixing 全为 false；
- T5.1.4 CNN negative branch、T4.4.5 bounded controller、T6.2.2 board-independent 与 T5.5.2
  non-board evidence 边界均被机器重算；
- GQF surpass、hardware speed、break-even 三项当前状态均为 `PROHIBITED_NOW`；
- 10/10 semantic mutations rejected，20/20 gates PASS；
- focused tests：`6 passed in 0.03s`；机器 verdict：
  `PASS_ROUTE_A_CLAIM_ROLE_AND_LANE_CONTRACT_FROZEN`。

## 深审与非简化实现检查

首版 machine contract 已 20/20 PASS，但深审仍做了三次收紧：第一，现有 student retention
从 official GQF lane 移到 governance/extension，避免用项目内 simulator 预占 official reproduction；
第二，将 system deployability 改为 `target_deployable_after_contract_and_promotion`，不冒充已集成；
第三，所有 future decoder performance wording 加 `[Conditional; not currently supported]`。
此外没有 hash-bind 会在每次进度变化时自我失效的整个 task board，而是以完整 future-task 集合和
治理测试锁定链路；稳定 source/result artifacts 继续 SHA-bound。

`nature-writing` workflow 用于冻结一句话 argument、terminology ledger、claim-evidence-boundary 与
abstract/discussion/conclusion 用语门；它没有生成或填补任何实验结果。

## 风险复核

R-N110 部分缓解：跨 decoder/GQF/hardware 的伪总榜已被机器合同和 10 类 mutation 封住，但只有
T6.7—T6.9 完成后才能关闭对应外部主张风险。R-N084 的 CNN 强主张继续保持证否状态；R-N111/
R-N112 的 hardware evidence boundary 不变。

## 是否需要插入新 task

否。执行 schema、预注册、统一 adapter、实验、official GQF 和真板 evidence 都已由 T6.5.2—
T6.9.3 正常顺序承接。

## 对任务板的同步

T6.5.1 `In Progress -> Done`；当前推荐任务切换为 T6.5.2 并置 `In Progress`。

## 对论文 claim 的影响

旧叙事被包容为 replaceable modules/ablation，而论文主张不再依赖 CNN 成功。当前允许写 contract、
CXXRTL qualification 与 P&R estimate 的严格限定表述；任何 LER/tail/GQF/speed/break-even 优势仍需
各自 gate，不能跨 lane 补证。

