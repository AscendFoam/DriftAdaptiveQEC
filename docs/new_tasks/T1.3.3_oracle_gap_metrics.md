# T1.3.3 完成记录：regret / oracle-gap 指标

**Task ID：** T1.3.3  
**标题：** Static/dual/oracle paired gap、closure 与不确定性  
**日期：** 2026-07-14  
**状态：** Done

## 输入材料

- `docs/new_task_board.md` T1.3.3；
- T1.3.2 nondeployable full-state decoder oracle；
- `docs/gap_statement.md` RQ4 同 seed/同 trace contract；
- 后续 T5.1.3 average/tail/oracle-gap reporting gate。

## 实际完成内容

- 新增 `physics/oracle_gap.py`；
- 区分 raw static-oracle gap、dual-oracle regret、absolute improvement、remaining ratio 和
  closed fraction；
- 对 zero/inverted denominator、dual worse static、dual better oracle point estimate 显式
  分型且不 clip；
- 实现 paired difference SE/CI、discordant counts、signed McNemar z；
- 用八种联合 outcomes 的 multinomial paired bootstrap 生成 ratio percentile CI；
- 增加 denominator stability、valid replicate count 和 ratio reliability gate；
- 新增 10 项 direct tests、公共惰性导出、任务文档和 README 入口。

## 产物路径

- `physics/oracle_gap.py`
- `tests/test_oracle_gap.py`
- `physics/__init__.py`
- `docs/tasks/T1.3.3_oracle_gap_metrics.md`
- `docs/new_tasks/T1.3.3_oracle_gap_metrics.md`
- `docs/new_risks.md`
- `docs/new_task_board.md`
- `README.md`

## 验证方式和结果

- focused：`10 passed`；
- M1.1--M1.3 邻近回归：`106 passed`；
- canonical `0.20/0.12/0.05` 精确得到 remaining `7/15`、closed `8/15`；
- 1000 paired samples 精确回收三种差值、SE、80/0 discordance 和 `sqrt(80)` McNemar z；
- 相同 rates、不同 pairing 得到不同 SE；
- 小 `1/40` denominator 保留 point ratio，但 reliability gate 为 false；
- 显式 tests/：`172 passed, 4 failed`，仍仅为 R-N012。

## 风险复核

- 新增 R-N019：有限样本 static-oracle gap 太小/反转时，ratio 会不稳定；代码已标旗，后续
  报告必须遵守 denominator/paired-bootstrap gate；
- R-N018 不变：decoder oracle 仍是 model reference，不是 channel bound；
- R-N012 不影响本任务。

## 是否需要插入新 task

不插入。R-N019 的正式报告 gate 已由本任务实现，并由 T5.1.3 average/tail/paired-seed
protocol 继续执行；T1.3.4 先验证 synthetic drift 是否有非退化 positive gap。当前不阻塞。

## 对论文 claim 的影响

只有 positive stable denominator 且 paired ratio CI 可靠时才能写 gap closure。负值、`>1`、
zero/inverted denominator 必须原样报告和解释，不能裁剪。

## 对任务板的同步

- T1.3.3：`In Progress -> Done`；
- T1.3.4：`Todo -> In Progress`；
- 当前推荐任务：`T1.3.4`。
