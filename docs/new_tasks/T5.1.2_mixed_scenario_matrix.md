# T5.1.2 完成记录：混合 noise/regime scenario matrix

- Task ID：T5.1.2
- 标题：运行混合 noise/regime scenario matrix
- 日期：2026-07-16
- 状态：Done

## 输入材料

任务板冻结的 10 类场景、T5.1.1 的 19-comparator/8-lane registry，以及 T2/T3 的 decoder、noise-transfer、
protocol ancilla、large-error 和 leakage production executors。

## 实际完成内容

建立四条不可混排的 native lanes。六个 syndrome-decoder 场景使用 3/6 disjoint train/eval seeds、32 windows、
36 个唯一 shared traces，共 589,824 paired decisions；loss、readout/ancilla、large-error、leakage 重新运行原生
物理/统计 gates。算法预期排序不参与 task PASS，variance/burst 中的反方向或弱差异结果原样保留。

开发期先后因相关轴 joint probability 未定义、signal offset 混杂 loss bias 两次 fail closed；最终通过拆分
correlation 与 loss isolation 修复，没有放宽阈值。新增 payload validator 和 6 类 failure mutation。

## 产物路径

- `cnn_fpga/benchmark/mixed_scenario_matrix.py`
- `tests/test_mixed_scenario_matrix.py`
- `docs/mixed_scenario_matrix.md`
- `docs/t5_1_2_mixed_scenario_matrix.json`
- `docs/t5_1_2_mixed_scenario_matrix_source_data.csv`
- `docs/protocol_hierarchy.json` / `docs/protocol_hierarchy.md`

## 验证方式和结果

runner 为 10/10 scenarios、36 decoder seed rows、116 source rows、15/15 gates。direct `11 passed`，
protocol/governance `51 passed`，8 个相关 parent test files `207 passed`。Large-error 10/10、leakage 11/11、
ancilla endpoint 全部 native gates 通过；loss 显式保留 localized→clipping boundary。

## 风险复核

R-N082 降为 Mitigated；新增 R-N083，阻止把四类不同模型层的 lane-local PASS 写成统一 finite-energy
closed-loop robust claim。T5.1.3--T5.4 已正常承接 gap、fault、channel、OOD/long-horizon，不插入风险 task。

## 任务板同步

T5.1.2 Done；T5.1.3 In Progress；当前推荐任务更新为 T5.1.3。
