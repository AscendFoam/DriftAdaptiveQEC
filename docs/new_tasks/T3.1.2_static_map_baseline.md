# T3.1.2 完成记录：static MAP

- **Task ID：** T3.1.2
- **标题：** 实现 training-average static MAP
- **日期：** 2026-07-15
- **状态：** Done

## 输入材料

Periodic 2D MAP reference、T3.1.1 comparison contract/R-N047、T1.3.4 paired harness 和新任务板
training-set average 参数要求。

## 实际完成内容

完成 total-covariance moment-matched static fit、冻结参数对象、observed-only decoder、active/future
comparison schema 接入、8-seed 576k 验收与 source-bound JSON/CSV。formal static 的增强使旧 EKF
primary gate 失败，该反证已写回报告和风险，不维持旧弱 baseline 叙事。

## 产物路径

- 实现：`cnn_fpga/benchmark/static_map_baseline.py` 及两个 comparison 模块更新；
- 测试：`tests/test_static_map_baseline.py` 及相邻测试更新；
- 报告：`docs/static_map_baseline.md`；
- 数据：`docs/t3_1_2_static_map_validation.json`、
  `docs/t3_1_2_static_map_source_data.csv`；
- 详细记录：`docs/tasks/T3.1.2_static_map_baseline.md`。

## 验证方式和结果

- 9/9 gates；8 seeds、576,000 paired samples；
- standard/static/oracle `0.058870/0.024498/0.011340`；paired improvement
  `0.034372 [0.033798,0.034946]`；
- 8/8 seed CI 下界正；single training hash、8 unique trace hashes；
- frozen parameters、source hashes 与 CSV/JSON 可重建；focused+adjacent `119 passed`。
- 全量 `844 passed, 4 skipped, 4 failed`；4 个失败均为 R-N012 既有 FR8/P4 文档缺失；
  `compileall` 与 `git diff --check` 通过。

## 风险复核

R-N047 降为 Mitigated；新增 R-N048 跟踪 same-family synthetic training metadata 与真实
calibration/mismatch 的差距。旧 T1.3.4 EKF 强结论已降级。

## 是否需要插入新 task

不插入；T5.1/T6 正常任务承接 distribution mismatch 和 device calibration。

## 对论文 claim 的影响

只允许 registered synthetic distribution 的 static-vs-standard 结论；禁止 universal/device claim。

## 对 `docs/new_task_board.md` 的同步说明

T3.1.2 更新为 Done，T3.1.3 更新为 In Progress，当前推荐切换至 T3.1.3。
