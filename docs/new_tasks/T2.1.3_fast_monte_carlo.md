# T2.1.3 完成记录：高速 Monte Carlo 与 rare-event 分层抽样

**Task ID：** T2.1.3  
**标题：** vectorized million-cycle simulator、cluster CI 与 target-weighted rare strata  
**日期：** 2026-07-14  
**状态：** Done

## 输入材料

- `docs/new_task_board.md` T2.1.3；
- `physics/drift_processes.py`；
- `physics/syndrome_stream.py`；
- `physics/control_memory.py`；
- `docs/protocol_hierarchy.md/.json`。

## 实际完成内容

- 实现按 round 循环、trajectory 维度向量化的 stateful core；
- 输出 cycle `P_L`、q/p rates、trajectory failure、cluster CI/SE、seed；
- 支持 burst/leakage/both trajectory-mixture stratification；
- 按 target probability 加权，allocation 只影响方差；
- zero-event upper bound、hazard failure、analytic calibration、recovery ablation；
- 实际运行并保存 1e6-cycle production artifact；
- CLI、JSON writer、public exports、说明和 protocol contract。

## 产物路径

- `physics/fast_monte_carlo.py`
- 更新 `physics/__init__.py`
- `tests/test_fast_monte_carlo.py`
- 更新 `tests/test_protocol_hierarchy.py`
- 更新 `tests/test_sbs_error_space.py`
- 更新 `tests/test_sbs_observation_reset.py`
- `docs/fast_monte_carlo_validation.md`
- `docs/t2_1_3_fast_monte_carlo.json`
- 更新 `docs/protocol_hierarchy.md/.json`
- `docs/tasks/T2.1.3_fast_monte_carlo.md`
- `docs/new_tasks/T2.1.3_fast_monte_carlo.md`
- 更新 `README.md`

## 验证方式和结果

- focused：`19 passed`；
- protocol/core/observation/fast-MC：`78 passed`；
- adjacent stack：`210 passed`；
- 显式 `tests/`：`439 passed, 4 failed`，失败仅 R-N012；
- production 1e6 cycles：`0.2531 s`、约 `3.95e6 cycles/s`；
- protocol/production JSON 与 `py_compile` 通过。

## 风险复核

R-N031 性能部分已实质关闭，状态降为 Mitigated。新增 R-N033：rare scenario weights/durations、leakage logical-fault law 与 host vectorized transitions 未经 device/Fock calibration；当前 throughput 是本机 host，不是 FPGA。

## 是否需要插入新 task

不需要。T2.2/T2.3 处理 finite-energy/ancilla/Fock fidelity，T2.4/T5.5/T6 处理 timing、bit-accurate 与 real-board evidence。

## 对论文 claim 的影响

允许写“one million syndrome-level cycles and target-weighted burst/leakage strata are supported with trajectory-cluster uncertainty”。禁止写实验 rare-event reproduction 或 hardware real-time throughput。

## 对 `docs/new_task_board.md` 的同步说明

- T2.1.3：`In Progress -> Done`；
- T2.2.1：`Todo -> In Progress`；
- 当前推荐任务：`T2.2.1`。
