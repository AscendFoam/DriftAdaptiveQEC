# T6.5.3 Route-A formal preregistration 完成记录

- Task ID：T6.5.3
- 标题：在访问正式结果前预注册场景、split、阈值和统计门
- 日期：2026-07-17
- 状态：Done

## 输入材料

- `docs/experiment_plan.md §18.2/§18.4` 与 Phase 6A task contract；
- T6.5.2 unified execution contract；
- 已披露先验：T5.1.3 `55/512 > 37/512`、T5.1.4 0 Holm discovery、T5.4.2
  compound/nominal counterexample。

## 实际完成内容

1. 冻结 calibration/pilot/formal 三组 12/12/24 independent seeds，rate/amplitude/duration 在
   split 间分别互斥；
2. 冻结 4 smooth、6 abrupt/OOD 与 nominal negative control；calibration/pilot 每 family 4
   tuples，formal 每 family 6 held-out tuples；
3. 冻结每方法 1,464 formal trajectories / 71,958,528 decisions，oracle 独立分栏；
4. 冻结 8-window preamble、SHA-256 domain-separated streams 与 method-independent onset/trace；
5. 冻结六维 common threshold grid、pilot-only lexicographic selector 和 no-candidate NO-GO；
6. 冻结 decision->cell->family->seed 等权聚合、24-cluster paired bootstrap、20k replicates、
   Holm families、512-window p95/worst 定义；
7. 冻结 aggregate smooth LCB、calibration-tail、每 abrupt/OOD catastrophic、nominal
   non-inferiority、integrity 与 CNN/board failure branches；
8. 生成 machine artifact、143-cell Source Data、23 gates、12 semantic mutations 与 15 tests。

## 产物路径

- `cnn_fpga/benchmark/route_a_preregistration.py`
- `tests/test_route_a_preregistration.py`
- `docs/route_a_preregistration.md`
- `docs/t6_5_3_route_a_preregistration.json`
- `docs/t6_5_3_route_a_preregistration_source_data.csv`

## 验证方式和结果

- verdict：`PASS_ROUTE_A_RESULT_BLIND_PREREGISTRATION_FROZEN`；
- protocol SHA：`547e66073cbf478352fc180b57a8fff8677a0858df7f4a9aef47f82d88bddd39`；
- 143 unique cells；24 formal clusters；71,958,528 decisions/method；
- 23/23 gates、12/12 semantic mutations；
- focused：`15 passed in 1.32s`。

## 深审与非简化实现检查

预注册没有只列 family 名称，而是给出 split-disjoint parameter levels、明确 tuple design、family
amplitude semantics、正式 workload 和 failure branches。首轮通过后深审发现 aggregate weighting
与 event onset 尚可成为研究者自由度，随后固定 equal-family hierarchy、domain-separated streams、
8-window preamble 和 method-independent onset；完整重生成 artifact 并重跑 tests。

阈值字段保持 `null` 不是漏实现：T6.6.2 policy 尚未完成，T6.6.3 的职责是在本任务冻结的 grid/
selector 上只用 pilot 选出一组 tuple 并生成新 lock。提前写一个未经 policy replay 的值反而是伪
预注册。

## 风险复核

新增 R-N114：本预注册是见过旧 pilot/counterexample 后设计，只对新 T6.7 formal data
result-blind；若写成从项目开始就 blind 会构成 HARKing。machine artifact 已披露旧证据和使用方式，
T6.7 必须使用全新 seeds/cells；由既有任务正常承接，不插入旁路 task。

## 是否需要插入新 task

否。T6.6.2/T6.6.3 实现 policy、校准与 threshold lock；T6.7.1/T6.7.2 执行 formal matrices。
任何协议修改必须新建 protocol ID 和 artifact，不能覆盖 V1。

## 对任务板的同步

T6.5.3 `In Progress -> Done`；当前推荐任务切换为 T6.6.1 并置 `In Progress`。

## 对论文 claim 的影响

当前仅支持 Methods 中描述预注册设计，尚不支持任何 Route-A 性能/安全 claim。后续 failure
会自动降级为 smooth-only、static+deterministic、CNN ablation 或 pre-board hardware-aware 版本。

