# T2.4.3 完成记录：fixed-point chain

- **Task ID：** T2.4.3
- **标题：** 加入 fixed-point/LUT/parameter-bank error model
- **日期：** 2026-07-15
- **状态：** Done

## 输入材料

现有 Q4.20 linear runtime、fast-loop emulator、parameter bank、AXI helper、periodic LLR reference，以及
T2.4.1/T2.4.2 的 latency/timing boundary。

## 实际完成内容

实现 ADC/replay uniform code、LUT address/value、signed LLR、confidence threshold、wrapped state estimate、
update granularity 和 double-bank integer image。新增 sign-burst、state-MSB、stale、torn fault，完成
6-axis OAT、joint profiles 与 8-seed paired validation。资源只给 exact bit proxy，所有 synthesis/board
字段 fail closed。生成机器 JSON、368-row CSV 和 Python-only 四格式科研图。

## 产物路径

- 实现：`cnn_fpga/runtime/fixed_point_chain.py`、`plot_fixed_point_chain.py`、
  `cnn_fpga/decoder/linear_runtime.py`；
- 测试：`tests/test_fixed_point_chain.py`；
- 报告：`docs/fixed_point_chain.md`；
- 数据/审计：`docs/t2_4_3_fixed_point_validation.json`、
  `docs/t2_4_3_precision_resource_ler.csv`、`docs/t2_4_3_figure_validation.json`；
- 图：`docs/figures/t2_4_3_precision_resource_ler.{svg,pdf,tiff,png}`。

## 验证方式和结果

- production：11/11 gates PASS，368 paired runs；
- high precision 相对 float `-3.46e-5`；最低 joint `+0.08624`；
- severe bank fault 相对 base 的 paired CI 均严格为正；stale/torn CI 跨零并保留；
- direct+adjacent `63 passed`；`compileall` 通过；
- 显式 `tests/` 全量：`796 passed, 4 skipped, 4 failed`；4 个失败均为 R-N012 的
  既有 FR8/P4 缺失文档，不属于 T2.4.3 回归；
- figure 5/5 gates + 人工 visual QA 通过；SVG editable，PDF/TIFF/PNG 齐全；
- artifact/source/figure hashes 一致。

## 风险复核

新增 R-N046：8-entry LUT 的 synthetic regularization 不是 universal optimum，joint curve 也非单调；exact
bit proxy 不等于 LUT/BRAM/DSP/Fmax。R-N036/R-N045 的实板/设备标定边界继续开放。

## 是否需要插入新 task

不插入。T5/T5.5/T6 已覆盖多场景选择、综合和实板计时；T3.1.1 可按顺序开始 baseline integration。

## 对论文 claim 的影响

可报告 model-qualified bit-accurate sensitivity；不得报告 synthesis/board/device 结论。

## 对 `docs/new_task_board.md` 的同步说明

T2.4.3 更新为 Done，T3.1.1 更新为 In Progress，当前推荐切换至 T3.1.1。
