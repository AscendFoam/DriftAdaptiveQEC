# T2.0.5 完成记录：sBs 位移故障 syndrome 趋势复现

**Task ID：** T2.0.5  
**标题：** 复现位移到最近逻辑操作距离驱动的 recovery-depth 与同象限 e-run 非单调趋势  
**日期：** 2026-07-14  
**状态：** Done

## 输入材料

- `docs/new_task_board.md` T2.0.5；
- Sivak 2023 Fig. 4(c) caption 与正文 displacement/same-quadrature 段落；
- `physics/sbs_error_space.py`；
- `physics/sbs_observation_reset.py`；
- `docs/protocol_hierarchy.md/.json`。

## 实际完成内容

- 实现 periodic nearest-logical-operation distance 和 main-lobe amplitude sweep；
- 实现 assumption-tagged depth injection、T2.0.2 transition sampling、T2.0.3 observation/reset sampling；
- 输出 depth、ideal/observed e-run、restricted recovery、affected/unaffected time traces；
- 实现固定 simulation/bootstrap seed、percentile bootstrap、10 个 tolerance gates 和具名 failure diagnostics；
- 完成 changed-seed、X/Z variant、解析 depth/recovery、坏 readout、source anchor 与报告重生成检查；
- 生成机器 JSON/CSV 和中文模型/结果说明。

## 产物路径

- `physics/sbs_displacement_fault.py`
- `physics/__init__.py`
- `tests/test_sbs_displacement_fault.py`
- `docs/sbs_displacement_fault_trend.md`
- `docs/t2_0_5_displacement_fault_trend.json`
- `docs/t2_0_5_displacement_fault_trend.csv`
- `docs/tasks/T2.0.5_sbs_displacement_fault_trend.md`
- `docs/new_tasks/T2.0.5_sbs_displacement_fault_trend.md`
- 更新 `docs/protocol_hierarchy.md/.json`
- 更新 `README.md`

## 验证方式和结果

- focused：`26 passed`；
- T2.0 protocol stack：`92 passed`；
- T1.4.4--T2.0.5 相邻：`120 passed`；
- 显式 `tests/`：`338 passed, 4 failed`，失败仅为 R-N012；
- `py_compile`、protocol/report JSON validation 通过；
- 生产 JSON 与同 seed 全量重跑逐字段一致。

## 风险复核

新增 R-N029：当前只复现文献约束的 qualitative nonmonotonic shape；Binomial depth、recovery/readout 参数均为显式 assumptions，不能替代 Fig. 4(c) 原始数据或 Fock/device calibration。

## 是否需要插入新 task

不需要。T2.3.2/T2.3.3 和 T5.2.1 已覆盖 Fock/coherent cross-check 与正式因果 fault campaign；当前降级 evidence scope 足以阻断错误 claim。

## 对论文 claim 的影响

允许写“protocol-aligned effective model reproduces the qualitative midpoint-peaked displacement syndrome trend”。禁止写实验曲线定量复现、装置参数拟合、真实 QPU fault injection 或低价 FPGA 已测。

## 对 `docs/new_task_board.md` 的同步说明

- T2.0.5：`In Progress -> Done`；
- T2.0.6：`Todo -> In Progress`；
- 当前推荐任务：`T2.0.6`。
