# T7.1.2 冻结主图 1--2

- Task ID：`T7.1.2`
- 标题：冻结主图 1--2
- 日期：2026-07-20
- 状态：Done
- verdict：`PASS_MAIN_FIGURES_1_2_RESTRICTED_PREBOARD_CONTRACT`

## 输入材料

- T7.1.1 的 29 条 claim--evidence--boundary 原子矩阵与 45 个 live artifact。
- T6.5.1 unified contract、T6.6.2 regime-aware safe policy、T6.7.3 integrated RTL qualification。
- T6.9.1 三 seed P&R estimate、T6.9.2 的 42 个 board-measured null 字段。
- T6.15.5 V5 early-stop：IMM/BOCPD、posterior-mixture MAP、risk compiler、V5 quantized/formal/CXXRTL/P&R 均未运行且已 Dropped。
- Nature-figure 的 Python-only、结论优先、可编辑矢量、多格式导出与视觉 QA 合同。

## 执行方案与实际完成内容

1. 先冻结每图唯一结论。Fig.1 只说明 observed-only host slow loop 经 versioned trusted bank 接入 6-cycle/II=1 的 pre-board FPGA fast path；Fig.2 只说明 observed evidence 驱动 typed stage/freeze/switch/reset/LKG action，atomic commit 与 hysteresis 负责 fail-closed recovery。
2. 建立 38-element 机器合同。每个 node、edge、timing、transaction 和 boundary 均保存 figure/panel/status/evidence layer、selector、claim ID 与 live report/Source Data/code SHA-256，不允许只凭手工图注追溯。
3. Fig.1 明确分离逐轮数字 fast path、4000-cycle host parameter cadence、optional CNN/teacher/student ablation sidecar、truth-only evaluation 和 physical-board measurement；P&R 只标 estimate，board 42 字段保持 null。
4. Fig.2 展示 q/p syndrome、health/leakage/integrity、version/CRC/age/ack 三组 observed-only 输入，四类 causal evidence branch、四类 typed action，以及 candidate→inactive A/B bank→safe-boundary commit→LKG/8-window recovery 的事务链。
5. 将 IMM/BOCPD、posterior-mixture MAP、V5 risk compiler 与 V5 quantized/formal/CXXRTL/P&R 放入无生产入边的 `NOT RUN / DROPPED` 边界框；board latency/jitter/deadline/power 放入 `BLOCKED / NULL` 边界框并写明无 measured speed advantage。
6. 用单一 Python/matplotlib backend 生成 183 mm 双栏图；两图分别导出 editable SVG、PDF、300-dpi PNG 和 600-dpi LZW-TIFF，manifest 对 contract、Source Data、renderer 和 8 个输出做 bytes/SHA-256 绑定。
7. 建立 16 个 fail-closed gate 和一门一 substantive mutation，覆盖父验证、38-element 唯一性、快慢层、6-cycle/II=1、P&R/board 分层、typed safety、atomic LKG、Dropped V5、禁止提升、claim 引用、Source Data、导出与人读合同。
8. 完成人工原始分辨率视觉检查；无裁切、遮挡和错误生产箭头。深审又发现合同高度原写 112/124 mm、渲染器实际为 127/137 mm；未把差异视作无关元数据，而是修正合同并把精确高度加入 G03 和测试。

## 产物路径

- `cnn_fpga/benchmark/main_figure_contract.py`
- `tests/test_main_figure_contract.py`
- `docs/t7_1_2_main_figure_contract.json`
- `docs/t7_1_2_main_figure_source_data.csv`
- `docs/main_figures_1_2_contract.md`
- `docs/figures/make_t7_1_2_main_figures.py`
- `docs/figures/t7_1_2_main_figures/figure_manifest.json`
- `docs/figures/t7_1_2_main_figures/figure1_contract_system.{svg,pdf,png,tiff}`
- `docs/figures/t7_1_2_main_figures/figure2_safe_adaptation.{svg,pdf,png,tiff}`

## 验证方式和结果

- 合同：38 elements、16/16 gates、16/16 mutations，verdict=`PASS_MAIN_FIGURES_1_2_RESTRICTED_PREBOARD_CONTRACT`。
- Bundle live verifier：identity、contract、Source Data、8 outputs、editable SVG、raster dimensions、visual contract 共 8/8 checks 通过。
- SVG 保留 97 个 text nodes，未把文字 path 化；TIFF 最短边分别为 3000 px 和 3236 px，满足 600-dpi 版芯输出。
- 人工视觉 QA=`PASS`：标题/节点/边/图注无裁切或覆盖，V5 Dropped 与 board-null 边界醒目且无生产入边。
- 专项回归：`8 passed`；包括 live hash 重算、38-row Source Data 无损性、生产层分离、timing/board 精确值、typed action/atomic recovery、V5 Dropped、格式/分辨率和 promotion mutation。
- 父链联合回归：`132 passed in 87.37s`；覆盖 T7.1.1、Phase 6C integrity/AQEC/CPD/multimode/learned/FPGA lanes、V4/V5/board final gates 与任务板治理。

## 风险复核

- 新增 R-N143（Mitigated/High/Monitor）：图形可能通过箭头、颜色或层级视觉复活未实现模块，或把估计误写成测量；当前逐元素合同、显式边界和 bundle verifier 已缓解。
- T6.9.2 仍 Blocked，42 个 measured 字段仍全 null；本 task 没有产生真板延迟、抖动、deadline 或功耗证据。
- T6.15.5 仍为 `NO_GO_V5_EARLY_HEADROOM_STOP`；V5 模块只作为 dropped boundary 出现，不形成结果或实现 claim。

## 是否需要插入新 task

不插入。T7.1.3 已承接主图 3--4，T7.1.4 承接 Supplement 图，T7.4 承接最终 provenance/claim/视觉边界审计；为填补 board-null 或把 Dropped V5 画成概念实现而新增任务会违反现有早停和证据门。

## 对任务板的同步

- T7.1.2：`In Progress -> Done`。
- T7.1.3：`Todo -> In Progress`，成为当前推荐任务。
- README 增加 T7.1.2 合同和多格式图包入口；风险表新增 R-N143 与本 task 的不插入判断。
