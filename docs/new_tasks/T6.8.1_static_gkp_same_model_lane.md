# T6.8.1 静态 GKP same-model lane

- **Task ID**：T6.8.1
- **日期**：2026-07-17
- **状态**：Done

## 输入材料

- T6.7.1 576 条 smooth formal trajectory 与 Source Data；
- T3.1.5 top-K lattice-coset MAP 实现与验证；
- GKP、analog QEC、finite-energy decoding 与 prior calibration 一手来源。

## 实际完成内容

1. 建立 standard binning、static full joint MAP、top-K=4 static MAP、Route-A 与 hidden-state oracle 的 same-trace表；
2. 从 raw trajectory counts 重算 24-seed paired static-vs-Route-A bootstrap；
3. 穷举全部 1,048,576 个 10-bit syndrome pair，证明 K=4/full hard action 零差异；
4. 报告 `p_L,p_X,p_Y,p_Z`、average/p95/worst、family LER、oracle gap、wall-clock/update cost 与 top-K/full operation/storage proxy；
5. 用 machine claim registry 冻结 static superiority falsified、global SOTA/physical break-even prohibited；
6. 一手文献只作方法/口径映射，不导入跨模型 raw 数字。

## 产物路径

- `cnn_fpga/benchmark/static_gkp_same_model_lane.py`
- `tests/test_static_gkp_same_model_lane.py`
- `docs/t6_8_1_static_gkp_same_model_lane.json`
- `docs/t6_8_1_static_gkp_same_model_lane_source_data.csv`
- `docs/static_gkp_same_model_lane.md`

## 验证方式和结果

- verdict：`PASS_STATIC_GKP_SAME_MODEL_LANE_ROUTE_A_SUPERIORITY_FALSIFIED`；
- gates `11/11`，mutations `8/8`，focused tests `8 passed in 2.31s`；
- paired contrast `-2.454828e-5 [-3.959514e-5,-9.112888e-6]`；
- complete grid `1,048,576` points、`0` hard disagreement；
- K=4/full retained state `512/3200 bits`、serial proxy `424/2209`，均非实测。

## 简化实现 / 伪实现复核

未抽样少量 formal trace 来声称 top-K 等价。因为输入合同是完整有限 10-bit 网格，任务对整个可能输入域做了穷举，再继承 static full 的完整 28,311,552-decision error counts。soft LLR 差、operation proxy 与未板测边界分别保留。父报告、实现和派生 CSV 每次读取时重新核哈希；8 个 mutation 覆盖缩小 trace、oracle 混入、删 Pauli 指标、伪 grid 等价、隐藏 soft 差和伪实测。

## 风险复核

新增 R-N122：不能把 K=4 的 model-specific 数值等价写成 universal exact，也不能用 Route-A 较低 p95 掩盖更差 average/worst。

## 是否需要插入新 task

不插入。T6.8.2 继续处理一般 drift-adaptive 外部方法，T6.8.6/T6.9 处理硬件实证。

## 任务板同步

`T6.8.1 -> Done`；当前推荐任务更新为 `T6.8.2 In Progress`。

