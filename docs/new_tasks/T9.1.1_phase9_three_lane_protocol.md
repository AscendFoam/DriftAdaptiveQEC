# T9.1.1：Phase 9 三条独立 task signature、claim ladder 与结果门

- **Task ID：** T9.1.1
- **标题：** 冻结单轮 LER、六态长时 lifetime 与数字链 latency 三个不可混排的 task signature、claim ladder 和失败分支
- **日期：** 2026-07-22
- **状态：** Done
- **协议 verdict：** `PASS_PHASE9_THREE_INDEPENDENT_LANE_PROTOCOL_FROZEN`
- **Immutable analysis SHA-256：** `c88110375c358794339e72d672e4624871425fe480e5da091ddd1d6595255e18`

## 输入材料

- `docs/new_task_board.md` 的 Phase 9 / T9.1.1、T9.1.2—T9.1.4、T9.6、T9.7、T9.8；
- `docs/experiment_plan.md §20.1`、`§20.2`、`§20.6`、`§20.7`；
- `docs/new_risks.md` 的 R-N162、R-N167、R-N168；
- `docs/t6_16_2_comparison_ontology.json`、`docs/t6_20_2_dual_evidence_lane_contract.json` 与现有 claim-ladder / provenance / mutation 实现模式；
- T7.3.4/T7.3.5 已冻结的 postselection、physical break-even、official/project-native Puviani 和 pre-board RTL 边界。

## 执行前方案

1. 把本 task 定义为 outcome-blind 的协议冻结器，而不是性能实验；协议通过时不得生成任何 LER/lifetime/HIL 性能 GO。
2. 将 Phase 6D 的 13-field signature 扩展为 24 个字段，覆盖 code/state、physics、observation/history、action/cost、cycle、完整分母、baseline、compute/wall-clock、precision、split、统计单位、multiplicity、missingness 和 evidence grade。
3. 对三条 lane 分别冻结未来二值 `GO/NO-GO` evaluator；在 formal 未打开、资产缺失或协议不完整时使用 null/INCOMPLETE，而不是把未运行误写为负结果。
4. 把 evidence grade 定义为 claim-scope 集合资格，不建立“simulation < official source < board”的伪全局线性等级。
5. official Puviani、paper-constrained、project-native、pre-board、board-HIL、QPU 各自使用独立 namespace；缺 official asset 或高速板只关闭对应 claim。
6. 使用 semantic projection 绑定会持续更新的 task board/risk/experiment plan，使用 exact SHA-256 绑定 config 与实现；状态/日志变化不得造成伪失活，科学阈值或 source 变化必须失活。
7. 每个 machine gate 配置独立 substantive mutation，并补直接跨 lane、accepted-only、hidden-truth、core-as-raw-IQ、simulator-as-physical 等负向测试。

## 实际完成内容

### 1. 三条独立 lane

- `ROUND_LER_SINGLE_MODE`
  - single-mode square approximate GKP，完整 `+/-X,+/-Y,+/-Z`；
  - raw/recorded IQ 或冻结的 derived representation stratum，只允许 causal observed prefix；
  - trusted codebook ID、bounded residual、phase frame、reset/leakage FSM；
  - 主指标 `p_L,p_X,p_Y,p_Z,logical_PTM`，全部 state × trajectory × round 进入分母。
- `SIX_STATE_LOGICAL_LIFETIME`
  - 机器强制继承 LER lane 的 physics、observation、action、action cost、noise、observability、online privilege 与 precision；
  - `>=10^4` cycles，保存 `T_X,T_Y,T_Z,T_ch`、area/e-folding、survival 与 fit diagnostics；
  - 六态等权报告，但 promotion 取所有六态及注册 lifetime endpoint 的最小增益，禁止 average-only promotion。
- `RAW_IQ_DIGITAL_HIL`
  - 同时保留 `decoder_core`、`discriminator_output_to_action`、`adc_last_sample_to_trigger`、`raw_iq_source_to_trigger` 四个边界；
  - 主 speed claim 只消费最后一个边界；至少三 implementation seeds、`>=10^6` transactions、真实 recorded/raw IQ、same-task measured comparator 和零 mismatch/undefined/overflow/deadline miss。

### 2. 结果状态机与门

- 当前结果：
  - LER：`NOT_EVALUATED_NULL / null`；
  - lifetime：`NOT_EVALUATED_NULL / null`；
  - HIL：`MISSING_BOARD / null`。
- 只有 evidence 完整时才进入二值终态：
  - `GO_LER_SOTA / NO_GO_LER_SOTA`；
  - `GO_LIFETIME / NO_GO_LIFETIME`；
  - `GO_HIL_SPEED / NO_GO_HIL_SPEED`。
- 任一 required field、mandatory baseline、same-task comparator 或 evidence grade 缺失时保持 `INCOMPLETE` 且 `result_verdict=null`；只有完整 formal 的阈值失败才是 NO-GO。
- LER 门冻结为对**每个** eligible strongest deployable baseline：relative point `>=15%`、simultaneous relative LCB `>=10%`、absolute LCB `>0`，并同时通过 stationary/OOD 与 calibration/telegraph tail 安全门。
- lifetime 门冻结六态最小 point `>=15%`、simultaneous LCB `>0`、完整成本/fit/双 backend；HIL 门冻结三 seed、百万 transactions、II=1、WCET/deadline、same-task measured comparator 与 speed LCB `>0`。

### 3. 防混排与缺失证据语义

- 15 种 scoped evidence grade、4 类 baseline（只有 `MATCHED_DEPLOYABLE_RANKED` 可排名）、13 项 forbidden transfer；
- Puviani official exact/surpass、physical break-even、raw-IQ speed 当前全部为 null；
- `GO_HIL_ENGINEERING_NONRANKING` 即使成立也不能补 `GO_HIL_SPEED`；
- timeout/OOM/corrupt/missing tool/missing board/missing external asset 均保留，禁止 complete-case deletion、zero imputation 或有利子集发布；registered lifetime censoring 单独定义为有效观测。

### 4. cycle time 与双 backend

- 没有凭空复用 `9.848 us` 等单一文献常数；冻结 action-conditioned 公式：base sBs + measurement/readout + action-conditioned reset + control + fallback + idle。
- 组件数值保持 `null`，必须由 T9.2/T9.3 在 pilot 前不可变填充；缺数值只能形成 incomplete physical-time claim，不能填零。
- 两个 physics backend 必须各自通过全部主门，禁止平均两个 backend 结果掩盖一个失败，也禁止共享 kernel/likelihood/truth/RNG。

### 5. provenance、Source Data 与反变异

- `experiment_plan §20.1/20.2/20.6/20.7`、Phase9 task title/source 和 R-N162/R-N167/R-N168 风险语义采用 selective semantic binding；
- config 与实现采用 exact SHA-256；
- 66-row Source Data 可逐行 canonical hash 重算；
- 36/36 machine gates 和 36/36 targeted semantic mutations，包含 duplicate lane、缺 signature、hidden truth、accepted-only、cycle/core 边界偷换、grade 晋升、null-as-zero、Puviani/physical/HIL 伪 promotion 和 weighted score。

## 产物路径

- `configs/phase9/t9_1_1_three_lane_protocol.json`
- `cnn_fpga/benchmark/phase9_three_lane_protocol.py`
- `tests/test_phase9_three_lane_protocol.py`
- `docs/t9_1_1_three_lane_protocol.json`
- `docs/t9_1_1_three_lane_protocol_source_data.csv`
- `docs/phase9_three_lane_protocol.md`
- `README.md`

## 验证方式和结果

- 生成器：`python -m cnn_fpga.benchmark.phase9_three_lane_protocol`；
- verifier：`python -m cnn_fpga.benchmark.phase9_three_lane_protocol --verify`，8 项检查全 true；
- focused：`python -m pytest tests/test_phase9_three_lane_protocol.py -q`，`24 passed`；
- focused + task-board governance：`34 passed`；
- adjacent contract regression：comparison ontology、dual lane、claim boundary、time-cost、protocol hierarchy、T7 reviewer 与 Phase6D final gate 共 `158 passed`；
- 邻接回归首先按预期检出旧 exact-hash 依赖链 stale；依次重生成 conservative fallback、algorithm branch、time-cost、learned eligibility、pre-board profile、external normalization、secondary integrity、claim boundary、dual-lane matrix 与 Phase6D final gate 后全部闭合。原科学 verdict、逻辑错误/故障计数和效应方向保持不变；其中 pre-board host profile 按原协议重新执行了 `3 x 1000` 次 wall-clock 测量，因此相应 timing samples/summary、live provenance/hash、派生 Source Data 与 atlas 元数据按实更新，且仍不得解释为板测或硬件 SOTA；
- `git diff --check`：通过。

## 非简化实现复核

本 task 没有用三张静态表代替可执行协议。三条未来结果门有真实 evaluator 和 null/incomplete/complete 真值夹具；每个 gate 有独立可杀 mutation；Source Data、human contract、config、实现、任务板、风险和实验计划均有 live provenance。深审后额外修复了四个常见 demo 陷阱：

1. 协议 PASS 与性能 GO 分离；
2. 未运行/缺证据与完整实验后的 NO-GO 分离；
3. cycle time 未知值保持 null，并冻结 action-conditioned ledger；
4. board/risk/plan 使用 selective semantic binding，避免后续状态/时间戳重写导致伪 stale，同时科学阈值变化仍会失活。

此外，邻接测试没有通过放宽 hash gate 来“消除失败”，而是按依赖拓扑重建全部受影响派生产物；T7.3.5 的生命周期 gate 只放宽为允许 T9.1.3 合法地从 `Todo` 推进到 `In Progress/Done`，并新增回归测试证明该状态推进不会改写 T7 的历史证据或性能结论。

## 风险复核

- R-N168：`Open/Critical/Immediate -> Mitigated/Critical/Soon`；三 lane 的跨门补偿风险已由协议、36 mutations 和 null 状态机实质缓解，但 T9.6/T9.8 仍需消费 immutable hash。
- R-N162：保持 `Open/Critical/Immediate`；official Puviani 四类资产仍缺失，T9.1.2 继续局部 Blocked。
- R-N167：保持 `Open/High/Soon`；高速实物板和 same-task measured comparator 仍缺失。
- 未新增风险；下游静默分叉通过 Phase9 总规则“必须消费 T9.1.1 analysis hash”防止。

## 是否需要插入新 task

不需要。T9.1.3 正常承接 paper-constrained Puviani artifacts；T9.2/T9.3 承接 cycle components/physics/codebook；T9.6/T9.7/T9.8 承接 formal、HIL 和三门终态。新增求正、替代 official asset 或伪 measured 的旁路会破坏本 seal。

## 对任务板的同步

- `T9.1.1`：`In Progress -> Done`；
- `T9.1.2`：继续 `Blocked`；
- `T9.1.3`：`Todo -> In Progress`，成为当前推荐任务；
- 任务板记录 66-row Source Data、36/36 gates/mutations、当前三个 null result 与 immutable analysis hash。
