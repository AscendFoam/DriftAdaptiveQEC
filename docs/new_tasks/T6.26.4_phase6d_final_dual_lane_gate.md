# T6.26.4：Phase 6D 最终双 lane GO/NO-GO

- **Task ID：** T6.26.4
- **标题：** 独立消费 multimode 与 single-mode RTL gate，输出 Phase 6D 终态并移交 Phase 7
- **日期：** 2026-07-21
- **状态：** Done

## 输入材料

- T6.26.3 的 10-claim/12-element 不可迁移 matrix；
- multimode 直接门：T6.24.5 task status、T6.20.4 strongest-baseline headroom report/raw/config/code/cancellation ledger；
- RTL 直接门：T6.25.2 formal、T6.25.3 million-cycle CXXRTL、T6.25.4 exact-top hardware report/raw/config/code/netlist；
- learning 直接门：T6.26.2 task status；
- T6.9.2 board blocker；
- T7.1.1--T7.1.4 historical restricted snapshots 与 T7.1.5/T7.2.6 delta tasks。

## 执行方案与实际完成内容

1. 冻结四项布尔真值表：`TT→GO_TWO_LANE`、`TF→GO_MULTIMODE_ONLY`、`FT→GO_RTL_ONLY`、`FF→NO_GO`；learning 不进入真值表。
2. 不复用 T6.26.3 的综合标签作判决，而是直接重算：
   - multimode 只认 T6.24.5：当前 `Dropped`，T6.20.4 strongest static-mixture exact MLD 与 proposed 均 `p_L=0.111979`，point/LCB=`0%/0%`，pilot/formal 未访问，因此 `gate_passed=false`；
   - RTL 只认 T6.25.4，并同时要求 T6.25.2 formal 与 T6.25.3 long-run live PASS：当前 `gate_passed=true`；
   - learning 读取 T6.26.2=`Dropped`，输出 `DROPPED_ABLATION_ONLY` 且 `changes_overall_verdict=false`。
3. 由 `multimode=false,rtl=true` 独立查表得到 `GO_RTL_ONLY`；没有加权总分、tie-break 或跨 lane rescue。
4. 将 T6.26.3 的 10 个 parent claim 全量转为 final claim：逐条提供 current evidence、blocking gaps、revocation conditions、paper placements、final/forbidden wording 和 parent payload hash。
5. Phase 7 移交：
   - T7.1.5=`READY_AFTER_T6_26_4`，为下一任务；
   - T7.2.6 等待 T7.1.5；
   - T7.3.8 写 strongest-baseline negative answer；
   - T7.3.9 写 contract bridge 而非共同性能分母；
   - T7.1.1--T7.1.4 保持历史快照，不允许无 delta 发布。
6. 生成 54-row lossless Source Data：3 lane decision + 10 final claim + 6 Phase7 handoff + 4 truth-table rows + 31 artifact bindings；逐行 canonical JSON/hash 重建。
7. 建立 21 项 gate 和 22 项 semantic mutation，覆盖 lane/overall/truth-table/learning/board/speed/SOTA/claim disposition/gap/revocation/placement/artifact/handoff/snapshot/CSV 伪造。

## 产物路径

- 实现与配置：
  - `cnn_fpga/benchmark/phase6d_final_dual_lane_gate.py`
  - `configs/phase6d/t6_26_4_final_dual_lane_gate.json`
- 测试：`tests/test_phase6d_final_dual_lane_gate.py`
- 报告：
  - `docs/t6_26_4_final_dual_lane_gate.json`
  - `docs/t6_26_4_final_dual_lane_gate_source_data.csv`
  - `docs/phase6d_final_dual_lane_gate.md`

## 验证方式和结果

- 生成：`python -m cnn_fpga.benchmark.phase6d_final_dual_lane_gate`；
- 验证：`python -m cnn_fpga.benchmark.phase6d_final_dual_lane_gate --verify`；
- verdict=`GO_RTL_ONLY`，truth key=`multimode=false,rtl=true`；
- lane decisions：MM=`NO_GO`、RTL=`GO`、learning=`DROPPED_ABLATION_ONLY`；
- 21/21 gates、22/22 semantic mutations；T7.1.5 状态移交后的 lifecycle-normalized analysis SHA-256=`f804110b11742f1343dc37a4825de353e60871f45147e8c0c5bc76369f19135d`；
- 16 focused tests；与 T6.26.3、headroom、formal/long/P&R、board blocker、旧 claim/main-figure 和任务板治理的 129 项联合回归通过。

## 深度检查与非简化实现复核

- 首次运行因错误假设 formal 模块导出 `VERDICT` 常量而 fail-fast；改为在当前 gate 内冻结 exact verdict literal，同时仍执行 formal verifier，没有绕过父门。
- 判决直接读取 T6.24.5/T6.25.4/T6.26.2 状态和各自 raw-derived reports；T6.26.3 只作为逐 claim payload/hash anchor，不能自己投票决定 overall。
- 真值表完整列出四种组合；当前 verdict 从两个 primary boolean 重算，learning 字段完全排除。mutation 分别翻转 MM/RTL、改真值表、让 learning 影响 verdict，均被拒绝。
- 初版 board snapshot 把 T7.1.5 固定为 `Todo`，完成本 task 后正常切换 `In Progress` 会导致报告自失效；T7.1.5 深审又发现其完成并启动 T7.2.6 时存在同类问题。现仅对 T7.1.5/T7.2.6 的正常生命周期规范化为 `TODO_ACTIVE_OR_DONE`，仍拒绝 Blocked/Dropped/Missing；终门数值、lane verdict 与 claim disposition 均未改变。
- `GO_RTL_ONLY` 只表示 exact single-mode pre-board lane 通过，不表示 universal Phase6D、整篇论文、真实板卡或 speed SOTA GO；publication boundary 将这些字段全部固定为 false。
- 每条 final claim 的缺口与撤销条件均为非空；不能只留下正 wording。MM task-local positive、strongest NO-GO 与 SOTA blocked 三条同时保留。
- board fields 继续全 null，T6.9.2 继续 Blocked；observability-harness Fmax caveat 未被 verdict 吞掉。
- 31 个父/历史 artifact 均 live hash-bound；Source Data 不只计数，而是逐行重建。

## 风险复核

- R-N147：保持 Mitigated/Monitor。最终 gate 使用两个独立 boolean，无 global score 或 cross-lane rescue。
- R-N158：保持 Open/High/Soon。T6.26.4 已冻结终态，但旧 T7 bundle 仍需 T7.1.5/T7.2.6 delta 才可发布。
- 新增 R-N159（Open/High/Soon）：`GO_RTL_ONLY` 可能被误写成“Phase6D/论文/硬件整体 GO”。T7.1.5/T7.2.6 必须把 exact pre-board scope 写进摘要、图题和 conclusion，不能省略 MM NO-GO/board-null。

## 是否需要插入新 task

不插入。R-N158/R-N159 已由下一顺序 T7.1.5、T7.2.6、T7.3.8--T7.3.9 与 T7.4 直接承接；新增并行发布任务会形成绕过 delta 的风险。

## 对任务板与论文 claim 的同步

- T6.26.4：`In Progress -> Done`；T7.1.5：`Todo -> In Progress`；
- Phase6D 当前终态：`GO_RTL_ONLY`；
- 允许：single-mode exact-top deterministic/atomic/fail-closed pre-board contribution；
- 必须同时写：multimode v1 strongest-baseline NO-GO、T6.18.3 仅 task-local context、learning Dropped、board-null、无 fastest/SOTA、current RTL 不执行 multimode decoder。
