# T6.7.4 Route-A promotion / falsification gate

- **Task ID**：T6.7.4
- **日期**：2026-07-17
- **状态**：Done

## 输入材料

- T6.5.3 预注册与 T6.6.3 V4 lock；
- T6.7.1 smooth formal JSON / 498,240-row Source Data；
- T6.7.2 abrupt/OOD/nominal JSON / 686,104-row Source Data；
- T6.7.3 integrated CXXRTL JSON / 131 MB trace；
- T6.6.1 legacy CNN matched-budget 资格结果。

## 实际完成内容

1. 实现独立 promotion/falsification runner，直接从 1,464 条 raw trajectory counts 重算统计分析与 20,000 次 cluster bootstrap；
2. 重新计算三个父任务的 evidence gates，并检查两份大 CSV、百万周期 trace、父 artifact 与当前源码哈希；
3. 建立 8 项 claim registry，把 restricted system GO 与 global/static/tail/hardware 强主张分开；
4. 建立 fail-closed current-state verifier 和 8 项 semantic mutation audit；
5. 明确分支：Route-A 合同系统受限晋级，CNN 降为消融；不触发 smooth-only 或全 static 退化，但 global rank 不晋级。

## 产物路径

- `cnn_fpga/benchmark/route_a_promotion_gate.py`
- `tests/test_route_a_promotion_gate.py`
- `docs/t6_7_4_route_a_promotion_gate.json`
- `docs/t6_7_4_route_a_promotion_gate_source_data.csv`
- `docs/route_a_promotion_falsification_gate.md`

## 验证方式与结果

- runner：`GO_ROUTE_A_CONTRACT_SYSTEM_RESTRICTED_SIMULATOR_AND_PREBOARD_CLAIMS`；
- promotion gates：`10/10`；semantic mutations：`8/8`；
- focused tests：`7 passed in 32.31s`；
- primary contrast：`2.168726e-5 [1.900284e-5, 2.454828e-5]`；
- strongest deployable：Window MAP `8.964185e-4`，Route-A `9.927396e-4`；
- tail confirmed-average-improvement family：`0/6`；五个 family 与 EWMA average 完全相等。

## 简化实现 / 伪实现复核

首版汇总器有两个不足并已修复：一是错误假设 RTL mismatch 存在 aggregate 字段，现改为强制逐 family 求和，缺键直接报错；二是只保存生成时的 expected/observed 哈希，不能发现之后的文件漂移，现改为每次 `verify_report` 都重新读取并核对父 artifact、Source Data、trace、runner 与派生 CSV。测试包含 raw parent analysis 全重算，不是仅检查 `PASS` 字符串。

## 风险复核

新增 `R-N121`：restricted GO 容易被误写为 global SOTA。T6.8 必须继续保留 static/Window 负结果，三条外部 lane 不混排。

## 是否需要插入新 task

不插入。T6.8.1—T6.8.7 已覆盖外部比较与 integration bridge，T6.9 已覆盖复现包、板测和最终论文门。

## 对任务板的同步

`T6.7.4 -> Done`，Milestone 6.7 完成；当前推荐任务更新为 `T6.8.1 In Progress`。

