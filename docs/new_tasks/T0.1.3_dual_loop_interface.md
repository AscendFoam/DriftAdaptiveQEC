# T0.1.3 完成记录：双回路接口冻结

- **Task ID：** `T0.1.3`
- **标题：** 冻结 host slow estimator 与 FPGA deterministic fast path 的 `DLIF-v1`
- **日期：** 2026-07-14
- **状态：** `Done`

## 输入材料

- `docs/new_task_board.md` T0.1.3；
- T0.1.1 scope 与 T0.1.2 platform abstraction；
- `cnn_fpga/runtime/param_bank.py`、`scheduler.py`、`fast_loop_emulator.py`、`slow_loop_runtime.py`、`feature_builder.py`；
- `cnn_fpga/decoder/linear_runtime.py`、`param_mapper.py`；
- `cnn_fpga/config/hardware_hil.yaml`、`p4_multiscenario.yaml`；
- 现有 runtime README、论文 dual-loop/claim 边界材料。

## 实际完成内容

1. 冻结 FPGA fast path、host slow path 和 atomic bank update 的职责；
2. 定义 `DLIF-v1` 的 per-cycle、window、parameter-update 三类接口；
3. 明确 Q4.20 在当前仓库中为 `signed(25, frac=20)`；
4. 冻结 `K[2x2]+b[2]`、syndrome、correction、version/epoch/status/header 的位宽；
5. 把当前 `float32[32,32]` histogram 与目标 `uint16 raw counts + n_valid` wire format 分层；
6. 冻结 fast/window/slow/commit 更新频率与预算证据边界；
7. 建立 boot、slow failure、bad CRC/version、stale bank、invalid input、saturation、deadline miss、host timeout、queue full、ack timeout 的 fallback policy；
8. 冻结 CRC/CAS/apply-epoch/read-back/rollback 等原子一致性规则；
9. 保持 estimator model-agnostic，不预设 CNN 胜出。

## 产物路径

- `docs/tasks/T0.1.3_dual_loop_interface.md`；
- `docs/new_tasks/T0.1.3_dual_loop_interface.md`；
- `docs/new_risks.md`；
- `docs/new_task_board.md`。

## 验证方式和结果

1. 源码审计：逐项核对 A/B bank、stage/commit、scheduler events、slow modes、Q4.20 与 runtime config；通过。
2. focused test：`C:\ProgramData\anaconda3\python.exe -m pytest -q tests\test_statcalib_runtime_smoke.py`，结果 `4 passed in 0.27s`。
3. direct bank smoke：stage version 1 到 inactive bank，epoch 1 不提交，epoch 2 commit，active version 变 1；通过。
4. direct fixed-point smoke：Q4.20 对 `[0.2,-0.1]` 和 `K=0.5I,b=[0.01,-0.02]` 输出 `[0.1100006103515625,-0.07000064849853516]`，无 saturation；通过。
5. 文档机械检查：三类接口均覆盖 input/output/frequency/bit width/fallback/current status；通过。

## 非简化实现复核

- 接口字段来自当前运行代码和配置，不是脱离仓库的理想化表格。
- 现有 A/B bank 和 fixed-point reference 被复用；未另造 demo runtime。
- 明确揭示当前 CRC/stale/deadline-action/rollback 缺失，未用 scheduler event 冒充安全 fallback 已执行。
- 为硬件友好 raw-count histogram 冻结了迁移 contract，同时保留当前 float32 adapter 兼容性。
- CNN 只是 slow estimator 候选，后续必须与强传统模型同预算比较。

## 风险复核

- 新增 R-N007：当前 runtime 多数 safety fault 只记录，未触发可执行 fallback；`High / Soon`，由 T4.2/T4.3/T6 关闭。
- 新增 R-N008：当前 float32 histogram transport 与目标 raw-count hardware wire format 不一致；`Medium / Soon`，由 T4/T5.5/T6 的 adapter/bit-accurate 验证关闭。
- R-N006 保持 Open：所有 timing 仍是 config/model，不是板级测量。
- R-N002 保持 Open；focused explicit-path pytest 可运行，但根级收集仍未修复。

## 是否需要插入新 task

不插入。R-N007/R-N008 已由正常 T4/T5.5/T6 明确覆盖，且不阻塞下一阶段文献矩阵任务；若首个依赖全仓回归的代码 task 被 R-N002 阻塞，再按规则插入。

## 现有代码冲突、行为变化与论文 claim 影响

- 未修改 runtime 行为；本 task 冻结 v1 target contract。
- 当前 default identity bank 不再被视为正式 safe fallback；正式实验必须显式提供预验证 safe-static bank。
- 当前 float32 histogram 是 compatibility representation，不是已冻结的硬件最佳格式。
- 论文可声称提出了可审计 dual-loop/stage-commit contract；不能声称 CRC/stale/deadline fallback 已在 RTL/board 实现或测量。

## 任务板同步

- T0.1.3：`Todo -> In Progress -> Done`；
- 当前推荐任务：`T0.2.1`；
- 进度日志已写入 focused tests、direct smoke 和开放安全差距。

