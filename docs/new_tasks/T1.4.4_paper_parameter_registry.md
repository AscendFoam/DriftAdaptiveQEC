# T1.4.4 完成记录：Paper-parameter registry

**Task ID：** T1.4.4  
**标题：** 建立核心实验与近邻理论的 paper-parameter registry  
**日期：** 2026-07-14  
**状态：** Done

## 输入材料

- `docs/new_task_board.md` T1.4.4；
- Campagne 2020、Sivak 2023、Puviani 2025、GKP 2001 本地全文/OCR；
- Wan 2020、Lachance 2024 repository reading cards；
- literature matrix、Zotero BibTeX、current HIL YAML 和 GKP constant。

## 实际完成内容

- DOI-first 核验 6 个论文 formal records，并避免 arXiv/formal 重复计数；
- 建立 8 sources / 51 parameters 的 JSON registry；
- 分类为 30 primary facts、3 secondary references、9 modeling assumptions、
  9 pending calibration gates；
- 覆盖 protocol、timing、readout、misclassification、reset、drift、leakage、
  squeezing/noise 和 logical metrics 九类；
- 每条非 pending 记录绑定本地路径、行号、原文 fragment；
- 每条记录保存 normalization、allowed/forbidden transfer 与 calibration gate；
- 额外保留 Sivak `1546/1548 ns` source discrepancy、双 cycle 口径、Puviani
  numerical idealization 和 best-of-20 caveat；
- 14 项 tests 直接检查原文锚、formal DOI 去重、分类层级、Markdown/JSON 同步、YAML/constant
  live values 和 `null + gate` fail-closed 状态。

## 产物路径

- `docs/paper_parameter_registry.md`
- `docs/paper_parameter_registry.json`
- `tests/test_paper_parameter_registry.py`
- `docs/tasks/T1.4.4_paper_parameter_registry.md`
- `docs/new_tasks/T1.4.4_paper_parameter_registry.md`

## 验证方式和结果

- focused：`14 passed`；
- T1.4.1--T1.4.4 adjacent：`42 passed`；
- 显式 `tests/`：`232 passed, 4 failed`；4 项仅为 R-N012 的两份旧 FR8/P4 文档缺失。

## 风险复核

- R-N003 更新：paper/project normalization 已分开，完整 conversion 仍为 `PC-N01=null`；
- R-N006 更新：constituent/full/model/runtime timing 已分开，实板仍为 `PC-T01=null`；
- R-N010 更新：核心 formal records 已去重，更广 2025--2026 metadata 仍需冻结前刷新。

## 是否需要插入新 task

不需要。九类缺失均已有 T2/T6/T7 正常任务和逐项 calibration gate，不阻塞 T1.4.5。

## 对论文 claim 的影响

允许写 source-grounded parameter registry 与 protocol/timing/readout taxonomy；禁止把论文设备值
迁移为项目实测，禁止把 Puviani numerical study 写成实验，禁止用 secondary card 支持强数值，
禁止在 `PC-*` 仍为 null 时声称 physical break-even、quantum readout/reset/leakage 或实板闭环。

## 对 `docs/new_task_board.md` 的同步说明

- T1.4.4：`In Progress -> Done`；
- T1.4.5：`Todo -> In Progress`；
- 当前推荐任务：`T1.4.5`。
