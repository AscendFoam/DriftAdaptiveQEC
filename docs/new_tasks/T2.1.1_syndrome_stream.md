# T2.1.1 完成记录：连续漂移与离散 regime syndrome stream

**Task ID：** T2.1.1  
**标题：** 完整 `DriftState` 驱动的 mixed-state syndrome stream generator  
**日期：** 2026-07-14  
**状态：** Done

## 输入材料

- `docs/new_task_board.md` T2.1.1；
- `physics/drift_processes.py`；
- `physics/ideal_gkp_decoder.py`；
- `physics/sbs_observation_reset.py`；
- `docs/protocol_hierarchy.md/.json`。

## 实际完成内容

- 完整消费 continuous mean/covariance/loss/outlier 与 discrete regime/burst/provenance；
- 实现 causal loss/channel/modular/measurement/recovery/leakage/logical stream；
- 分离 deployable observed record 与 read-only truth record；
- 输出 analog/residual syndrome、g/e/leakage、phase、runs、recovery depth 和 true logical label；
- 修复 leakage-ended recovery deadlock 与双轴误纠正风险；
- 增加统计长跑、负路径、schema、logical、seed/prefix 和 public-export tests；
- 同步 protocol machine contract 和中文模型说明。

## 产物路径

- `physics/syndrome_stream.py`
- `physics/__init__.py`
- `tests/test_syndrome_stream.py`
- 更新 `tests/test_protocol_hierarchy.py`
- 更新 `tests/test_sbs_error_space.py`
- 更新 `tests/test_sbs_observation_reset.py`
- `docs/syndrome_stream_model.md`
- 更新 `docs/protocol_hierarchy.md/.json`
- `docs/tasks/T2.1.1_syndrome_stream.md`
- `docs/new_tasks/T2.1.1_syndrome_stream.md`
- 更新 `README.md`

## 验证方式和结果

- focused：`21 passed`；
- protocol+stream：`35 passed`；
- adjacent stack：`161 passed`；
- SBS focused stack：`76 passed`；
- 显式 `tests/`：`390 passed, 4 failed`，失败仅为 R-N012；
- `py_compile` 与 protocol JSON validation 通过。

## 风险复核

新增 R-N031：本 stream 的 depth/recovery/leakage kernel 是显式项目 assumption，不是装置标定；当前逐周期对象路径也不是 T2.1.3 要求的高速/rare-event production engine。hidden truth 虽与 deployable schema 分离，后续 controller/benchmark 仍需禁止错接。

## 是否需要插入新 task

不需要。T2.1.2/T2.1.3 已直接覆盖 controller memory 与高速 rare-event simulation；T2.2/T2.3/T6 覆盖 finite-energy、ancilla/Fock、校准和硬件证据。

## 对论文 claim 的影响

允许写“a causal, reproducible syndrome-level stream consumes the complete synthetic drift state while structurally separating deployable observations from simulator truth”。禁止写真实 cavity--transmon dynamics、实验误差率复现或 hard-real-time 性能。

## 对 `docs/new_task_board.md` 的同步说明

- T2.1.1：`In Progress -> Done`；
- T2.1.2：`Todo -> In Progress`；
- 当前推荐任务：`T2.1.2`。
