# T3.1.1 完成记录：standard binning

- **Task ID：** T3.1.1
- **标题：** 实现并接入 standard binning
- **日期：** 2026-07-15
- **状态：** Done

## 输入材料

T1.1.1 standard-binning reference、T1.3.4 paired alignment harness、当前 sensitivity artifacts、
legacy P4 frozen runner 与新任务板通过标准。

## 实际完成内容

新增 observed-only fixed half-cell decision、hidden-truth paired evaluator、无调参 baseline descriptor、
major-comparison registry 和 production audit。当前 T1.3.4 主比较已从四行扩为五行；未来 T5
主 decoder schema 被锁定必须包含 standard。旧 P4 `static_linear` 保持 parameter-estimator 语义。

## 产物路径

- 实现：`cnn_fpga/benchmark/standard_binning_baseline.py`、
  `cnn_fpga/benchmark/adaptive_drift_alignment.py`；
- 测试：`tests/test_standard_binning_baseline.py`、`test_adaptive_drift_alignment.py`；
- 报告/机器证据：`docs/standard_binning_baseline.md`、
  `docs/t3_1_1_standard_binning_validation.json`；
- 详细任务记录：`docs/tasks/T3.1.1_standard_binning_baseline.md`。

## 验证方式和结果

- 10/10 production gates；72,000 paired samples；
- standard LER `0.060417`，旧 static-calibration MAP `0.061389`；paired 差
  `-0.000972 [-0.001280,-0.000664]`，反证原样保留；
- focused+adjacent `92 passed`；source hash 与 artifact 一致；
- 全量 `817 passed, 4 skipped, 4 failed`；4 个失败均为 R-N012 既有 FR8/P4 文档缺失；
- `compileall` 与 `git diff --check` 通过；
- boundary/truth isolation/registry mutation/accounting/reproduction 全覆盖。

## 风险复核

新增 R-N047，要求 T3.1.2 不能沿用旧 static calibration 冒充 formal training-average static MAP，
也不能删除 standard 行隐藏 counterevidence。

## 是否需要插入新 task

不插入。R-N047 由紧接的 T3.1.2 正常任务直接承接。

## 对论文 claim 的影响

允许有边界的 synthetic decoder-integration/counterevidence；禁止 general MAP、hardware 或 QPU claim。

## 对 `docs/new_task_board.md` 的同步说明

T3.1.1 更新为 Done，T3.1.2 更新为 In Progress，当前推荐切换至 T3.1.2。
