# T2.3.8 实现 Heisenberg noise-transfer 中保真度代理

## 基本信息

- **Task ID：** T2.3.8
- **标题：** 实现 Heisenberg noise-transfer 中保真度代理
- **日期：** 2026-07-14
- **状态：** Done
- **顺序说明：** 为解除 T2.3.3 的依赖倒置前移执行，不改变既有 ID/范围

## 输入材料

- `docs/new_task_board.md` T2.3.8；
- `docs/experiment_plan.md` §16.1；
- Ralph et al. 2024 noise-transfer primary paper；
- T1.2.1 normalized damped-projector state 与 T2.3.1 finite-Fock reference。

## 实际完成内容

完成 lattice signal、continuous covariance、logical jump 分离；loss、measurement
efficiency、matrix feedforward gain；exact 1D Gaussian cell/alias moments；diagonal Pauli law
与 correlated Fréchet-only fail close；seedable sampling；dB→Delta mapping；四逻辑态
state/Fock q-domain alignment；3--12 dB production sweep、200k MC 与 45 项 direct tests。

## 产物路径

- `physics/noise_transfer_surrogate.py`
- `tests/test_noise_transfer_surrogate.py`
- `docs/noise_transfer_surrogate.md`
- `docs/t2_3_8_noise_transfer_validation.json`
- `docs/tasks/T2.3.8_noise_transfer_surrogate.md`
- `docs/new_tasks/T2.3.8_noise_transfer_surrogate.md`

## 验证方式和结果

- `python -m pytest tests/test_noise_transfer_surrogate.py -q`：`45 passed`；
- adjacent Fock/finite-squeezing/protocol slice：`194 passed`；
- full `tests/`：`675 passed, 4 failed`，失败仍仅 R-N012；
- `python -m physics.noise_transfer_surrogate --output docs/t2_3_8_noise_transfer_validation.json --monte-carlo-samples 200000 --seed 2380`：14/14 gates PASS；
- 独立 quadrature 与 analytic alias probabilities/moments 相符；
- 10/12 dB state/Fock q-domain 对齐通过；3 dB 明确暴露 `29.79%` proxy 偏差与
  `42.33%` 四态 spread；
- MC alias 最大 z `1.3966`，covariance relative error `0.002934`。

## 风险复核

新增 R-N040。T2.3.8 关闭 R-N039 的 dependency inversion，但不关闭 protocol-specific
SBS、完整 q/p coherent fidelity、correlated joint Pauli rate、Fock cutoff 或 device
calibration 风险。

## 是否需要插入新 task

不需要。上述边界是 T2.3.3 的正常误差归因输入，且后续 T5/T6 已承接 channel/device
evidence，不应扩大当前任务。

## 对论文 claim 的影响

允许：Heisenberg-inspired operational-coordinate middle-fidelity surrogate；在预注册门控
下，约 10 dB 及以上与 state/Fock q-domain moments 对齐。

禁止：paper-exact teleportation/SBS channel、完整逻辑 fidelity、装置校准、真实硬件结果。

## 对 `docs/new_task_board.md` 的同步说明

- T2.3.8：`In Progress -> Done`；
- T2.3.3：`Blocked -> In Progress`；
- 当前推荐任务恢复为 `T2.3.3`。
