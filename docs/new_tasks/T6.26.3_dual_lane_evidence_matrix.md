# T6.26.3：双 lane claim-evidence / Source Data / 主图合同

- **Task ID：** T6.26.3
- **标题：** 生成不混淆的双 lane claim-evidence matrix、无损 Source Data 和主图合同
- **日期：** 2026-07-21
- **状态：** Done

## 输入材料

- T6.20.2 双证据 lane/task-signature/forbidden-transfer 合同；
- T6.18.3 opened project-native multimode task-local positive 结果；
- T6.20.4 strongest retained development baseline 下的 causal-headroom NO-GO 与 v1 cancellation ledger；
- T6.25.2 formal/property、T6.25.3 1,000,000-cycle CXXRTL、T6.25.4 exact-top 三 seed P&R；
- T6.24.5、T6.26.1、T6.26.2 的 Dropped 状态及 T6.9.2 board blocker；
- 既有 T7.1.1--T7.1.4 restricted snapshot，仅作兼容回归，不在本 task 中覆盖。

## 执行方案与实际完成内容

1. 冻结三个 panel：
   - `MULTIMODE_SOFTWARE` 只允许 LER/TAIL/SCALING/COMPUTE/EVIDENCE_STATE；
   - `SINGLE_MODE_RTL` 只允许 cycle/II/property/CXXRTL/post-route/resource/board-null；
   - `LEARNING_EXTENSION` 只允许 STATUS/EVIDENCE_STATE。
2. 建立 10 条原子 claim：
   - 同时保留 T6.18.3 task-local positive 与 T6.20.4 strongest-baseline zero-headroom mandatory negative；
   - 将 frozen-benchmark multimode SOTA 标成 `BLOCKED_NOT_RUN`，不让 opened 结果顶替未运行的 T6.24.5；
   - 将同一 exact single-mode top 的 six-cycle/II=1、atomic/fail-closed、million-cycle CXXRTL 和 post-route estimate 标为 `CURRENT_RESTRICTED`；
   - board measurement 为 null、speed advantage 为 prohibited；
   - CNN/student 为 `DROPPED_ABSENT`，不是第三 primary lane；
   - 明确禁止跨 lane weighted score 和 gate substitution。
3. 建立 12 个主图元素，并在反简化审查中把混装字段拆开：multimode LER/TAIL/COMPUTE 分元素，RTL POST_ROUTE/RESOURCE 分元素；每个元素都有独立 allowed/forbidden wording。
4. 建立 10 条 forbidden transfer，覆盖 MM LER→RTL、RTL latency→MM、opened-positive→SOTA、RTL safety 救援 MM NO-GO、LER 补 RTL property、learning 补任一 primary lane、pre-board→measured、harness Fmax→bare core 与跨 lane 总分。
5. 为每条 claim 和每个图元素强制提供 `reports/raw_data/configs/code/sources/selectors` 五类证据键；全部指向 35 个 SHA-256/bytes live artifacts。
6. 生成 67-row Source Data：10 claim + 12 figure element + 10 forbidden transfer + 35 artifact binding。每行保存完整 canonical payload JSON 与独立 payload SHA-256，生成器会重新读回并逐行比较。
7. 建立 21 项 gate 和 21 项 semantic mutation，覆盖 SOTA/fastest/board/learning promotion、删除 strongest denominator、伪造 headroom/Fmax、跨 namespace、跨 lane edge、坏 hash、缺 config、缺 allowed wording 与伪造 CSV rows。

## 产物路径

- 实现与配置：
  - `cnn_fpga/benchmark/phase6d_dual_lane_evidence_matrix.py`
  - `configs/phase6d/t6_26_3_dual_lane_evidence_matrix.json`
- 测试：`tests/test_phase6d_dual_lane_evidence_matrix.py`
- 报告：
  - `docs/t6_26_3_dual_lane_evidence_matrix.json`
  - `docs/t6_26_3_dual_lane_evidence_source_data.csv`
  - `docs/phase6d_dual_lane_evidence_matrix.md`

## 验证方式和结果

- 生成：`python -m cnn_fpga.benchmark.phase6d_dual_lane_evidence_matrix`；
- 验证：`python -m cnn_fpga.benchmark.phase6d_dual_lane_evidence_matrix --verify`；
- verdict：`PASS_NONTRANSFERABLE_DUAL_LANE_EVIDENCE_AND_FIGURE_CONTRACT`；
- 21/21 gates、21/21 semantic mutations；analysis SHA-256=`a6c1ee637ada6b0f65bbd130dd2dd5966f808666ba1c95cf224dd5a2a0ee4965`；
- lane outcomes：
  - multimode=`NO_GO_PRIMARY_WITH_OPENED_TASK_LOCAL_CONTEXT`；
  - single-mode RTL=`GO_EXACT_TOP_PREBOARD_HARDWARE_LANE`；
  - learning=`DROPPED_ABSENT`；
- 15 focused tests；父 contract/headroom/formal/long/P&R、旧 claim/main-figure contract 与任务板治理共 122 项联合回归全部通过。

## 深度检查与非简化实现复核

- 首轮 mutation audit 为 19/20；`forge_fmax` 能改 parent summary 而不改 claim 内数值，暴露“双真相”缺口。新增 parent-summary/claim/raw 三向一致性后 mutation 被拒绝，没有删除或放宽它。
- 初版图元素把 LER 与 tail、post-route 与 resource 混在单一 namespace。现拆成独立元素并对 namespace 白名单做 gate，避免图中用一个 label 隐藏不同证据边界。
- 初版 Source Data 只检查 hash/rows；现由生成器重新读取 CSV，并与 67 个 canonical payload 逐行完全比较，同时重算每行 payload hash。
- 初版 figure element 只有 annotation/forbidden interpretation；现显式增加 allowed wording，并新增删除 allowed wording mutation。
- 所有父 verifier 在生成前实时执行；父 verdict、live artifact hash、task status canonical snapshot、claim value 与图元素均独立交叉检查。
- 没有绘制会暗示跨 lane 因果或排名的箭头，没有输出 LER+latency 加权总分；CNN/student inset 只有 Dropped/absent 状态。
- 没有把 T6.18.3 的 27.3% task-local gain 写成 strongest-baseline/SOTA；T6.20.4 的 `0% [0%,0%]` headroom 和 formal/pilot 未访问是强制可见负证据。
- 没有把 36.794 MHz observability-harness Fmax 写成 bare core/board speed；所有 physical fields 保持 null。

## 风险复核

- R-N147：保持 Mitigated。10 claim、12 figure element、10 forbidden transfer 和无跨 lane edge/global score 已将两个 primary lane 分开。
- R-N151：保持 Mitigated。learning 仅 `DROPPED_ABSENT` inset，无法成为第三 primary lane或救援主门。
- R-N153：保持 Mitigated。opened task-local positive 与 strongest-baseline NO-GO 同屏，frozen/universal SOTA 保持关闭。
- 新增 R-N158（Open/High/Soon）：既有 T7.1.1--T7.1.4 是 Phase6D 前的 restricted snapshot；若跳过 T6.26.4/T7 delta 直接发布旧图文，会漏掉最新 dual-lane verdict。既有 T7.1.5/T7.2.6/T7.3.8--T7.3.9/T7.4 已承接，不新增旁路。

## 是否需要插入新 task

不插入。下一顺序 T6.26.4 将独立消费两条 lane 并给 Phase6D 终态 verdict；T7.1.5/T7.2.6 已负责把新矩阵作为 delta 写入论文。另开 task 会与既有终态门重复。

## 对任务板与论文 claim 的同步

- T6.26.3：`In Progress -> Done`；T6.26.4：`Todo -> In Progress`；
- 当前论文可并列写 single-mode RTL pre-board hardware-lane GO 与 multimode v1 strongest-baseline NO-GO/task-local positive context；
- 禁止将二者相加、互补或暗示现有 RTL 部署 multimode decoder；最终 Phase6D GO/NO-GO 仍由 T6.26.4 决定。
