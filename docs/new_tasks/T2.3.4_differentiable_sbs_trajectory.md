# T2.3.4 完成记录：短时域可微 sBs trajectory simulator

- **Task ID：** T2.3.4
- **日期：** 2026-07-14
- **状态：** Done

## 输入与目标

按本地 Puviani et al. 一手全文的 physical gates、Table S1 时序、Feedback-GRAPE
probability-gradient 和 Table S4 15 参数合同，实现可控 cutoff 的短时域随机 `g/e`
联合 cavity--ancilla simulator；输出 reward、trajectory probability 与资源画像。

## 实际输出

- `physics/differentiable_sbs_trajectory.py`：显式 `R/ECD/D/VR`、逐段 CPTP idle、投影/reset、open-loop 与 causal history-policy 两种控制路径。
- `tests/test_differentiable_sbs_trajectory.py`：37 项 direct tests。
- `docs/differentiable_sbs_trajectory.md`：公式、时序、接口、结果与 claim boundary。
- CPU/CUDA 两份 cutoff-8 machine validation JSON，均 17/17 PASS。

## 验证结果

- DLEnv `37 passed`；
- minimal-env 全量 `tests/` 为 `752 passed, 1 skipped, 4 failed`；新增代码无回归，四个失败仍是 R-N012；
- 四条 two-measurement branch probability 合计 `1.0`；
- CPU/CUDA 两类 gradient norm 在 float64 rounding 内一致；
- gate/CPTP/trace/Hermiticity/PSD gates 全通过；
- seeded random trajectory 可由 forced outcome 精确 replay；
- 3000-shot known ancilla probability calibration 通过；
- 资源画像带 `target_hardware_measured=false`。

## 风险与插入任务

R-N042 登记 instantaneous gate、二能级 ancilla、无 leakage/SPAM/device calibration 的
边界。不插入风险任务：T2.3.5--T2.3.7 已按顺序承接 gradient、feasibility envelope 与
ranking，T5/T6 承接高保真/装置/硬件证据。

## 同步

已同步 `docs/new_risks.md`、`docs/protocol_hierarchy.*`、README 与
`docs/new_task_board.md`；下一项为 T2.3.5。
