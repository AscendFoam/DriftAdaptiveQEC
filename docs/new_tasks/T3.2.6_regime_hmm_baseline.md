# T3.2.6 完成记录：四态 causal HMM regime baseline

- **Task ID：** T3.2.6
- **标题：** normal/burst/leakage/calibration-shift posterior baseline
- **日期：** 2026-07-15
- **状态：** Done

## 输入材料

新任务板 T3.2.6、observed/truth-separated syndrome stream、two-domain hidden-truth contract 和后续
CNN matched-budget 要求。

## 实际完成内容

实现 32×8 observed window、14-summary、full-covariance four-state Gaussian HMM、strict causal
forward posterior、validation-only hyperparameter/temperature selection、same-emission memoryless ablation、
四类 semi-Markov production trajectories 和 future-CNN shared budget contract。

## 产物路径

- 实现：`cnn_fpga/decoder/regime_hmm.py`；
- benchmark：`cnn_fpga/benchmark/regime_hmm_baseline.py`；
- 测试：`tests/test_regime_hmm.py`、`tests/test_regime_hmm_baseline.py`；
- 报告：`docs/regime_hmm_baseline.md`；
- 数据：`docs/t3_2_6_regime_hmm_validation.json`、`.csv`；
- 详细记录：`docs/tasks/T3.2.6_regime_hmm_baseline.md`。

## 验证方式和结果

- 3/3/8 disjoint seeds；229,376 total simulator cycles，4,096 evaluation windows/rows；
- HMM accuracy `0.846191`，memoryless `0.660889`，paired gain CI `[0.173322,0.197284]`；
- NLL gain `0.401514 [0.366352,0.436676]`；四类 recall 均超过 `0.80`；
- HMM false switches 较低，但 transition delay 为 `1.802333` windows，显式保留；
- 15/15 gates、37 focused tests、51 focused+governance、131 adjacent；显式全量为
  `1083 passed, 4 skipped, 4 failed`，4 个失败均为已登记 R-N012 的两份历史治理文档缺失；
  prefix causality、posterior calibration、hash separation、896-float/800-MAC budget 均通过。

## 风险复核

新增 R-N057：synthetic regime law 和 transition prior 不能外推设备；future CNN 尚未实现，当前只有共享
预算合同，不能声称 latency parity。

## 是否需要插入新 task

不插入。T3.2.11、T4.1、T5.1/T5.2/T5.4、T5.5/T6 已覆盖 memory/OOD、matched selection 和硬件证据。

## 对任务板和论文 claim 的同步

T3.2.6 标为 Done，T3.2.7 标为 In Progress，当前推荐改为 T3.2.7。只允许 synthetic host-regime
posterior estimator claim，不允许 decoder/controller/hardware claim。
