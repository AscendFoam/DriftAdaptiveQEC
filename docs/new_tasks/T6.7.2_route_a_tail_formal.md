# T6.7.2 Route-A abrupt/OOD tail formal 完成记录

- **Task ID**：T6.7.2
- **标题**：运行 abrupt/OOD 与 tail-safety formal matrix
- **日期**：2026-07-17
- **状态**：Done
- **结果分支**：预注册 tail safety/non-inferiority 门通过；只支持“相对锁定 EWMA 不劣”，不支持 tail LER 优势或相对 static 优势

## 输入材料

- T6.5.3 immutable formal protocol；
- T6.6.3 V4 lock `9347edb2...f67aa`；
- pilot-only primary baseline `ewma_adaptive_map`；
- formal abrupt/OOD cells：step、telegraph、burst、readout/reset、leakage、compound 各 6 cells；
- nominal negative control 1 cell；
- 24 个新 formal seed clusters。

## 实际执行

- 新增 `cnn_fpga/benchmark/route_a_tail_formal.py`。
- 在首次访问 abrupt/OOD formal syndrome 前写入 `runs/t6_7_2_formal_access_ledger.json`。
- 完成 24 seeds×37 cells = **888 trajectories**。
- 每方法 **43,646,976 scored decisions**；六个 deployable comparator 共 **261,881,856** scored decisions。
- 每 trajectory 保存：
  - 六方法 96 个 512-decision 窗口的 I/Z/X/Y 原始计数；
  - EWMA/Route-A paired outcome；
  - fallback/unnecessary-fallback；
  - commit/bank/false-update；
  - tail onset detection 与 recovery-to-OPEN lag/censoring。
- Source Data 共 **686,104 rows**。
- hash-bound cache 全量重放：**888 hits / 0 misses**。

## 预注册门结果

### 六 family catastrophic-degradation 门

每个 family 均分别通过：

- average proposed−EWMA 95% UCB ≤0.002；
- p95 proposed−EWMA 95% UCB ≤4/512；
- seed-worst proposed−EWMA 95% UCB ≤8/512；
- 任一单窗口 excess ≤16 errors。

实际差异：

- step、telegraph、readout/reset、leakage、compound：average/p95/seed-worst/single-window excess 全为 0；
- burst：aggregate 差约 0，p95/worst 差 0，最大单窗口 excess 1；全门通过。

### calibration-shift strict gate

- locked EWMA global worst：`181/512`
- Route-A global worst：`181/512`
- seed-worst paired difference：`0 [0,0]`
- strict gate：通过（相等，不是改善）

但 static joint MAP 的 global worst 仅 `32/512`，average `0.01087` 也低于 Route-A/EWMA `0.01227`。因此旧反例只在“相对 pilot 锁定 EWMA”口径下被消除，不能声称相对 static tail 优势。

### nominal non-inferiority

- average Route-A−EWMA：`0 [0,0]`
- fallback rate：`0.0011936 < 0.01`
- unnecessary fallback rate：`0.0011783 < 0.0075`
- induced−avoided：`0 [0,0]`
- 四门全部通过。

## Action / lag 边界

| family | fallback | unnecessary fallback | false updates / 3456 commits | avoided / induced |
| --- | ---: | ---: | ---: | ---: |
| step | 95.85% | 94.63% | 3365 | 0 / 0 |
| telegraph | 59.45% | 58.80% | 2044 | 0 / 0 |
| burst | 73.52% | 68.69% | 2617 | 2 / 2 |
| readout/reset | 74.96% | 74.96% | 2605 | 0 / 0 |
| leakage | 74.84% | 74.84% | 2598 | 0 / 0 |
| compound | 75.03% | 70.51% | 2615 | 0 / 0 |
| nominal | 0.119% | 0.118% | 0 / 576 | 0 / 0 |

- tail detection：所有事件 0 censored；step/leakage/compound p95=0 decisions，burst p95=96 decisions。
- recovery-to-OPEN：telegraph/burst/readout/leakage/compound 0 censored；p95 分别约 288/320/256/256/256 decisions。
- step 是持续 calibration shift，没有 recovery event。

false update 采用预注册 evaluation-only 定义：tail truth interval 内仍发生已提交更新。V4 的持续 EWMA shadow 避免 stale，但与旧“tail 冻结更新”叙事不一致；报告必须写成持续 trusted shadow，而不是 freeze-all。

## 产物

- `cnn_fpga/benchmark/route_a_tail_formal.py`
- `tests/test_route_a_tail_formal.py`
- `docs/route_a_tail_formal.md`
- `docs/t6_7_2_abrupt_ood_tail_formal_matrix.json`
- `docs/t6_7_2_abrupt_ood_tail_formal_matrix_source_data.csv`
- `runs/t6_7_2_formal_access_ledger.json`
- `runs/t6_7_2_tail_formal_cache_v1/`

## 验证

- 11/11 evidence gates；
- 6/6 semantic mutations rejected；
- 888-hit deterministic replay；
- `python -m pytest tests/test_route_a_tail_formal.py -q` → **8 passed**；
- Source Data 独立闭合六方法 errors、paired avoided/induced、fallback/unnecessary；
- online-prefix 源码审计确认 logical truth 值不参与 posterior/action/expert/bank。

## 反简化检查

本 task 不是仅靠“返回 EWMA”写一个表：

- 六个 comparator 都真实执行并逐窗口保存；
- 相等结果通过同轨 hash 与 paired counts 证明；
- static/Window 负面 comparator 不删除；
- 真实报告 tail 高 fallback/high false-update；
- lag 逐事件保存 censoring；
- strict/catastrophic/nominal 门独立，不用 T6.7.1 periodic gain抵消。

同时，结果说明 V4 在 tail 的“安全”主要是复制 locked EWMA 的行为，因此是 non-inferiority/pass-through safety，不是新增 decoder performance advantage。

## 风险与插入任务判断

新增 `R-N119`：tail gates 主要以 Route-A≈EWMA 通过，static 在 calibration/telegraph 更强，tail fallback/unnecessary 与 false-update 很高。若写作只引用“all tail gates pass”，会把 non-inferiority 包装成安全性能创新。

不插入新 task：T6.7.3 负责 integrated fixed-point/RTL fault qualification，T6.7.4 会综合执行 promotion/falsification，T6.8.1 将正面对比 static。

## 任务板同步

- T6.7.2：`In Progress -> Done`
- T6.7.3：`Todo -> In Progress`
- 当前推荐任务更新为 T6.7.3。

