# T4.4.5 完成记录：strong/falsified 双分支冻结

- Task ID：T4.4.5
- 标题：冻结 teacher-student strong/falsified 双分支
- 日期：2026-07-16
- 状态：Done

## 输入材料

T4.4.1--T4.4.4 的 machine JSON、checkpoint/student/source files、源码实现 hash、预注册 retention threshold
与 MF cutoff reversal counterevidence。

## 实际完成内容

实现只读、hash-bound、evaluation-free 的分支判定器。8/8 evidence predicates 通过时才激活
`qualified_student_retention`；任一 predicate 失败会自动切到 `drift_regime_aware_map_lut`，并从 active
claim registry 删除 teacher/distillation claims。九类 mutation tests 逐一验证失败分支。

## 产物与验证

runner/test/report/JSON/112-row CSV，详见 `docs/tasks/T4.4.5_teacher_student_branch_freeze.md`。11/11 gates、
17 direct tests、129 个 T4.4.1--T4.4.5 邻接/protocol tests；全量在 605.7 s 上限运行到 19%，终止前未见失败，
因此不记为全量通过。当前激活 qualified branch，但只允许 finite-model dual-cutoff student-retention 和 float
解析压缩。cutoff-dependent MF reversal、leakage/OOD/long-horizon/RTL/board 禁区均保留。

## 风险与插入任务

新增 R-N079。T5.2、T5.4、T5.5、T6 以及任一 parent provenance 失效均会撤销 qualified branch；正常任务
已覆盖，不插入风险任务。

## 任务板同步

T4.4.5 Done；T5.0.1 In Progress；当前推荐任务更新为 T5.0.1。
