# T4.2.2 完成记录：实验式事件状态机与硬件动作

- Task ID：T4.2.2
- 标题：设计实验式事件状态机和硬件动作
- 日期：2026-07-15
- 状态：Done

## 输入材料

T4.2.1 version-bound MAP-LUT、observed syndrome/phase contract、ParamBank version 语义、T4.2.2 event/frame/
latency 要求和 T3.2.5 旧 run-length baseline。

## 实际完成内容

实现与旧 baseline 分离的六态 FSM、六个 3-bit saturation counters、leakage/reset handshake、health fallback
hysteresis、双轴 recovery tie-break、Pauli/phase-frame 原子更新和 validation-before-mutation 负路径。对齐 MAP
valid cycle 后用 1-cycle action register 输出，总 source-to-action latency 6 cycles、II=1。

## 产物路径

`cnn_fpga/runtime/experimental_event_fsm.py`、`cnn_fpga/benchmark/experimental_event_fsm_validation.py`、
两份 tests、`docs/experimental_event_fsm.md`、20-gate JSON 与 1,024-row CSV。

## 验证方式和结果

20/20 gates、26 focused、98 focused+adjacent；显式全量 `tests/` 为
`1335 passed, 14 skipped, 4 failed`，4 个失败仍仅为 R-N012 缺失旧 FR8/P4 文档。8 个 128-cycle 场景覆盖全部 mode/transition/counter saturation、两种 phase
tie、reset sticky/ack、四类 health fault、安全态抑制、双轴 frame wrap、8-bank hash/version、6-cycle/II=1、
transactional negatives、bit-deterministic replay 和 no-truth schema。bank 0 全-I 的量化事实被保留，不伪造
不可达 action；资源/timing hardware 字段保持 `null/false`。

## 风险复核

新增 R-N069：event/recovery/reset/phase-frame 仍是 software representation contract；物理动作、完整
conservative fallback、定点 LER、RTL/post-route/FPGA/board 均未验证。

## 是否需要插入新 task

不插入；T4.2.3--T4.2.4、T4.3、T5、T6 已覆盖后续安全、量化、鲁棒性和硬件证据。

## 对任务板同步说明

T4.2.2 Done；T4.2.3 In Progress；当前推荐任务更新为 T4.2.3。
