# T1.3.4 完成记录：现有 adaptive baseline 因果对齐

**Task ID：** T1.3.4  
**标题：** 同 trace、one-window-delay 的 Window/EKF 与 model oracle 对齐  
**日期：** 2026-07-14  
**状态：** Done

## 输入材料

- `docs/new_task_board.md` T1.3.4；
- `physics/drift_processes.py`、`physics/oracle_map.py`、`physics/oracle_gap.py`；
- 现有 `WindowVarianceBaseline`、`EKFBaseline` 及其真实 runtime 路由；
- 旧 P4 same-window diagnostic / paired-seed 语义审计。

## 实际完成内容

- 新增 `cnn_fpga/benchmark/adaptive_drift_alignment.py`；
- 直接调用现有 Window Variance 与 EKF `predict(histogram, window_id)`，没有复制简化版；
- 冻结 24-window step-drift、独立 calibration/observation/evaluation 和一窗因果延迟；
- static、Window、EKF、full-state oracle 使用同一逐样本 trace 和 truth；
- 将四参数 prediction 显式映射到固定轴比 Gaussian covariance；
- 生成逐窗/全局 trace SHA-256，并对配置总工作量、child seed 和参数范围 fail closed；
- 用 paired CI、McNemar 和 3000 次 ratio bootstrap 执行 acceptance gate；
- 新增 18 项 direct tests、任务说明、README 入口和风险同步。

## 产物路径

- `cnn_fpga/benchmark/adaptive_drift_alignment.py`
- `tests/test_adaptive_drift_alignment.py`
- `docs/tasks/T1.3.4_adaptive_baseline_alignment.md`
- `docs/new_tasks/T1.3.4_adaptive_baseline_alignment.md`
- `docs/new_risks.md`
- `docs/new_task_board.md`
- `cnn_fpga/benchmark/README.md`
- `README.md`

## 验证方式和结果

- 默认 72,000 paired samples：static / Window / EKF / oracle 分别为
  `0.061389 / 0.022639 / 0.025319 / 0.011389`；
- 预注册 primary EKF 关闭 `0.721389` 的 static-to-oracle gap，bootstrap 95% CI
  `[0.704449, 0.737794]`，`ratio_ci_reliable=True`；
- static-EKF paired improvement `0.036069`，95% CI `[0.034658, 0.037481]`；
- change window 仍使用 before estimate，下一窗才消费 change-window histogram；
- fixed seed 短/长 prefix 的逐窗 trace hash、prediction 和 failure counts 完全一致；
- focused `18 passed`；T1.3/现有 baseline 邻近 `69 passed`；
- 显式 `tests/` 为 `190 passed, 4 failed`，4 项仍仅为 R-N012 的旧治理文档缺失。

## 风险复核

- 新增 R-N020：现有 baseline 是固定轴比四参数 Gaussian estimator，folded histogram 仍使用
  线性矩；本任务也只验收一个预注册 Gaussian step 场景；
- R-N017 不变：synthetic law 尚未由真实装置时序校准；
- R-N018 不变：oracle 是 assumed-model decoder reference；
- R-N019 已在本场景缓解：denominator CI 明确为正且 3000/3000 bootstrap 有效；
- R-N012 不影响本任务 focused/adjacent 验证。

## 是否需要插入新 task

不插入。R-N020 的能力扩展已由 T2.1.1、T3.2.2、T3.2.3、T5.1.2 和 T5.1.3 正常
任务覆盖；本任务保持窄范围 Gaussian alignment，避免提前实现 Phase 2/3。

## 对论文 claim 的影响

可以记录“pre-registered causal Gaussian step drift 下 existing adaptive baseline 显著关闭
static-to-model-oracle gap”。不能外推到 loss/outlier/leakage、完整七场景、finite-energy optimal
recovery、CNN 优势或真实 FPGA/QPU 测量。

## 对任务板的同步

- T1.3.4：`In Progress -> Done`；
- T1.4.1：`Todo -> In Progress`；
- 当前推荐任务：`T1.4.1`。
