# T4.1.3 完成记录：混合状态输出

- Task ID：T4.1.3
- 标题：定义混合状态输出
- 日期：2026-07-15
- 状态：Done

## 输入材料

任务板、T4.1.2 256×53 history、T4.1.1 selected HMM/checkpoint、periodic-moment estimator、
ParamMapper、ParamBank 和 run-length FSM profile contract。

## 实际完成内容

完成 future-only hybrid state schema 与 observed-only estimator；连续 9 参数、四态 posterior、
leakage/recovery burden、9×9 block-bootstrap uncertainty、future bank K/b recommendation 均有严格
范围/normalization/provenance。CRC、base version、validity、pending conflict 和 atomic commit 负路径完整。

## 产物路径

decoder/benchmark、两份 tests、主说明、JSON validation、456-row CSV，并同步 README、benchmark README、
protocol hierarchy、terminology、任务板和风险表。

## 验证方式和结果

17 gates、16 core、27 focused、181 adjacent；8-seed nominal/stress output replay；58/398 stage/hold，
58 次 commit 全部精确，五类 profiles 全覆盖；deterministic replay、PSD、posterior normalization、source
hash 和 checkpoint hash 均通过。显式全量 `tests/` 为 `1230 passed, 14 skipped, 4 failed`；4 个失败
均为 R-N012 已知缺失旧 FR8/P4 文档，未出现 T4.1.3 新回归。

## 风险复核

R-N065 保留 synthetic calibration、recovery-burden proxy、14-summary HMM bridge、53-feature retraining、
OOD/risk calibration、logical/control gain 与 hardware evidence 边界。

## 是否需要插入新 task

不插入；T4.1.4 紧接多目标 loss/calibration，T4.2 训练与消融，T5.1/T5.4 做 OOD/long-horizon，
T5.5/T6 做 fixed-point/RTL/wire/board/device。

## 对任务板同步说明

T4.1.3 Done；T4.1.4 In Progress；当前推荐任务更新为 T4.1.4。
