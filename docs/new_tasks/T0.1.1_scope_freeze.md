# T0.1.1 完成记录：研究对象冻结

- **Task ID：** `T0.1.1`
- **标题：** 冻结 repeated-memory single-mode square approximate GKP 主对象
- **日期：** 2026-07-14
- **状态：** `Done`

## 输入材料

- `docs/new_task_board.md` 的 T0.1.1 目标与通过标准；
- `AGENTS.md` 的证据优先、风险复核和非简化实现要求；
- 现有 `physics/gkp_state.py`、`noise_channels.py`、`syndrome_measurement.py`、`error_correction.py`、`logical_tracking.py`；
- 现有 `cnn_fpga/` 工程基础、`README.md`、`docs/paper_materials/high_quality_paper_target_criteria.md` 与 `paper_claim_evidence_ledger.md`；
- `relative_papers/` 中的 GKP 实验、反馈和综述材料仅作为 scope 背景，不在本 task 生成新数值 claim。

## 实际完成内容

1. 将主线冻结为 repeated quantum-memory 场景下的 single-mode square approximate GKP qubit；
2. 定义主线、独立验证线、未来兼容接口和明确排除项；
3. 冻结平台无关 cycle 语义、指标语义和论文允许/禁止表述；
4. 逐模块映射现有代码，明确优先复用路径与尚未验证的缺口；
5. 增加单位/晶格约定失败分支，不把当前 `sqrt(2*pi)` 直接视为跨模块认证；
6. 完成非 demo 审计，明确 smoke、直接单测、legacy simplified branch 和根级 pytest 收集的不同证据等级。

## 产物路径

- `docs/tasks/T0.1.1_scope_freeze.md`：scope contract；
- `docs/new_tasks/T0.1.1_scope_freeze.md`：本完成记录；
- `docs/new_risks.md`：首轮风险台账；
- `README.md`：新任务序列治理入口；
- `docs/new_task_board.md`：状态、当前推荐任务和进度日志。

## 验证方式和结果

1. 代码结构审计：逐个读取五个 `physics/` 主模块并提取公开类/函数；通过。
2. 本地 conda smoke：使用 `C:\ProgramData\anaconda3\python.exe` 运行近似 Wigner、modular syndrome、linear correction 和 20-round `full_qec` tracking；得到 `(16,16)` finite Wigner、合法 wrapped syndrome 和 `total_rounds=20`；通过。
3. 测试覆盖审计：当前 `tests/` 未直接引用核心 physics API；未通过，已登记风险，不误报为本 task 的科学验证。
4. 根级收集审计：`python -m pytest --collect-only -q` 在 `relative_papers/...Advances...md` 路径处触发 pytest `AssertionError`；未通过，登记为独立风险；本 task 不隐瞒。
5. 文档边界检查：scope 明确排除 outer code、多模格码主线和 GKP 态制备优化；通过。

## 非简化实现复核

- 本 task 没有新建与主仓库脱节的 toy/demo 仿真器，而是把 scope 映射到现有 `physics/` 和 `cnn_fpga/`。
- 一次 smoke 只证明接口可执行，不证明模型正确；因此没有把现有解析近似、Wigner-level noise 或线性 decoder 升级为 paper-grade 证据。
- 缺失测试、单位约定和 legacy simplified path 已写入风险与失败分支，后续必须由 T1/T2 的代码和交叉验证 task 关闭。

## 风险复核

- 任务板主表与旧进度日志冲突：采取“主表状态 + 当前仓库产物 + 当前验证”三者共同验收，旧日志不作为自动完成证据；风险已缓解但持续监控。
- 根级 pytest 收集失败：`Medium / Soon`，当前窄验证可继续，进入首个代码 task 前复核。
- GKP 晶格/单位约定未统一：`High / Soon`，由 T1.1.1 正常任务直接处理。
- physics 缺少直接单测且存在 legacy simplified branch：`High / Soon`，由 T1.1 起逐项补齐和隔离。

## 是否需要插入新 task

当前不插入。单位与 physics 测试缺口已有 T1.1/T1.2 覆盖；pytest 收集问题尚不阻塞本 task 的窄验证。若其在首个代码 task 中继续阻塞要求的回归验证，再按 `T-RISK-YYYYMMDD-NN` 规则插入修复任务。

## 现有代码冲突、行为变化与论文 claim 影响

- 未修改 `physics/` 或 `cnn_fpga/` 行为；本 task 是 scope/治理工作。
- 当前代码中的 `sqrt(2*pi)`、解析 Wigner 和 `simplified` branch 只被标记为待验证基础，不被删除也不被认证。
- 论文 claim 被收紧到 single-mode、simulation/protocol-aligned、证据分层表述；禁止 outer-code threshold、真实量子实验和态制备优化外溢。

## 任务板同步

- T0.1.1：`Todo -> In Progress -> Done`；
- 当前推荐任务：`T0.1.2`；
- 已在 2026-07-14 进度日志中记录“重新验收”原因、产物与验证边界。

