# T4.1.2 完成记录：实验式 history 输入

- Task ID：T4.1.2
- 标题：定义实验式 history 输入
- 日期：2026-07-15
- 状态：Done

## 输入材料

任务板、T2.1.1 observed syndrome schema、T3.2.5 run-length FSM、T2.4 scheduler/fault contract、
T1.1.2 periodic-GKP LLR 与 T4.1.1 slow-loop model-selection boundary。

## 实际完成内容

冻结并实现 10 groups/53 features/256 cycles 的 observed-only causal history；加入独立 padding
mask/cycle indices、同周期 action/runtime 对齐、LLR/action provenance allowlist、clipping saturation、
real-producer adapters 和递归 information-leakage audit。用真实仓库生产者执行 8-seed stress replay，
而非手写 demo features。

## 产物路径

核心 data/benchmark 模块、两份 tests、规范文档、JSON validation 和 16,384-row CSV；README、
benchmark README、protocol hierarchy 与 terminology 映射同步。

## 验证方式和结果

17 gates、39 core、50 focused、138 focused+governance、156 adjacent；生产数据覆盖全部 6 update statuses、5 FSM modes、
g/e/leakage、padding/full history、deadline、通信、CRC、failure、stale、conflict、commit、FIFO 和
所有 saturation flags。Source Data hash 与 implementation hash 可重算。显式 `tests/` 全量为
`1203 passed / 14 skipped / 4 failed`，4 个失败仍仅是 R-N012 的两份旧 FR8/P4 文档缺失。

## 风险复核

R-N064 保留 synthetic/software、fixed observed-calibration、raw wire/device/RTL/board 和 richer-input
model compatibility 边界；禁止把 schema completion 写成 HMM/learned-controller/device gain。

## 是否需要插入新 task

不插入；T4.1.3/T4.1.4、T5.1/T5.4/T5.5/T6 已按顺序覆盖输出、兼容性、多目标、OOD、
fixed-point/RTL/board 与 device calibration。

## 对任务板同步说明

T4.1.2 Done；T4.1.3 In Progress；当前推荐任务更新为 T4.1.3。
