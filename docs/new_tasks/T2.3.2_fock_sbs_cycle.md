# T2.3.2 在 Fock space 中实现一轮协议对齐纠错

## 基本信息

- **Task ID：** T2.3.2
- **标题：** 在 Fock space 中实现一轮协议对齐纠错
- **日期：** 2026-07-14
- **状态：** Done

## 输入材料

- `docs/new_task_board.md` T2.3.2；
- `docs/experiment_plan.md:447`、§14.1；
- `physics/fock_density_model.py`；
- Sivak 2023 本地正文/补充材料与官方 arXiv TeX 的 SBS analytic Kraus 原式。

## 实际完成内容

实现 canonical-coordinate GKP basis、论文 analytic X/Z Kraus、raw cutoff defect
diagnostics、共享右侧 CPTP completion、idle、hidden/observed readout、观测分支 residual
classical phase action、X→Z frame、branch/unconditional logical projection、exact 16 branch、
sampling、六逻辑态/光子误差/噪声负对照/五点 cutoff production validation。

原先存在的 modular stabilizer-pump 近似虽然形式完整，但不等同论文 SBS Kraus，已在
反简化检查中删除并用一手公式路径替换。

## 产物路径

- `physics/fock_sbs_cycle.py`
- `tests/test_fock_sbs_cycle.py`
- `physics/fock_density_model.py`
- `tests/test_fock_density_model.py`
- `docs/fock_sbs_cycle.md`
- `docs/t2_3_2_fock_sbs_cycle_validation.json`
- `docs/tasks/T2.3.2_fock_sbs_cycle.md`
- `docs/new_tasks/T2.3.2_fock_sbs_cycle.md`

## 验证方式和结果

- direct：`python -m pytest tests/test_fock_density_model.py tests/test_fock_sbs_cycle.py -q`
  → `99 passed`；
- adjacent protocol/Fock/error-space/control slice：`177 passed`；
- full `tests/`：`629 passed, 4 failed`，失败仍仅 R-N012；
- production：100k branch MC + `N=18/24/30/36/42` sweep，16 gates 全通过；
- exact branch 概率误差 `1.33e-15`，最小 branch eigenvalue `1.55e-10`；
- clean/noisy code-weighted fidelity `0.822670/0.765350`；
- photon loss/gain 平均回泵增益 `0.423853`；
- raw defect、Gram 谱、completion change 与 completed error 均写入机器 JSON。

## 风险复核

新增 R-N038：finite-cutoff shared completion 会改变 raw operator pair，且 raw full-space
defect 随边界存在；跨 cutoff/code-subspace 趋势已量化，但它不是无限维证明。R-N037
部分缓解为“已有一轮 single-oscillator reference”，explicit ancilla/transmon 与 device
calibration 仍开放。

## 是否需要插入新 task

不需要。T2.3.3 已直接承接 cross-fidelity、低 squeezing/clipping 失效区与误差归因，
现有风险不阻塞其执行。

## 对论文 claim 的影响

允许：finite-cutoff completed analytic SBS one-round reference、paired Kraus branches、
scenario readout/classical residual action、frame/logical projection 与 cutoff/CPTP checks。

禁止：infinite-dimensional exact SBS、pulse-level ECD、cavity--transmon master equation、
装置 break-even/lifetime、真实 FPGA/hardware fidelity。

## 对 `docs/new_task_board.md` 的同步说明

- T2.3.2：`In Progress -> Done`；
- T2.3.3：`Todo -> In Progress`；
- 当前推荐任务改为 `T2.3.3`。
