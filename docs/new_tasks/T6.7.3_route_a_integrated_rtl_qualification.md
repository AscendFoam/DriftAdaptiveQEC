# T6.7.3 Route-A integrated RTL 长序列资格验证完成记录

- **Task ID**：T6.7.3
- **标题**：对 integrated Route-A stack 做长序列 fixed-point/RTL 与故障资格验证
- **日期**：2026-07-17
- **状态**：Done
- **证据等级**：board-independent integer golden + CXXRTL + Yosys structural check；非板测

## 输入材料

- T6.2.2 production core 与百万周期资格报告；
- T6.6.3 V4 lock `9347edb2...f67aa`；
- T6.7.1/T6.7.2 frozen formal observed trajectories/caches；
- Window/EWMA bank、6-cycle action path、event/reset/fallback 与 CRC/version/age contract。

## 实际完成内容

- 实现 integer policy reference 与 synthesizable Route-A overlay；
- 实现 production core + overlay qualification top；
- 加入 host commit admission，禁止 host 绕过 policy router；
- 修复 action/version metadata 的第6周期对齐错误；
- 构造 131-byte/cycle ABI，逐 cycle 比较原 core 与 Route-A action/state/version/host-block；
- 真实重放 20 条 frozen formal HMM trajectory：每 family 两条，20/20 cache hit；
- 99.5802% cycles 为 unified replay，0.4198% 为独立标记的 threshold/race/fault vectors；
- 完成 10×100,000=1,000,000 cycles CXXRTL。

## 结果

- verdict：`PASS_ROUTE_A_INTEGRATED_LONG_RTL_QUALIFICATION`；
- 19/19 gates；
- 0 bit mismatch、0 undefined、0 CRC word error、0 silent overflow；
- 130/130 comparator mutations、12/12 semantic mutations rejected；
- 99 auto commits：85 ack、14 deliberate untrusted rejection；
- 75 host attempts全部 blocked，14 次与 auto LKG commit 同周期；
- 47/42 tail entry/recovery、28 leakage、59 integrity entry；
- FIFO 1,451 overflow全部 accounted，最终0 pending。

## 失败与修正历史

1. directed-only 版本虽然通过，但被反简化审计判定不足以满足 unified replay；
2. formal-replay 版本 bit-exact 通过，但 natural readout posterior 从不选 Window，race gate FAIL；
3. 增加不依赖 posterior 分布的最小 Window→version-fault→untrusted LKG→retry vector，先短轨验证，再完整重跑 PASS。

没有放宽门、删除 family 或用旧 PASS 覆盖新 FAIL。

## 产物路径

- `docs/route_a_integrated_rtl_qualification.md`
- `docs/t6_7_3_route_a_integrated_rtl_qualification.json`
- `docs/t6_7_3_route_a_integrated_rtl_source_data.csv`
- `cnn_fpga/benchmark/route_a_integrated_rtl_qualification.py`
- `cnn_fpga/runtime/route_a_fixed_policy_reference.py`
- `cnn_fpga/rtl/route_a_policy_overlay.sv`
- `cnn_fpga/rtl/route_a_integrated_qualification_top.sv`
- `cnn_fpga/rtl/route_a_integrated_cxxrtl_driver.cc`
- `tests/test_route_a_integrated_rtl_qualification.py`
- `build/t6_7_3_route_a_integrated_rtl/route_a_integrated_trace.bin`

## 验证方式和结果

- 正式 runner：1,000,000 rows、10 family digest 全等；
- Yosys structural check：0 problems；
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q tests/test_route_a_integrated_rtl_qualification.py tests/test_route_a_smooth_formal.py tests/test_route_a_tail_formal.py`：23 passed；
- source hash 绑定：9/9 当前文件重算一致。

## 风险复核

新增 `R-N120`：correctness PASS 不等于 LER/tail/latency/board优势；HMM仍在软件；0.30 smooth bank threshold在当前OPEN门下非绑定；若把这些混写会制造算法/硬件创新。

## 是否插入新 task

不插入。T6.7.4 已负责 promotion/falsification，并会把 T6.7.1/2 的性能负结果与本 task 的 correctness PASS 分开判定；P&R 与板测由 T6.9.1/T6.9.2 承接。

## 对论文 claim 的影响

新增可支持主张：在 frozen formal-observed posterior 接口和 directed fault contract 下，integrated fast stack 达到百万周期 bit-exact/fail-closed qualification。

仍禁止：真实 FPGA measured latency、零 deadline miss、资源/功耗、HMM on-FPGA、相对 static/Window/NMF/FPGA decoder 优势。

## 任务板同步

- T6.7.3：`In Progress -> Done`；
- T6.7.4：`Todo -> In Progress`；
- 当前推荐任务更新为 T6.7.4。
