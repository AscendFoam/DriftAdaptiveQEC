# T1.4.3 完成记录：两个计算域、三个时间尺度

**Task ID：** T1.4.3  
**标题：** 冻结 FPGA fast/event domain 与 host slow domain 的三尺度接口  
**日期：** 2026-07-14  
**状态：** Done

## 输入材料

- `docs/new_task_board.md` T1.4.3；
- `docs/tasks/T0.1.3_dual_loop_interface.md`；
- `docs/low_cost_fpga_boundary.md/.json`；
- `cnn_fpga/config/hardware_hil.yaml`；
- scheduler、ParamBank、fast-loop、slow-loop 和 latency injector 的当前源码。

## 实际完成内容

- 冻结 `CD1/CD2` 两域 ownership 和 prohibited responsibility；
- 冻结 `TS1/TS2/TS3` 的 trigger、I/O、deadline identity、failure action 和 current gap；
- 冻结 `XIF01--XIF04` observed-window、staging、commit、ack/readback；
- 冻结 `AP01--AP09` 原子更新步骤及逐项实现状态；
- 冻结 `FB01--FB14` 的失败动作，禁止 undefined action；
- 分离 observed online input 与 mock hidden `target_params`；
- 新增 11 项 tests，直接执行 commit、slow-failure、queue-overflow 和 deadline-gap 路径。

## 产物路径

- `docs/two_domains_three_timescales.md`
- `docs/two_domains_three_timescales.json`
- `tests/test_two_domains_three_timescales.py`
- `docs/tasks/T1.4.3_two_domains_three_timescales.md`
- `docs/new_tasks/T1.4.3_two_domains_three_timescales.md`
- `README.md`
- `docs/new_risks.md`
- `docs/new_task_board.md`

## 验证方式和结果

- JSON 解析；
- focused `11 passed`；
- T1.4.1--T1.4.3 相邻 contract `28 passed`；
- 显式 `tests/` 为 `218 passed, 4 failed`；4 项仍仅因 R-N012 的
  `docs/fr8_statcalib_extension_lane_benchmark.md` 与 `docs/P4_benchmark_formal_protocol.md` 缺失。

## 风险复核

新增 R-N023：mock truth 与 observed payload 当前同容器，fast/slow deadline enforcement 与本地
event/stale fallback 仍不完整；若后续时间尺度或通道折叠，可能造成 truth leakage、host 阻塞或
错误实时 claim。由 T2/T3/T4/T6 正常任务关闭。

## 是否需要插入新 task

不插入。T2 建 protocol/event schema，T3 锁 baseline 的 online input，T4.2/T4.3 实现本地 FSM、
deadline/stale/atomic update，T5.5/T6 做 golden/RTL/board negative path，已覆盖全部缺口。

## 对论文 claim 的影响

当前只支持 software-contract/emulation 的 two-domain/three-timescale architecture。所有配置时序
保持 non-measured；CRC/age/ack/rollback/event FSM 未实现，不能写成安全闭环或真实硬件实时性。

## 对任务板的同步

- T1.4.3：`In Progress -> Done`；
- T1.4.4：`Todo -> In Progress`；
- 当前推荐任务：`T1.4.4`。
