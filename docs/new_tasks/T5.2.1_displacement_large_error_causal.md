# T5.2.1 完成记录：displacement / large-distance 因果故障注入

- Task ID：T5.2.1
- 标题：注入 displacement / large-distance error
- 日期：2026-07-16
- 状态：Done

## 输入材料

T2.0.5 displacement trend、T5.1.2 component lane、T2.3.3 cross-fidelity coordinate evidence、T5.1.6
feasibility/nonmixing contract，以及 Sivak 2023 的最近逻辑操作距离与同象限 e-run 一手锚。

## 实际完成内容

新增独立正式 causal lane：17 幅度×8 evaluation seeds×4096 shots，使用 frozen recovery/readout/reset 与
common-random-number pairing；输出 recovery depth、ideal/observed e-run、recovery time/availability、unaffected-axis
negative control。另用两个 frozen injection-jitter profiles 分列 nearest-operation logical failure 与 identity-reference
logical flip，truth 仅供 evaluator，禁止把 recovery censoring 改名为 logical failure。

## 产物路径

- `cnn_fpga/benchmark/displacement_large_error_causal.py`
- `tests/test_displacement_large_error_causal.py`
- `docs/displacement_large_error_causal.md`
- `docs/t5_2_1_displacement_large_error_causal.json`
- `docs/t5_2_1_displacement_large_error_source_data.csv`
- `docs/protocol_hierarchy.json` / `docs/protocol_hierarchy.md`

## 验证方式和结果

runner：136 recovery + 272 logical seed rows、1,863 Source Data rows、20/20 gates。direct/mutation
`43 passed`；T2.0.5/T5.1.2/T2.3.3/T5.0.2/T5.1.6 邻接 `137 passed`。midpoint depth/e-run 为
`6.0/4.84512`；primary/confirmation nearest-op failure 为 `0.495300/0.497864`；全网格解析差 `<0.005`。

## 非 demo 检查

既有 T2.0.5 只有单 seed 与 shot bootstrap，T5.1.2 仍为 component-only，且两者均无 logical estimand；本任务没有
改名复用旧 PASS。开发期还纠正了“要求解析公式落入 34 个逐点 CI”的错误 simultaneous-band 表述：保留预注册
全网格最大误差门与两个 midpoint CI，不降低数值门。semantic mutations 包含同步篡改 seed rows/summary、
identity endpoint 隐藏、truth 泄漏、parent/source/code stale 与 hardware claim promotion。

## 风险复核

R-N029 得到 independent seed-cluster/causal-isolation 缓解，但 coarse depth law、recovery/readout 与 jitter 仍是
project assumptions。新增 R-N089：nearest-operation logical assay 不是 repeated physical-memory LER，且尚无 coherent
Fock/device calibration。T5.3/T5.4/T6 已正常承接，不插入风险 task。

## 任务板同步

T5.2.1 Done；T5.2.2 In Progress；当前推荐任务更新为 T5.2.2。
