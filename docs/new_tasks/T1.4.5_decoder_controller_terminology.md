# T1.4.5 完成记录：Decoder / controller 术语冻结

**Task ID：** T1.4.5  
**标题：** 冻结 decoder/controller、oracle/teacher/student/bound 术语  
**日期：** 2026-07-14  
**状态：** Done

## 输入材料

- `docs/new_task_board.md` T1.4.5 及后续 oracle/teacher/student/bound tasks；
- current decoder/oracle/gap code；
- slow-loop、fast-loop runtime；
- NMF/Petz 任务改进记录和前序 T1.4 contracts。

## 实际完成内容

- 建立 11-role Markdown/JSON terminology registry；
- 每个角色冻结 question、information、output、objective、causality、horizon、deployability；
- 建立 12 个 artifact bindings，区分 current code 与 planned task；
- 建立 6 个 legacy aliases 和 10 条 high-risk conflation rules；
- 三分 decoder oracle、finite-horizon control oracle、channel-recovery bound；
- 分离 Feedback-GRAPE control teacher、distilled controller student、host estimator 和 FPGA executor；
- 将 `oracle_delayed` 降为 mock hidden-truth parameter reference；
- 将 `teacher_mode` 降为 legacy classical reference-estimator feature source；
- 14 项 tests 直接复核源码行、planned/implemented 状态、causal control tree 和 forbidden pairs。

## 产物路径

- `docs/decoder_controller_terminology.md`
- `docs/decoder_controller_terminology.json`
- `tests/test_decoder_controller_terminology.py`
- `docs/tasks/T1.4.5_decoder_controller_terminology.md`
- `docs/new_tasks/T1.4.5_decoder_controller_terminology.md`

## 验证方式和结果

- focused：`14 passed`；
- T1.4.1--T1.4.5 adjacent：`56 passed`；
- 显式 `tests/`：`246 passed, 4 failed`；仅 R-N012 两份旧文档缺失。

## 风险复核

新增 R-N024：历史 `teacher_mode`/`teacher_prediction`、`oracle_delayed` 会与新角色混称；关闭条件为
新 artifact/prose 携带 canonical role，T2 truth schema 分离，并在 T3/T4/T5 表中分列三种 gap。

## 是否需要插入新 task

不需要。风险由 T2/T3/T4/T5 正常任务覆盖，当前兼容保留历史 runtime 字段更安全。

## 对论文 claim 的影响

允许区分 software decoder oracle、planned control oracle、planned channel bound、offline teacher、
conditional student、host estimator 和 software-emulated fast path。禁止单写 oracle，禁止把 RNN
control teacher 叫 decoder/oracle，禁止把 Petz bound 叫 MAP decoder，禁止把 planned 角色写成完成。

## 对 `docs/new_task_board.md` 的同步说明

- T1.4.5：`In Progress -> Done`；
- T2.0.1：`Todo -> In Progress`；
- 当前推荐任务：`T2.0.1`。
