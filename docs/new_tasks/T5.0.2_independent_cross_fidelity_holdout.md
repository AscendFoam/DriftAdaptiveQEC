# T5.0.2 完成记录：跨保真度和独立 holdout 验证

- Task ID：T5.0.2
- 标题：执行跨保真度和独立 holdout 验证
- 日期：2026-07-16
- 状态：Done

## 输入材料

T2.3.3/T5.0.1 frozen artifacts、四条 cross-fidelity lanes、P-Steane 一手预印本公式，以及预冻结的
calibration/pilot/formal-point/seed/threshold contract。

## 实际完成内容

正式 `2.5/10.25/11.75 dB` 与 calibration `3/5/8/10/12 dB`、pilot `4/11/14 dB` 完全分离；
每点 4 个 fresh seeds、400k samples。主 family 因 `10.25 dB` pooled z-score `2.293338 > 2.0` 判
`FAIL`，没有重选或掩盖；secondary P-Steane 252 点 analytic family 判 `PASS`。task 总 PASS 只表示至少
一个独立 family 通过并完整保存失败 family。

## 产物与验证

runner、JSON、291-row CSV、human report、protocol contract、public single-point evaluator 和测试，详见
`docs/tasks/T5.0.2_independent_cross_fidelity_holdout.md`。runner 为 `6/6` task gates；focused
`118 passed`，跨 finite-squeezing/noise-transfer/Fock/quadrature 的 adjacent `319 passed`。

## 风险复核

新增 R-N081。main high-domain claim 保持 calibration-qualified；secondary analytic PASS 不进入 sBs 主排名，
不代表 physical squeezing、FPGA 或 device 实现。既有 T5.1/T5.2/T5.4 可承接，不插入风险 task。

## 任务板同步

T5.0.2 Done；T5.1.1 In Progress；当前推荐任务更新为 T5.1.1。
