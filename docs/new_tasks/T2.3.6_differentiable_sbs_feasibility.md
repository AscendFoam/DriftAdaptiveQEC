# T2.3.6 完成记录：可微 SBS teacher-training 资源包络

- **Task ID：** T2.3.6
- **标题：** 完成 cutoff/batch/horizon 可行性扫描
- **日期：** 2026-07-14
- **状态：** Done

## 输入材料

- T2.3.4 finite-cutoff joint cavity--two-level-ancilla simulator；
- T2.3.5 Feedback-GRAPE reward/score gradient contract；
- Puviani 等的 GRU10 + 256 + 256 + 15-output architecture scale；
- `docs/new_task_board.md` 的 2--10 cycle resource/falsification gate。

## 实际完成内容

实现 72,913 参数 causal recurrent policy 与完整 Adam training-step benchmark；65 个隔离点
覆盖 56 个 CUDA 和 9 个 CPU 组合，记录 raw repeats、median/p90、RSS、allocated/reserved
VRAM、probability、trace/Hermiticity/PSD、gradient 与 parameter-update norm。初版全绿后继续
扩展至 cutoff 48/batch 576，实际得到 memory/runtime 两类拒绝边界；随后因审计发现缺 Adam
update，废弃旧 scope 并完整重跑。

## 产物路径

- 实现：`physics/differentiable_sbs_feasibility.py`；
- 绘图：`physics/plot_differentiable_sbs_feasibility.py`；
- 测试：`tests/test_differentiable_sbs_feasibility.py`；
- 报告：`docs/differentiable_sbs_feasibility.md`；
- 机器/source data：`docs/t2_3_6_differentiable_sbs_feasibility.json/.csv`；
- 图：`docs/figures/t2_3_6_differentiable_sbs_feasibility.{svg,pdf,tiff,png}`。

## 验证方式和结果

- 65 点状态：63 pass、1 memory exceeded、1 runtime exceeded；没有 exception/OOM/timeout/
  numerical failure；
- cutoff 16、batch 16 的每个 2--10-cycle 点通过；最大已测可行 batch 512；
- batch 576 的 `7.015 GB/81.71%` 触发 memory gate；cutoff 48、batch 16 的
  `13.954 s` 触发 runtime gate；
- 全局 max trace `6.66e-16`、min eigenvalue `-9.37e-16`、min gradient/update norm
  `0.3277/0.0150`；
- Python-only figure QA：SVG text 保持 `<text>`，PDF 嵌入 CID TrueType 且可提取文字，
  TIFF 为 600 dpi，CSV 65 rows；
- focused `40 passed`；显式 `tests/` 全量回归为
  `754 passed, 3 skipped, 4 failed`，四项失败与既有 R-N012 完全一致（仅缺
  `docs/fr8_statcalib_extension_lane_benchmark.md` 和
  `docs/P4_benchmark_formal_protocol.md`），没有本 task 新失败。

## 风险复核与插入任务判断

R-N043 的 resource/horizon 子风险被实测缓解，但 72,913 参数策略只是 resource audit
probe，没有执行长程 convergence/seed/lifetime 研究。R-N042 的 two-level、instantaneous
gate、无 leakage/SPAM/device calibration 边界保持。无需插入 task：T2.3.7 负责 ranking，
T4.4.1 负责真实 teacher training。

## 对论文 claim 的影响

允许写：当前 host、finite-cutoff two-level model 的 2--10-cycle recurrent Adam kernel
feasible，并报告实测 frontier。不得写：RNN 已学得 NMF 策略、优于 MF/standard、物理
cutoff 已收敛、模拟速度等于量子周期或 FPGA timing。

## 任务板同步

T2.3.6 标为 Done；T2.3.7 标为 In Progress；当前推荐任务更新为 T2.3.7。
