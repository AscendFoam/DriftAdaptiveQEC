# T2.1.2 完成记录：observed-only 多轮控制 memory

**Task ID：** T2.1.2  
**标题：** accumulated residual、action、frame、run、bank 与 deadline 的统一多轮 memory  
**日期：** 2026-07-14  
**状态：** Done

## 输入材料

- `docs/new_task_board.md` T2.1.2；
- `physics/syndrome_stream.py`；
- `physics/sbs_error_space.py` / `sbs_observation_reset.py`；
- `physics/logical_tracking.py`；
- `cnn_fpga/runtime/fast_loop_emulator.py`；
- `cnn_fpga/runtime/param_bank.py` / `scheduler.py`。

## 实际完成内容

- 实现 observed-only `MultiRoundControlMemory` 与 frozen state/update/trajectory；
- 跟踪 accumulated residual、previous correction、confidence、Pauli/phase frame、e/leakage runs、bank version 和 deadline flag/run/count；
- 以 nearest periodic lift 解决 modular wrap continuity；
- correction subtraction 与现有 fast loop 对齐；
- run redundancy validation、counter saturation、bank rollback、transactional failure；
- T2.1.1 stream、真实 ParamBank commit、segmented replay 和 truth-step rejection 集成；
- 增加中文模型说明、protocol machine contract 与 public exports。

## 产物路径

- `physics/control_memory.py`
- 更新 `physics/__init__.py`
- `tests/test_control_memory.py`
- 更新 `tests/test_protocol_hierarchy.py`
- 更新 `tests/test_sbs_error_space.py`
- 更新 `tests/test_sbs_observation_reset.py`
- `docs/multiround_control_memory.md`
- 更新 `docs/protocol_hierarchy.md/.json`
- `docs/tasks/T2.1.2_multiround_control_memory.md`
- `docs/new_tasks/T2.1.2_multiround_control_memory.md`
- 更新 `README.md`

## 验证方式和结果

- focused：`26 passed`；
- protocol/core/observation/control：`83 passed`；
- stream/control/runtime contract：`58 passed`；
- adjacent stack：`189 passed`；
- 显式 `tests/`：`418 passed, 4 failed`，失败仅为 R-N012；
- `py_compile`、protocol JSON validation 通过。

## 风险复核

新增 R-N032：nearest-lift 依赖相邻有效观测的半格内可辨识性，超过时可能 cycle slip；confidence 当前由 decision 输入但未 calibration；deadline miss 仅作为 state，现有 scheduler callback 仍执行；bank version 尚无 CRC/age/CAS/ack。

## 是否需要插入新 task

不需要。T2.1.3、T2.4、T3.2、T4.2/T4.3 与 T5.5 已覆盖高速 simulation、timing、memory baseline、fallback/atomic update 和 golden hardware model。

## 对论文 claim 的影响

允许写“the multi-round memory causally tracks residual/action/frame/run/bank/deadline state from deployable observations”。禁止写实时安全控制闭环、confidence calibration 或 wire-level atomicity 已完成。

## 对 `docs/new_task_board.md` 的同步说明

- T2.1.2：`In Progress -> Done`；
- T2.1.3：`Todo -> In Progress`；
- 当前推荐任务：`T2.1.3`。
