# T3.2.11 完成记录：memory-specific 消融

- Task ID：T3.2.11
- 标题：执行 memory-specific 消融
- 日期：2026-07-15
- 状态：Done

## 输入材料

任务板、T2.3.7 NMF artifact/checkpoint、T3.2.7 exact-budget latest-only artifact/checkpoint、
registered cutoff12/16 evaluation protocol。

## 实际完成内容

实现 prefix-consistent history shuffle、sliding truncation、periodic hidden reset、frozen
latest-only，并复用 independently retrained exact-budget latest-only。全部 frozen intervention
只改变 causal observed-prefix replay，不改 parent weights、不读 future/truth。双 cutoff 闭环
重放而非离线复用 outcome 序列。

## 产物路径

核心/runner、两份 tests、JSON/CSV、`docs/memory_specific_ablation.md` 与治理同步。

## 验证方式和结果

15 gates、28,230 rows、27 focused tests；full-view bit-exact，所有 intervention 改变 action，
parent hash 前后一致。cutoff12 的 frozen history 趋势支持较长 memory，但 retrained latest-only
不差于 full；cutoff16 的 frozen latest/reset 显著优于 full。两 cutoff 均未通过预注册四对照
联合规则，结论为 `cross_cutoff_memory_mechanism_not_supported`。DLEnv 治理/相邻组合
`131 passed`；显式 `tests/` 全量回归 `1122 passed / 12 skipped / 4 failed`，4 个失败仍仅为
R-N012 两份旧 FR8 文档缺失。

## 风险复核

R-N062 记录 cutoff/intervention 方向反转、短 horizon、two-level leakage 缺口和 optimizer/model
依赖。禁止普遍 memory claim 或 paper/device/hardware 外推。

## 是否需要插入新 task

不插入；T5.2/T5.4 已覆盖。

## 对任务板同步说明

T3.2.11 Done；T4.1.1 In Progress；当前推荐任务更新为 T4.1.1。
