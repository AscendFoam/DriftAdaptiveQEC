# T1.1.3 完成记录：二维相关 Gaussian joint MAP

**Task ID：** T1.1.3  
**标题：** 四 logical-coset likelihood、joint MAP 与 independent-axis 对照  
**日期：** 2026-07-14  
**状态：** Done

## 输入材料

- `docs/new_task_board.md` T1.1.3；
- T1.1.1/T1.1.2 当前验收后的 `physics/ideal_gkp_decoder.py`；
- shared `LATTICE_CONST=sqrt(2pi)` 和 centered syndrome contract；
- T0.2.2 中 direct baseline、oracle/bound 与 claim downgrade 约束。

## 实际完成内容

- 新增严格正定 covariance builder/validator；
- 实现完整 correlated bivariate Gaussian 的四 logical-coset log likelihood；
- 实现带四类 prior 的 joint MAP、完整 posterior、top-two confidence 和确定性 tie；
- 实现同 covariance marginals 下的 independent-axis 对照；
- 支持单样本和 batch、可广播二维 mean、跨 cell parity；
- 实现 covariance/prior/shape/boundary/index 与 alias workload 失败关闭；
- 新增 10 项二维直接测试，并修复反查发现的 parity 浮点索引、prior overflow、
  covariance 小尺度对称容差和无界网格分配风险；
- 修正模块顶层说明，不再把已扩展模块错误描述为“只处理单 quadrature”。

## 产物路径

- `physics/ideal_gkp_decoder.py`
- `physics/__init__.py`
- `tests/test_ideal_gkp_decoder.py`
- `docs/tasks/T1.1.3_2d_correlated_map.md`
- `docs/new_tasks/T1.1.3_2d_correlated_map.md`

## 验证方式和结果

- focused + warnings-as-errors：`34 passed`；
- 邻近 statcalib runtime：`38 passed`；
- correlated likelihood 与独立二维 alias 穷举一致；
- `rho=0` likelihood、posterior 和 hard decision 退化为两个 1D MAP；
- 四类 density 的 syndrome-cell 二维积分为 1；
- 固定强相关配对 MC：joint error `0.23067`、independent `0.30883`、绝对改善
  `0.07817`，paired McNemar `z=29.60`；
- 显式 `tests/`：`100 passed, 4 failed`，四项仍仅为 R-N012 的旧文档缺失。

## 风险复核

- R-N004 的新增 ideal decoder 范围已充分缓解：1D/2D 均有解析/穷举、退化、物理
  恒等式、MC、边界和失败分支；风险保持 Open 仅因为旧 full/legacy physics 尚未
  全面直接测试；
- R-N003 保持 `Mitigated`：二维接口继续复用 shared operational spacing；
- R-N012 不变且不阻塞 T1.2.1。

## 是否需要插入新 task

不插入。finite-energy state/channel/trend 已由 T1.2.1—T1.2.3 正常任务覆盖；
non-Gaussian/drift 与 hardware 路线也在主表后续任务中，不应提前扩大 T1.1.3。

## 对论文 claim 的影响

允许把 ideal 2D correlated-Gaussian joint MAP 写成已实现，并用独立穷举、rho=0
因子化、二维归一化和强相关配对 MC 验证；不允许写成 finite-energy、实验或普适
decoder gain。

## 对任务板的同步

- T1.1.3：`In Progress -> Done`；
- T1.2.1：`Todo -> In Progress`；
- 当前推荐任务：`T1.2.1`。
