# T4.1.5 完成记录：offline teacher / online student 分离

- Task ID：T4.1.5
- 标题：实现 offline teacher / online student 分离
- 日期：2026-07-15
- 状态：Done

## 输入材料

T2.3.7 五个冻结 NMF checkpoint/manifest、T4.1.3 host/fast-path contract、任务板 §14.1/§14.4/§15.1
离线 teacher 边界。

## 实际完成内容

冻结 5-agent teacher provenance，生成 3 个 disjoint seed 的 training/validation/evaluation trajectory；以
3 restarts 拟合 75 参数指数递推 student，由 validation-only 选 checkpoint。在线 artifact 保存 105 scalars
和精确软件资源 proxy，API 只消费 observed outcome/health flags；leakage 与五类 health failure 均 fail closed
到 nominal-control 的零 raw residual。

## 产物路径

`cnn_fpga/control/teacher_student.py`、`cnn_fpga/benchmark/offline_teacher_student_distillation.py`、
两份 tests、`docs/reports/phase4/offline_teacher_student_distillation.md`、21-gate JSON、15,360-row CSV 与 student checkpoint。

## 验证方式和结果

21/21 production gates、28 focused、207 adjacent（2 skipped）；显式全量 `tests/` 为
`1283 passed, 14 skipped, 4 failed`，4 个失败仍仅为 R-N012 缺失旧 FR8/P4 文档。evaluation student/latest-only/zero-safe MSE
`1.453624e-6/1.404389e-4/5.265504e-3`，MSE-defined imitation retention `0.999724`；teacher/student prefix
causal，online/batch bit-exact，重复 replay deterministic。初版训练上限退化已通过 1200 epochs 与 validation
plateau gate 修复；三 restart 全参数梯度覆盖，但 best epoch 接近上限，未声称全局收敛。

## 风险复核

新增 R-N067：优秀 imitation MSE 不等于 physical gain retention；当前无 leakage training、fixed-point、RTL、
FPGA timing 或 device calibration。

## 是否需要插入新 task

不插入。T4.4.3--T4.4.5、T5.2/T5.4/T5.5/T6 已覆盖物理同轨 gate、leakage、长时稳健性与硬件证据。

## 对任务板同步说明

T4.1.5 Done；T4.2.1 In Progress；当前推荐任务更新为 T4.2.1。
