# 历史 smoke / pilot 临时输出

本目录保存从 `docs/` 顶层隔离出的 7 个 `_tmp_*` 文件。它们来自 T2.4.3、T3.2.5、T3.2.6、T4.4.1 和 T4.4.2 附近的 smoke/pilot 尝试，部分内容包含失败或非正式结果。

这些文件只用于历史诊断，不是正式 evidence、当前 benchmark 结论或可直接引用的论文结果。后续临时运行应写入系统临时目录，不应重新写回 `docs/` 顶层。
