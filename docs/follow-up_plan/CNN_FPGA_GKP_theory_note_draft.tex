\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{array}
\usepackage{booktabs}
\usepackage{hyperref}
\emergencystretch=2em

\title{A Teacher-Anchored Residual Calibration Framework for Drift-Adaptive GKP Decoding}
\author{Draft research note}
\date{May 29, 2026}

\begin{document}
\maketitle

\begin{abstract}
Approximate Gottesman--Kitaev--Preskill (GKP) quantum error correction
encodes a qubit into an oscillator and uses continuous-valued modular
syndromes to infer displacement errors.  In realistic finite-energy GKP
states, syndrome statistics are shaped by state-preparation width,
measurement noise, circuit-level imperfections, and slow calibration drift.
This note develops a two-timescale decoding framework for such settings.  A
low-latency fast loop applies an FPGA-friendly affine correction
\(\Delta_t = K_t s_t + b_t\), while a slower calibration loop estimates the
current effective noise state from recent syndrome histograms and updates the
runtime parameters.  The learned component is deliberately narrow: a
lightweight CNN is anchored by a classical teacher estimator and predicts only
a bounded residual calibration term, currently focused on \(b_t\), rather than
replacing the GKP decoder end-to-end.

The method is positioned against four nearby bodies of work: analog
soft-information decoding for bosonic/GKP codes, syndrome-statistics and
calibration-based decoder-prior adaptation, learned low-latency QEC decoders,
and real-time FPGA decoding systems.  The intended contribution is therefore
not a new maximum-likelihood GKP decoder or a board-level FPGA demonstration.
It is a deployment-constrained calibration layer whose per-shot path remains a
fixed-point affine rule, while the slow loop performs teacher-anchored residual
adaptation under explicit clipping, smoothing, stage-and-commit, and
software-HIL evidence boundaries.
\end{abstract}

\tableofcontents

\section{Scope and Evidence Boundary}

% This document is a theory-facing draft for the front matter and early methods
% sections of a future paper.  It is written to clarify the intended scientific
% claim, the mathematical model, the architecture, and the benchmark structure.
% It does not upgrade the current evidence level beyond the repository state.

% The validated execution boundary remains mock-backed software HIL using
% \texttt{artifact\_npz} and \texttt{inproc} inference for the recovered paths.
% Existing frozen-set evidence supports a bounded software-revalidation claim.
% It does not establish true \texttt{.tflite} runtime validation, real-board HIL,
% or paper-grade expanded benchmark completion.  The T64 result adds a clean
% provenance, four-scenario \texttt{statcalib} extension lane, but that lane must
% remain separately labeled and must not rewrite the historical T24 frozen
% five-mode table.  T65 is a consistency-guard and report-closeout task; it does
% not create new benchmark evidence.

% This boundary matters for writing.  The paper can motivate an FPGA-compatible
% decoder contract, but it should not say that an FPGA board has validated the
% full loop.  It can report a strong software-HIL extension-lane result for
% \texttt{statcalib}, but it should not call that result a mature SOTA comparator
% or a deployment result.  It can discuss residual-b mechanisms, but after the
% post-T55 evidence the mechanism story should remain hedged rather than written
% as causal closure.

\section{Introduction}

Bosonic codes protect quantum information by encoding a logical qubit into the
Hilbert space of an oscillator.  The GKP code is a central example: ideal code
states form a periodic lattice in phase space, so small shift errors can be
diagnosed through modular syndrome measurements and corrected by displacement
operations \cite{gkp2001,grimsmo2021}.  This structure is attractive because
the measured syndrome is analog rather than merely binary; it carries
information about both the likely displacement and the confidence of the
correction.

The practical challenge is that physical GKP states are approximate
finite-energy states.  Their comb peaks have finite width and an envelope, and
syndrome measurements are further affected by measurement inefficiency, noisy
ancilla states, circuit-level imperfections, oscillator loss, and calibration
error \cite{grimsmo2021,roy2025}.  Consequently, the effective noise model
seen by the decoder can change over time.  A fixed syndrome-to-correction map
may be well tuned for one operating point but mismatched after bias, variance,
or covariance drift.

Recent GKP and bosonic-decoding work makes clear that analog soft information
is valuable.  Surface-GKP and concatenated bosonic-QLDPC decoders use
continuous syndrome information to set matching weights, belief-propagation
messages, or effective outer-code priors \cite{noh2020,noh2022,berent2024,
borah2025}.  At the same time, recent QEC systems papers show that useful
learned modules tend to be narrow, modular, and latency-aware rather than
unconstrained replacements for the full decoder \cite{chamberland2026,
stein2026}.  Hardware and calibration oriented work similarly emphasizes that
decoder priors must be kept aligned with device statistics
\cite{spitz2018,wagner2021,chen2022,sivak2024}.

This project applies that systems principle at the physical GKP correction
layer.  The fast loop is intentionally simple:
\begin{equation}
    \Delta_t = K_t s_t + b_t ,
\end{equation}
where \(s_t\) is the measured two-quadrature GKP syndrome and
\((K_t,b_t)\) are quantized runtime parameters.  The slow loop observes recent
syndrome histograms, estimates an effective noise state, and updates
\((K_t,b_t)\) at a lower rate.  The CNN is not asked to learn the entire GKP
decoder.  It is anchored by a classical teacher estimator and learns a bounded
residual calibration term.

The resulting claim is deliberately narrow: this is a teacher-anchored
residual calibration layer for a deployment-constrained affine GKP fast path.
That narrower formulation is important because several broad claims would be
unsafe.  The project is not the first use of GKP analog syndrome information,
not the first adaptive decoder-prior idea, not the first calibration-conditioned
neural QEC decoder, and not the first real-time FPGA QEC decoder.  Its niche is
the combination of GKP physical-layer affine correction, histogram-driven
drift adaptation, teacher-residual learning, and explicit runtime contracts.

\section{Summary of Contributions}

The target paper can be organized around three contributions.

\paragraph{1. A two-timescale adaptive affine formulation for approximate GKP
decoding.}
We formulate the per-shot correction path as a bounded affine estimator
\(\Delta_t = K_t s_t + b_t\).  The expensive adaptation problem is moved into a
slow loop that estimates the current effective noise state from recent syndrome
statistics.  This links the continuous-syndrome GKP picture to a runtime
contract that can be implemented as a quantized matrix-vector operation.

\paragraph{2. A teacher-anchored residual calibration strategy.}
The mainline uses a classical teacher estimator to produce a stable baseline
\((K_t^{\rm teacher},b_t^{\rm teacher})\).  The CNN predicts a small residual,
currently
\begin{equation}
    \delta b_t=f_\phi(H_{t-c+1:t},\Delta H_t,z_t^{\rm teacher}),
\end{equation}
and the committed parameters are
\begin{equation}
    K_t=K_t^{\rm teacher}, \qquad
    b_t={\rm EMA}\!\left(b_t^{\rm teacher}+\delta b_t\right).
\end{equation}
This treats learning as calibration of a low-dimensional control surface, not
as a full neural GKP decoder.

\paragraph{3. A deployment-aware runtime architecture.}
The architecture exposes the constraints that matter for real-time use:
fixed-point quantization, clipping, saturation diagnostics, parameter smoothing,
stale-parameter behavior, and double-buffered stage-and-commit updates.  These
constraints become measurable objects in the benchmark, rather than prose
assumptions after the algorithm is designed.

% \paragraph{4. A paper-facing benchmark and ablation protocol.}
% Following the structure of recent low-latency pre-decoder work
% \cite{chamberland2026}, the evaluation should report not only final logical
% error metrics, but also syndrome-density or correction-density proxies,
% adaptation lag, saturation/overflow rates, fixed-point degradation, and runtime
% boundary status.  T57 and T64 provide bounded result packs that can seed this
% section, while the full paper still needs wider unseen-drift and deployment
% checks.

% \paragraph{5. A bounded evidence protocol for software-HIL claims.}
% The current evidence is useful because its boundary is explicit.  T24 is the
% authoritative frozen five-mode software-HIL revalidation.  T57 supplies a
% bounded feature/teacher ablation table.  T64 adds a separately labeled
% \texttt{statcalib} extension lane.  None of these results validate true
% \texttt{.tflite} inference or real hardware; the paper should preserve that
% distinction.

\section{Brief Review of the GKP Code}

\subsection{Ideal and approximate code states}

The square-lattice GKP code encodes a qubit into an oscillator by using
periodic structure in the \(q\) and \(p\) quadratures \cite{gkp2001}.  In the
project convention, the lattice constant is
\begin{equation}
    \lambda=\sqrt{2\pi}.
\end{equation}
An ideal logical state may be represented heuristically as an infinite comb,
for example
\begin{equation}
    |\bar 0\rangle \propto \sum_{n\in\mathbb{Z}} |n\lambda\rangle_q .
\end{equation}
Ideal comb states are not physical because they require infinite energy.
Approximate GKP states replace the infinitely sharp comb with finitely squeezed
peaks and an envelope.  The PRX Quantum review of the GKP code emphasizes that
finite-energy structure is central to practical GKP QEC rather than a minor
implementation detail \cite{grimsmo2021}.  It determines the intrinsic
syndrome uncertainty and contributes an effective displacement-like noise.

\subsection{Syndrome measurement as modular displacement information}

Let the accumulated phase-space displacement error before correction be
\begin{equation}
    e_t=\begin{bmatrix}e_{q,t}\\e_{p,t}\end{bmatrix}.
\end{equation}
An ideal GKP syndrome observes this displacement modulo the lattice:
\begin{equation}
    s_t = e_t \bmod \lambda,
    \qquad
    s_{q,t},s_{p,t}\in[-\lambda/2,\lambda/2).
\end{equation}
In a finite-energy and noisy-measurement setting, the measured syndrome is more
accurately described as
\begin{equation}
    \tilde{s}_t={\rm mod}(e_t,\lambda)+\eta_t^{\rm meas}+\eta_t^{\rm GKP}.
\end{equation}
Here \(\eta_t^{\rm meas}\) represents measurement and ancilla contributions,
while \(\eta_t^{\rm GKP}\) summarizes finite-squeezing uncertainty.  The
decoder therefore observes a noisy modular representative, not the absolute
displacement.

\subsection{Local affine decoding and its limits}

The modulo structure makes exact GKP decoding nonlinear and branch dependent.
Near a single lattice branch, however, a Gaussian approximation gives a useful
local model.  If \(e\) and \(s\) are jointly Gaussian,
\begin{equation}
\begin{bmatrix}e\\s\end{bmatrix}
\sim
\mathcal{N}
\left(
\begin{bmatrix}\mu_e\\\mu_s\end{bmatrix},
\begin{bmatrix}
\Sigma_{ee} & \Sigma_{es}\\
\Sigma_{se} & \Sigma_{ss}
\end{bmatrix}
\right),
\end{equation}
then the linear-MMSE estimate is
\begin{equation}
    \hat e=\mu_e+\Sigma_{es}\Sigma_{ss}^{-1}(s-\mu_s)=Ks+b,
\end{equation}
where
\begin{equation}
    K=\Sigma_{es}\Sigma_{ss}^{-1},
    \qquad
    b=\mu_e-K\mu_s.
\end{equation}
This derivation explains why an affine fast path is a plausible low-latency
approximation.  It also marks the boundary of the approximation.  Near lattice
decision boundaries, the posterior can be multimodal; maximum-likelihood,
closest-lattice-point, or wrapped-Gaussian decoders may outperform a single
global affine rule \cite{fukui2017,roy2025}.  
% The paper should present the
% affine rule as a hardware-compatible approximation, not as a global
% Bayes-optimal GKP decoder.

\subsection{Logical failure criterion}

After correction, the residual displacement is wrapped into the GKP fundamental
cell.  A logical error occurs when the residual crosses a logical decision
boundary, for example
\begin{equation}
    |r_{q,t}|>\lambda/2 \quad \Rightarrow \quad X_L\ {\rm error},
\end{equation}
and similarly for the \(p\)-quadrature.  This is why closed-loop logical error
rate, or the repository's \texttt{final\_ler\_mean} proxy under a fixed
protocol, is more important than offline parameter-regression error.

\section{Noise and Drift Model}

\subsection{Effective noise state}

The slow loop does not attempt to infer every microscopic hardware parameter.
It uses a low-dimensional effective state,
\begin{equation}
    \theta_t^{\rm noise}
    =
    (\sigma_t,\mu_{q,t},\mu_{p,t},\vartheta_t),
\end{equation}
where \(\sigma_t\) controls the displacement scale, \(\mu_{q,t}\) and
\(\mu_{p,t}\) represent mean bias, and \(\vartheta_t\) captures covariance-axis
rotation.  This is a deliberately compact parameterization: it can be mapped
to affine runtime parameters and estimated from finite histogram windows.

\subsection{Noise sources represented by the model}

The present model is best understood as an effective displacement-noise model
with additional measurement uncertainty.  It is meant to absorb several
physical effects:
\begin{enumerate}
    \item finite-energy GKP peak width and envelope effects;
    \item Gaussian random displacement noise in \(q\) and \(p\);
    \item biased displacement means caused by calibration offsets;
    \item anisotropic or rotated covariance caused by asymmetric noise;
    \item syndrome measurement noise and noisy auxiliary-state contributions.
\end{enumerate}
This is narrower than a full circuit-level GKP noise model, but it is aligned
with the current affine fast-path contract.

\subsection{Drift scenarios}

The current frozen software-HIL protocol uses four named drift scenarios:
\begin{enumerate}
    \item \texttt{static\_bias\_theta}: a biased and rotated operating point;
    \item \texttt{linear\_ramp}: slowly changing effective noise parameters;
    \item \texttt{step\_sigma\_theta}: a sudden variance/orientation change;
    \item \texttt{periodic\_drift}: periodic nonstationarity.
\end{enumerate}
These scenarios are useful because they separate different adaptation demands:
steady calibration, tracking, shock response, and periodic following.  A full
paper should add unseen families such as random-walk drift, burst measurement
noise, coupled bias-variance drift, faster-than-window drift, and low-shot
histogram regimes.

\section{Model Architecture}

\subsection{Fast loop: affine fixed-point decoder}

The fast loop receives the current measured syndrome \(s_t\), reads the active
parameter bank, and computes
\begin{equation}
    s_t^{\rm clip}={\rm clip}(s_t,-s_{\max},s_{\max}),
\end{equation}
\begin{equation}
    \Delta_t^{\rm raw}=K_t s_t^{\rm clip}+b_t,
\end{equation}
\begin{equation}
    \Delta_t=Q\!\left({\rm clip}(\Delta_t^{\rm raw},
    -\Delta_{\max},\Delta_{\max})\right),
\end{equation}
where \(Q(\cdot)\) denotes fixed-point quantization.  The project convention is
an FPGA-oriented Q4.20 representation for syndrome values and runtime
parameters.  The fast loop also records diagnostic counters, including
histogram-input saturation, correction saturation, overflow, aggressive
parameter events, commit counts, and fallback-related signals.

\subsection{Parameter mapping}

Given an estimated noise state, the parameter mapper constructs an error
covariance
\begin{equation}
    C =
    R(\vartheta)
    \begin{bmatrix}
        \sigma_q^2 & 0\\
        0 & \sigma_p^2
    \end{bmatrix}
    R(\vartheta)^\top ,
\end{equation}
and a measurement covariance
\begin{equation}
    R_{\rm meas}=(\sigma_{\rm meas}^2+\Delta_{\rm eff}^2)I.
\end{equation}
The raw gain is
\begin{equation}
    K_{\rm raw}=C(C+R_{\rm meas})^{-1},
\end{equation}
followed by gain clipping or scaling.  The bias target is
\begin{equation}
    b_{\rm target}=\alpha(I-K_{\rm target})\mu,
    \qquad
    \mu=\begin{bmatrix}\mu_q\\\mu_p\end{bmatrix}.
\end{equation}
Finally, exponential smoothing produces staged parameters:
\begin{equation}
    K_t=(1-\beta)K_{t-1}+\beta K_{\rm target},
    \qquad
    b_t=(1-\beta)b_{t-1}+\beta b_{\rm target}.
\end{equation}

\subsection{Teacher estimators}

The teacher family provides stable estimates of \(\theta_t^{\rm noise}\) from
recent syndrome history.  A simple teacher computes moments over a histogram
window.  Stronger teachers such as EKF, UKF, RLS, or particle-filter variants
add state-space assumptions over time.  Abstractly,
\begin{equation}
    \hat{\theta}_t^{\rm teacher}={\rm Teacher}(H_{1:t}),
\end{equation}
and
\begin{equation}
    (K_t^{\rm teacher},b_t^{\rm teacher})
    =
    {\rm ParamMapper}(\hat{\theta}_t^{\rm teacher}).
\end{equation}
The teacher is the stability anchor.  It keeps the learned branch near an
interpretable calibration surface and creates meaningful ablation baselines.

\subsection{CNN residual branch}

The CNN consumes a short context of normalized syndrome histograms and
histogram deltas:
\begin{equation}
    X_t^{\rm hist}=[H_{t-c+1},\ldots,H_t,\Delta H_{t-c+2},\ldots,\Delta H_t].
\end{equation}
Teacher-side features may include
\begin{equation}
    z_t^{\rm teacher}
    =
    (\hat{\theta}_t^{\rm teacher},
     K_t^{\rm teacher},
     b_t^{\rm teacher},
     \Delta b_t^{\rm teacher},\ldots).
\end{equation}
The current gated branch narrows this to a small set of teacher-\(b\) and
teacher-\(\Delta b\) scalars.  The learned branch predicts
\begin{equation}
    \widehat{\delta b}_t=f_\phi(X_t^{\rm hist},z_t^{\rm teacher}),
\end{equation}
then clips the result:
\begin{equation}
    \delta b_t={\rm clip}(s_b\widehat{\delta b}_t,-b_{\max},b_{\max}).
\end{equation}
The committed parameter is
\begin{equation}
    K_t=K_t^{\rm teacher},
    \qquad
    b_t={\rm EMA}(b_t^{\rm teacher}+\delta b_t).
\end{equation}

\subsection{Stage-and-commit runtime contract}

The slow loop does not directly mutate active fast-loop parameters.  It stages
a candidate \((K,b)\) into an inactive bank and commits it at a safe epoch
boundary:
\begin{equation}
(K_t,b_t)=
\begin{cases}
(K^A,b^A), & t<t_{\rm commit},\\
(K^B,b^B), & t\ge t_{\rm commit}.
\end{cases}
\end{equation}
This contract allows the benchmark to measure stale-parameter effects, update
latency, commit success, rollback/fallback behavior, and fixed-point stability.

\section{Relationship to Existing Work}

\subsection{GKP and bosonic analog soft information}

GKP analog syndrome information has already been shown to be valuable.  Work
on analog QEC, surface-GKP codes, and concatenated bosonic-QLDPC decoding uses
continuous measurement information to set decoder weights or soft messages
\cite{fukui2017,noh2020,noh2022,berent2024,borah2025}.  This project should
not claim novelty for using analog information itself.  Its distinction is
that the analog statistics are compressed into a physical-layer affine GKP
fast-path parameter update.

\subsection{Adaptive priors and syndrome-statistics estimation}

Adaptive-weight and syndrome-statistics methods estimate decoder weights or
noise parameters from measured data \cite{spitz2018,wagner2021}.  Calibrated
decoders and decoder-prior optimization show that prior mismatch can materially
affect logical performance \cite{chen2022,sivak2024}.  This project borrows
that lesson, but maps the adaptation target to the GKP affine parameters
\((K,b)\) and evaluates it under a stage-and-commit runtime contract.

\subsection{Learned low-latency QEC modules}

AI pre-decoders and calibration-conditioned FiLM decoders demonstrate a
systems-oriented pattern: a learned module is useful when its input/output
contract, latency role, and integration with a classical decoder are explicit
\cite{chamberland2026,stein2026}.  This project follows the same philosophy,
but its learned module is slower than the per-shot path and updates only
calibration parameters.  The per-shot path remains affine fixed-point logic.

\subsection{Real-time and FPGA QEC decoders}

Real-time QEC decoders and FPGA-integrated systems have already reported
low-latency hardware decoding in stabilizer-code settings \cite{yang2026,
caune2024}.  Those works define the standard for true deployment claims:
closed-loop timing, worst-case latency, resource use, and hardware integration.
The present project is not yet at that evidence level.  The safe near-term
claim is FPGA-friendly architecture and software-HIL validation, not FPGA
demonstration.

\section{Numerical Results and Benchmark Plan}

% \subsection{Current bounded evidence}

% The current paper-facing evidence should be reported in layers.

% \begin{center}
% \small
% \begin{tabular}{>{\raggedright\arraybackslash}p{0.20\linewidth}
%                 >{\raggedright\arraybackslash}p{0.34\linewidth}
%                 >{\raggedright\arraybackslash}p{0.34\linewidth}}
% \toprule
% Evidence item & Supported reading & Boundary \\
% \midrule
% T24 frozen set & In the locked four-scenario, five-mode software-HIL matrix,
% \texttt{hybrid\_residual\_b} ranks first in all four scenarios. & Mock-backed
% software HIL only; no \texttt{.tflite} or board validation. \\
% T57 feature/teacher ablation & Histogram-delta removal clearly hurts; teacher
% prediction removal mildly hurts; teacher-delta removal is near-neutral/mixed;
% no-teacher-params unexpectedly performs best under the reused lane. & Useful
% for bounded ablation, not causal proof that all teacher channels are necessary. \\
% T64 statcalib extension lane & A separately labeled \texttt{statcalib} sixth
% lane beats the frozen winner in all four locked scenarios with clean
% provenance. & Extension-lane result only; must not rewrite T24 or imply mature
% deployment validation. \\
% T65 consistency guard & Expected to harden T64 report consistency and audit
% coverage. & No new benchmark evidence. \\
% \bottomrule
% \end{tabular}
% \end{center}

% \subsection{T64 extension-lane table}

% T64 preserves the T24 frozen subset exactly and appends \texttt{statcalib} as a
% sixth lane.  The current bounded results are:

% \begin{center}
% \small
% \begin{tabular}{lccc}
% \toprule
% Scenario & Frozen winner & Winner \(\texttt{final\_ler\_mean}\) &
% \texttt{statcalib} \(\texttt{final\_ler\_mean}\) \\
% \midrule
% \texttt{static\_bias\_theta} & \texttt{hybrid\_residual\_b} & 0.810902 & 0.431708 \\
% \texttt{linear\_ramp} & \texttt{hybrid\_residual\_b} & 0.787755 & 0.467083 \\
% \texttt{step\_sigma\_theta} & \texttt{hybrid\_residual\_b} & 0.788800 & 0.460016 \\
% \texttt{periodic\_drift} & \texttt{hybrid\_residual\_b} & 0.806392 & 0.438751 \\
% \bottomrule
% \end{tabular}
% \end{center}

% Lower \(\texttt{final\_ler\_mean}\) is better under this fixed repository
% metric.  The large gap is scientifically interesting, but it should be written
% carefully.  It supports further investigation of statistical calibration
% baselines, not a claim that the project has already established a final
% state-of-the-art decoder.

% \subsection{Recommended main benchmark structure}

% The full paper should follow a result structure closer to modern low-latency
% decoder papers:
% \begin{enumerate}
%     \item closed-loop logical performance under drift, with paired seeds and
%     confidence intervals;
%     \item regret to oracle affine or wrapped-Gaussian baselines;
%     \item adaptation lag after step drift and periodic tracking error;
%     \item ablation tables for histogram context, teacher features, residual
%     target, EMA, clipping, and fixed-point quantization;
%     \item runtime-contract metrics: commit latency, stale-parameter penalty,
%     saturation/overflow rate, fallback count, and fixed-point-vs-float gap;
%     \item comparator lanes: nearest-lattice/hard GKP, static affine, moment
%     teacher, EKF/UKF/RLS teacher, \texttt{statcalib}, direct CNN-to-parameters,
%     teacher-residual-b, and a FiLM-style conditioned affine head.
% \end{enumerate}

% \subsection{Figure and table plan}

% The near-term paper figures can be staged as follows:
% \begin{enumerate}
%     \item architecture diagram: GKP syndrome, fast affine loop, slow
%     histogram/teacher/CNN loop, parameter bank, software-HIL boundary;
%     \item theory figure: local branch affine approximation versus modular
%     boundary failure cases;
%     \item frozen benchmark table: T24 five-mode scenario results;
%     \item T57 ablation table: feature/teacher ablation with hedge wording;
%     \item T64 extension-lane table: \texttt{statcalib} as a separately labeled
%     sixth lane;
%     \item runtime-contract table: quantization, saturation, commit, and stale
%     update metrics, currently partly available and to be hardened after T65.
% \end{enumerate}

\section{Writing Position and Near-Term Plan}

% The safest paper thesis is:

% \begin{quote}
% We study a deployment-constrained, teacher-anchored residual calibration layer
% for drift-adaptive affine GKP decoding.  The fast path remains a bounded
% fixed-point affine correction, while the slow path estimates noise drift from
% syndrome histograms and updates runtime parameters through a stage-and-commit
% contract.
% \end{quote}

% The paper should avoid claims that would collide with nearby work or exceed the
% evidence boundary:
% \begin{enumerate}
%     \item not ``first adaptive neural QEC decoder'';
%     \item not ``first calibration-conditioned decoder'';
%     \item not ``first use of GKP analog soft information'';
%     \item not ``FPGA-validated real-time GKP decoder'';
%     \item not ``mechanism proven by residual-b amplitude.''
% \end{enumerate}

% The next writing update after T65 should separate three layers: (i) the frozen
% historical T24 table, (ii) the T64 \texttt{statcalib} extension lane, and
% (iii) the remaining open runtime and deployment checks.  If T65 adds an audit
% helper and corrects T64 report wording as planned, the note can cite T64 more
% confidently as a clean, bounded extension-lane result while still keeping the
% mock-backed software-HIL boundary explicit.

\begin{thebibliography}{99}

\bibitem{gkp2001}
D. Gottesman, A. Kitaev, and J. Preskill,
``Encoding a qubit in an oscillator,''
\emph{Physical Review A}, 2001.

\bibitem{grimsmo2021}
A. L. Grimsmo and S. Puri,
``Quantum Error Correction with the Gottesman--Kitaev--Preskill Code,''
\emph{PRX Quantum} 2, 020101, 2021.

\bibitem{fukui2017}
K. Fukui, A. Tomita, A. Okamoto, and K. Fujii,
``High-threshold fault-tolerant quantum computation with analog quantum error
correction,''
\emph{Physical Review X}, 2018.

\bibitem{noh2020}
K. Noh and C. Chamberland,
``Fault-tolerant bosonic quantum error correction with the surface-GKP code,''
\emph{Physical Review A}, 2020.

\bibitem{noh2022}
K. Noh, C. Chamberland, and F. G. S. L. Brandao,
``Low-overhead fault-tolerant quantum error correction with the surface-GKP
code,''
\emph{PRX Quantum}, 2022.

\bibitem{berent2024}
L. Berent et al.,
``Analog information decoding of bosonic quantum LDPC codes,''
\emph{PRX Quantum}, 2024.

\bibitem{borah2025}
S. K. Borah, A. K. Pradhan, N. Raveendran, M. Pacenti, and B. Vasic,
``Fault Tolerant Decoding of QLDPC-GKP Codes with Circuit Level Soft
Information,''
preprint, 2025.

\bibitem{roy2025}
M.-A. Roy, T. Pousset, and B. Royer,
``Decoding Multimode Gottesman--Kitaev--Preskill Codes with Noisy Auxiliary
States,''
preprint, 2025.

\bibitem{spitz2018}
S. Spitz, B. Tarasinski, C. W. J. Beenakker, and T. E. O'Brien,
``Adaptive weight estimator for quantum error correction,''
\emph{Advanced Quantum Technologies}, 2018.

\bibitem{wagner2021}
T. Wagner, H. Kampermann, and D. Bru{\ss},
``Optimal noise estimation from syndrome statistics of quantum codes,''
\emph{Physical Review Research}, 2021.

\bibitem{chen2022}
E. H. Chen et al.,
``Calibrated decoders for experimental quantum error correction,''
\emph{Physical Review Letters}, 2022.

\bibitem{sivak2024}
V. Sivak, M. Newman, and P. Klimov,
``Optimization of decoder priors for accurate quantum error correction,''
\emph{Physical Review Letters} 133, 150603, 2024.

\bibitem{chamberland2026}
C. Chamberland, J. Olle, M. Li, S. Thornton, and I. Baratta,
``Fast and accurate AI-based pre-decoders for surface codes,''
arXiv:2604.12841, 2026.

\bibitem{stein2026}
S. Stein, S. Kan, C. Liu, A. Harkness, S. Garner, Z. Du, Y. Ding, Y. Mao, and
A. Li,
``Calibration-Conditioned FiLM Decoders for Low-Latency Decoding of Quantum
Error Correction Evaluated on IBM Repetition-Code Experiments,''
preprint, 2026.

\bibitem{yang2026}
X. Yang et al.,
``Real-time Surface-Code Error Correction Using an FPGA-based Neural-Network
Decoder,''
preprint, 2026.

\bibitem{caune2024}
L. Caune et al.,
``Demonstrating real-time and low-latency quantum error correction with
superconducting qubits,''
preprint, 2024.

\end{thebibliography}

\end{document}
