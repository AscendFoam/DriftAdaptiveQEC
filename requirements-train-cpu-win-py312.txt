# T39 draft CPU-only training-chain dependency spec for Windows / Python 3.12.
#
# Verified scope in T39:
# - python -m cnn_fpga.data.dataset_builder --config cnn_fpga/config/experiment_static_theta_v2.yaml --dry-run
# - python -m cnn_fpga.data.runtime_dataset_builder --config cnn_fpga/config/experiment_runtime_b_residual.yaml --dry-run
# - python -m cnn_fpga.model.train --help
#
# Explicitly not verified in T39:
# - real dataset generation
# - model training execution
# - benchmark / .tflite / hardware / GPU-CUDA portability
#
# This file is intentionally separate from requirements-recovery.txt.

numpy==2.4.5
PyYAML==6.0.3
