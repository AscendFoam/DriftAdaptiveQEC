# 强 baseline smoke 配置：
# - 仅用于快速验证 `RLS Residual-B / UKF` 是否能接入现有 benchmark 主线

experiment:
  name: p4_multiscenario_strong_baselines_smoke_v1
  stage: p4_strong_baselines_smoke
  seed: 20260403

base_config: p4_multiscenario_strong_baselines.yaml

timing:
  n_slow_updates: 120
  n_fast_cycles: 480000

p4_benchmark:
  protocol:
    protocol_id: p4_strong_baselines_smoke_v1
    repeats: 1
