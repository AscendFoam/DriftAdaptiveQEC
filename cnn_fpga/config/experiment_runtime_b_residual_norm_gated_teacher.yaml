# Runtime-consistent residual-b variant:
# - keep reencoded scalar teacher params
# - add normalized scalar branch with learned gating

experiment:
  name: runtime_b_residual_norm_gated_teacher_v1
  stage: p4_ablation_norm_gated_teacher
  seed: 20260420

base_config: experiment_runtime_b_residual_reencoded_teacher_params.yaml

paths:
  dataset_dir: artifacts/datasets/runtime_b_residual_norm_gated_teacher_v1
  model_dir: artifacts/models/runtime_b_residual_norm_gated_teacher_v1
  report_dir: artifacts/reports/runtime_b_residual_norm_gated_teacher_v1

training:
  tiny_cnn:
    scalar_fusion_mode: gated
    scalar_gate_init_bias: 2.0
