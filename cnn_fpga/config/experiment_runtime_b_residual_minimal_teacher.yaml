# Runtime-consistent residual-b variant:
# - keep only minimal deployment-facing teacher information in the scalar branch
# - remove teacher prediction planes and teacher delta planes

experiment:
  name: runtime_b_residual_minimal_teacher_v1
  stage: p4_ablation_minimal_teacher
  seed: 20260420

base_config: experiment_runtime_b_residual_reencoded_teacher_params.yaml

paths:
  dataset_dir: artifacts/datasets/runtime_b_residual_minimal_teacher_v1
  model_dir: artifacts/models/runtime_b_residual_minimal_teacher_v1
  report_dir: artifacts/reports/runtime_b_residual_minimal_teacher_v1

runtime_dataset:
  include_teacher_prediction: false
  include_teacher_deltas: false
  include_teacher_params: true
  teacher_params_layout: scalar_branch

slow_loop:
  hybrid_residual_b:
    include_teacher_prediction: false
    include_teacher_deltas: false
    include_teacher_params: true
    teacher_params_layout: scalar_branch
