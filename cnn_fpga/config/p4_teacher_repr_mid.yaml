# Intermediate-length paired benchmark for teacher-representation variants.
# Purpose:
# - clearly longer than smoke runs
# - still materially shorter than the longest formal confirm run

experiment:
  name: p4_teacher_repr_mid_v1
  stage: p4_teacher_repr_mid
  seed: 20260427

base_config: p4_multiscenario.yaml

timing:
  n_slow_updates: 300
  n_fast_cycles: 1200000

p4_benchmark:
  protocol:
    protocol_id: p4_teacher_repr_mid_v1
    repeats: 2
