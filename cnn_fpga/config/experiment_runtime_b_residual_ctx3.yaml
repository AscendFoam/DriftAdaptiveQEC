# Hybrid Residual-B ablation：
# - 把时间上下文改为 3 窗，作为 `1 -> 3 -> 5` 的中间档

experiment:
  name: runtime_b_residual_ctx3_v1
  stage: p4_ablation_ctx3
  seed: 20260404

base_config: experiment_runtime_b_residual.yaml

paths:
  dataset_dir: artifacts/datasets/runtime_b_residual_ctx3_v1
  model_dir: artifacts/models/runtime_b_residual_ctx3_v1
  report_dir: artifacts/reports/runtime_b_residual_ctx3_v1

runtime_dataset:
  context_windows: 3

slow_loop:
  hybrid_residual_b:
    context_windows: 3
