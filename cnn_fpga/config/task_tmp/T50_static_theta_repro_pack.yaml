base_config: ../experiment_static_theta_v2.yaml

paths:
  model_dir: artifacts/t50_training_repro_pack/models/static_theta_v2
  report_dir: artifacts/t50_training_repro_pack/reports/static_theta_v2

training:
  max_train_samples: 2048
  max_val_samples: 512
  tiny_cnn:
    epochs: 5
    patience: 3
